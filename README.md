# (アイン、ソフ、オウル)

**Three.js のための数学と実装** — 数学の予備知識ゼロから、作品を人に見せられる形にするところまで。全106章の学習サイトです。

Three.js は箱を 1 つ出すところまでは簡単ですが、その先でベクトル・回転・行列・投影の壁にぶつかります。
このサイトは、その壁をこえるために必要な数学**だけ**を、
3D に固有でない土台（第1部）と 3D の数学（第2部）の 2 段で身につけ、
それを実際の Three.js のコードにし（第3部）、
作品を 2 つ最初から最後まで組み上げ（第4部）、
それを人に見せられる形に仕上げます（第5部）。

- 公式の暗記は求めません。スライダーを動かして 3D が変わるのを見ながら理解します
- 各章は「Three.js で何ができなくて困るか → 直感 → 図とデモ → はじめて数式 → 実際のコード」の順です
- **数式には必ず「日本語で言うと」と「実際に計算してみる」を併記しています。**
  意味が分かることと自分で回せることは別なので、全107個の数式に実数値での手順を添えました
- **第3部より後のコードは、ページの中で書き換えて実行できます**
- **画像もモデルも 1 つも使いません。**リポジトリにバイナリはありません

## 第1部　数と形の基礎（全42章）

3D に固有でない土台です。**数直線から始めて、3D に入る前に数そのものを片付けます。**
すべての数式に「実際に計算してみる」が付き、演習は**手で計算する**ものを中心に組んでいます。

| # | 章 |
|---|---|
| 1 | 数直線 ― マイナスは「反対向き」 |
| 2 | 文字と式 ― 数学の = と、コードの = |
| 3 | 関数とグラフ ― 入れると出てくる箱 |
| 4 | 二乗と平方根 ― 面積から辺を戻す |
| 5 | 割合 ― 0 から 1 で考える癖をつける |
| 6 | 小数の落とし穴 ― 0.1 + 0.2 は 0.3 ではない |
| 7 | 座標平面 ― 2 つの数で場所を指す |
| 8 | 3D空間と座標系 ― 3 つめの軸を足す |
| 9 | 右手系 ― 軸の並べ方には流儀がある |
| 10 | ピタゴラスの定理 ― 斜めの長さを出す |
| 11 | 3D の距離と、二乗のままで比べる |
| 12 | ベクトル ― 位置と向き |
| 13 | ベクトルの足し算 ― 矢印を継ぎ足す |
| 14 | ベクトルの引き算 ― あそこへ向かう矢印を作る |
| 15 | スカラー倍 ― 向きはそのまま、長さだけ変える |
| 16 | ベクトルの長さ ― 「どれだけ」を取り出す |
| 17 | 正規化 ― 「どっち」だけを取り出す |
| 18 | 角度 ― 「どれだけ回ったか」を数にする |
| 19 | 弧度法 ― 角度を「長さ」で測る |
| 20 | 単位円 ― sin と cos は円周上の座標 |
| 21 | 円運動 ― cos と sin で物を回す |
| 22 | 波 ― 振幅・周期・位相の 3 つのつまみ |
| 23 | atan2 ― 座標から角度に戻す |
| 24 | 内積 ― 成分を掛けて、足す |
| 25 | 内積と角度 ― 符号だけで位置関係が分かる |
| 26 | 視界の判定 ― 前か後ろか、正面何度以内か |
| 27 | 面の明るさ ― 内積が陰影になる |
| 28 | 射影 ― ある向きに落とした影の長さ |
| 29 | 外積 ― 両方に垂直な向きを作る |
| 30 | 外積の長さ ― 平行だと消える |
| 31 | 三角形の法線 ― 頂点の順番が表裏を決める |
| 32 | 右か左か ― 外積の符号で側を判定する |
| 33 | lerp ― 2 つの値を混ぜる |
| 34 | 逆補間とクランプ ― 値を割合に戻す |
| 35 | イージング ― t をひとひねりする |
| 36 | smoothstep ― 境目をぼかす |
| 37 | 追従 ― 毎フレーム少しずつ近づける |
| 38 | 乱数 ― 毎回違う数を作る |
| 39 | シード ― 再現できるランダム |
| 40 | 分布の偏り ― 一様に散らすのは難しい |
| 41 | ノイズ ― 近い場所は似た値 |
| 42 | 重ね合わせ ― 地形らしさの正体 |

## 第2部　3D の数学（全36章）

土台の上に、3D を動かすための数学を積みます。

| # | 章 |
|---|---|
| 1 | 行列 ― 点を入れると、動いた点が返る装置 |
| 2 | 行列とベクトルの掛け算 ― 中で何が起きているか |
| 3 | 行列の列は「軸の行き先」 |
| 4 | なぜ 4×4 なのか ― 同次座標 |
| 5 | 掛ける順番 ― 入れ替えると別の変換になる |
| 6 | TRS ― Three.js が内部でやっている合成 |
| 7 | 逆行列 ― 変換を巻き戻す |
| 8 | 法線行列 ― 拡大すると陰影が壊れる |
| 9 | 回転の式 ― 点を角度だけ回す |
| 10 | オイラー角 ― 3 つの角度で向きを表す |
| 11 | ジンバルロック ― 自由度が 1 つ消える |
| 12 | 軸と角度 ― 回転のいちばん素直な表し方 |
| 13 | クォータニオン ― 4 つの数で姿勢を持つ |
| 14 | slerp ― 姿勢をなめらかに繋ぐ |
| 15 | lookAt ― 向きを 1 行で決める |
| 16 | 親子関係 ― 座標系を入れ子にする |
| 17 | ローカルとワールド ― どちらの座標で言っているか |
| 18 | matrixWorld ― 親をさかのぼって積み上げる |
| 19 | 回転の中心 ― ピボットをずらす |
| 20 | 親を付け替える ― add と attach |
| 21 | 極座標 ― 角度と距離で場所を指す |
| 22 | 球面座標 ― 緯度と経度から x y z へ |
| 23 | 球面に一様に散らす ― 極に溜めない |
| 24 | 軌道 ― 何かのまわりを回る |
| 25 | カメラ ― 世界を、カメラから見た形に置き直す |
| 26 | 透視投影 ― 遠いものが小さくなる仕組み |
| 27 | 視錐台 ― near と far が決めるもの |
| 28 | 画面に出るまで ― 正規化デバイス座標とピクセル |
| 29 | 正射影 ― 大きさが変わらない写し方 |
| 30 | 法線 ― 面の向きを、頂点が持ち歩く |
| 31 | 反射 ― 法線に沿った分だけを裏返す |
| 32 | ハイライト ― 鏡の向きに、どれだけ近いか |
| 33 | フレネル ― 浅い角度ほど、よく映る |
| 34 | 屈折 ― 透明なものの中で、道が曲がる |
| 35 | 曲線とパス |
| 36 | 総合演習 ― 全部を使って作る |

## 第3部　Three.js 編（全14章）

各章の冒頭に「この章で使う数学」として土台への呼び戻しリンクがあり、
本文のコードはその場で編集して実行できます。

| # | 章 |
|---|---|
| 1 | 最初のシーン |
| 2 | 形を作る ― ジオメトリ |
| 3 | 見た目を決める ― マテリアル |
| 4 | テクスチャ ― 面に絵を貼る |
| 5 | ライトと影 |
| 6 | 動かす ― ループと時間 |
| 7 | 視点を操作する |
| 8 | マウスで触る ― Raycaster |
| 9 | モデルを読み込む |
| 10 | シーンを組み立てる |
| 11 | 速くする |
| 12 | シェーダの入口 |
| 13 | 頂点シェーダ ― 形を動かす |
| 14 | フラグメントシェーダ ― 色を決める |

## 第4部　実践編（全8章）

作品を 2 つ、最初から最後まで作ります。**素材は 1 つも用意せず、すべてコードで生成します。**

| # | 章 | 作るもの |
|---|---|---|
| 1 | 惑星ビューアー ― 骨組みと星空 | 球面への一様分布 |
| 2 | 惑星ビューアー ― 表面をコードで描く | 方向ベクトルで引く3次元ノイズ |
| 3 | 惑星ビューアー ― 大気と雲と、夜 | 内積で作る大気の縁と昼夜境界 |
| 4 | 惑星ビューアー ― 軌道と、寄っていく視点 | 傾いた自転・公転・クリックで寄る視点 |
| 5 | ローポリの街 ― 街路をひく | 決め打ちの乱数と再帰的な分割 |
| 6 | ローポリの街 ― 建物を生やす | 数百棟をドローコール1回で |
| 7 | ローポリの街 ― 朝から夜へ | 時刻1つから光・空・影・窓を導く |
| 8 | ローポリの街 ― 車を走らせて仕上げる | 曲線に沿う動きとカリングの両立 |

## 第5部　仕上げ編（全6章）

作ったものを「見せられるもの」にします。**素材ゼロの方針はここでも変えません。**

| # | 章 | 中身 |
|---|---|---|
| 1 | 映り込みを作る ― 環境マップ | 金属が黒い理由と、PMREM で環境マップを手続き的に作る |
| 2 | 色の通り道 | リニアワークフロー・outputColorSpace・トーンマッピング |
| 3 | ポストプロセス入門 | Composer の3点セット、ブルーム、OutputPass を忘れたときの壊れ方 |
| 4 | 自分でパスを書く | ShaderPass、ビネット・彩度・走査線・色収差 |
| 5 | React Three Fiber ― もうひとつの書き方 | 素の three との対応表、効く場面と効かない場面。**この章のコードだけは実行できません** |
| 6 | アプリにする | 品質プリセット・読み込み・触り方・公開 |

**各章の末尾に演習があります（全304問）。**土台の 2 部はデモの操作課題と考える課題、第3部より後は
「このコードのここを変えて〜にする」形で、ヒントと解答例が開けます。答え合わせはしません。
**`#/drill` に全問を集めてあり**、部で絞り込んだ一覧から入るか、1 問ずつ通しで解けます
（「解いていない問だけ」に絞れるので復習に使えます）。

ほかに、**逆引き（症状から探す・25項目）**、用語集（本文の下線付き用語をホバーで引ける・85語）、
前提知識マップ、サイト内検索、読了の進捗保存があります。
「画面が真っ黒」「金属が黒い」「影が出ない」「重い」から、確かめる順に並んだ手順と該当章へ飛べます。

## 詰まらないための作り

独学が止まるのは知識が足りないときではなく、**次の一手が決まらないとき**です。
そこで情報を増やすのではなく、判断を減らす方向で 3 つだけ手当てしています。

- **ホームには「続きから」が 1 枚だけ出ます。** 進捗から一意に決まり、選択肢は並べません
  （読んでいない最初の章 → 残りの演習 → 公開、の順に送ります）
- **全章の冒頭に戻り先があります。** 第3部より後は「この章で使う数学」、
  土台の 2 部は `requires` から「この章の前に読んでおく章」を出します
- **クイズを間違えたときだけ、前提の章へのリンクが出ます。** 正解した人には出しません

各部の見出しには「ここまでで何が作れるようになるか」を 1 行添えています。
章末の API チップは threejs.org の該当ページへのリンクになっています。

## 動かす

Node.js 22 以上が必要です。

```bash
npm install
npm run dev        # 開発サーバ（http://localhost:5173/webgl_manabe/）
```

| コマンド | 内容 |
|---|---|
| `npm run dev` | 開発サーバ |
| `npm run build` | 型チェック＋本番ビルド（`dist/`） |
| `npm run preview` | ビルド結果をローカルで配信 |
| `npm run typecheck` | `tsc --noEmit` |
| `npm run check` | コンテンツの整合性チェック（デモ id・用語・章リンク・クイズ・計算例・演習・逆引き） |
| `npm run smoke` | Playwright で全ルートを実際に開いて検証（要 `npm run build`）。`-- --part=basics` で部を絞れる |

`npm run smoke` は、全106章を同じタブで連続して開き、次を確認します。

- console error / pageerror が 1 件も出ないこと
- 各章のデモのキャンバスが実際に生成されていること
- **各サンドボックスが自動実行され、iframe の中にキャンバスを作ること**
- 106章を渡り歩いても WebGL コンテキストが枯渇しないこと（＝破棄漏れがないこと）
- ダーク／ライト両テーマで、本文 7:1・補助テキスト 4.5:1 のコントラスト比を満たすこと
- **すべての数式に計算例が描画されていること**
- **演習ページに全304問が出て、通しで解けて、「解いた」印がリロード後も残ること**
- ホームの「続きから」が 1 枚だけ出ていること
- 動きを減らす設定でも壊れないこと、狭い画面で横にはみ出さないこと

スクリーンショットは `artifacts/` に出力されます（Git 管理外）。

この 4 つ（`typecheck` → `check` → `build` → `smoke`）は、pull request と `main` への push で
GitHub Actions でも走ります（`.github/workflows/ci.yml`）。
**smoke だけは部ごとに分けて並列に**回しています ― 章が増えるほど直線的に伸びるためです。
3D は SwiftShader のソフトウェア描画で動かしています。失敗したときだけ、
スクリーンショットが実行結果の artifact として残ります。

## 作りの要点

コードを読むときの地図です。

- **章はデータ**（`src/content/chapters/*.ts`）。本文・数式・デモ・クイズを 1 つのオブジェクトに書き、
  共通のレンダラ（`src/app/pages/chapter.ts`）が描画します。目次・検索索引・前提知識マップ・用語リンクも、
  すべてこの同じデータから導かれます。
- **3D は Stage に集約**（`src/three/stage.ts`）。レンダラ・カメラ・操作・リサイズ・破棄をここで一手に持ちます。
  SPA で章を行き来するため、破棄漏れは即座に致命傷（ブラウザが同時に持てる WebGL コンテキストは 8〜16 程度）になるので、
  画面外では描画を止め、ページ離脱時にすべて解放します。
- **デモの UI も仕様から生成**（`src/ui/controls.ts`）。各デモは three のロジックだけを書きます。
- **ベクトルの色は全章で固定**（`src/styles/base.css` の `--vec-*`）。シアン＝入力 a、ピンク＝入力 b、
  アンバー＝計算結果、白＝法線／光、破線の灰＝補助線。赤緑の組を避け、ラベルと線種でも区別できるようにしています。
- **ネオンは線にだけ乗せる。** 本文にグローやグラデーションはかけず、コントラスト比を実測で担保しています。
- **サンドボックス**（`src/sandbox/`, `src/ui/sandbox.ts`）。第3部より後のコードは iframe の中で実行します。
  three をもう一部バンドルすると 570KB 増えるので、**親ページが読み込んでいる three の名前空間を列挙し、
  それを再エクスポートするだけの小さな Blob モジュール**を作り、インポートマップで `three` に対応づけています。
  おかげで iframe の中でも `import * as THREE from 'three'` がそのまま書け、
  **教材のコードと実際に走るコードが完全に同じ形**になります。バンドルは 1 バイトも増えません。
  読み込むのは**そのコードが実際に `import` しているものだけ**なので、ブルームを使わない章の読者に
  postprocessing のコードが降ってくることはありません。
  利用者のコードは独立した Blob モジュールとして読み込むので、エラーの行番号がエディタの行番号と一致します。
  エディタは透明な `textarea` を色付きの `<pre>` に重ねる方式で、既存の `highlight()` を再利用しています
  （新しい依存はありません）。

  three は親ページ側で動くため、iframe で作った `Float32Array` は realm が違い、three の `instanceof`
  判定を通りません。そこで iframe の型付き配列コンストラクタを親のものに向けています
  （`src/sandbox/runtime.ts`）。

- **素材はすべてコードで作る**（第4部）。惑星の地表・雲・街明かりは 3 次元ノイズから、
  街は決め打ちの乱数と再帰的な分割から生成しています。画像を用意しないので、
  「なぜこの見た目なのか」が全部コードに書いてあり、数値を変えれば別のものが出ます。
  球にテクスチャを貼るときは **UV ではなく方向ベクトルでノイズを引く**（`src/three/noise.ts`）。
  こうすると経度 0 度の継ぎ目と極の歪みが原理的に発生しません。

依存は three / KaTeX / フォント（@fontsource）だけです。Markdown パーサ・シンタックスハイライタ・
検索ライブラリ・グラフ描画ライブラリ・ノイズライブラリはいずれも入れず、必要な範囲だけ自前で書いています。

## 公開

`main` に push すると GitHub Actions が `dist/` を GitHub Pages へデプロイします
（`.github/workflows/deploy.yml`）。

初回のみ、リポジトリの **Settings → Pages → Source** を **GitHub Actions** に設定してください。

配信先が `https://<user>.github.io/webgl_manabe/` である前提で、`vite.config.ts` の `base` を
`/webgl_manabe/` に固定しています。別のパスで配信する場合はここを変えてください。

## ライセンス

MIT License. 詳細は [LICENSE](./LICENSE) を参照してください。
