import type { Chapter } from '../types.ts';

export const chapterM31: Chapter = {
  slug: 'm31-reflect',
  part: 'math3d',
  number: 31,
  title: '反射 ― 法線に沿った分だけを裏返す',
  goal: '反射ベクトルを手で計算できるようになり、鏡・跳ね返り・映り込みの向きを自分で求められるようになります。',
  requires: ['11-normal-light', 'b28-projection'],
  threeApis: ['Vector3.reflect', 'Vector3.dot', 'Vector3.projectOnVector', 'CubeCamera'],
  blocks: [
    {
      kind: 'md',
      text: `
## 跳ね返る向きは、どう決まるか

ボールが床に当たって跳ね返る。光が鏡に当たって曲がる。
どちらも同じ計算で求まります。

規則は 1 つだけです。

**面に沿った成分はそのまま、法線に沿った成分だけを裏返す。**

床にまっすぐ落としたボールはまっすぐ戻り、斜めに投げたボールは斜めに跳ねる ―
その両方が、この 1 行から出てきます。
`,
    },
    {
      kind: 'md',
      text: `
## 2 つの成分に分ける

[](#/ch/b28-projection)でやった分解を、そのまま使います。

入ってくる向き $d$ を、次の 2 つに分けます。

- **法線に沿った成分** … $(d \\cdot n)\\, n$。壁に向かって進んでいる分
- **面に沿った成分** … 残り。壁に沿って滑っている分

跳ね返るとき、**壁に向かっていた分は逆向きになり、滑っていた分はそのまま**です。

こうして求まる向きを{{反射ベクトル}}と呼びます。

だから引くのは「法線に沿った成分の 2 倍」―
1 つぶんで 0 になり、もう 1 つぶんで逆向きになるからです。
`,
    },
    {
      kind: 'formula',
      tex: 'r = d - 2(d \\cdot n)\\, n',
      readAloud:
        '反射ベクトルは、入ってくる向きから「法線に沿った成分の 2 倍」を引いたものです。1 つぶん引くと面に沿った成分だけになり、もう 1 つぶん引くと法線方向が裏返ります。',
      worked: {
        given:
          '床（法線 $n = (0,\\; 1,\\; 0)$）に、$d = (0.6,\\; -0.8,\\; 0)$ の向きで入ってきました。',
        steps: [
          { calc: 'd·n = 0.6x0 + (-0.8)x1 + 0x0 = -0.8', note: '負＝面に向かっている' },
          { calc: '法線に沿った成分 : -0.8 x (0,1,0) = (0, -0.8, 0)' },
          { calc: 'その2倍を引く  : (0.6,-0.8,0) - (0,-1.6,0)' },
          { calc: '             = (0.6, 0.8, 0)' },
          { calc: '【確認】長さ : √(0.36 + 0.64) = 1', note: '変わらない' },
        ],
        result:
          '**$(0.6,\\; 0.8,\\; 0)$** ― 横方向の $0.6$ はそのまま、縦方向だけ $-0.8$ から $+0.8$ に裏返りました。**入る角度と出る角度が等しい**という反射の法則が、この計算から自動的に出てきます。',
      },
    },
    {
      kind: 'demo',
      id: 'reflect-vector',
      caption:
        '「2 つの成分に分けて見る」を入れてください。ピンクの破線が面に沿った成分で、入る側と出る側で同じ長さのまま。バイオレットの破線が法線に沿った成分で、こちらだけ向きが逆になります。読み出しの「入る角度 → 出る角度」は、面をどう傾けても一致します。',
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '$d$ の向きに注意してください',
      text: `
反射の式は「面に向かって進む向き」を前提にしています。
d·n が負になる向きです。

ところが光の計算では、面から光源へ向かう向き（l）を使うことが多く、
こちらは符号が逆です。

three の Vector3.reflect も「面に向かう向き」を期待します。
符号を間違えると、反射が面の裏側へ飛んでいきます。

分からなくなったら、d·n の符号を出力してください。
負なら向かっている、正なら離れています。
`,
    },
    {
      kind: 'md',
      text: `
## 使いどころ ― 光だけではない

**① 鏡面反射（次の章）**

視線が反射した先に光源があれば、そこが光って見えます。

**② 映り込み**

反射した向きに環境マップを引くと、周囲の景色が映ります。
[](#/ch/q01-environment)でやった金属の見た目は、この計算です。

**③ 跳ね返り**

ボールが壁で跳ねる、弾が跳弾する。まったく同じ式です。
**エネルギーを失う場合は、跳ね返った速度に係数を掛けます。**

**④ 水面**

水面の映り込みは、カメラを水面で反射させた位置に置いてもう一度描き、
その結果を水面に貼るという方法で作れます。
`,
    },
    {
      kind: 'code',
      title: '反射を使う',
      code: `import * as THREE from 'three';

// three に計算させる（呼ばれた側が書き換わる）
const d = new THREE.Vector3(0.6, -0.8, 0);
const n = new THREE.Vector3(0, 1, 0);
d.reflect(n);                       // d が (0.6, 0.8, 0) になる

// 自分で書くとこうなる
function reflect(d, n) {
  return d.clone().addScaledVector(n, -2 * d.dot(n));
}

// 跳ね返り（跳ねるたびに勢いを失わせる）
if (ball.position.y < radius) {
  ball.position.y = radius;
  velocity.reflect(new THREE.Vector3(0, 1, 0));
  velocity.multiplyScalar(0.8);     // 反発係数
}

// GLSL にも同じ関数がある
//   vec3 r = reflect(viewDir, normal);

// 映り込み：反射した向きで環境マップを引く
material.envMap = envTexture;
material.metalness = 1;
material.roughness = 0;`,
    },
    {
      kind: 'md',
      text: `
## 面が粗いと、反射がぼやける

現実の面は完全な鏡ではありません。**細かい凹凸がある**ので、
少しずつ違う向きの法線が集まっています。

その結果、反射も少しずつ違う向きに散らばり、**ぼやけた映り込み**になります。

これが \`roughness\` の正体です。

- **$0$** … 完全な鏡。周囲がくっきり映る
- **$0.5$** … すりガラスのような、ぼんやりした映り込み
- **$1$** … ほぼ散乱してしまい、映り込みが見えない

一本一本の凹凸を計算するわけにはいかないので、
実際には「どれくらい散らばるか」を統計的に扱います。
**\`roughness\` は、その散らばりの幅を 1 つの数で表したもの**です。
`,
    },
  ],
  exercises: [
    {
      prompt: `法線 $n = (0,\\; 0,\\; 1)$ の壁に、$d = (0.8,\\; 0,\\; -0.6)$ の向きで入ってきました。
反射ベクトルを手で計算してください。`,
      hint: 'まず $d \\cdot n$ を求めます。',
      answer: `$d \\cdot n = 0.8 \\times 0 + 0 \\times 0 + (-0.6) \\times 1 = -0.6$

法線に沿った成分は $-0.6 \\times (0,0,1) = (0,\\; 0,\\; -0.6)$

その 2 倍を引きます。
$(0.8, 0, -0.6) - (0, 0, -1.2) = (0.8,\\; 0,\\; 0.6)$

答えは **$(0.8,\\; 0,\\; 0.6)$** です。

$x$ の $0.8$ はそのまま、$z$ だけ $-0.6$ から $+0.6$ に裏返りました。

**検算** … 長さは $\\sqrt{0.64 + 0.36} = 1$ で変わっていません。
反射は向きを変えるだけなので、長さが変わったら計算が間違っています。`,
    },
    {
      prompt: `ボールを跳ね返らせるコードを書いたら、床にめり込んだまま
細かく震えて止まらなくなりました。原因と対策を答えてください。`,
      hint: '反射させたあと、ボールはまだどこにいますか。',
      answer: `**位置を戻していない**からです。

判定は「$y$ が半径より下がったら」で行いますが、
そのとき既にボールは床の**下**にいます。
速度だけ反転しても、位置は床の下のままです。

すると次のフレームでも判定に引っかかり、また反転 ―
**上向きと下向きを交互に繰り返して、その場で震え続けます。**

対策は 2 つとも必要です。

- **位置を床の上に戻す** … \`position.y = radius\`
- **速度を反転して減衰させる** … 反発係数を掛ける

さらに「十分に遅くなったら止める」を足すと、より安定します。
反発係数が 0.8 なら跳ねるたびに勢いが減りますが、
数値上は完全には 0 にならないので、微小な振動が残り続けます。`,
      answerCode: `if (ball.position.y < radius) {
  ball.position.y = radius;              // まず位置を戻す
  velocity.reflect(new THREE.Vector3(0, 1, 0));
  velocity.multiplyScalar(0.8);          // 勢いを失わせる

  if (Math.abs(velocity.y) < 0.05) {     // 十分に遅くなったら止める
    velocity.y = 0;
  }
}`,
    },
    {
      prompt: `金属の球に \`metalness = 1\`、\`roughness = 0\` を設定したのに、真っ黒です。
なぜですか。また \`roughness\` を上げると、どう変わりますか。`,
      hint: '金属は自分では光りません。何を映していますか。',
      answer: `**映すものが無い**からです。

金属は入ってきた光をほぼすべて反射します。
自分の色で光るのではなく、**周りの景色を映して見えている**だけです。

環境マップ（\`scene.environment\`）が無いと、映すものが何もありません。
反射した向きに何も無いので、**真っ黒**になります。

これは不具合ではなく、物理的に正しい振る舞いです。
真っ暗な部屋の鏡が黒く見えるのと同じことです。

**\`roughness\` を上げると** … 面の凹凸が増えたことになり、
反射がさまざまな向きに散らばります。
映り込みがぼやけ、$1$ に近づくとほとんど見えなくなります。

ただし**環境マップが無ければ、どちらにしても黒いまま**です。
まず \`scene.environment\` を用意してください。`,
      answerCode: `import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';

const pmrem = new THREE.PMREMGenerator(renderer);
scene.environment = pmrem.fromScene(new RoomEnvironment()).texture;

// これで金属が「部屋を映して」見えるようになる`,
    },
  ],
  quiz: [
    {
      q: '反射で裏返るのは、どの成分ですか。',
      choices: [
        '法線に沿った成分',
        '面に沿った成分',
        '両方',
        'どちらも裏返らない',
      ],
      answer: 0,
      explain:
        '面に沿って滑っている分はそのままで、面に向かっている分だけが逆向きになります。だから引くのは「法線に沿った成分の 2 倍」で、1 つぶんで 0、もう 1 つぶんで逆向きになります。',
    },
    {
      q: '`roughness` は物理的に何を表していますか。',
      choices: [
        '面の細かい凹凸による、反射の向きの散らばり',
        '面の色の濃さ',
        '光の量',
        '面の厚み',
      ],
      answer: 0,
      explain:
        '現実の面には細かい凹凸があり、法線が少しずつ違います。反射も散らばるので映り込みがぼやけます。一本一本は計算できないので、散らばりの幅を 1 つの数で表しています。',
    },
    {
      q: '`metalness = 1` の球が真っ黒です。原因はどれですか。',
      choices: [
        '環境マップが無く、映すものが存在しない',
        'roughness が 0 だから',
        '法線が無いから',
        'ライトが弱いから',
      ],
      answer: 0,
      explain:
        '金属は自分の色で光らず、周りを映して見えています。真っ暗な部屋の鏡が黒く見えるのと同じで、不具合ではありません。`scene.environment` を用意する必要があります。',
    },
  ],
};
