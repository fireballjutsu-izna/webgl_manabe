import type { Chapter } from '../types.ts';

export const chapterM02: Chapter = {
  slug: 'm02-matrix-vector',
  part: 'math3d',
  number: 2,
  title: '行列とベクトルの掛け算 ― 中で何が起きているか',
  goal: '行列にベクトルを掛ける計算が手でできるようになり、それが「行ごとの内積」であることが分かります。',
  requires: ['06-matrix', '03-dot'],
  threeApis: ['Vector3.applyMatrix4', 'Matrix4.set', 'Matrix3'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章で「行列は点を入れると動いた点が返る装置」と言いました。
この章では、**その装置の中を開けます**。

とはいえ中身は拍子抜けするほど単純で、**内積を何回かやっているだけ**です。
[](#/ch/03-dot)で身につけた「掛けて足す」が、そのまま出てきます。
`,
    },
    {
      kind: 'md',
      text: `
## 行ごとに、内積を取る

行列にベクトルを掛けるとき、やることはこうです。

- **出てくるベクトルの 1 つ目の成分** … 行列の **1 行目**と、入力ベクトルの内積
- **2 つ目の成分** … **2 行目**との内積
- **3 つ目の成分** … **3 行目**との内積

それだけです。行が 3 つあれば、内積を 3 回やって、3 つ組を作ります。

**「行と、入ってきたベクトル」の内積**。これが行列とベクトルの掛け算のすべてです。
4×4 になっても同じで、内積を 4 回やるだけです。
`,
    },
    {
      kind: 'formula',
      tex: '\\begin{pmatrix} a & b \\\\ c & d \\end{pmatrix}\\begin{pmatrix} x \\\\ y \\end{pmatrix} = \\begin{pmatrix} ax + by \\\\ cx + dy \\end{pmatrix}',
      readAloud:
        '2 行 2 列の行列にベクトルを掛けると、1 行目と内積を取ったものが上の成分、2 行目と内積を取ったものが下の成分になります。行が横に並んでいるので、入力を縦に置いて重ねる、と覚えてください。',
      worked: {
        given: '$\\begin{pmatrix} 2 & 0 \\\\ 0 & 3 \\end{pmatrix}$（x を 2 倍、y を 3 倍する拡大）を、点 $(4,\\; 1)$ に掛けます。',
        steps: [
          { calc: '1 行目 (2, 0) と (4, 1) の内積 : 2x4 + 0x1 = 8', note: 'x が 2 倍になった' },
          { calc: '2 行目 (0, 3) と (4, 1) の内積 : 0x4 + 3x1 = 3', note: 'y が 3 倍になった' },
        ],
        result:
          '$(4,\\; 1) \\to (8,\\; 3)$。**対角線にだけ数がある行列は、成分ごとの拡大**です。0 が並んでいるおかげで、x が y に混ざりません。',
      },
    },
    {
      kind: 'md',
      text: `
## 0 の位置が「混ぜるかどうか」を決める

上の例では、行列の 0 の位置が効いていました。

- $\\begin{pmatrix} 2 & 0 \\\\ 0 & 3 \\end{pmatrix}$ … 対角線だけ。**混ざらない**（拡大）
- $\\begin{pmatrix} 0 & -1 \\\\ 1 & 0 \\end{pmatrix}$ … 対角線が 0。**入れ替わる**（90 度回転）

2 つ目を $(1, 0)$ に掛けてみると $(0, 1)$ になります。右向きが上向きになった ―
つまり 90 度回転です。

**回転が「混ぜ合わせ」だ**と前の章で言ったのは、これのことです。
新しい x は古い y から、新しい y は古い x から作られています。
`,
    },
    {
      kind: 'formula',
      tex: 'R(\\theta) = \\begin{pmatrix} \\cos\\theta & -\\sin\\theta \\\\ \\sin\\theta & \\cos\\theta \\end{pmatrix}',
      readAloud:
        '2D の回転行列です。対角線にコサイン、それ以外にサインが入り、片方だけマイナスが付きます。角度を入れると、その角度だけ回す装置になります。',
      worked: {
        given: '$\\theta = 90^\\circ$（$\\cos = 0$、$\\sin = 1$）で、点 $(1,\\; 0)$ を回します。',
        steps: [
          { calc: '行列は  ( 0  -1 ; 1  0 )', note: 'cos 90 = 0、sin 90 = 1 を入れた' },
          { calc: '1 行目 : 0x1 + (-1)x0 = 0' },
          { calc: '2 行目 : 1x1 +   0 x0 = 1' },
        ],
        result:
          '$(1,\\; 0) \\to (0,\\; 1)$。**右を向いていた矢印が、真上を向きました。** 反時計回りに 90 度 ― [](#/ch/b18-angle)で決めた向きどおりです。$\\cos$ と $\\sin$ が並んでいるのは偶然ではなく、[](#/ch/b20-sin-cos)で見た「角度から座標を作る装置」がそのまま埋め込まれています。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '掛ける向きが決まっています',
      text: `
行列とベクトルの掛け算は、**行列が左、ベクトルが右**です。
$M\\mathbf{v}$ とは書けますが、$\\mathbf{v}M$ とは（この形では）書けません。

これは行の数と列の数を合わせる必要があるためです。
4×4 の行列に 4 つ組を掛けるので、$M$（4×4）と $\\mathbf{v}$（4×1）で辻褄が合います。

ライブラリによっては逆の流儀（行ベクトルを左から掛ける）を使うものもあり、
その場合は行列の中身が転置されています。**Three.js は列ベクトル・左から掛ける流儀**です。
`,
    },
    {
      kind: 'md',
      text: `
## 3D では 3 行、同次座標では 4 行

2D で見てきた形は、そのまま 3D に広がります。

- 3×3 の行列に 3 つ組を掛ける → 内積 3 回
- 4×4 の行列に 4 つ組を掛ける → 内積 4 回

なぜ 4 つ組が要るのかは[](#/ch/m04-homogeneous)で扱います。
いまは「行が増えれば内積の回数が増えるだけ」と押さえてください。

**計算そのものは、最後まで内積です。**
どんなに複雑な変換行列でも、中でやっているのは掛けて足すことだけです。
`,
    },
    {
      kind: 'code',
      title: '手で書くと、こうなっている',
      code: `import * as THREE from 'three';

// three に任せる
const p = new THREE.Vector3(4, 1, 0);
p.applyMatrix4(matrix);

// 中でやっていること（3x3 の部分だけを書き出すと）
function applyByHand(m, v) {
  const e = m.elements;   // 列優先で並んでいることに注意
  return new THREE.Vector3(
    e[0] * v.x + e[4] * v.y + e[8]  * v.z + e[12],
    e[1] * v.x + e[5] * v.y + e[9]  * v.z + e[13],
    e[2] * v.x + e[6] * v.y + e[10] * v.z + e[14],
  );
}

// 行列を自分で書くときは行優先で渡す
const rot90 = new THREE.Matrix3().set(
  0, -1, 0,
  1,  0, 0,
  0,  0, 1,
);`,
    },
  ],
  exercises: [
    {
      prompt: `$\\begin{pmatrix} 3 & 0 \\\\ 0 & 0.5 \\end{pmatrix}$ を、つぎの点に掛けてください。

1. $(2,\\; 4)$
2. $(0,\\; 1)$
3. $(1,\\; 0)$`,
      hint: '行ごとに内積を取ります。0 が多いので、実質は成分ごとの掛け算です。',
      answer: `1. $(3 \\times 2,\\; 0.5 \\times 4) = $ **$(6,\\; 2)$**
2. $(0,\\; 0.5)$
3. $(3,\\; 0)$

2 と 3 に注目してください。$(1, 0)$ と $(0, 1)$ ― つまり**もとの軸**を入れると、
**行列の列がそのまま出てきます**（1 列目が $(3, 0)$、2 列目が $(0, 0.5)$）。

これは偶然ではありません。次の章の主題です。`,
    },
    {
      prompt: `2D の回転行列に $\\theta = 180^\\circ$ を入れると、どんな行列になりますか。
$\\cos 180^\\circ = -1$、$\\sin 180^\\circ = 0$ です。
そして点 $(3,\\; 2)$ を掛けた結果も求めてください。`,
      hint: '行列の 4 か所に値を入れてから、行ごとに内積を取ります。',
      answer: `行列は $\\begin{pmatrix} -1 & 0 \\\\ 0 & -1 \\end{pmatrix}$ になります。

$(3,\\; 2)$ に掛けると $(-3,\\; -2)$。**原点をはさんだ真反対**へ移りました。

180 度の回転が「全部の符号を反転する」のと同じ、という当たり前のことが
行列の形にそのまま出ています。
これは $-1$ を掛けるスカラー倍（[](#/ch/b15-vector-scale)）と同じ結果です。
**180 度回転と、$-1$ 倍は同じもの**、と言えます。`,
    },
    {
      prompt: `行列 $\\begin{pmatrix} 1 & 1 \\\\ 0 & 1 \\end{pmatrix}$ を、つぎの点に掛けてください。
そのあと、**この行列が何をしているか**を言葉で説明してください。

1. $(1,\\; 0)$
2. $(0,\\; 1)$
3. $(2,\\; 3)$`,
      hint: '1 と 2 の答えを見ると、軸がどこへ行ったかが分かります。',
      answer: `1. $(1 \\times 1 + 1 \\times 0,\\; 0 + 0) = $ **$(1,\\; 0)$**。x 軸は動かない
2. $(0 + 1,\\; 0 + 1) = $ **$(1,\\; 1)$**。y 軸が右に傾いた
3. $(2 + 3,\\; 3) = $ **$(5,\\; 3)$**

**y が大きいほど右にずれる**、つまり**斜めに引き倒す**変換です。
これを**せん断**（shear）と呼びます。斜体の文字を作るときの変形と同じです。

高さは変わらず（2 つ目の成分がそのまま）、横だけがずれています。
移動でも回転でも拡大でもない ― **行列は、その 3 つ以外の変換も表せます。**`,
    },
  ],
  quiz: [
    {
      q: '行列にベクトルを掛けるとき、出力の 1 つ目の成分はどう作られますか。',
      choices: [
        '行列の 1 行目と、入力ベクトルの内積',
        '行列の 1 列目と、入力ベクトルの内積',
        '行列の左上の数と、入力の x を掛けたもの',
        '行列の全成分の合計',
      ],
      answer: 0,
      explain:
        '行ごとに内積を取り、その結果を並べます。行が 4 つあれば内積を 4 回やるだけで、計算としては最後まで「掛けて足す」です。',
    },
    {
      q: '$\\begin{pmatrix} 0 & -1 \\\\ 1 & 0 \\end{pmatrix}$ を $(1,\\; 0)$ に掛けると、どうなりますか。',
      choices: ['$(0,\\; 1)$', '$(1,\\; 0)$', '$(-1,\\; 0)$', '$(0,\\; -1)$'],
      answer: 0,
      explain:
        '右向きの矢印が真上を向きました。反時計回りに 90 度回す行列です。対角線が 0 で、たすきがけの位置に値が入っているのが「混ぜ合わせ」＝回転の形です。',
    },
    {
      q: 'Three.js での行列とベクトルの掛け算について、正しいのはどれですか。',
      choices: [
        '行列が左、ベクトルが右（列ベクトルの流儀）',
        'ベクトルが左、行列が右',
        'どちらでもよい',
        'ベクトルは常に 3 つ組で扱う',
      ],
      answer: 0,
      explain:
        '$M\\mathbf{v}$ の形です。ライブラリによっては行ベクトルを左から掛ける流儀もあり、その場合は行列の中身が転置されています。他所のコードを写すときに引っかかるところです。',
    },
  ],
};
