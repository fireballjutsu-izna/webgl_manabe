import type { Chapter } from '../types.ts';

export const chapterX23: Chapter = {
  slug: 'x23-roads',
  part: 'project',
  number: 23,
  title: '道路は作らない ― 隙間が、道になる',
  goal: '道路をモデリングせずに街路網を作れるようになり、道路率が $2$ つのつまみの結果として決まってしまうことを、測った数字で説明できるようになります。',
  requires: ['x22-subdivision', 'b05-ratio'],
  threeApis: ['PlaneGeometry', 'MeshBasicMaterial', 'Scene.background'],
  mathRecall: [
    { slug: 'b05-ratio', note: '割合。全体から街区を引けば道路' },
    { slug: 'x22-subdivision', note: '割るたびに、道路幅ぶんの土地が消える' },
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 引き算で作る

ここがこの街づくりで、いちばん気持ちのいいところです。

**道路をモデリングしません。**

土地を $2$ つに割るとき、境目に**道路の幅だけ隙間をあけて**割ります。

- 左の土地の右端を、切る位置より \`ROAD / 2\` 手前で終わらせる
- 右の土地の左端を、切る位置より \`ROAD / 2\` 先から始める

すると、残った街区の**あいだが自動的に道路になります。**

道路の形を計算する必要も、交差点を特別扱いする必要もありません。
**「街区を置いていったら、隙間が道になっていた」**という順番です。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '石畳の目地と同じ',
      text: `
石畳を敷くとき、目地（すきま）を先に設計する人はいません。
**石を少しずつ離して置けば、目地は勝手にできます。**

しかも、そのやり方なら**交差点という概念が要りません。**

$4$ つの街区の角が集まったところが、たまたま広く空いている ―
それが交差点に見えるだけです。

$T$ 字路も、袋小路も、五叉路も、**全部同じ $1$ つの規則から出てきます。**
道路を「線」として持つと、この $3$ つは別々の処理になります。
`,
    },
    {
      kind: 'md',
      text: `
## 道路率は、指定できない

気持ちよく作れる代わりに、$1$ つ手放しているものがあります。

**「道路を街の $20$ パーセントにしたい」と指定できません。**

道路率は \`ROAD\` と \`MIN_LOT\` の**結果として出てくる**数字だからです。
測ってみると、こうなります。
`,
    },
    {
      kind: 'formula',
      tex: '\\text{道路率} \\;=\\; 1 - \\dfrac{\\sum_i w_i\\,d_i}{C^{2}}',
      readAloud:
        '街全体の面積 $C^2$ から街区の面積の合計を引いて、全体で割ったものが道路率です。道路を数えるのではなく、**街区でないところ**を道路と呼んでいます。',
      worked: {
        given: '街 $120 \\times 120$、道路幅 $3.2$、\\`MIN\\_LOT\\` $= 9$、種 $20260730$ で実測しました。',
        steps: [
          { calc: '街全体 : 120 x 120 = 14400' },
          { calc: '街区 54 個の面積の合計' },
          { calc: '  = 54 x 183.3 = 9899.6' },
          { calc: '道路率 = 1 - 9899.6 / 14400' },
          { calc: '       = 1 - 0.6875 = 0.3125', note: '31.3%' },
          { calc: '道路幅を変えると（MIN_LOT は 9 のまま）' },
          { calc: '  1.6 → 19.2%（街区 74 個）' },
          { calc: '  3.2 → 31.3%（街区 54 個）' },
          { calc: '  8.0 → 52.0%（街区 31 個）' },
        ],
        result:
          '**道路幅を $2$ 倍にすると、道路率は $19.2\\%$ から $31.3\\%$ へ。$2$ 倍ではなく $1.6$ 倍**です。道路を広げると割れる回数が減り、街区の数も $74$ から $54$ に減るので、$2$ つの効果が打ち消し合います。**つまみと結果が比例していないので、目標の道路率から逆算することはできません** ― 出た数字を見て、つまみを回し直すことになります。',
      },
    },
    {
      kind: 'md',
      text: `
## MIN_LOT も、道路率を動かす

やっかいなことに、\`MIN_LOT\` も道路率に効きます。

| \`MIN_LOT\` | 街区 | 道路率 |
|---|---|---|
| $20$ | $16$ 個 | $15.4\\%$ |
| $9$ | $54$ 個 | $31.3\\%$ |
| $5$ | $128$ 個 | $47.5\\%$ |

**道路の幅は $1$ ミリも変えていないのに、道路率が $3$ 倍以上動きます。**

理由は単純で、**割るたびに道路幅ぶんの土地が消える**からです。
細かく割るほど、消える回数が増えます。

$MIN\\_LOT = 5$ では区画の辺が $10$ 前後なので、
$3.2$ の道路は**区画の $3$ 分の $1$ の幅**です。街の半分が道になります。

**「区画の細かさ」と「道の広さ」は、独立ではありません。**
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '実在の街と比べておく',
      text: `
できあがった $31.3\\%$ が高いのか低いのか、目安がないと判断できません。

- **マンハッタン**（碁盤の目、広い街路）… **およそ $25$〜$30\\%$**
- **東京 $23$ 区**（細い道が多い）… **およそ $15$〜$20\\%$**

この街の $31.3\\%$ は、**マンハッタンよりやや広い**あたりです。

遠景の作品としてはこれで構いません ―
道が細すぎると、上から見たときに街区の境目が潰れて $1$ 枚の板に見えます。

**逆に、目線を下ろす作品なら $31\\%$ は広すぎます。**
広い道は「郊外」や「計画都市」に見え、路地の密集した雰囲気は出ません。

**どちらを作りたいかで、\`ROAD\` と \`MIN_LOT\` の組が変わります。**
`,
    },
    {
      kind: 'sandbox',
      title: '道路幅を変えて、道路率を測る',
      guide: { focus: ['隙間をあけて割る', '道路率を測る'] },
      code: `import * as THREE from 'three';

const SEED = 20260730;
const CITY = 120;
const ROAD = 3.2;     // 1.6 と 8 も試してください
const MIN_LOT = 9;

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a24);   // 街区でないところ ＝ 道路の色

// 街 120 がちょうど収まるように、画面の縦横比から左右を決める
const HALF = 78;   // 街 120 に対して余白を持たせる
const aspect = window.innerWidth / window.innerHeight;
const camera = new THREE.OrthographicCamera(-HALF * aspect, HALF * aspect, HALF, -HALF, 0.1, 200);
camera.position.set(0, 80, 0);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

function makeRandom(seed) {
  let state = seed >>> 0;
  return function () {
    state = (state + 0x6d2b79f5) >>> 0;
    let t = state;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ---- 隙間をあけて割る ---- */
// 切る位置の両側に ROAD / 2 ずつ空ける。それだけで、あいだが道路になる

function splitLots(rect, rand, out) {
  const canX = rect.w > MIN_LOT * 2 + ROAD;
  const canZ = rect.d > MIN_LOT * 2 + ROAD;
  if (!canX && !canZ) { out.push(rect); return out; }

  const alongX = canX && (!canZ || rect.w >= rect.d);
  const length = alongX ? rect.w : rect.d;
  const cut = length * (0.35 + rand() * 0.3);

  if (alongX) {
    // 左の土地は cut の手前で終わり、右の土地は cut の先から始まる
    splitLots({ x: rect.x, z: rect.z, w: cut - ROAD / 2, d: rect.d }, rand, out);
    splitLots({ x: rect.x + cut + ROAD / 2, z: rect.z, w: rect.w - cut - ROAD / 2, d: rect.d }, rand, out);
  } else {
    splitLots({ x: rect.x, z: rect.z, w: rect.w, d: cut - ROAD / 2 }, rand, out);
    splitLots({ x: rect.x, z: rect.z + cut + ROAD / 2, w: rect.w, d: rect.d - cut - ROAD / 2 }, rand, out);
  }
  return out;
}

const lots = splitLots({ x: -CITY / 2, z: -CITY / 2, w: CITY, d: CITY }, makeRandom(SEED), []);

// 街区だけを描く。道路は 1 枚も描かない（背景がそのまま道になる）
for (const lot of lots) {
  const plane = new THREE.Mesh(
    new THREE.PlaneGeometry(lot.w, lot.d),
    new THREE.MeshBasicMaterial({ color: 0x3a5470 }),
  );
  plane.rotation.x = -Math.PI / 2;
  plane.position.set(lot.x + lot.w / 2, 0, lot.z + lot.d / 2);
  scene.add(plane);
}

/* ---- 道路率を測る ---- */
// 道路を数えるのではなく、街区でないところを道路と呼ぶ

const lotArea = lots.reduce((sum, lot) => sum + lot.w * lot.d, 0);
const roadRatio = 1 - lotArea / (CITY * CITY);

const readout = document.createElement('div');
readout.textContent =
  'ROAD ' + ROAD + '  /  MIN_LOT ' + MIN_LOT + '\\n' +
  '街区   ' + lots.length + ' 個（合計 ' + lotArea.toFixed(0) + '）\\n' +
  '街全体 ' + (CITY * CITY) + '\\n' +
  '道路率 ' + (roadRatio * 100).toFixed(1) + ' %';
readout.style.cssText =
  'position:absolute; bottom:16px; left:16px; color:#9fb4d8;' +
  'font:12px/1.7 ui-monospace, monospace; white-space:pre; pointer-events:none;' +
  'background:rgba(10,12,18,0.78); padding:8px 10px; border-radius:5px;';
document.body.appendChild(readout);

renderer.render(scene, camera);

window.addEventListener('resize', () => {
  const next = window.innerWidth / window.innerHeight;
  camera.left = -HALF * next;
  camera.right = HALF * next;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
  renderer.render(scene, camera);
});`,
      caption:
        '**描いているのは街区だけです。**道路に見えているのは、背景がそのまま透けているところ ― 交差点も $T$ 字路も、$1$ 枚も描いていません。`ROAD` を $1.6$ にすると道路率 $19.2\\%$、$8$ にすると $52.0\\%$。**そして `ROAD` を触っていないのに、`MIN_LOT` を $5$ にすると $47.5\\%$ まで上がります。** つまみが独立していないことを、数字で確かめてください。',
    },
    {
      kind: 'md',
      text: `
## 道路を「持たない」ことの代償

隙間で作る方法には、当然できないこともあります。

- **道路の上に何かを置けない。** 車を走らせるには、道路の中心線が要ります
- **道が繋がっている保証がない。** 見た目には繋がりますが、
  「$A$ 地点から $B$ 地点へ行ける」を保証する構造は持っていません
- **道幅を場所ごとに変えられない。** 大通りと路地の区別が付けられません

$1$ つめは[](#/ch/p08-city-motion)で実際に困ります。
そのときは**街区の境目から道路の中心線を割り出す**ことになります ―
隙間の真ん中を通れば、それが道です。

**「持たない」で済むうちは持たない。** 必要になったら、そのとき導きます。
最初から道路のグラフを組み立てておくより、
**必要になってから隙間から導くほうが、コードも概念も少なくて済みます。**
`,
    },
  ],
  exercises: [
    {
      prompt: `道路率を $20\\%$ にしたい。\`ROAD\` と \`MIN_LOT\` をどう決めますか。

計算で $1$ 回で求められますか。`,
      hint: '実測の表を見てください。$2$ つのつまみは独立に効いていますか。',
      answer: `**計算では求められません。試して測る、を繰り返します。**

**なぜ計算できないか**

道路率は $2$ つのつまみの結果です。しかも、

- \`ROAD\` を増やす → 道路率は上がるが、**割れる回数が減って街区が減る**
- \`MIN_LOT\` を増やす → 割る回数が減り、**道路率は下がる**

$2$ つが互いに打ち消し合うので、閉じた式にはなりません。

実測でも、道路幅を $2$ 倍（$1.6 \\to 3.2$）にして道路率は $1.6$ 倍にしかなっていません。

**実際の手順**

$20\\%$ に近いのは、表からこのあたりです。

- \`ROAD\` $= 1.6$、\`MIN_LOT\` $= 9$ … $19.2\\%$

ほぼ目標どおりです。ここから微調整するなら \`ROAD\` を $1.7$ にする、といった具合です。

**測れるようにしておくことが本体**

大事なのは「計算できないこと」ではなく、
**$1$ 秒で測れる状態にしてあること**です。

道路率を画面に出しておけば、つまみを回して数字を見るだけで決まります。

**逆算できない量は、測って合わせる。**
手続き的生成では、これがほとんどの場面で正解になります。`,
    },
    {
      prompt: `\`MIN_LOT\` を $3$ にすると、道路率はどうなると予想しますか。

そして、そのとき街区はどう見えるでしょう。`,
      hint: '$MIN\\_LOT = 5$ で $47.5\\%$、区画の辺は $10$ 前後でした。道路幅は $3.2$ のままです。',
      answer: `**道路率は $60\\%$ を超え、街区より道路のほうが広くなります。**

**なぜそうなるか**

$MIN\\_LOT = 3$ なら、区画の辺は $6$ 前後です。

そのあいだに幅 $3.2$ の道が入るので、
**区画と道路がほぼ同じ幅**になります。

$MIN\\_LOT = 5$ ですでに $47.5\\%$ なので、$3$ では過半数が道路です。

**見た目**

街区は小さな点のように散らばり、**格子状の広い道**だけが目立ちます。

これは街ではなく、**駐車場**や**区画整理前の更地**に見えます。

**教訓 ― 下限は道路幅との比で決める**

\`MIN_LOT\` を単独で決めてはいけません。

目安は **\`MIN_LOT\` が \`ROAD\` の $3$ 倍以上**です。

- $MIN\\_LOT = 9$、$ROAD = 3.2$ … 比 $2.8$。道路率 $31\\%$（すでにやや広い）
- $MIN\\_LOT = 20$、$ROAD = 3.2$ … 比 $6.3$。道路率 $15\\%$（東京の $23$ 区あたり）

**つまみが独立でないときは、比を持つほうを見てください。**
$2$ つの数字それぞれではなく、その比が絵を決めています。`,
    },
    {
      prompt: `この作り方では、道路を $1$ 本も引いていません。

それでも道路が見えるのはなぜでしょう。この構造のおかげで**書かずに済んだ処理**を $2$ つ挙げてください。`,
      hint: '$4$ つの街区の角が集まる場所は、何と呼ばれますか。',
      answer: `**街区でないところが、そのまま道路として見えているからです。**

**書かずに済んだ処理**

**1. 交差点**

道路を線として持つと、交差点は特別扱いが要ります。

- $2$ 本の線が交わる点を求める
- 角を丸める、あるいは四角い板を置く
- $T$ 字路・十字路・五叉路で場合分けする

隙間で作れば、**$4$ つの街区の角が空いているだけ**です。
場合分けは $1$ つもありません。

**2. 道路の端の処理**

線で持つと、道路の端（袋小路、街の外周）をどう閉じるかを決める必要があります。

隙間なら、**街区が無いところは全部道路**なので、端という概念がありません。

**ほかにも**

- 道路どうしの重なり（同じ場所に $2$ 本引いてしまう事故）
- 道路と街区の食い込み（座標の計算違い）

**どちらも、構造的に起こりえません。**
街区の座標がそのまま道路の境界だからです。

**「作らない」がいちばん強い最適化**

速さの話ではありません。**書かないコードには不具合が入りません。**

道路を作らなかったので、道路に関する不具合が $1$ つも存在しない ―
これは、どんなに丁寧に道路を実装しても届かない状態です。`,
    },
  ],
  quiz: [
    {
      q: 'この街づくりで、道路はどうやって作られていますか。',
      choices: [
        '作っていない。街区を道路の幅だけ離して置くので、あいだが自動的に道路になる',
        '街区を置いたあと、隙間を計算して板を敷いている',
        '格子状の道路を先に引き、そのあいだに街区を置いている',
        '道路のテクスチャを地面に貼っている',
      ],
      answer: 0,
      explain:
        '土地を 2 つに割るとき、切る位置の両側に道路幅の半分ずつ空けます。残った街区のあいだが道路です。交差点も T 字路も袋小路も、この 1 つの規則から出てくるので、場合分けが 1 つもありません。書かなかったコードには不具合が入らない、という意味でもいちばん強い作り方です。',
    },
    {
      q: '道路幅を 1.6 から 3.2 に倍にすると、道路率はどうなりますか。',
      choices: [
        '19.2% から 31.3% へ。2 倍ではなく 1.6 倍にしかならない',
        'ちょうど 2 倍の 38.4% になる',
        '変わらない',
        '半分になる',
      ],
      answer: 0,
      explain:
        '道路を広げると割れる回数が減り、街区の数も 74 個から 54 個に減ります。「道が広くなる」と「割る回数が減る」が打ち消し合うので、比例しません。だから目標の道路率から逆算することはできず、測ってつまみを回し直すことになります。',
    },
    {
      q: '`ROAD` を変えずに `MIN_LOT` を 9 から 5 にすると、道路率はどうなりますか。',
      choices: [
        '31.3% から 47.5% へ上がる。細かく割るほど、道路幅ぶんの土地が消える回数が増えるから',
        '変わらない。道路幅を変えていないから',
        '下がる。街区が増えるから',
        '街区の数だけが変わり、面積の合計は変わらない',
      ],
      answer: 0,
      explain:
        '割るたびに道路幅ぶんの土地が消えるので、割る回数が増えれば道路の総面積も増えます。MIN_LOT = 5 では区画の辺が 10 前後で、幅 3.2 の道は区画の 3 分の 1 ― 街の半分が道になります。区画の細かさと道の広さは独立ではなく、目安としては MIN_LOT が ROAD の 3 倍以上あると落ち着きます。',
    },
  ],
};
