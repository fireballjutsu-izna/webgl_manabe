import type { Chapter } from '../types.ts';

export const chapterM36: Chapter = {
  slug: 'm36-catmull',
  part: 'math3d',
  number: 36,
  title: 'Catmull-Rom ― 置いた点を、全部通る',
  goal: '通ってほしい点を並べるだけで道が作れるようになり、ふくらみすぎたときの直し方も分かります。',
  requires: ['12-curve', 'b14-vector-sub'],
  threeApis: [
    'CatmullRomCurve3',
    'CatmullRomCurve3.curveType',
    'CatmullRomCurve3.tension',
    'CatmullRomCurve3.closed',
    'Curve.getSpacedPoints',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 「ここを通ってほしい」に応える

前の章のベジェ曲線は強力ですが、実務では面倒な点があります。

**通ってほしい場所を指定できない。**

「この 5 箇所を順に通るカメラワークが欲しい」と思っても、
ベジェでは制御点を試行錯誤で調整することになります。
しかも繋ぎ目の向きまで自分で管理しなければなりません。

{{制御点}}をそのまま通ってくれる曲線があれば、話はずっと簡単です。
それが **Catmull-Rom スプライン**です。
`,
    },
    {
      kind: 'md',
      text: `
## 前後の点から、通過する向きを決める

考え方は素直です。

ある点 $P_i$ を通るとき、**そこでどちらを向いて通るか**を決めればよい。
そして向きは、**前後の点を見れば決まります。**

$P_{i-1}$ から $P_{i+1}$ へ向かう向き ―
つまり**両隣を結んだ線と平行**に通り抜ければ、自然な曲がりになります。

$P_i$ での向きは $\\frac{P_{i+1} - P_{i-1}}{2}$。
[](#/ch/b14-vector-sub)の引き算がそのまま出てきます。

**通る点と、そこでの向きが決まれば、区間ごとにベジェ曲線と同じ計算ができます。**
Catmull-Rom は「ベジェの制御点を、前後の点から自動で決める仕組み」だと言えます。
`,
    },
    {
      kind: 'formula',
      tex: 'm_i = \\frac{P_{i+1} - P_{i-1}}{2}',
      readAloud:
        '点 P i を通過するときの向きは、その前後の点を結んだベクトルの半分です。両隣を見れば、そこをどちらへ向かって通り抜けるかが決まります。',
      worked: {
        given:
          '$P_0 = (0,\\;0)$、$P_1 = (2,\\;3)$、$P_2 = (6,\\;1)$、$P_3 = (8,\\;4)$ が並んでいます。$P_1$ と $P_2$ での通過方向を求めます。',
        steps: [
          { calc: 'P1 での向き : (P2 - P0) / 2' },
          { calc: '         = ((6,1) - (0,0)) / 2 = (3, 0.5)' },
          { calc: 'P2 での向き : (P3 - P1) / 2' },
          { calc: '         = ((8,4) - (2,3)) / 2 = (3, 0.5)' },
          { calc: '【意味】どちらも右へ 3、上へ 0.5 の向きで通る' },
        ],
        result:
          '$P_1$ でも $P_2$ でも **$(3,\\; 0.5)$** ― たまたま同じ向きになりました。**$P_1$ は上に飛び出た点なのに、そこでの向きは水平に近い**のが要点です。両隣が低いので、山の頂上を水平に通り抜ける形になります。これが「置いた点で折れずに通る」仕組みです。',
      },
    },
    {
      kind: 'demo',
      id: 'curve-editor',
      caption:
        '「曲線の種類」を Catmull-Rom にして、制御点をドラッグしてください。曲線が必ずすべての点を通ります。3 次ベジェに切り替えると、途中の 2 点は通らなくなります。同じ点の配置で見比べるのが分かりやすいです。',
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '両端では、前後の片方がありません',
      text: `
最初の点には「前の点」が、最後の点には「次の点」がありません。
だから通過方向を決められません。

three は端の点を複製して補う（既定）か、closed を true にして輪にするかで対処します。

輪にする場合、最初と最後が繋がるので、点が 1 つ余分に要るように見えて要りません。
「道が最後で急に折れる」ときは、端の扱いを疑ってください。
`,
    },
    {
      kind: 'md',
      text: `
## 点が偏ると、ふくらみすぎる

Catmull-Rom には有名な弱点があります。

**点の間隔が極端に違うと、狭いところで大きくふくらみます。**
ときには曲線が自分自身と交差することさえあります。

原因は「通過方向を前後の点だけで決めている」ことです。
遠くの点が強く効いてしまい、狭い区間には過剰な向きが与えられます。

**three にはこれを抑える設定があります。**

- \`curveType: 'centripetal'\`（既定） … 点の間隔を考慮する。**ふくらみと自己交差が起きにくい**
- \`curveType: 'chordal'\` … 間隔をより強く考慮する
- \`curveType: 'catmullrom'\` … 素朴な形。\`tension\` で張り具合を調整できる

**まず既定のままで試し、ふくらみが気になったら点の間隔を見直す** ―
これが実務の順序です。設定より、点の置き方のほうが効きます。
`,
    },
    {
      kind: 'code',
      title: 'Catmull-Rom を使う',
      code: `import * as THREE from 'three';

const points = [
  new THREE.Vector3(-8, 0, 0),
  new THREE.Vector3(-3, 4, 2),
  new THREE.Vector3(2, 1, -3),
  new THREE.Vector3(7, 3, 0),
];

// 全部の点を通る曲線
const curve = new THREE.CatmullRomCurve3(points);

// 輪にする（最後と最初が繋がる）
const loop = new THREE.CatmullRomCurve3(points, true);

// ふくらみを抑える（既定は centripetal）
curve.curveType = 'centripetal';

// 素朴な形にして、張り具合を自分で決める
curve.curveType = 'catmullrom';
curve.tension = 0.3;             // 小さいほど直線的、大きいほどふくらむ

// 曲線の上に等間隔で並べる（配置の下敷きとして便利）
for (const p of curve.getSpacedPoints(20)) {
  const tree = new THREE.Mesh(treeGeometry, treeMaterial);
  tree.position.copy(p);
  scene.add(tree);
}

// 管にして可視化する
const tube = new THREE.Mesh(
  new THREE.TubeGeometry(curve, 100, 0.15, 8, false),
  material,
);`,
    },
    {
      kind: 'md',
      text: `
## どちらを選ぶか

**Catmull-Rom** … 通ってほしい場所が決まっているとき。
カメラの巡回、レール、道路、キャラクタの移動経路。
**点を置くだけでよい**ので、データから道を作る場合にも向きます。

**ベジェ** … 形そのものを狙って作りたいとき。
フォントの字形、UI のアニメーション曲線、デザインツールでの作図。
**制御点で微妙な張り具合まで決められる**のが利点です。

**迷ったら Catmull-Rom** です。
「ここを通ってほしい」のほうが、人にとって自然な指定だからです。

なお **Catmull-Rom は内部でベジェと同じ計算をしています。**
別物ではなく、制御点の決め方が違うだけです。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '点は少ないほうがきれいです',
      text: `
なめらかにしたいからと点を増やすと、かえってガタつきます。
曲線は少ない点から自然な形を作るための道具です。

まず 4〜6 点で大まかな流れを作り、
どうしても曲がりが足りないところにだけ足してください。

密に置いた点は、結局のところ折れ線を描いているのと同じです。
`,
    },
  ],
  exercises: [
    {
      prompt: `$P_0 = (0,\\;0)$、$P_1 = (4,\\;2)$、$P_2 = (6,\\;-2)$、$P_3 = (10,\\;0)$ のとき、
$P_1$ と $P_2$ での通過方向を手で計算してください。`,
      hint: '両隣を引いて 2 で割ります。',
      answer: `**$P_1$ での向き** … $\\frac{P_2 - P_0}{2} = \\frac{(6,-2) - (0,0)}{2} = (3,\\; -1)$

**$P_2$ での向き** … $\\frac{P_3 - P_1}{2} = \\frac{(10,0) - (4,2)}{2} = (3,\\; -1)$

どちらも **$(3,\\; -1)$** です。

$P_1$ は上（$y = 2$）、$P_2$ は下（$y = -2$）にあるのに、
**通過方向はどちらも右下向きで同じ**になりました。

両隣を見て向きを決めるので、「点そのものの高さ」ではなく
「その前後で全体がどちらへ向かっているか」が効くからです。

なお $P_0$ と $P_3$ の通過方向は、この式では決められません。
前後の片方が存在しないからです。three は端の点を複製して補います。`,
    },
    {
      prompt: `Catmull-Rom で道を作ったら、点が密に並んでいる区間で
曲線が大きくふくらんで、自分自身と交差してしまいました。
原因と、対策を 2 つ答えてください。`,
      hint: '通過方向は、どの点から決まっていましたか。',
      answer: `**遠くの点が、狭い区間に過剰な向きを与えている**からです。

通過方向は $\\frac{P_{i+1} - P_{i-1}}{2}$ で決まります。
$P_{i-1}$ が遠く、$P_i$ と $P_{i+1}$ が近いと、
**短い区間に長いベクトルぶんの勢いが与えられ**、はみ出します。

**対策 1 ― 点の間隔をならす。** いちばん確実です。
極端に密なところを間引くか、疎なところに足して、間隔を揃えます。

**対策 2 ― \`curveType\` を \`'centripetal'\` にする。**
点の間隔を考慮して向きを決めるので、ふくらみと自己交差が起きにくくなります。
three の既定はこれなので、明示的に \`'catmullrom'\` にしていた場合は戻すだけです。

**順序としては 1 が先**です。設定より、点の置き方のほうが効きます。`,
      answerCode: `// まず点の間隔を見直す
const curve = new THREE.CatmullRomCurve3(evenlySpacedPoints);

// それでも足りなければ
curve.curveType = 'centripetal';   // 既定。間隔を考慮する
// もしくは
curve.curveType = 'catmullrom';
curve.tension = 0.3;               // 張りを弱めて、ふくらみを抑える`,
    },
    {
      prompt: `曲線に沿って、木を 30 本**等間隔で**並べたい。
どう書きますか。また \`getPoint()\` を 30 回呼ぶ方法との違いは何ですか。`,
      hint: '$t$ を等分するのと、距離を等分するのは同じですか。',
      answer: `**\`getSpacedPoints(30)\`** を使います。距離で等分した点を返してくれます。

\`getPoint(i / 30)\` を 30 回呼ぶと、**$t$ を等分することになります。**
ところが $t$ は道のりではないので、
**曲がりのきつい区間では詰まり、ゆるい区間では空きます。**

木の間隔がばらつくので、並べたときに不自然に見えます。

この「$t$ と距離は別物」という話は、次の章の主題です。
いまは **並べるなら \`getSpacedPoints\`、走らせるなら \`getPointAt\`** と覚えておけば足ります。

なお \`getSpacedPoints(n)\` が返すのは $n + 1$ 個です。
両端を含むので、区間の数が $n$ になります。30 本並べたいなら 29 を渡すか、
返ってきた配列の最後を捨ててください。`,
      answerCode: `// 距離で等分する（両端を含むので 30 個ほしければ 29 を渡す）
for (const p of curve.getSpacedPoints(29)) {
  const tree = new THREE.Mesh(treeGeometry, treeMaterial);
  tree.position.copy(p);
  scene.add(tree);
}`,
    },
  ],
  quiz: [
    {
      q: 'Catmull-Rom 曲線が通る点はどれですか。',
      choices: [
        '置いたすべての点',
        '最初と最後だけ',
        '中間の点だけ',
        'どれも通らない',
      ],
      answer: 0,
      explain:
        'これがベジェとの最大の違いです。「ここを通ってほしい」という指定がそのまま使えるので、カメラの巡回やレールに向きます。',
    },
    {
      q: 'ある点での通過方向は、何から決まりますか。',
      choices: [
        'その点の前後 2 点を結んだ向き',
        'その点の座標だけ',
        '最初と最後の点',
        '曲線の全長',
      ],
      answer: 0,
      explain:
        '$\\frac{P_{i+1} - P_{i-1}}{2}$ です。両隣を結んだ線と平行に通り抜けるので、山の頂上は水平に通ることになり、折れずに繋がります。',
    },
    {
      q: '点の間隔が偏ったときにふくらみを抑える設定はどれですか。',
      choices: [
        "`curveType: 'centripetal'`",
        '`closed: true`',
        '`tension: 1`',
        '点をもっと増やす',
      ],
      answer: 0,
      explain:
        '点の間隔を考慮して通過方向を決めるので、狭い区間へ過剰な勢いが与えられなくなります。ただし、まず点の間隔をならすほうが効果的です。',
    },
  ],
};
