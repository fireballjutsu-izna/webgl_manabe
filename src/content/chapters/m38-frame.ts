import type { Chapter } from '../types.ts';

export const chapterM38: Chapter = {
  slug: 'm38-frame',
  part: 'math3d',
  number: 38,
  title: '曲線に沿った姿勢 ― 進む向きだけでは足りない',
  goal: '曲線に沿って物体を正しく向けられるようになり、パイプがねじれる不具合の原因が分かります。',
  requires: ['m37-arclength', 'm15-lookat'],
  threeApis: [
    'Curve.getTangentAt',
    'Curve.computeFrenetFrames',
    'TubeGeometry',
    'Object3D.up',
    'Quaternion.setFromUnitVectors',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 進む向きは分かる。では「上」は？

曲線に沿って車を走らせます。進行方向は \`getTangentAt()\` で取れます。

ところが**それだけでは姿勢が決まりません。**

[](#/ch/m15-lookat)で見たとおり、前を向く向きが決まっても、
**その軸のまわりにひねる自由が残っている**からです。
車が横倒しになっているのか、正しく起きているのかは、まだ決まっていません。

必要なのは 3 本の軸です。

- **接線** … 進む向き。これは曲線から取れる
- **上** … どこを上とするか
- **右** … 残りの 1 本。外積で作れる
`,
    },
    {
      kind: 'md',
      text: `
## いちばん簡単な方法 ― 世界の上を使う

道路や地面の上を走るものなら、これで足ります。

**「上」は世界の $(0, 1, 0)$ ということにして、あとは外積で埋める。**

$\\hat{x} = \\hat{up} \\times \\hat{z}$、$\\hat{y} = \\hat{z} \\times \\hat{x}$ ―
[](#/ch/m15-lookat)でやった手順そのものです。

three なら \`lookAt()\` に「いまの位置＋接線」を渡すだけで済みます。

**弱点** … 曲線が真上を向くと破綻します。
接線が $(0,1,0)$ と平行になり、外積がゼロベクトルになるからです。
ジェットコースターのループのように**垂直に上がる道では使えません。**
`,
    },
    {
      kind: 'formula',
      tex: '\\hat{t} = \\frac{P(u + h) - P(u)}{|P(u + h) - P(u)|}',
      readAloud:
        '接線は、少し先の点と現在の点の差を長さ 1 にしたものです。three が内部でやっているのもこれで、h は非常に小さな値です。',
      worked: {
        given:
          '曲線の上の点 $P(0.5) = (2,\\; 1,\\; 0)$、少し先の $P(0.51) = (2.06,\\; 1.08,\\; 0)$ から、接線と姿勢の 3 軸を求めます。',
        steps: [
          { calc: '差 : (2.06, 1.08, 0) - (2, 1, 0) = (0.06, 0.08, 0)' },
          { calc: '長さ : √(0.0036 + 0.0064) = 0.1' },
          { calc: '接線 z : (0.6, 0.8, 0)', note: '長さ 1 に直した' },
          { calc: '右 x : up × z = (0,1,0)×(0.6,0.8,0)' },
          { calc: '     = (1x0 - 0x0.8, 0x0.6 - 0x0, 0x0.8 - 1x0.6)' },
          { calc: '     = (0, 0, -0.6) を正規化 → (0, 0, -1)' },
          { calc: '上 y : z × x = (0.6,0.8,0)×(0,0,-1) = (-0.8, 0.6, 0)' },
        ],
        result:
          '3 軸は $(0,0,-1)$、$(-0.8,\\;0.6,\\;0)$、$(0.6,\\;0.8,\\;0)$。**「上」が世界の $(0,1,0)$ ではなく $(-0.8, 0.6, 0)$ になっている**ことに注目してください。坂を登っているので、車の上も一緒に傾きます。世界の up はあくまで**参考にする向き**で、そのまま使われるわけではありません。',
      },
    },
    {
      kind: 'md',
      text: `
## ねじれない枠 ― Frenet フレーム

垂直に上がる道でも壊れない方法が要ります。

そこで **前のフレームの「上」を持ち回る**という手を使います。

1. 最初の点で、適当に 3 軸を決める
2. 次の点では、**接線だけ更新し、上は前の値をできるだけ保つ**
3. これを最後まで繰り返す

こうすると、世界の上を参照しないので**真上を向いても破綻しません。**
「前の姿勢から、必要最小限だけ回す」という考え方で、
[](#/ch/m12-axis-angle)の「2 つの向きを繋ぐ最小の回転」がそのまま使えます。

three ではこれを \`computeFrenetFrames()\` が計算してくれます。
\`TubeGeometry\` が内部で呼んでいるのも、これです。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '輪にすると、始点と終点で継ぎ目が出ます',
      text: `
前の姿勢を持ち回る方式には弱点があります。

一周して戻ってきたとき、最初の姿勢と一致するとは限りません。
曲線のねじれ具合によっては、何度かずれた姿勢で戻ってきます。

輪にしたパイプの継ぎ目でテクスチャがずれるのは、これが原因です。

three の computeFrenetFrames は closed が true のとき、
ずれを全体に少しずつ配って辻褄を合わせています。
`,
    },
    {
      kind: 'code',
      title: '曲線に沿って向ける',
      code: `import * as THREE from 'three';

// ① いちばん簡単：世界の上を使う（道路など）
function follow(u) {
  const p = curve.getPointAt(u);
  const t = curve.getTangentAt(u);
  car.position.copy(p);
  car.lookAt(p.clone().add(t));      // +z が進行方向を向く
}

// ② ねじれない枠を作る（ループのある道でも壊れない）
const frames = curve.computeFrenetFrames(200, false);
// frames.tangents / frames.normals / frames.binormals

function followFrame(u) {
  const i = Math.min(Math.round(u * 200), 200);
  const m = new THREE.Matrix4().makeBasis(
    frames.binormals[i],   // 右
    frames.normals[i],     // 上
    frames.tangents[i],    // 前
  );
  car.position.copy(curve.getPointAt(u));
  car.quaternion.setFromRotationMatrix(m);
}

// ③ パイプにする（中で ② をやっている）
const tube = new THREE.Mesh(
  new THREE.TubeGeometry(curve, 200, 0.2, 12, false),
  material,
);

// カーブで車体を傾けたいとき（バンク）
const bank = -curvature * speed * 0.3;
car.rotateZ(bank);`,
    },
    {
      kind: 'md',
      text: `
## どちらを選ぶか

**世界の上を使う（\`lookAt\`）**

- 道路、レール、地面を走るもの
- 川、パイプライン、配線
- **真上を向かないと分かっている**なら、これで十分

**Frenet フレーム（\`computeFrenetFrames\`）**

- ジェットコースター、宙返りする飛行機
- 3D 空間を自由に曲がるパイプ
- **上下が反転しうる**なら必須

判断は 1 点です ―
**曲線が垂直に近くなることがあるか。** あるなら後者、無いなら前者。

そして **\`TubeGeometry\` を使うなら意識する必要はありません。**
中で Frenet フレームを計算しているので、
ジェットコースターの形でもねじれずに管が作れます。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでが曲線です

4 章で分かったことを並べます。

- **ベジェ** … lerp の入れ子。制御点は通らない
- **Catmull-Rom** … 置いた点を全部通る。向きは両隣から決まる
- **弧長** … $t$ は道のりではない。等速にするには測り直す
- **姿勢** … 進む向きだけでは決まらない。上をどう決めるか

**次からは、第2部の最後の 3 章です。**
形を「作る」のではなく、**同じ手順を繰り返して生やす**話に変わります。
`,
    },
  ],
  exercises: [
    {
      prompt: `曲線の上の点 $P(0.3) = (1,\\; 2,\\; 0)$、少し先の $P(0.31) = (1.08,\\; 2.06,\\; 0)$ です。
接線を求め、世界の上 $(0,1,0)$ を使って「右」の軸も計算してください。`,
      hint: '差をとって正規化し、外積を 1 回です。',
      answer: `**接線** … 差は $(0.08,\\; 0.06,\\; 0)$、長さは $\\sqrt{0.0064 + 0.0036} = 0.1$
→ $\\hat{t} = (0.8,\\; 0.6,\\; 0)$

**右** … $\\hat{up} \\times \\hat{t} = (0,1,0) \\times (0.8, 0.6, 0)$

$= (1 \\times 0 - 0 \\times 0.6,\\;\\; 0 \\times 0.8 - 0 \\times 0,\\;\\; 0 \\times 0.6 - 1 \\times 0.8)$
$= (0,\\; 0,\\; -0.8)$

正規化して **$(0,\\; 0,\\; -1)$** です。

曲線が $xy$ 平面の中にあるので、「右」は $z$ 方向を向きます。当然の結果です。

なお**「上」は $(0,1,0)$ のままにはなりません。**
$\\hat{t} \\times \\hat{x} = (0.8,0.6,0) \\times (0,0,-1) = (-0.6,\\; 0.8,\\; 0)$ ―
坂を登っているぶん、上も傾きます。`,
    },
    {
      prompt: `ジェットコースターのループ（垂直に一回転する部分）で、
\`lookAt\` を使って車両を向けたところ、頂上付近で車両が激しく回転しました。
原因と対策を答えてください。`,
      hint: '接線が真上を向くと、何が計算できなくなりますか。',
      answer: `**接線が世界の up $(0,1,0)$ と平行になり、外積がゼロベクトルになる**からです。

「右」の軸が $\\hat{up} \\times \\hat{t}$ で決まらなくなり、
[](#/ch/m15-lookat)で見た縮退がそのまま起きます。

three は軸をわずかにずらして計算を続けますが、
**その付近では入力のごくわずかな違いが姿勢を大きく変える**ため、激しく回ります。

**対策は Frenet フレームです。** \`computeFrenetFrames()\` は
世界の up を参照せず、**前のフレームの「上」を持ち回ります。**
だから接線が真上を向いても破綻しません。

判断の目安は 1 つ ―
**曲線が垂直に近くなることがあるなら Frenet、無いなら \`lookAt\` で十分**です。`,
      answerCode: `const frames = coaster.computeFrenetFrames(300, true);   // 輪なら true

function ride(u) {
  const i = Math.min(Math.round(u * 300), 300);
  const m = new THREE.Matrix4().makeBasis(
    frames.binormals[i],
    frames.normals[i],
    frames.tangents[i],
  );
  cart.position.copy(coaster.getPointAt(u));
  cart.quaternion.setFromRotationMatrix(m);
}`,
    },
    {
      prompt: `\`TubeGeometry\` で輪になったパイプを作ったところ、
1 箇所だけテクスチャの継ぎ目が見えました。なぜですか。`,
      hint: '前の姿勢を持ち回る方式で、一周したらどうなりますか。',
      answer: `**一周して戻ってきたとき、最初の姿勢と一致しなかった**からです。

Frenet フレームは「前の姿勢をできるだけ保つ」方式なので、
曲線のねじれ具合によっては、**一周したときに何度かずれた姿勢**で戻ってきます。

そのずれが、始点と終点の継ぎ目として見えます。

**three は \`closed\` を \`true\` にすると、このずれを全体に少しずつ配って**辻褄を合わせます。
だから \`new THREE.TubeGeometry(curve, 200, 0.2, 12, true)\` のように、
**最後の引数を \`true\` にする**のが対策です。

それでも完全には消えないことがあります。
ずれを全体に配るので、**継ぎ目は消えるかわりに、パイプ全体がわずかにねじれる**からです。
気になる場合は、テクスチャを継ぎ目が目立たない模様（無地、ノイズ）にするか、
継ぎ目を見えない位置へ回すのが現実的です。`,
    },
  ],
  quiz: [
    {
      q: '曲線に沿って物体を向けるとき、接線だけでは足りないのはなぜですか。',
      choices: [
        '接線のまわりにひねる自由が残っているから',
        '接線は長さが 1 でないから',
        '接線は曲線から取れないから',
        '接線は位置を含まないから',
      ],
      answer: 0,
      explain:
        '前を向く向きが決まっても、その軸まわりの回転が決まりません。横倒しなのか起きているのかを決めるには、もう 1 本の参考になる向きが要ります。lookAt の `up` と同じ話です。',
    },
    {
      q: 'Frenet フレームが、世界の up を使う方法より優れている点はどれですか。',
      choices: [
        '接線が真上を向いても破綻しない',
        '計算が軽い',
        '曲線が短くなる',
        'テクスチャが不要になる',
      ],
      answer: 0,
      explain:
        '世界の up を参照せず、前のフレームの「上」を持ち回るためです。ジェットコースターのループのように垂直に上がる道でも壊れません。',
    },
    {
      q: '輪にしたパイプの 1 箇所に継ぎ目が出るのはなぜですか。',
      choices: [
        '一周したとき、姿勢が最初と一致しないことがあるから',
        '頂点が足りないから',
        '曲線が閉じていないから',
        'テクスチャの解像度が低いから',
      ],
      answer: 0,
      explain:
        '前の姿勢を持ち回る方式の宿命です。three は `closed` を true にするとずれを全体に配って辻褄を合わせますが、そのぶんパイプ全体がわずかにねじれます。',
    },
  ],
};
