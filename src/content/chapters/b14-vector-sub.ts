import type { Chapter } from '../types.ts';

export const chapterB14: Chapter = {
  slug: 'b14-vector-sub',
  part: 'basics',
  number: 14,
  title: 'ベクトルの引き算 ― あそこへ向かう矢印を作る',
  goal: '「行き先 引く 出発点」で 2 点をつなぐベクトルを作れるようになり、引く順番を間違えると真後ろを向くことが図で分かります。',
  requires: ['b13-vector-add'],
  threeApis: ['Vector3.sub', 'Vector3.subVectors', 'Vector3.negate'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D のコードで**いちばん多く書く 1 行**が、この章の中身です。

- 敵をプレイヤーのほうに向かせたい
- カメラを対象のほうに向けたい
- 弾を的のほうへ飛ばしたい
- マウスで掴んだ物を、掴んだ位置との差だけ動かしたい

どれも「A から B へ向かう矢印」が要ります。それを作るのが引き算です。
そして**引く順番を間違えると、きれいに真後ろを向きます。**
ここは覚え方を 1 つ決めて、それを守るのが早いです。
`,
    },
    {
      kind: 'md',
      text: `
## 引き算は「逆向きにして足す」

まず計算の意味から。$\\mathbf{a} - \\mathbf{b}$ は $\\mathbf{a} + (-\\mathbf{b})$ のことです。

$-\\mathbf{b}$ は「b の向きを反転したベクトル」で、成分の符号を全部ひっくり返したものです。
数直線の章で「マイナスは反対向き」と決めたのが、そのまま 3 成分に広がっただけです。

なので計算そのものは足し算と同じで、**成分ごとに引く**だけです。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{b} - \\mathbf{a} = (b_x - a_x,\\; b_y - a_y,\\; b_z - a_z)',
      readAloud:
        'b から a を引くには、x どうし・y どうし・z どうしをそれぞれ引くだけです。この結果が「a から b へ向かう矢印」になります。',
      worked: {
        given: '点 $A = (-2,\\; 0,\\; 1)$ にいる敵から、点 $B = (2,\\; 3,\\; 1)$ のプレイヤーへ向かう矢印を作ります。',
        steps: [
          { calc: 'x :  2 - (-2) = 4', note: '右へ 4 ぶんずれている' },
          { calc: 'y :  3 -   0  = 3', note: '上へ 3 ぶん' },
          { calc: 'z :  1 -   1  = 0', note: '奥行きのずれは無い' },
          { calc: '長さ = √(16 + 9 + 0) = √25 = 5', note: 'これが 2 点の距離' },
        ],
        result:
          '$B - A = (4,\\; 3,\\; 0)$ で、長さは **5**。これが「敵からプレイヤーへ向かう向き」であり、同時に「2 点の距離」でもあります。**1 回の引き算で、向きと距離が同時に手に入る**のがこの計算の効きどころです。',
      },
    },
    {
      kind: 'demo',
      id: 'vector-between',
      caption:
        '矢印が A から生えて B を指しています。「どちらを引くか」を切り替えると、まったく同じ長さのまま真逆を向くことが見えます。A と B を動かしても、矢印は常に 2 点を結びます。',
    },
    {
      kind: 'md',
      text: `
## 覚え方は「行き先 引く 出発点」

順番を間違えないための言い方を 1 つだけ決めます。

> **行き先 引く 出発点**

A から B へ行きたいなら $\\mathbf{B} - \\mathbf{A}$。
「B へ行きたいのだから B が先」と読むと迷いません。

コードでも同じ順に書けます。

- \`subVectors(target, current)\` … 行き先が先、いまいる場所があと
- \`target.clone().sub(current)\` … これも同じ並び

デモで切り替えたとおり、逆にすると**長さはそのまま、向きだけが真逆**になります。
「敵が近づいてこないで逃げていく」ときは、まずここを疑ってください。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'b.sub(a) と書くと、b のほうが壊れます',
      text: `
\`sub\` も呼ばれた側を書き換えます。
プレイヤーの位置に対して \`player.position.sub(enemy.position)\` と書くと、
**プレイヤーが原点付近にワープします。**
2 点から矢印を作るときは \`new THREE.Vector3().subVectors(b, a)\` を使うのが安全です。
`,
    },
    {
      kind: 'md',
      text: `
## 引き算がそのまま「距離」にもなる

引き算で作った矢印の長さは、そのまま 2 点の距離です。
[](#/ch/b10-pythagoras)で「引いてから測る」と言ったのが、まさにこれでした。

three にはこの 2 段（引く → 測る）をまとめた \`distanceTo\` があります。
**距離だけが欲しいときはこちらを使ってください。**
途中のベクトルを作らないぶん速く、書き換え事故も起きません。

逆に「向きも要る」場合は、引き算をして手元に残しておくほうが得です。
1 回の引き算から、向き（正規化して取り出す）と距離（長さを測る）の両方が出せます。
`,
    },
    {
      kind: 'code',
      title: '2 点から向きと距離を作る',
      code: `import * as THREE from 'three';

const enemy = new THREE.Vector3(-2, 0, 1);
const player = new THREE.Vector3(2, 3, 1);

// 「行き先 引く 出発点」。どちらも壊さない書き方
const toPlayer = new THREE.Vector3().subVectors(player, enemy);
// toPlayer は (4, 3, 0)

toPlayer.length();          // 5 ― 2 点の距離

// 距離だけが欲しいなら、こちらのほうが素直
enemy.distanceTo(player);   // 5

// 反転はこれだけでできる（プレイヤーから敵へ向かう矢印になる）
toPlayer.negate();          // (-4, -3, 0)

// 危ない書き方：player 自身が書き換わる
// player.sub(enemy);`,
    },
  ],
  exercises: [
    {
      prompt: `手で計算してください。

1. $(5,\\; 5,\\; 5) - (1,\\; 2,\\; 3)$
2. $(0,\\; 0,\\; 0) - (3,\\; -4,\\; 0)$
3. 2 の答えの長さ`,
      hint: '2 は「原点へ向かう矢印」を作っています。符号に注意してください。',
      answer: `1. $(4,\\; 3,\\; 2)$
2. $(-3,\\; 4,\\; 0)$。もとのベクトルの符号がすべて反転しました
3. $\\sqrt{9 + 16 + 0} = \\sqrt{25} = $ **5**

2 は「点 $(3, -4, 0)$ から原点へ向かう矢印」です。
反転しても長さは変わらないので、$(3, -4, 0)$ の長さも同じ 5 です。
**向きを反転しても距離は変わらない** ― これは引く順番を間違えても
距離の計算だけは正しいままである、ということでもあります。`,
    },
    {
      prompt: `点 $A = (1,\\; 1,\\; 0)$ の物体を、点 $B = (4,\\; 5,\\; 0)$ のほうへ **1 だけ**近づけたい。
$B - A$ を計算し、その長さを求め、**1 進むにはどうすればよいか**を言葉で説明してください
（計算は次の 3 章で手に入る道具を先取りして構いません）。`,
      hint: '長さ 5 の矢印で 1 だけ進みたいなら、何倍すればよいでしょう。',
      answer: `$B - A = (3,\\; 4,\\; 0)$、長さは **5** です。

長さ 5 の矢印で 1 だけ進みたいので、**$\\frac{1}{5}$ 倍**します。
$(3,\\; 4,\\; 0) \\times 0.2 = (0.6,\\; 0.8,\\; 0)$ で、これを A に足せば
$(1.6,\\; 1.8,\\; 0)$ になります。

この「自分の長さで割って、進みたいぶんを掛ける」が正規化です。
$(0.6, 0.8, 0)$ という数字は[](#/ch/b17-normalize)でもう一度出てきます。`,
    },
    {
      prompt: `敵がプレイヤーに近づくはずが、**遠ざかっていきます。**
コードは \`const dir = new THREE.Vector3().subVectors(enemy.position, player.position);\` です。
何が起きているか、どう直すかを答えてください。`,
      hint: '「行き先 引く 出発点」の順になっているか確かめてください。',
      answer: `引く順番が逆です。この式は「**プレイヤーから敵へ**向かう矢印」を作っています。
それを敵に足しているので、敵はプレイヤーから離れていきます。

行き先はプレイヤーなので、\`subVectors(player.position, enemy.position)\` が正しい順です。

なお症状が「まっすぐ遠ざかる」であることが手がかりになります。
向きが少しずれているだけなら別の原因ですが、**きれいに真後ろ**を向いているときは、
ほぼ確実に引く順番です。`,
      answerCode: `// 誤り：プレイヤーから敵へ向かってしまう
const dir = new THREE.Vector3().subVectors(enemy.position, player.position);

// 正しい：行き先 引く 出発点
const dir = new THREE.Vector3().subVectors(player.position, enemy.position);`,
    },
  ],
  quiz: [
    {
      q: '位置 $\\mathbf{a}$ にいる物体から、位置 $\\mathbf{b}$ にいる物体へ**向かう**ベクトルはどれですか。',
      choices: [
        '$\\mathbf{b} - \\mathbf{a}$',
        '$\\mathbf{a} - \\mathbf{b}$',
        '$\\mathbf{a} + \\mathbf{b}$',
        '$\\mathbf{a} \\times \\mathbf{b}$',
      ],
      answer: 0,
      explain:
        '「行き先 引く 出発点」です。$\\mathbf{a} - \\mathbf{b}$ にすると、b から a へ向かう真逆のベクトルになります。長さは同じなので、距離だけを見ていても間違いに気づけません。',
    },
    {
      q: '$\\mathbf{b} - \\mathbf{a}$ の**長さ**が表しているものはどれですか。',
      choices: [
        '2 点 a と b の距離',
        'a の長さと b の長さの差',
        'a から原点までの距離',
        '常に 1',
      ],
      answer: 0,
      explain:
        '引き算で作った矢印は 2 点を結んでいるので、その長さがそのまま距離です。距離だけが欲しいときは `distanceTo` を使うと、途中のベクトルを作らずに済みます。',
    },
    {
      q: '`player.position.sub(enemy.position)` を実行すると何が起きますか。',
      choices: [
        'player.position 自身が書き換わり、プレイヤーが移動してしまう',
        '新しいベクトルが返り、player は動かない',
        'enemy.position が書き換わる',
        'エラーになる',
      ],
      answer: 0,
      explain:
        '`sub` は呼ばれた側を書き換えます。2 点から矢印を作りたいだけなら `new THREE.Vector3().subVectors(player.position, enemy.position)` を使ってください。',
    },
  ],
};
