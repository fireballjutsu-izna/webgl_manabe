import type { Chapter } from '../types.ts';

export const chapterB39: Chapter = {
  slug: 'b39-seed',
  part: 'basics',
  number: 39,
  title: 'シード ― 再現できるランダム',
  goal: '同じ入力から同じ乱数列を得られるようになり、「気に入った配置」を数値ひとつで保存できることが分かります。',
  requires: ['13-random'],
  threeApis: ['MathUtils.seededRandom', 'Math.imul', 'Math.random'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

\`Math.random()\` は毎回違う結果を返します。ふつうはそれでよいのですが、
困る場面が 3 つあります。

- **デバッグ** … 「さっきの配置で起きたバグ」を再現できない
- **見た目の作り込み** … 「昨日いい感じだった街」が二度と戻ってこない
- **共有** … 「この地形いいね」と人に伝える方法がない

どれも「**同じものをもう一度出したい**」という話です。
そのための仕組みが{{シード}}です。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '本当のランダムではありません',
      text: `
コンピュータが作る乱数は、実は**計算で作られた数列**です。
決まった手順で次の数を作っているだけなので、出発点が同じなら結果も同じになります。

その出発点が「種」＝{{シード}}です。だから正式には**疑似乱数**と呼びます。
「予測できないように見えるが、実は完全に決まっている」というのが正体です。
`,
    },
    {
      kind: 'md',
      text: `
## 種を渡せる乱数を作る

\`Math.random()\` にはシードを渡せません（仕様でそうなっています）。
なので**自分で用意します**。数行で書けます。

よく使われるのが \`mulberry32\` という短い疑似乱数です。
中身は「状態を決まった手順でかき混ぜて、0〜1 に直す」だけです。

かき混ぜ方の中身（\`Math.imul\` や \`>>>\` といったビット演算）は、
**いまは読み飛ばして構いません**。第3部で自分で書くときに、あらためて扱います。
ここで大事なのは**性質**のほうです。

- **同じシードなら、必ず同じ並び**が出る
- シードを 1 変えるだけで、まったく別の並びになる
- 呼ぶ順番が変われば、出てくる数も変わる

3 つ目が落とし穴になります。
`,
    },
    {
      kind: 'formula',
      tex: 's_{n+1} = f(s_n), \\qquad r_n = g(s_n)',
      readAloud:
        '状態 s を決まった手順 f で次の状態に進め、その状態から g で 0〜1 の値を取り出します。f も g も決まった計算なので、最初の s が同じなら、そのあとの列は完全に同じになります。',
      worked: {
        given: 'ごく単純な疑似乱数（線形合同法）で、シード 7 から 3 つ取り出してみます。$s \\leftarrow (5s + 3) \\bmod 16$、$r = s / 16$ とします。',
        steps: [
          { calc: 's0 = 7' },
          { calc: 's1 = (5x7 + 3) mod 16 = 38 mod 16 = 6  → r = 0.375' },
          { calc: 's2 = (5x6 + 3) mod 16 = 33 mod 16 = 1  → r = 0.0625' },
          { calc: 's3 = (5x1 + 3) mod 16 =  8 mod 16 = 8  → r = 0.5' },
        ],
        result:
          '**シード 7 からは、必ず 0.375、0.0625、0.5 の順で出ます。** 何度実行しても同じです。実用の疑似乱数はもっと複雑にかき混ぜますが、**仕組みはこれと同じ**で、「決まった計算を繰り返しているだけ」です。',
      },
    },
    {
      kind: 'md',
      text: `
## 呼ぶ順番が変わると、全部ずれる

シードつき乱数は「**呼ばれた順に数を配っていく**」だけです。
なので、途中に 1 回呼び出しを足すと、**それ以降が全部ずれます**。

たとえば木を 200 本置くコードに、あとから「葉の色も振ろう」と 1 行足したとします。
すると 2 本目以降の位置がすべて変わり、**せっかく作り込んだ配置が消えます**。

対策は 2 つあります。

- **順番を変えない** … 新しい要素は、既存の呼び出しのあとに足す
- **用途ごとに別の乱数を持つ** … 位置用と色用でシードを分ける

後者のほうが確実です。シードに定数を足すだけで別の列が作れます
（\`makeRandom(seed)\` と \`makeRandom(seed + 1000)\` のように）。
`,
    },
    {
      kind: 'code',
      title: 'シードつき乱数',
      code: `// 短くて質のよい疑似乱数。中身は第3部で扱う
function mulberry32(seed) {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

const rand = mulberry32(12345);
rand();   // 何度実行しても、必ず同じ値
rand();   // 2 つ目も必ず同じ

// 用途ごとに分けておくと、あとから要素を足しても崩れない
const randPos = mulberry32(12345);
const randColor = mulberry32(12345 + 1000);

// three にも用意されている（内部状態を持つので、順番の話は同じ）
import * as THREE from 'three';
THREE.MathUtils.seededRandom(12345);`,
    },
    {
      kind: 'md',
      text: `
## シードは、作品そのものになる

シードを保存しておけば、**数値ひとつで世界をまるごと再現できます**。
地形も、木の配置も、街の形も、全部 1 つの整数から出てきます。

これは軽さの面でも効きます。
5000 本の木の座標を保存すれば数十キロバイトですが、
**シードなら 4 バイト**です。同じものが再現できるなら、座標を持つ必要がありません。

第4部の「ローポリの街」は、この性質の上に作られています。
街の形はデータとして持っておらず、**シードから毎回組み立てています**。

「素材を 1 つも用意しない」というこのサイトの方針も、
突き詰めると**決定的な計算だけで作る**ということです。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '暗号には使えません',
      text: `
ここで作った疑似乱数は、**次に出る数が予測できます**。
見た目のばらつきには十分ですが、パスワードやトークンの生成には使わないでください。

そちらには \`crypto.getRandomValues()\` という別のものが用意されています。
「再現できる」ことが利点になる場面と、致命的になる場面がある、ということです。
`,
    },
  ],
  exercises: [
    {
      prompt: `$s \\leftarrow (5s + 3) \\bmod 16$、$r = s / 16$ という疑似乱数について、
**シード 2** から 3 つの値を手で計算してください。`,
      hint: '$\\bmod 16$ は「16 で割った余り」です。',
      answer: `- $s_1 = (5 \\times 2 + 3) \\bmod 16 = 13 \\bmod 16 = 13$ → $r = $ **0.8125**
- $s_2 = (5 \\times 13 + 3) \\bmod 16 = 68 \\bmod 16 = 4$ → $r = $ **0.25**
- $s_3 = (5 \\times 4 + 3) \\bmod 16 = 23 \\bmod 16 = 7$ → $r = $ **0.4375**

シード 7 のときとはまったく違う並びが出ました。
**シードを 1 つ変えるだけで、別の世界が生まれる**わけです。

なお $s_3 = 7$ になったので、ここから先はシード 7 の列と同じになります。
状態が 16 通りしかないので、**必ずどこかで繰り返しに入ります**。
実用の疑似乱数は状態をずっと大きく取るので、繰り返しは実質起きません。`,
    },
    {
      prompt: `「シードを変える」を何度か押してから、元のシードの数字に戻してください。
地形はどうなりますか。これが実務で効くのはどんな場面でしょう。`,
      hint: '同じ入力から同じ出力が出ることを、決定的（deterministic）と言います。',
      answer: `**まったく同じ地形が戻ってきます**。

\`Math.random()\` は毎回違う結果になるので、「昨日の見た目」を二度と再現できません。
シード付きの乱数なら、**数値をひとつ保存しておくだけで、街や地形をまるごと再現できます**。

第4部のローポリの街は、この性質の上に作られています。
街の形をデータとして持たず、シードから毎回組み立てているので、
**リポジトリに 1 バイトの素材も入っていません。**`,
    },
    {
      prompt: `木を 200 本置くコードがあり、各本について \`rand()\` を 3 回（x、z、向き）呼んでいます。
ここに「大きさも振ろう」と 4 回目の呼び出しを**位置の直後**に足しました。
**配置が丸ごと変わってしまいます。** なぜか、どう直すかを答えてください。`,
      hint: 'シードつき乱数は、呼ばれた順に数を配ります。',
      answer: `疑似乱数は**呼ばれた順に数を配る**ので、途中に 1 回足すと**それ以降が全部ずれます**。
1 本目の大きさに使われた数は、もともと 2 本目の x に使われるはずだった数です。

直し方は 2 つあります。

1. **既存の呼び出しの「あと」に足す** … ループの末尾に置けば、その回のずれで済みます。
   ただし次のループの先頭がずれるので、結局は全部変わります
2. **用途ごとに別の乱数を持つ** … 位置用と大きさ用でシードを分ければ、
   どちらを足しても他方に影響しません

**確実なのは 2 です。** シードに定数を足すだけで別の列が作れるので、
「あとから要素を足しても壊れない」構造にしておくのが定石です。`,
      answerCode: `// 崩れやすい：1 つの列から全部取る
const rand = mulberry32(seed);

// 崩れない：用途ごとに分ける
const randPos = mulberry32(seed);
const randSize = mulberry32(seed + 1000);
const randRot = mulberry32(seed + 2000);`,
    },
  ],
  quiz: [
    {
      q: '同じ配置を毎回再現できるようにしたいとき、必要なものはどれですか。',
      choices: ['シードつきの疑似乱数', '`Math.random()`', 'より細かいノイズ', 'InstancedMesh'],
      answer: 0,
      explain:
        '同じシードを与えれば必ず同じ並びが返るので、デバッグでも「気に入った配置」の保存でも再現できます。`Math.random()` にはシードを渡せないので、自分で用意します。',
    },
    {
      q: 'シードつき乱数を使ったコードに、呼び出しを 1 つ足しました。何が起きますか。',
      choices: [
        'それ以降に取り出される数がすべてずれる',
        '何も変わらない',
        'エラーになる',
        '最初の値だけが変わる',
      ],
      answer: 0,
      explain:
        '呼ばれた順に数を配っているためです。あとから要素を足しても壊れないようにするには、用途ごとにシードを分けて別々の列を持つのが定石です。',
    },
    {
      q: 'ここで作った疑似乱数を使ってはいけないのはどれですか。',
      choices: [
        'パスワードやトークンの生成',
        '木の配置',
        '地形の起伏',
        '敵の出現位置',
      ],
      answer: 0,
      explain:
        '疑似乱数は次に出る数が予測できます。見た目のばらつきには十分ですが、暗号用途には `crypto.getRandomValues()` を使ってください。「再現できる」ことが利点にも欠点にもなります。',
    },
  ],
};
