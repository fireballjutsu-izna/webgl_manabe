import type { Chapter } from '../types.ts';

export const chapterM07: Chapter = {
  slug: 'm07-inverse',
  part: 'math3d',
  number: 7,
  title: '逆行列 ― 変換を巻き戻す',
  goal: '逆行列が「変換を元に戻す装置」だと分かり、ワールド座標とローカル座標を行き来する道具として使えるようになります。',
  requires: ['m06-trs', 'm03-basis'],
  threeApis: ['Matrix4.invert', 'Object3D.worldToLocal', 'Object3D.localToWorld', 'Matrix4.determinant'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

行列は「点を運ぶ装置」でした。では**運んだ点を元に戻したい**とき、どうするか。

これが必要になる場面は、思っているより多いです。

- クリックした画面の位置が、この物体から見てどこに当たるのか
- 世界の座標で置きたい物を、親の中に入れたい
- カメラから見た座標に直したい（それが描画そのものです）

どれも「変換を逆にたどる」操作で、そのための行列が{{逆行列}}です。
`,
    },
    {
      kind: 'md',
      text: `
## 掛けると「何もしない」に戻る

逆行列 $M^{-1}$ は、**$M$ と掛けると単位行列になる行列**です。
単位行列は「何もしない装置」なので、
**$M$ で運んだ点に $M^{-1}$ を掛けると、元の場所に戻ります。**

具体的な逆行列は、それぞれ素直な形をしています。

- **移動** $(2, 0, 0)$ の逆 … 移動 $(-2, 0, 0)$
- **回転** 90 度の逆 … 回転 $-90$ 度
- **拡大** 3 倍の逆 … 拡大 $\\frac{1}{3}$ 倍

**合成した行列の逆は、順番も逆になります。**
$(A \\cdot B)^{-1} = B^{-1} \\cdot A^{-1}$ です。

靴と靴下にたとえると分かりやすい ―
「靴下を履いてから靴を履く」の逆は「靴を脱いでから靴下を脱ぐ」です。
`,
    },
    {
      kind: 'formula',
      tex: 'M M^{-1} = I \\qquad (A B)^{-1} = B^{-1} A^{-1}',
      readAloud:
        '行列と逆行列を掛けると、何もしない行列（単位行列 I）になります。そして合成したものの逆は、それぞれの逆を逆順に並べたものになります。',
      worked: {
        given:
          '「x に 3 移動してから 2 倍に拡大」した点 $(1,\\; 0,\\; 0)$ を、逆行列で元に戻します。',
        steps: [
          { calc: '元の変換 : (1,0,0) → 移動 → (4,0,0) → 拡大 → (8,0,0)' },
          { calc: '逆は「拡大の逆」→「移動の逆」の順', note: '順番も逆になる' },
          { calc: '1/2 倍 : (8,0,0) → (4,0,0)' },
          { calc: '-3 移動 : (4,0,0) → (1,0,0)', note: '元に戻った' },
        ],
        result:
          '**$(1,\\; 0,\\; 0)$** に戻りました。順番を逆にせず「移動の逆 → 拡大の逆」でやると、$(8,0,0) \\to (5,0,0) \\to (2.5,0,0)$ となって**戻りません**。順番が逆になるのが要点です。',
      },
    },
    {
      kind: 'md',
      text: `
## 逆行列が作れない場合

[](#/ch/m03-basis)で見たとおり、**行列式が 0 のときは逆行列が存在しません**。

つぶれた変換は情報を捨ててしまうので、戻しようがないのです。
すべての点を 1 枚の平面に押しつぶしたあとで、
「元はどの高さにいましたか」と聞かれても答えられません。

実務でこれが起きるのは、ほぼ **\`scale\` に 0 を入れたとき**です。
three は逆行列の計算で警告を出し、行列をゼロで埋めます。
その行列を使った以降の計算は全部おかしくなります。
`,
    },
    {
      kind: 'md',
      text: `
## ワールドとローカルを行き来する

逆行列のいちばん実用的な使い道がこれです。

物体の \`matrixWorld\` は「**ローカル座標 → ワールド座標**」の変換です。
その逆行列は「**ワールド座標 → ローカル座標**」になります。

- **ローカル → ワールド** … その物体から見た座標を、世界の座標に直す
- **ワールド → ローカル** … 世界の座標を、その物体から見た座標に直す

three には \`localToWorld()\` と \`worldToLocal()\` があり、
中でこの掛け算をやっています。

**「どの座標系で考えているか」を意識するだけで、多くのバグが消えます。**
これは次の章（階層）の主題です。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '回転だけなら、逆行列は転置',
      text: `
回転行列には特別な性質があります。**逆行列が転置（行と列を入れ替えたもの）と一致する**のです。

理由は[](#/ch/m03-basis)の見方で説明できます。
回転行列の列は「長さ 1 で、互いに直交する 3 本の軸」です。
このような行列を**直交行列**と呼び、逆行列が転置になります。

転置は行と列を入れ替えるだけなので、**逆行列の計算よりずっと軽い**です。
シェーダで法線を変換するときに、この性質がよく使われます。
`,
    },
    {
      kind: 'code',
      title: '逆行列を使う',
      code: `import * as THREE from 'three';

// 逆行列を作る（呼ばれた側が書き換わる）
const inv = matrix.clone().invert();

// ワールド座標を、その物体から見た座標に直す
const worldPoint = new THREE.Vector3(5, 0, 3);
const localPoint = mesh.worldToLocal(worldPoint.clone());

// 逆向き
const backToWorld = mesh.localToWorld(localPoint.clone());

// 中でやっていること
mesh.updateMatrixWorld(true);
const m = mesh.matrixWorld.clone().invert();
worldPoint.clone().applyMatrix4(m);   // worldToLocal と同じ

// 行列式が 0 だと逆行列が作れない
if (Math.abs(matrix.determinant()) < 1e-10) {
  // scale に 0 が入っていないか疑う
}`,
    },
    {
      kind: 'md',
      text: `
## 描画そのものが、逆行列でできている

カメラの話を先取りすると ―
**「カメラから見た世界」を作るのは、カメラの変換の逆行列**です。

カメラを右に 3 動かすのと、世界を左に 3 動かすのは、見た目が同じです。
だから描画では、カメラの \`matrixWorld\` の逆行列を世界全体に掛けます。
これを**ビュー行列**と呼びます。

three の \`camera.matrixWorldInverse\` がそれです。
名前のとおり、ただの逆行列です。

**カメラを動かすコードが、実は世界を逆に動かしている** ―
この話はカメラの章で正面から扱います。
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの変換の逆を、言葉で答えてください。

1. y 軸まわりに 45 度回す
2. $(0,\\; 5,\\; 0)$ 移動する
3. 全体を 0.5 倍に拡大する
4. 「1 を実行してから 2 を実行する」変換`,
      hint: '4 は順番にも注意してください。',
      answer: `1. y 軸まわりに **−45 度**回す
2. $(0,\\; -5,\\; 0)$ 移動する
3. **2 倍**に拡大する
4. **「2 の逆を実行してから、1 の逆を実行する」** ―
   つまり $(0, -5, 0)$ 移動してから、−45 度回す

4 が要点です。**順番も逆になります。**
先に −45 度回してしまうと、そのあとの移動の向きが変わってしまい、元に戻りません。

$(A \\cdot B)^{-1} = B^{-1} \\cdot A^{-1}$ ―
靴下を履いてから靴を履いたなら、脱ぐときは靴が先です。`,
    },
    {
      prompt: `物体が \`position = (10, 0, 0)\`、\`rotation.y = 90 度\` で置かれています。
ワールド座標 $(10,\\; 0,\\; 5)$ は、この物体から見るとどこになりますか。手で計算してください。`,
      hint: 'まず移動を戻し、そのあと回転を戻します（順番が逆）。',
      answer: `1. **移動を戻す** … $(10, 0, 5) - (10, 0, 0) = (0,\\; 0,\\; 5)$
2. **回転を戻す** … y 軸まわりに $-90$ 度回す

y 軸まわりの回転は $x' = x\\cos\\theta + z\\sin\\theta$、$z' = -x\\sin\\theta + z\\cos\\theta$ です。
$\\theta = -90^\\circ$ なら $\\cos = 0$、$\\sin = -1$ なので、
$(0, 0, 5) \\to (0 \\times 0 + 5 \\times (-1),\\; 0,\\; 0) = (-5,\\; 0,\\; 0)$。

答えは **$(-5,\\; 0,\\; 0)$** です。

つまり「その物体から見ると、**左**へ 5 の位置」にあります。
物体が反時計回りに 90 度回っているので、物体のローカル x 軸は世界の奥（$-z$）を向いています。
だから世界の $+z$ 方向は、物体から見ると $-x$ 方向になります。

**符号でつまずいたら、\`worldToLocal()\` に計算させて確かめてください。**
行列の符号を紙で詰めるより、走らせて確かめるほうが確実です。`,
    },
    {
      prompt: `\`scale.set(2, 0, 2)\` にした物体で \`worldToLocal()\` を呼んだところ、
返ってきた座標が \`NaN\` だらけになりました。原因を説明してください。`,
      hint: '行列式が 0 の行列に、逆行列はありますか。',
      answer: `y の拡大率が 0 なので、**行列式が 0** です。
この変換はすべての点を 1 枚の平面に潰してしまうので、**逆行列が存在しません**。

three は逆行列を計算しようとして 0 で割ることになり、
成分が \`NaN\` になります。そして \`NaN\` は伝染するので、
それを使った座標も全部 \`NaN\` になります。

[](#/ch/b06-float)で見たとおり、\`NaN\` が座標に入ると物体は黙って消えます。

**「非表示にしたいから scale を 0 に」は禁じ手**です。
\`visible = false\` を使ってください。
どうしても潰したいなら、0 ではなく 0.0001 のような小さな値にします。`,
    },
  ],
  quiz: [
    {
      q: '$(A \\cdot B)^{-1}$ はどれと等しいですか。',
      choices: [
        '$B^{-1} \\cdot A^{-1}$',
        '$A^{-1} \\cdot B^{-1}$',
        '$A \\cdot B$',
        '$(A \\cdot B)$ の転置',
      ],
      answer: 0,
      explain:
        '逆をたどるときは順番も逆になります。靴下を履いてから靴を履いたなら、脱ぐときは靴が先です。順番を逆にしないと元に戻りません。',
    },
    {
      q: '逆行列が作れないのは、どんなときですか。',
      choices: [
        '行列式が 0 のとき（scale に 0 が入っているなど）',
        '回転が含まれているとき',
        '平行移動が大きすぎるとき',
        '行列が 4×4 でないとき',
      ],
      answer: 0,
      explain:
        'つぶれた変換は情報を捨てるので、戻しようがありません。実務では `scale` に 0 を入れたときに起き、以降の座標計算が NaN で壊れます。',
    },
    {
      q: '回転行列の逆行列について、正しいのはどれですか。',
      choices: [
        '転置（行と列の入れ替え）と一致する',
        '逆行列は存在しない',
        'もとの行列と同じになる',
        '行列式で割る必要がある',
      ],
      answer: 0,
      explain:
        '回転行列の列は長さ 1 で互いに直交しており、こうした行列（直交行列）は逆行列が転置になります。計算がずっと軽いので、シェーダでよく使われます。',
    },
  ],
};
