import type { Chapter } from '../types.ts';

export const chapterM10: Chapter = {
  slug: 'm10-euler',
  part: 'math3d',
  number: 10,
  title: 'オイラー角 ― 3 つの角度で向きを表す',
  goal: 'オイラー角が「3 つの回転を順番に重ねる手順」だと分かり、順番を変えると結果が変わることを手で確かめられるようになります。',
  requires: ['07-rotation', 'm05-matrix-order'],
  threeApis: ['Euler', 'Object3D.rotation', 'Euler.order', 'Object3D.setRotationFromEuler'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章で「1 つの軸のまわりに回す」式を手に入れました。
では**任意の向き**を作りたいときは、どうするか。

いちばん素直な答えは「3 つの軸のまわりに、順に回せばいい」です。
Three.js の \`rotation\` がまさにこれで、{{オイラー角}}と呼びます。

\`rotation.set(0.3, 1.2, 0)\` と書けば、それで向きが決まる。
読みやすいし、スライダーにも直結できる。**では、なぜこれで話が終わらないのでしょう。**
`,
    },
    {
      kind: 'md',
      text: `
## 3 つの数は、値ではなく手順

つまずきの根はここ 1 点です。

**オイラー角は「向き」を直接書いているのではなく、「回す手順」を書いています。**

$(30^\\circ,\\; 45^\\circ,\\; 0^\\circ)$ は「そういう向き」ではなく、
「x に 30 度回して、そのあと y に 45 度回して、そのあと z には回さない」という**作業指示**です。

そして[](#/ch/m05-matrix-order)で見たとおり、**変換は順番を変えると結果が変わります。**
オイラー角も変換の合成なので、当然この性質を引き継ぎます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '本を回してみると分かります',
      text: `
手元の本を 1 冊とって、机の上に置いてください。

まず手前に 90 度倒し、そのあと真上から見て 90 度回す。向きを覚えておきます。
次にもとに戻して、逆の順 ― 先に真上から 90 度回してから、手前に 90 度倒す。

**別の向きになります。** 同じ 2 つの操作を、順番を変えただけなのに。

回転は足し算ではありません。3 つの数字を並べて書くと足し算のように見えますが、
実際にやっているのは掛け算（合成）です。
`,
    },
    {
      kind: 'formula',
      tex: 'R = R_z \\, R_y \\, R_x \\qquad R_x R_y \\neq R_y R_x',
      readAloud:
        '全体の回転 R は、3 つの軸の回転を掛け合わせたものです。掛ける順番が変われば結果も変わるので、同じ 3 つの角度でも順番が違えば別の向きになります。',
      worked: {
        given:
          '点 $(1,\\; 0,\\; 0)$ に「x 軸まわり 90 度」と「y 軸まわり 90 度」を、順番を変えて適用します。',
        steps: [
          { calc: '【x が先】 x90 : (1,0,0) → (1,0,0)', note: '回す軸の上にいるので動かない' },
          { calc: '        y90 : (1,0,0) → (0,0,-1)' },
          { calc: '【y が先】 y90 : (1,0,0) → (0,0,-1)' },
          { calc: '        x90 : (0,0,-1) → (0,1,0)', note: "y' = y cos - z sin = 0 - (-1) = 1" },
        ],
        result:
          '**$(0,\\; 0,\\; -1)$ と $(0,\\; 1,\\; 0)$ ― まったく別の場所**に着きました。同じ 2 つの回転を、順番だけ変えた結果です。だからオイラー角には「どの順で適用するか」を必ず添えなければなりません。',
      },
    },
    {
      kind: 'md',
      text: `
## three の順番と、その変え方

Three.js の既定は **XYZ** です。\`Euler\` の 4 つ目の引数、または \`rotation.order\` で変えられます。

読み方に注意が要ります。XYZ と書いてあっても、行列としては $R_z R_y R_x$ の並びになります。
**「x から順に適用していく」と読むのが正しく**、掛け算の式では右から効くからです
（[](#/ch/m05-matrix-order)の「右から効く」と同じ話です）。

順番を変えたくなる場面は、実際にあります。

- **カメラ** … YXZ にすると、左右を振ってから上下を向く操作になり、地面が傾かない
- **キャラクタ** … 向き（y）を先に決めたい場面が多い

ただし**順番を変えるときは、値の意味も変わります。**
同じ数字を入れたまま \`order\` だけ差し替えると、向きが変わってしまいます。
`,
    },
    {
      kind: 'code',
      title: 'オイラー角を扱う',
      code: `import * as THREE from 'three';

// 3 つの角度で向きを決める（単位はラジアン）
mesh.rotation.set(0, Math.PI / 2, 0);

// 呼び名。ゲームや航空でよく使われる
mesh.rotation.x = pitch;  // ピッチ（うなずく）
mesh.rotation.y = yaw;    // ヨー（首を振る）
mesh.rotation.z = roll;   // ロール（傾ける）

// 適用する順番を変える
mesh.rotation.order = 'YXZ';   // カメラ向きの順番

// Euler そのものを作る場合は 4 つ目が順番
const e = new THREE.Euler(0.3, 1.2, 0, 'YXZ');
mesh.setRotationFromEuler(e);

// order を変えても数値はそのまま残る＝向きが変わる
console.log(mesh.rotation.y);  // 値は同じ。意味だけ変わった`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'オイラー角が向いている場面',
      text: `
欠点ばかり並べましたが、オイラー角は今でも現役です。使いどころははっきりしています。

固定の向きを 1 回だけ置くとき（壁を y に 90 度回して立てる）、
人が数字を読み書きする UI、そして 1 軸だけ回すとき（回る床、時計の針）。

これらは順番の問題が起きようがないので、読みやすさの利点だけが残ります。
問題になるのは「3 軸を同時に、動かし続ける」場合だけです。
`,
    },
    {
      kind: 'md',
      text: `
## 順番だけが問題なら、決めてしまえばよいのでは

そう思うのが自然です。「XYZ に統一する」と決めれば、順番の曖昧さは消えます。

ところが**順番を固定しても消えない問題**が、もう 1 つ残ります。
3 つの角度を順に重ねるという仕組みそのものから出てくる、避けようのない欠陥です。

それが次の章の{{ジンバルロック}}です。
`,
    },
  ],
  exercises: [
    {
      prompt: `点 $(0,\\; 1,\\; 0)$ に「z 軸まわり 90 度」と「y 軸まわり 90 度」を適用します。
順番を変えた 2 通りの答えを、手で計算してください。

z 軸まわりは $x' = x\\cos\\theta - y\\sin\\theta$、$y' = x\\sin\\theta + y\\cos\\theta$ です。`,
      hint: '90 度なので $\\cos = 0$、$\\sin = 1$。まず 1 つ目の回転の答えを出し、それを 2 つ目に入れます。',
      answer: `**z が先の場合**

1. z 軸まわり 90 度 … $x' = 0 - 1 \\times 1 = -1$、$y' = 0 + 0 = 0$ → $(-1,\\; 0,\\; 0)$
2. y 軸まわり 90 度 … $x' = -1 \\times 0 + 0 \\times 1 = 0$、$z' = 1 \\times 1 + 0 = 1$ → **$(0,\\; 0,\\; 1)$**

**y が先の場合**

1. y 軸まわり 90 度 … $(0, 1, 0)$ は y 軸の上なので**動きません** → $(0,\\; 1,\\; 0)$
2. z 軸まわり 90 度 … $x' = 0 - 1 = -1$、$y' = 0 + 0 = 0$ → **$(-1,\\; 0,\\; 0)$**

$(0, 0, 1)$ と $(-1, 0, 0)$ で、**90 度ぶんずれています**。

2 つ目の計算で「y 軸の上にある点は y 回転で動かない」ことに気づけると速いです。
回す軸の上に乗っている点は、どれだけ回しても動きません。`,
    },
    {
      prompt: `キャラクタの向きを \`rotation.set(0.3, 1.2, 0)\` で決めていました。
カメラのために \`rotation.order = 'YXZ'\` に変えたところ、キャラクタの向きまで変わってしまいました。
なぜですか。`,
      hint: '3 つの数字は「向き」でしたか、それとも「手順」でしたか。',
      answer: `**3 つの数字は手順であって、向きそのものではない**からです。

\`order\` を変えると、同じ $(0.3,\\; 1.2,\\; 0)$ という指示の意味が変わります。
XYZ なら「x に 0.3 → y に 1.2」、YXZ なら「y に 1.2 → x に 0.3」の順で適用されるので、
[](#/ch/m05-matrix-order)のとおり結果は別物になります。

**向きを保ったまま order だけ変えたい**なら、いったんクォータニオンを経由します。
向きそのものを保存してから、新しい順番で読み直させる形です。

なおこの手当てが必要になること自体が、
「オイラー角は姿勢の保管場所に向かない」ことの現れでもあります。`,
      answerCode: `// 向きを保ったまま order を変える
const q = mesh.quaternion.clone();      // いまの向きそのものを控える
mesh.rotation.order = 'YXZ';
mesh.quaternion.copy(q);                // 新しい順番で読み直させる

// rotation の 3 つの数値は変わるが、向きは変わらない`,
    },
    {
      prompt: `次の 3 つのうち、オイラー角で書いても問題が起きないものはどれですか。理由も答えてください。

1. 観覧車のゴンドラを、y 軸まわりに毎フレーム少しずつ回す
2. 一人称カメラを、マウスの上下左右で自由に向ける
3. 看板を「y に 90 度」だけ回して壁に立てる`,
      hint: '順番の影響が出るのは、いくつの軸を同時に使うときでしたか。',
      answer: `**1 と 3 は問題ありません。2 は避けるべきです。**

**1** … 使っている軸が y の 1 本だけなので、順番の問題が起きようがありません。
毎フレーム \`rotation.y += dt\` と書いても壊れません。

**3** … 1 回設定するだけの固定の向きで、しかも 1 軸です。
むしろ「y に 90 度」と読めるぶん、クォータニオンより保守しやすいです。

**2** … 上下（x）と左右（y）を同時に使い、しかも上を向ききることがあります。
これがまさに次の章のジンバルロックが出る条件です。
一人称カメラは \`order = 'YXZ'\` にして x の範囲を $\\pm 89$ 度に制限するか、
クォータニオンで持つのが定石です。`,
    },
  ],
  quiz: [
    {
      q: 'オイラー角の 3 つの数値は、何を表していますか。',
      choices: [
        '3 つの回転を順に適用する手順',
        '向きそのものを一意に表す 3 つの値',
        '3 つの軸それぞれの長さ',
        '回転の速さ',
      ],
      answer: 0,
      explain:
        '手順なので、適用する順番が変われば結果の向きも変わります。「値」ではなく「作業指示」だと捉えると、順番の問題も、次章のジンバルロックも見通しがよくなります。',
    },
    {
      q: 'Three.js の `rotation.order` を変えると、どうなりますか。',
      choices: [
        '同じ数値でも、できあがる向きが変わる',
        '数値が自動で書き換わり、向きは保たれる',
        '見た目は変わらず、内部表現だけが変わる',
        '回転が無効になる',
      ],
      answer: 0,
      explain:
        '数値はそのまま残り、適用の順番だけが変わるので、結果の向きが変わります。向きを保ったまま order を変えたいときは、`quaternion` を控えておいて戻します。',
    },
    {
      q: 'オイラー角を使っても安全なのは、次のうちどれですか。',
      choices: [
        '1 軸だけを回す（回る床、時計の針）',
        '3 軸を同時に動かし続ける自由なカメラ',
        '2 つの姿勢のあいだを補間する',
        '毎フレーム 3 軸に足し込む',
      ],
      answer: 0,
      explain:
        '軸が 1 本なら順番の影響が出ようがなく、読みやすさの利点だけが残ります。問題が出るのは、複数の軸を同時に、動かし続ける場合です。',
    },
  ],
};
