import type { Chapter } from '../types.ts';

export const chapterM20: Chapter = {
  slug: 'm20-attach',
  part: 'math3d',
  number: 20,
  title: '親を付け替える ― add と attach',
  goal: '親を変えるときに見た目を保つか保たないかを選べるようになり、「持たせる・落とす」を正しく書けるようになります。',
  requires: ['m18-matrix-world', 'm07-inverse'],
  threeApis: [
    'Object3D.attach',
    'Object3D.add',
    'Object3D.remove',
    'Object3D.matrixWorld',
    'Matrix4.invert',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

キャラクタに剣を持たせたい。持ったあと、地面に落としたい。

- \`hand.add(sword)\` としたら、剣が**変な場所に飛んだ**
- \`scene.add(sword)\` で落としたら、剣が**原点まで戻った**

どちらも正しく動いています。**\`add\` は「ローカル座標を保つ」操作**だからです。
見た目を保ちたいなら、別の関数を使う必要があります。
`,
    },
    {
      kind: 'md',
      text: `
## 保つものが違う 2 つ

three には親を変える方法が 2 つあり、**何を保つか**が違います。

- **\`newParent.add(child)\`** … **ローカル座標を保つ**。\`position\` の値はそのまま。だから見た目の位置が飛ぶ
- **\`newParent.attach(child)\`** … **ワールド座標を保つ**。見た目は動かない。かわりに \`position\` の値が計算し直される

どちらが正しいかは、**やりたいことによります。**

- 剣を手の中の決まった場所に持たせる → **\`add\`**（ローカルで位置を指定したい）
- 持っていた剣を、その場に落とす → **\`attach\`**（見た目を動かしたくない）
- 拾った石をポケットに入れる → **\`add\`**（見た目は変わってよい）
- 車から降りた人を地面に置く → **\`attach\`**（その場に立っていてほしい）
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '船から桟橋へ降りる',
      text: `
船の甲板の「船首から 3 歩」に立っている人が、桟橋に降ります。

add は「桟橋の、桟橋の付け根から 3 歩の場所」へ人を置きます。
数字（3 歩）を持っていくので、立っている場所は変わってしまいます。

attach は「いま立っているその場所」に人を残します。
かわりに「桟橋の付け根から見て何歩か」を計算し直します。

降りるときに保ちたいのは、ふつう後者です。
`,
    },
    {
      kind: 'formula',
      tex: 'M_{\\text{local}}^{\\text{new}} = \\left(M_{\\text{newParent}}^{\\text{world}}\\right)^{-1} M^{\\text{world}}',
      readAloud:
        'attach が中でやっている計算です。いまのワールド行列に、新しい親のワールド行列の逆行列を左から掛けると、新しい親から見たローカル行列が求まります。ワールドでの見た目を保ったまま、親だけを差し替えていることになります。',
      worked: {
        given:
          '剣がワールドの $(5,\\; 2,\\; 0)$ にあります。これを、$(3,\\; 0,\\; 0)$ にいる（回転していない）キャラクタに attach します。',
        steps: [
          { calc: '剣のワールド位置    : (5, 2, 0)' },
          { calc: '新しい親のワールド位置 : (3, 0, 0)' },
          { calc: '親の変換を巻き戻す : (5,2,0) - (3,0,0)' },
          { calc: '新しいローカル位置  : (2, 2, 0)' },
          { calc: '【確かめ】(3,0,0) + (2,2,0) = (5,2,0)', note: '見た目は動いていない' },
        ],
        result:
          '\`position\` が $(5, 2, 0)$ から **$(2,\\; 2,\\; 0)$** に書き換わり、見た目の位置は $(5, 2, 0)$ のままです。もし \`add\` を使っていたら値が $(5,2,0)$ のまま残り、**見た目は $(8, 2, 0)$ に飛びます**。',
      },
    },
    {
      kind: 'md',
      text: `
## 親が回っていると、引き算では済まない

上の計算例は、親が回転も拡大もしていない単純な場合でした。
親が回っていれば、[](#/ch/m18-matrix-world)のときと同じく**引き算では合いません。**

そこで一般には逆行列を使います ―
[](#/ch/m07-inverse)の「変換を巻き戻す装置」が、そのままここで働いています。

**\`attach\` の中身は、逆行列 1 回**です。それ以上のことは何もしていません。
`,
    },
    {
      kind: 'code',
      title: '持たせる・落とす',
      code: `import * as THREE from 'three';

// 持たせる：手の中の決まった場所に置きたいので add
hand.add(sword);
sword.position.set(0, 0.1, 0.3);   // 手から見た握りの位置
sword.rotation.set(0, 0, Math.PI / 2);

// 落とす：いま見えている場所に残したいので attach
scene.attach(sword);
// position が自動で書き換わり、見た目は動かない

// attach が中でやっていること
sword.updateWorldMatrix(true, false);
const worldMatrix = sword.matrixWorld.clone();
scene.add(sword);                                  // まず親を替える
const inv = scene.matrixWorld.clone().invert();
sword.matrix.copy(inv.multiply(worldMatrix));      // 新しいローカル行列
sword.matrix.decompose(sword.position, sword.quaternion, sword.scale);

// 落としたあとに地面へ落下させるなら、y だけ書き換える
sword.position.y = 0;`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '親の拡大は attach でも残ります',
      text: `
親が 2 倍に拡大されていた場合、attach で外すと
子の scale が 2 倍に書き換わります。見た目を保つには、そうするしかないからです。

そのあと scale を 1 に戻すと、当然ながら見た目が半分になります。
「落としたら急に小さくなった」はこれです。

親に拡大を使わないのがいちばん安全です。
どうしても必要なら、拡大は末端の子だけに掛けてください。
`,
    },
    {
      kind: 'md',
      text: `
## 外したあとの後片付け

\`remove\` や \`attach\` でシーンから外しただけでは、**メモリは解放されません。**

three が持っているのは、ジオメトリ・マテリアル・テクスチャという GPU 側の資源で、
これは参照が外れても自動では消えません。

**もう二度と使わないなら、明示的に捨てる必要があります。**

- \`geometry.dispose()\`
- \`material.dispose()\`（マテリアルが持つテクスチャも個別に）
- 階層があるなら \`traverse\` で全部たどる

「章を移るたびにメモリが増える」「長く遊んでいると重くなる」の原因は、ほぼこれです。
`,
    },
    {
      kind: 'code',
      title: '階層ごと捨てる',
      code: `function disposeTree(root) {
  root.traverse((obj) => {
    if (!obj.isMesh) return;
    obj.geometry.dispose();

    const materials = Array.isArray(obj.material) ? obj.material : [obj.material];
    for (const m of materials) {
      // マテリアルが持つテクスチャも個別に捨てる
      for (const key of Object.keys(m)) {
        const value = m[key];
        if (value && value.isTexture) value.dispose();
      }
      m.dispose();
    }
  });
  root.parent?.remove(root);
}

// 共有しているジオメトリやマテリアルを捨てないよう注意する
// （同じ geometry を 100 個の Mesh で使っていたら、捨てるのは 1 回だけ）`,
    },
    {
      kind: 'md',
      text: `
## ここまでが階層です

5 章で手に入れたものを並べます。

- **親子関係** … 座標系を入れ子にして、まとまりを作る
- **ローカルとワールド** … 座標には必ず「どこから見た」が付く
- **matrixWorld** … 親をさかのぼった行列の積
- **回転の中心** … 物体は必ずローカル原点まわりに回る
- **付け替え** … \`add\` はローカルを保ち、\`attach\` はワールドを保つ

**次からは、場所の指し方そのものを変えます。**
$(x, y, z)$ で書きにくい場所 ― 球の上の点や、中心のまわりを回る軌道 ―
それを扱う座標を 4 章で身につけます。
`,
    },
  ],
  exercises: [
    {
      prompt: `剣がワールドの $(8,\\; 1,\\; 3)$ にあります。
$(2,\\; 0,\\; 1)$ にいる（回転していない）キャラクタに \`attach\` したとき、
剣の \`position\` は何になりますか。また \`add\` を使っていたら、見た目はどこへ行きますか。`,
      hint: '親が回っていないので、引き算と足し算で済みます。',
      answer: `**attach の場合** … $(8, 1, 3) - (2, 0, 1) = (6,\\; 1,\\; 2)$

\`position\` が **$(6,\\; 1,\\; 2)$** に書き換わり、見た目は $(8, 1, 3)$ のままです。

**add の場合** … \`position\` は $(8, 1, 3)$ のまま残るので、
見た目は $(2, 0, 1) + (8, 1, 3) = $ **$(10,\\; 1,\\; 4)$** へ飛びます。

キャラクタの位置ぶん、まるごとずれた形です。
**「持たせたら剣がどこかへ飛んだ」の正体がこれ**です。`,
    },
    {
      prompt: `車に乗っている人を \`scene.attach(person)\` で降ろしました。
車は $(0,\\; 0,\\; 0)$ にいて、y 軸まわりに 90 度回っています。
人は車の中の $(0,\\; 0,\\; 2)$ に座っていました。人はどこに立ちますか。`,
      hint: '親が回っているので、まず回転を適用します。',
      answer: `車が 90 度回っているので、人のローカル位置も回されて運ばれています。

$(0, 0, 2)$ を y 軸まわりに 90 度回すと、
$x' = 0 \\times 0 + 2 \\times 1 = 2$、$z' = -0 \\times 1 + 2 \\times 0 = 0$
→ $(2,\\; 0,\\; 0)$

車の位置が原点なので、ワールド座標もそのまま **$(2,\\; 0,\\; 0)$** です。
\`attach\` は見た目を保つので、人はここに立ちます。
\`position\` も $(2, 0, 0)$ に書き換わります（新しい親が \`scene\` なので、ローカル＝ワールド）。

**引き算だけで済ませて $(0, 0, 2)$ と答えると間違いです。**
親が回っている以上、回転を巻き戻す必要があります。
これが \`attach\` が逆行列を使う理由です。`,
    },
    {
      prompt: `ステージを切り替えるたびにメモリ使用量が増えていきます。
\`scene.remove(stage)\` は呼んでいます。何が足りませんか。`,
      hint: 'シーンから外すことと、GPU の資源を返すことは別です。',
      answer: `**ジオメトリ・マテリアル・テクスチャを \`dispose()\` していない**からです。

\`remove\` はシーングラフから外すだけで、GPU が持っている資源には触れません。
描画されなくなるので気づきにくく、**メモリだけが積み上がります**。

階層がある場合は \`traverse\` で全部たどって捨てる必要があります。

**注意点が 2 つ**あります。

- **共有しているものを二重に捨てない** … 同じジオメトリを 100 個の Mesh で使っているなら、捨てるのは 1 回
- **まだ使っているものを捨てない** … 捨てたあとに描画すると、警告が出て真っ黒になります

安全策として、ステージごとに「そのステージで作ったもの」を配列に控えておき、
切り替え時にその配列だけを捨てる、という管理がよく使われます。`,
    },
  ],
  quiz: [
    {
      q: '`newParent.add(child)` は何を保ちますか。',
      choices: [
        'ローカル座標（position の値）',
        'ワールド座標（見た目の位置）',
        '両方',
        'どちらも保たない',
      ],
      answer: 0,
      explain:
        '値をそのまま持っていくので、新しい親の位置ぶん見た目が飛びます。見た目を保ちたいときは `attach` を使います。',
    },
    {
      q: '`attach` が中で使っている道具はどれですか。',
      choices: [
        '新しい親のワールド行列の逆行列',
        '外積',
        'クォータニオンの補間',
        '正規化',
      ],
      answer: 0,
      explain:
        'ワールドでの見た目を保ったまま親を差し替えるので、新しい親の変換を巻き戻す必要があります。逆行列が「変換を巻き戻す装置」だったことの、そのままの応用です。',
    },
    {
      q: '`scene.remove(mesh)` を呼んだだけでは解放されないものはどれですか。',
      choices: [
        'ジオメトリ・マテリアル・テクスチャ（GPU 側の資源）',
        'position の値',
        '親への参照',
        'イベントリスナー',
      ],
      answer: 0,
      explain:
        'シーングラフから外すことと、GPU の資源を返すことは別です。`dispose()` を呼ばないとメモリが積み上がり、「長く遊んでいると重くなる」原因になります。',
    },
  ],
};
