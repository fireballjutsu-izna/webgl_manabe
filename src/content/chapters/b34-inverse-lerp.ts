import type { Chapter } from '../types.ts';

export const chapterB34: Chapter = {
  slug: 'b34-inverse-lerp',
  part: 'basics',
  number: 34,
  title: '逆補間とクランプ ― 値を割合に戻す',
  goal: '値から割合を取り出せるようになり、ある範囲から別の範囲へ値を移す書き方と、はみ出しを押し込める書き方が身につきます。',
  requires: ['08-interp'],
  threeApis: ['MathUtils.inverseLerp', 'MathUtils.mapLinear', 'MathUtils.clamp', 'MathUtils.smoothstep'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

lerp は「割合 → 値」でした。実際のコードでは、**その逆**が同じくらい要ります。

- マウスの x 座標（0〜800）を、カメラの角度（−45〜45 度）にしたい
- 体力（0〜100）を、バーの長さ（0〜1）にしたい
- 距離（0〜50）を、霧の濃さ（0〜1）にしたい

どれも「ある範囲の値を、別の範囲に移す」形です。
そしてそれは、**逆補間して、lerp する**の 2 段でできています。
`,
    },
    {
      kind: 'md',
      text: `
## 逆補間 ― 引いて、幅で割る

[](#/ch/b05-ratio)で出てきた計算です。あらためて名前を付けます。

「$a$ から $b$ のあいだで、$v$ はどのあたりか」を 0〜1 で答えるのが**逆補間**
（inverse lerp）です。lerp の $t$ を求める計算、と言ってもいいです。
`,
    },
    {
      kind: 'formula',
      tex: 't = \\dfrac{v - a}{b - a}',
      readAloud:
        'いまの値から下限を引いて、幅（上限 − 下限）で割ると、0 から 1 の割合になります。lerp が t から値を作るのに対し、こちらは値から t を作ります。',
      worked: {
        given: '$a = 10$、$b = 30$ の範囲で、3 つの値を割合に直します。',
        steps: [
          { calc: 'v = 15 → (15 - 10) / 20 = 5 / 20  = 0.25' },
          { calc: 'v = 20 → (20 - 10) / 20 = 10 / 20 = 0.5', note: 'ちょうど真ん中' },
          { calc: 'v = 35 → (35 - 10) / 20 = 25 / 20 = 1.25', note: '範囲の外は 1 を超える' },
        ],
        result:
          '$v = 15$ で **0.25**。[](#/ch/08-interp)の計算例で $\\mathrm{lerp}(10, 30, 0.25) = 15$ を出したので、**ちょうど往復して戻ってきた**ことになります。範囲外の値は 0〜1 からはみ出します。',
      },
    },
    {
      kind: 'md',
      text: `
## 2 つをつなぐと、範囲の移し替えになる

逆補間で 0〜1 にして、そのまま lerp に渡す。これで**範囲から範囲へ移せます**。

たとえばマウスの x（0〜800）を角度（−45〜45）にするなら、

1. **逆補間** … $(600 - 0) / (800 - 0) = 0.75$
2. **lerp** … $-45 + 90 \\times 0.75 = 22.5$ 度

three にはこの 2 段をまとめた \`mapLinear\` があります。
ただし**中で何をしているかを知らずに使うと、範囲外のときに驚くことになります**。
そのまま外挿されるので、入力が範囲を出れば出力も出ます。
`,
    },
    {
      kind: 'md',
      text: `
## クランプ ― はみ出しを押し込める

範囲外を許したくないときは、**上限と下限で切り落とします**。これが clamp です。

$\\mathrm{clamp}(v,\\; \\text{min},\\; \\text{max})$ は、
$v$ が下限より小さければ下限、上限より大きければ上限、あいだならそのままを返します。

順番が大事です。**逆補間したあと、lerp する前に挟みます。**

- 逆補間 → **clamp** → lerp

こうすれば、入力が範囲外でも出力は必ず範囲内に収まります。
体力バーが画面をはみ出したり、霧が濃さ 1.3 になったりしません。

clamp を入れる場所を間違えて、lerp のあとに挟むと、
**色や角度としては正しい範囲でも、途中の対応関係が狂います。**
`,
    },
    {
      kind: 'formula',
      tex: '\\mathrm{clamp}(v,\\, l,\\, h) = \\min(\\max(v,\\, l),\\, h)',
      readAloud:
        'v と下限のうち大きいほう、それと上限のうち小さいほうを取ります。max で下限を保証し、min で上限を保証する、という 2 段の入れ子です。',
      worked: {
        given: '$\\mathrm{clamp}(v,\\; 0,\\; 1)$ を、3 つの値で追います。',
        steps: [
          { calc: 'v = 1.25 → max(1.25, 0) = 1.25 → min(1.25, 1) = 1', note: '上限で止まった' },
          { calc: 'v = 0.5  → max(0.5, 0)  = 0.5  → min(0.5, 1)  = 0.5', note: 'そのまま' },
          { calc: 'v = -0.3 → max(-0.3, 0) = 0    → min(0, 1)    = 0', note: '下限で止まった' },
        ],
        result:
          '**0〜1 に押し込まれました。** max と min を組み合わせただけで、場合分けを書かずに済んでいます。GLSL では `clamp` が組み込み関数として用意されているほど、よく使う操作です。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '分母が 0 になっていないか',
      text: `
$a$ と $b$ が同じ値だと、幅が 0 になって 0 で割ることになります。
JavaScript はエラーを出さず \`Infinity\` や \`NaN\` を返すので、
**そのまま座標や色に入って物体が消えます。**

範囲が動的に決まる場合（データの最小値と最大値から作る、など）は、
**幅が 0 のときに何を返すかを決めておいてください。**
`,
    },
    {
      kind: 'code',
      title: '範囲を移す',
      code: `import * as THREE from 'three';

// 値 → 割合
THREE.MathUtils.inverseLerp(10, 30, 15);   // 0.25

// 範囲から範囲へ（逆補間 → lerp をまとめたもの）
THREE.MathUtils.mapLinear(600, 0, 800, -45, 45);   // 22.5

// はみ出しを押し込める
THREE.MathUtils.clamp(1.25, 0, 1);   // 1
THREE.MathUtils.clamp(-0.3, 0, 1);   // 0

// 手で書くなら、順番はこの 3 段
const t = (v - min) / (max - min);            // 逆補間
const tc = THREE.MathUtils.clamp(t, 0, 1);    // 押し込める
const out = THREE.MathUtils.lerp(lo, hi, tc); // lerp

// 0〜1 に収めつつ、両端をなめらかにする（次の章で扱う）
THREE.MathUtils.smoothstep(v, min, max);`,
    },
    {
      kind: 'md',
      text: `
## この 3 段が、実は至るところにある

「逆補間 → clamp → lerp」の 3 段は、名前を変えて何度も出てきます。

- **霧** … 距離を 0〜1 にして、その割合で背景色と混ぜる
- **フェード** … 経過時間を 0〜1 にして、その割合で透明度を決める
- **グラデーション** … 高さを 0〜1 にして、その割合で 2 色を混ぜる
- **明るさの調整** … 内積（−1〜1）を 0〜1 に移して、色に掛ける

**「何かの値を、別の何かの見た目に対応させる」** という作業は、
ほぼすべてこの 3 段でできています。
そして次の章で、この 3 段の $t$ をひとひねりすると動きに表情が出ます。
`,
    },
  ],
  exercises: [
    {
      prompt: `$a = 20$、$b = 60$ の範囲について、逆補間の値を手で計算してください。

1. $v = 30$
2. $v = 60$
3. $v = 10$`,
      hint: '引いて、幅（40）で割ります。3 は範囲の外です。',
      answer: `1. $(30 - 20) \\div 40 = $ **0.25**
2. $(60 - 20) \\div 40 = $ **1.0**
3. $(10 - 20) \\div 40 = $ **−0.25**

3 が**マイナス**になるのが要点です。範囲の下に外れているので、負の割合が返ります。

これを clamp すれば 0 になり、そのまま lerp に渡せば下限の値が出ます。
**clamp を挟まなければ、下限より外側の値が出てしまいます。**`,
    },
    {
      prompt: `画面の幅が 1000 のとき、マウスの x 座標 250 を **−30 度から 30 度**の範囲に移してください。
逆補間 → lerp の 2 段で、手で計算します。`,
      hint: 'まず 0〜1 にしてから、−30 と 30 のあいだで lerp します。',
      answer: `1. **逆補間** … $250 \\div 1000 = 0.25$
2. **lerp** … $-30 + 60 \\times 0.25 = -30 + 15 = $ **−15 度**

マウスが左寄り（画面の 4 分の 1）なので、角度もマイナス側になりました。

three なら \`mapLinear(250, 0, 1000, -30, 30)\` の 1 行です。
ただし**マウスが画面外に出たときは範囲外の角度が返る**ので、
clamp を挟むかどうかを決めておく必要があります。`,
      answerCode: `const t = THREE.MathUtils.clamp(event.clientX / window.innerWidth, 0, 1);
const angleDeg = THREE.MathUtils.lerp(-30, 30, t);

// まとめて書くなら（clamp は別途）
THREE.MathUtils.mapLinear(250, 0, 1000, -30, 30);   // -15`,
    },
    {
      prompt: `体力 0〜100 を、バーの長さ 0〜1 に移すコードを書きました。
ところが**回復アイテムで体力が 120 になったとき、バーが画面をはみ出しました**。
どこに何を入れれば直るか、そして **lerp のあとに clamp を入れてはいけない理由**を説明してください。`,
      hint: '押し込めるのは、割合の段階です。',
      answer: `**逆補間のあと、lerp の前**に \`clamp(t, 0, 1)\` を入れます。

lerp のあとに clamp を入れても、この例では同じ結果になります（0〜1 に切るので）。
ですが**それは範囲が偶然 0〜1 だから**です。

たとえば角度 −30〜30 に移す場合、lerp のあとに \`clamp(x, 0, 1)\` と書くと、
**マイナスの角度がすべて 0 に潰れます**。範囲の下半分が消えるわけです。

**押し込めるのは割合（0〜1）の段階**、と決めておけば、
出力範囲が何であっても同じ書き方で通ります。`,
      answerCode: `// 正しい：割合の段階で押し込める
const t = THREE.MathUtils.clamp((hp - 0) / (100 - 0), 0, 1);
bar.scale.x = THREE.MathUtils.lerp(0, 1, t);

// 危ない：出力範囲が 0〜1 でないときに壊れる
const angle = THREE.MathUtils.clamp(THREE.MathUtils.lerp(-30, 30, t), 0, 1);`,
    },
  ],
  quiz: [
    {
      q: '$a = 0$、$b = 4$ のとき、$v = 3$ の逆補間はいくつですか。',
      choices: ['0.75', '3', '1.33', '0.25'],
      answer: 0,
      explain:
        '$(3 - 0) \\div (4 - 0) = 0.75$ です。「引いて、幅で割る」の 2 手だけで出ます。lerp の逆をやっている、と考えてください。',
    },
    {
      q: '「値を別の範囲に移す」処理で、clamp を入れる場所として適切なのはどれですか。',
      choices: [
        '逆補間したあと、lerp する前',
        'lerp したあと',
        '逆補間する前',
        'どこでも同じ',
      ],
      answer: 0,
      explain:
        '割合（0〜1）の段階で押し込めるのが正解です。lerp のあとに 0〜1 で切ると、出力範囲が 0〜1 でないとき（角度など）に範囲の一部が潰れます。',
    },
    {
      q: '逆補間で $a$ と $b$ が同じ値のとき、何が起きますか。',
      choices: [
        '0 で割ることになり、Infinity や NaN が返る',
        '常に 0 が返る',
        '常に 0.5 が返る',
        'エラーで処理が止まる',
      ],
      answer: 0,
      explain:
        '幅が 0 になるためです。JavaScript はエラーを出さないので、そのまま座標や色に入って物体が消えます。範囲が動的に決まるときは、幅 0 のときの扱いを決めておいてください。',
    },
  ],
};
