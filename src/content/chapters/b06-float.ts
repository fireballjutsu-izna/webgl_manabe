import type { Chapter } from '../types.ts';

export const chapterB06: Chapter = {
  slug: 'b06-float',
  part: 'basics',
  number: 6,
  title: '小数の落とし穴 ― 0.1 + 0.2 は 0.3 ではない',
  goal: 'コンピュータの小数がわずかにずれることを知り、値が等しいかどうかを「差が十分小さいか」で判定できるようになります。',
  requires: ['b05-ratio'],
  threeApis: ['Math.round', 'Math.floor', 'Math.ceil', 'Number.EPSILON'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

ブラウザのコンソールに \`0.1 + 0.2\` と打ってみてください。返ってくるのは

\`0.30000000000000004\`

です。バグではありません。**すべての言語でこうなります。**

この 1 行を知らないまま 3D を書くと、いつか必ず刺さります。

- 「箱がちょうど 1.0 に着いたら止める」が、いつまでも止まらない
- 回転を毎フレーム足していたら、じわじわ狙いからずれる
- 同じはずの 2 点が「同じ」と判定されない

この章はごく短いですが、**あとで 3 時間溶かさないための予防接種**です。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '3 分の 1 を、小数で書ききれますか',
      text: `
0.333… と書いても、どこかで打ち切るしかありません。
打ち切った時点で、それは $\\frac{1}{3}$ そのものではなく**ほんの少し小さい数**です。
10 進数で $\\frac{1}{3}$ が書ききれないのと同じことが、
2 進数のコンピュータでは $0.1$ に起きています。
コンピュータにとって 0.1 は、割り切れない数なのです。
`,
    },
    {
      kind: 'md',
      text: `
## なぜずれるのか

コンピュータは数を 2 進数で持ちます。2 進数の小数で正確に書けるのは、
$0.5$、$0.25$、$0.125$ のような**$2$ で割っていける数だけ**です。

$0.1$ は $2$ で割っていっても届かないので、いちばん近い 2 進数で代用します。
その「ほんの少し違う値」どうしを足すと、ずれが表に出ます。

逆に言えば、**$0.5$ や $0.25$ はぴったり**です。
\`0.5 + 0.25 === 0.75\` は true になります。
`,
    },
    {
      kind: 'md',
      text: `
## だから、小数は $=$ で比べない

やってはいけない書き方はこれです。

- \`if (x === 0.3)\`
- \`if (mesh.position.y === 1.0)\`
- \`if (t === 1)\`（$t$ を 0.1 ずつ足してきた場合）

代わりに、**差が十分小さいかどうか**で判定します。
「十分小さい」の基準を $\\varepsilon$（イプシロン）と書くのが慣習です。
`,
    },
    {
      kind: 'formula',
      tex: '|a - b| < \\varepsilon',
      readAloud:
        'a と b の隔たりが、決めておいた十分小さい値 ε より小さければ、同じとみなす、と言っています。絶対値を使うのは、どちらが大きくても同じに扱いたいからです。',
      worked: {
        given: '$a = 0.1 + 0.2$、$b = 0.3$、$\\varepsilon = 0.000001$ として判定します。',
        steps: [
          { calc: 'a = 0.30000000000000004', note: '足した結果。少しだけ大きい' },
          { calc: 'b = 0.3' },
          { calc: 'a - b ≈ 0.00000000000000006', note: 'ずれの正体。0 ではない' },
          { calc: '|a - b| ≈ 0.00000000000000006', note: 'プラスなので、絶対値でも変わらない' },
          { calc: '0.00000000000000006 < 0.000001 → true', note: 'ε より十分小さい' },
        ],
        result:
          '**同じとみなせます。** $\\varepsilon$ の値は用途で決めます。座標なら $0.001$ 程度、角度ならもっと小さくても構いません。**大きすぎると別物を同じと判定してしまう**ので、扱う値の大きさを見て決めてください。',
      },
    },
    {
      kind: 'md',
      text: `
## 足し続けると、ずれは積み上がる

1 回ぶんのずれは無視できる大きさです。問題は、それを毎フレーム足すときです。

毎秒 60 回、10 秒間で 600 回。小さなずれが 600 回積み上がると、目で見て分かる差になります。
とくに「回転を毎フレーム足していく」書き方は、時間が経つほど狙いからずれます。

**対策は、足し続けないこと。** 経過時間から毎回計算し直すほうが安全です。

- 積み上げる書き方 … \`mesh.rotation.y += 0.01\`（ずれが溜まる）
- 計算し直す書き方 … \`mesh.rotation.y = elapsed * 0.6\`（いつでも正しい）

後者なら、途中で一時停止しても、フレームが飛んでも、結果が狂いません。
この話は時間とループの章でもう一度出てきます。
`,
    },
    {
      kind: 'code',
      title: '小数を扱うときの型',
      code: `0.1 + 0.2;              // 0.30000000000000004
0.1 + 0.2 === 0.3;      // false ← これを踏む
0.5 + 0.25 === 0.75;    // true  ← 2 で割り切れる数はぴったり

// 差で判定する
const nearlyEqual = (a, b, eps = 1e-6) => Math.abs(a - b) < eps;
nearlyEqual(0.1 + 0.2, 0.3);  // true

// 用意されている「最小の刻み」。基準として使えるが、
// 積み上がった誤差にはこれでは小さすぎることが多い
Number.EPSILON;         // 2.220446049250313e-16

// 丸める
Math.round(2.5);        // 3  ― いちばん近い整数
Math.floor(2.9);        // 2  ― 小さいほうへ
Math.ceil(2.1);         // 3  ― 大きいほうへ
(0.1 + 0.2).toFixed(2); // "0.30" ← 文字列になることに注意`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'toFixed が返すのは、数ではなく文字列',
      text: `
\`(1.005).toFixed(2)\` は \`"1.00"\` という**文字列**です。
これをそのまま座標に入れると、three が数として扱えず物体が消えます。
表示のためだけに使い、計算には戻さないでください。
数として丸めたいときは \`Math.round(x * 100) / 100\` のように書きます。
`,
    },
    {
      kind: 'md',
      text: `
## 数でなくなる値 ― NaN と Infinity

小数のもう 1 つの罠が、**計算に失敗しても止まらない**ことです。

- $0$ で割ると \`Infinity\`
- 負の数の平方根を取ると \`NaN\`（Not a Number ＝ 数ではない）
- \`NaN\` は何と計算しても \`NaN\` のまま伝染する

そして \`mesh.position.x = NaN\` を代入しても、Three.js はエラーを出しません。
**その物体が黙って画面から消えるだけ**です。

「さっきまで映っていたのに、突然真っ黒になった」の原因はたいていこれです。
疑うのは、割り算の分母と、根号の中身の 2 つです。
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの式が \`true\` になるか \`false\` になるか、**実行する前に予想**してから確かめてください。

1. \`0.5 + 0.5 === 1\`
2. \`0.1 + 0.7 === 0.8\`
3. \`0.25 * 4 === 1\``,
      hint: '2 で割っていける数（0.5、0.25、0.125…）はぴったり表せます。',
      answer: `1. **true**。0.5 は 2 進数でぴったり表せます
2. **false**。実際には 0.7999999999999999 になります
3. **true**。0.25 もぴったりです

「小数は必ずずれる」ではなく、**2 で割り切れない小数がずれる**が正確な言い方です。
とはいえ実務では見分けるより、最初から差で比べるほうが安全です。`,
    },
    {
      prompt: `$a = 1.0000001$、$b = 1.0$ のとき、$|a - b|$ を計算してください。
$\\varepsilon = 0.001$ とした場合と、$\\varepsilon = 0.00000001$ とした場合で、
「同じとみなす」判定の結果はどうなりますか。`,
      hint: '$\\varepsilon$ より小さければ「同じ」です。$\\varepsilon$ の桁数を数えてください。',
      answer: `$|a - b| = 0.0000001$（$1 \\times 10^{-7}$）です。

- $\\varepsilon = 0.001$（$10^{-3}$）… $10^{-7} < 10^{-3}$ なので **同じとみなす**
- $\\varepsilon = 0.00000001$（$10^{-8}$）… $10^{-7} > 10^{-8}$ なので **別物とみなす**

**同じ 2 つの値でも、$\\varepsilon$ の決め方で答えが変わります。**
だから $\\varepsilon$ は「何を同じと呼びたいか」から決めます。
1 が 1 メートルのシーンで座標を比べるなら $0.001$（1 ミリ）で十分です。`,
    },
    {
      prompt: `箱を毎フレーム \`mesh.position.x += 0.1\` で動かし、
\`if (mesh.position.x === 10) stop()\` で止めようとしています。
このコードの何が危ないか、そして**どう書き換えるべきか**を説明してください。`,
      hint: '0.1 を 100 回足したとき、ぴったり 10 になるでしょうか。',
      answer: `0.1 はぴったり表せないので、100 回足しても 10 きっかりにはならず、
\`=== 10\` が一度も成立しないまま箱は通り過ぎます。

書き換え方は 2 つあります。

1. **通り過ぎたかで判定する** … \`if (mesh.position.x >= 10)\`。
   等号ではなく不等号にするだけで、ずれがあっても必ず止まります
2. **そもそも足し続けない** … 経過時間から位置を計算し直す。
   \`mesh.position.x = Math.min(elapsed * 6, 10)\` なら、ずれも溜まらず上限も守られます

実務では 2 のほうが好まれます。**ずれの原因そのものを消せる**からです。`,
      answerCode: `// 危ない
mesh.position.x += 0.1;
if (mesh.position.x === 10) stop();

// 直した（不等号にする）
mesh.position.x += 0.1;
if (mesh.position.x >= 10) stop();

// もっと良い（足し続けない）
mesh.position.x = Math.min(elapsed * 6, 10);`,
    },
  ],
  quiz: [
    {
      q: 'JavaScript で `0.1 + 0.2 === 0.3` の結果はどれですか。',
      choices: ['false', 'true', 'エラーになる', 'ブラウザによる'],
      answer: 0,
      explain:
        '0.1 も 0.2 も 2 進数では割り切れないため、足した結果は 0.30000000000000004 になります。どのブラウザでも、ほとんどの言語でも同じです。小数は等号で比べず、差が十分小さいかで判定してください。',
    },
    {
      q: '`mesh.position.x` に `NaN` が入ってしまったとき、何が起きますか。',
      choices: [
        'エラーは出ず、その物体が画面から消える',
        '例外が投げられてページが止まる',
        '自動的に 0 に直される',
        '見た目は何も変わらない',
      ],
      answer: 0,
      explain:
        'Three.js は NaN をエラーにしません。位置が計算できないので描画されなくなるだけです。「急に真っ黒になった」ときは、割り算の分母が 0 になっていないか、平方根の中が負になっていないかを疑ってください。',
    },
    {
      q: '毎フレーム値を足していく書き方より、経過時間から計算し直すほうが良いのはなぜですか。',
      choices: [
        '誤差が積み上がらず、フレームが飛んでも結果が狂わないから',
        '書く行数が減るから',
        '足し算より掛け算のほうが速いから',
        'three がそれ以外の書き方を許さないから',
      ],
      answer: 0,
      explain:
        '足し続けると小さなずれが回数ぶん積み上がります。経過時間から毎回計算し直せば、途中で何が起きても値はいつでも正しくなります。時間とループの章で、この考え方をもう一度扱います。',
    },
  ],
};
