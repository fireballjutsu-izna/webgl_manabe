import type { Chapter } from '../types.ts';

export const chapterB01: Chapter = {
  slug: 'b01-number-line',
  part: 'basics',
  number: 1,
  title: '数直線 ― マイナスは「反対向き」',
  goal: '正の数と負の数を「大きさ」と「向き」に分けて読めるようになり、コードの中の -1 や Math.abs が何をしているのか分かるようになります。',
  requires: [],
  threeApis: ['Math.abs', 'Math.sign', 'Math.min', 'Math.max'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D のコードには、\`camera.position.z = 5\`、\`light.position.set(-3, 4, -2)\`、
\`mesh.rotation.y = -Math.PI / 4\` のように、マイナスがやたらと出てきます。

このマイナスを「小さい数」だと思って読んでいると、いつまでも
「なぜここだけマイナスなのか」が腑に落ちません。3D のマイナスは
**小さいという意味ではなく、反対を向いているという意味**です。

この章では、線を 1 本引くだけでその区別を付けます。まだ 3D は出てきません。
急がば回れで、ここが[](#/ch/02-vector)にそのまま効きます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: 'エレベーターの階数だと思ってください',
      text: `
1 階、2 階、3 階と上がっていって、地下に行くと B1、B2 になります。
このとき B2 は「2 という大きさで、下向き」です。
「2 階より小さい」とは言いますが、**動く距離としては 2 階と同じ 2 つぶん**です。
マイナスの数は、この「向きが逆の 2」だと思ってください。
`,
    },
    {
      kind: 'md',
      text: `
## 線を 1 本引いて、数を場所にする

紙に横線を 1 本引き、まんなかに 0 を書きます。右へ 1 ずつ数えて 1, 2, 3…、
左へ 1 ずつ数えて -1, -2, -3… と書き入れます。これが{{数直線}}です。

これで、**数がすべて「線の上の場所」になりました。**
「5 は 3 より大きい」は「5 は 3 より右にある」と言い換えられます。
そして -5 は -3 より左なので、**-5 のほうが小さい**ということになります。

ここでいったん、大事な区別を作ります。

- **どちら向きか** … 0 より右か左か。これが**符号**です
- **どれだけ離れているか** … 0 からの隔たり。これが{{絶対値}}です

-5 は「左向きで、隔たりは 5」。5 は「右向きで、隔たりは 5」。
**向きは反対、隔たりは同じ**です。3D で「反対を向く」を書くときは、
この「隔たりはそのまま、符号だけ変える」をやっています。
`,
    },
    {
      kind: 'demo',
      id: 'number-line',
      caption:
        'a を動かして、-a がいつも 0 をはさんだ反対側に来ることを確かめてください。|a| は 0 からの隔たりなので、a がマイナスでも増えていきます。「a + b を移動として見る」を入れると、足し算が「その向きに進む」ことだと分かります。',
    },
    {
      kind: 'md',
      text: `
## 絶対値 ― 符号を落として、隔たりだけ取り出す

$|a|$ と書いて「a の絶対値」と読みます。縦棒ではさむだけの記法です。
やっていることは 1 つ、**マイナスならプラスにする**。それだけです。

JavaScript では \`Math.abs(a)\` がこれにあたります。
`,
    },
    {
      kind: 'formula',
      tex: '|a| = \\begin{cases} a & (a \\ge 0) \\\\ -a & (a < 0) \\end{cases}',
      readAloud:
        'a がゼロ以上ならそのまま、マイナスなら符号をひっくり返す、と言っているだけです。場合分けの書き方（大きな中かっこ）は数学でよく出るので、形だけ見慣れておいてください。',
      worked: {
        given: '$a = -5$ と $a = 5$ の 2 つで、実際に手順を追います。',
        steps: [
          { calc: 'a = -5 → マイナスなので下の行', note: '-a を計算する' },
          { calc: '     -(-5) = 5', note: 'マイナスのマイナスはプラス' },
          { calc: 'a =  5 → 0 以上なので上の行', note: 'そのまま' },
          { calc: '      5' },
        ],
        result: 'どちらも **5**。符号は消え、0 からの隔たりだけが残りました。これが絶対値です。',
      },
    },
    {
      kind: 'md',
      text: `
## 足し算は「その向きに進む」

$3 + (-5)$ を計算してみます。学校では「引き算に直す」と教わりますが、
数直線ではもっと素直です。

- まず 0 から右へ 3 進む（3 の場所に立つ）
- そこから **-5 のぶん進む**。-5 は左向きの 5 なので、左へ 5 歩

着いた先は -2 です。**足し算は、いまいる場所からその数のぶん進むこと。**
プラスなら右へ、マイナスなら左へ進むだけで、規則は 1 つしかありません。

デモの「a + b を移動として見る」が、まさにこれをやっています。
この見方は、あとで矢印を継ぎ足す{{ベクトル}}の足し算にそのまま化けます。
`,
    },
    {
      kind: 'code',
      title: '符号と絶対値を扱う',
      code: `// 隔たりだけが欲しいとき
Math.abs(-5);        // 5
Math.abs(5);         // 5

// 向きだけが欲しいとき（1 / 0 / -1 が返る）
Math.sign(-5);       // -1
Math.sign(0);        // 0

// 符号をひっくり返す ＝ 反対を向かせる
const speed = 2;
const back = -speed; // -2

// 大きい・小さいは「右にある・左にある」
Math.max(-5, -3);    // -3（-3 のほうが右にある）
Math.min(-5, -3);    // -5`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '「マイナスのほうが大きい」と言いたくなるのは、絶対値を見ているから',
      text: `
-100 と -1 なら、-100 のほうが「大きい数」に見えます。見ているのは隔たり（絶対値）で、
数そのものの大小ではありません。数としては **-100 のほうが小さい**（左にある）。
コードで \`if (x > -1)\` のような判定を書くときは、必ず数直線の左右で考えてください。
`,
    },
    {
      kind: 'md',
      text: `
## 3D では、この符号がそのまま「向き」になる

先ほどのコードに戻ります。

- \`camera.position.z = 5\` … z の**プラス側**に 5 だけ離れて立つ
- \`light.position.set(-3, 4, -2)\` … x はマイナス側、y はプラス側、z はマイナス側

3 つの数それぞれに、**符号（どちら側か）と絶対値（どれだけ離れているか）**があります。
どの向きがプラスなのかは、このあと座標の章で決めます。
いまは「数 1 つが、向きと隔たりの両方を持っている」ことだけ持ち帰ってください。
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの値を手で計算してください。デモを使ってもかまいませんが、**先に自分で答えを出してから**確かめてください。

1. $|-7|$
2. $|3 - 10|$
3. $-(-4)$
4. $-8 + 3$`,
      hint: '2 は、かっこの中を先に計算してから絶対値を取ります。4 は「-8 の場所から右へ 3 歩」です。',
      answer: `1. **7**。符号を落として隔たりだけを残します
2. $3 - 10 = -7$ なので $|-7| = $ **7**。「3 と 10 の隔たり」を出す典型的な書き方です
3. **4**。マイナスを 2 回かけるとプラス。「反対の反対」は元どおり
4. **-5**。-8 の場所から右へ 3 歩進むと -5。0 は越えられていません`,
    },
    {
      prompt: `$|a - b|$ は「a と b の隔たり」を表します。
$a = 2$、$b = 9$ のときと、$a = 9$、$b = 2$ のときで計算し、**なぜ順番を入れ替えても同じになるのか**を説明してください。`,
      hint: '引き算の結果は符号が逆になります。そのあと絶対値で何が起きますか。',
      answer: `どちらも **7** です。

- $|2 - 9| = |-7| = 7$
- $|9 - 2| = |7| = 7$

引く順番を変えると符号だけがひっくり返り、絶対値がその符号を落とすからです。
「隔たり」に向きは無いので、これは望ましい性質です。
この式は 2 点の距離を測るときの土台になります。`,
    },
    {
      prompt: `箱を「いまの位置から、向きを変えずに 2 だけ戻す」コードを書きたい。
\`mesh.position.z\` が 5 のとき、\`mesh.position.z -= 2\` と \`mesh.position.z = -2\` は
それぞれ結果がどうなりますか。`,
      hint: '前者は「いまの場所から進む」、後者は「場所を決め打ちする」です。',
      answer: `- \`mesh.position.z -= 2\` … 5 から左へ 2 進んで **3**。これが「戻す」
- \`mesh.position.z = -2\` … いまの位置を無視して **-2** に置き直す。原点の反対側へ飛ぶ

足し算・引き算は**いまの場所からの移動**、代入は**場所の指定**です。
3D で「物が思わぬところへ飛ぶ」ときの原因は、たいていこの取り違えです。`,
      answerCode: `mesh.position.z = 5;

mesh.position.z -= 2;  // 3  ← 相対的な移動
mesh.position.z = -2;  // -2 ← 絶対的な指定`,
    },
  ],
  quiz: [
    {
      q: '$-3$ と $-7$ ではどちらが大きいですか。',
      choices: ['-3', '-7', '同じ', '比べられない'],
      answer: 0,
      explain:
        '数直線で右にあるほうが大きい数です。-3 は -7 より右にあるので -3 のほうが大きくなります。「-7 のほうが大きい気がする」と感じたときは、絶対値（0 からの隔たり）を見ています。',
    },
    {
      q: '`Math.abs(x)` が返すものはどれですか。',
      choices: [
        'x が 0 からどれだけ離れているか',
        'x の符号（プラスかマイナスか）',
        'x を四捨五入した値',
        'x の逆数',
      ],
      answer: 0,
      explain:
        '絶対値は符号を落として隔たりだけを残します。符号だけが欲しいときは `Math.sign(x)` を使います。',
    },
    {
      q: '$-4 + 9$ はいくつですか。',
      choices: ['5', '-5', '13', '-13'],
      answer: 0,
      explain:
        '-4 の場所から右へ 9 歩進みます。4 歩で 0 に着き、さらに 5 歩進むので 5 です。足し算は「その向きに進む」と読むと、符号が混ざっても迷いません。',
    },
  ],
};
