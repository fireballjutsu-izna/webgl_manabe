import type { Chapter } from '../types.ts';

export const chapterM39: Chapter = {
  slug: 'm39-recursion',
  part: 'math3d',
  number: 39,
  title: '再帰 ― 自分を呼ぶ形',
  goal: '再帰の 2 つの部品（止まる条件と、小さくして呼ぶ部分）が分かり、階層を安全にたどれるようになります。',
  requires: ['09-hierarchy', 'b02-variable'],
  threeApis: ['Object3D.traverse', 'Object3D.children', 'Object3D.getObjectByName'],
  blocks: [
    {
      kind: 'md',
      text: `
## 実はもう、何度も使っています

3D で「同じ形が入れ子になっている」ものは、驚くほど多くあります。

- **木** … 幹から枝が出て、その枝からまた枝が出る
- **階層** … 親の中に子がいて、その子の中にまた子がいる
- **地形** … 大きな起伏の上に、小さな起伏が乗っている
- **曲線の分割** … 線分を割ったら、その半分をまた割る

これらを扱う道具が**再帰**です。
そして[](#/ch/09-hierarchy)で使った \`traverse()\` は、**中身が再帰**です。
知らないうちに使っていたことになります。
`,
    },
    {
      kind: 'md',
      text: `
## 部品は 2 つだけ

再帰の関数は、必ず 2 つの部分でできています。

**① 止まる条件（基底）**

「これ以上は呼ばない」という線です。**これが無いと、永遠に呼び続けて落ちます。**

**② 小さくして、自分を呼ぶ部分**

問題を一回り小さくしてから、同じ関数を呼びます。
**必ず小さくなっている**ことが重要です。小さくならなければ、①に到達しません。

木で言えば ― 「枝の長さが 0.1 以下になったら葉を付けて終わり（①）、
そうでなければ短い枝を 2 本生やして、それぞれに同じ処理をする（②）」。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '合わせ鏡',
      text: `
向かい合わせた 2 枚の鏡を覗くと、像が奥へ無限に続いて見えます。

鏡が映すのは「相手の鏡が映しているもの」で、
その相手も同じことをしている ― これが再帰の形です。

現実の合わせ鏡が無限に続かないのは、
反射のたびに少し暗くなり、やがて見えなくなるからです。

これが「止まる条件」に当たります。
`,
    },
    {
      kind: 'formula',
      tex: 'N(d) = b^{\\,d}',
      readAloud:
        '深さ d まで再帰したときの、いちばん外側の数です。b は 1 回ごとに分かれる数。深さに対して指数で増えるので、深さを 1 増やすだけで全体が b 倍になります。',
      worked: {
        given: '1 本の枝から 3 本ずつ枝分かれする木で、深さごとの枝の数を数えます。',
        steps: [
          { calc: '深さ 0 : 3^0 = 1 本', note: '幹だけ' },
          { calc: '深さ 3 : 3^3 = 27 本' },
          { calc: '深さ 6 : 3^6 = 729 本' },
          { calc: '深さ 10 : 3^10 = 59049 本' },
          { calc: '全部の合計（深さ10まで）: (3^11 - 1) / 2 = 88573 本' },
        ],
        result:
          '**深さ 10 で 6 万本近く**になります。深さを 1 増やすたびに 3 倍 ―  10 から 11 にするだけで 12 万本です。**再帰で形を作るときは、深さ 5 か 6 で止めるのが現実的**です。それ以上は目に見える違いより、重さのほうが先に来ます。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '止まる条件を忘れると、タブごと固まります',
      text: `
無限に自分を呼ぶと、呼び出しの記録が積み上がって
RangeError: Maximum call stack size exceeded で落ちます。

これはまだ良いほうで、
1 回ごとに Mesh を作る処理だと、落ちる前にメモリを食い尽くします。

再帰を書くときは、止まる条件を先に書いてください。
関数の 1 行目に書く癖をつけると、忘れようがなくなります。
`,
    },
    {
      kind: 'code',
      title: '再帰の形',
      code: `import * as THREE from 'three';

// 木を生やす。止まる条件を先に書く
function branch(parent, length, depth) {
  if (depth <= 0 || length < 0.1) return;    // ① 止まる条件

  const stick = new THREE.Mesh(
    new THREE.CylinderGeometry(length * 0.06, length * 0.08, length, 5),
    barkMaterial,
  );
  stick.position.y = length / 2;
  parent.add(stick);

  // ② 小さくして、自分を呼ぶ
  for (let i = 0; i < 3; i += 1) {
    const child = new THREE.Group();
    child.position.y = length;                        // 枝の先へ
    child.rotation.y = (i / 3) * Math.PI * 2;
    child.rotation.z = THREE.MathUtils.degToRad(28);
    parent.add(child);
    branch(child, length * 0.7, depth - 1);           // 短くして呼ぶ
  }
}

const tree = new THREE.Group();
branch(tree, 3, 5);                                   // 深さ 5 で止める
scene.add(tree);

// three の traverse も、中身は再帰
scene.traverse((obj) => {
  if (obj.isMesh) obj.castShadow = true;
});

// 自分で書くとこうなる
function walk(obj, fn) {
  fn(obj);
  for (const child of obj.children) walk(child, fn);  // 子に同じことをする
}`,
    },
    {
      kind: 'md',
      text: `
## 深さを 1 増やすと、世界が変わる

計算例で見たとおり、再帰の重さは**深さに対して指数**で増えます。

これは 3D では実害に直結します。

- **枝分かれ 3、深さ 8** … 6561 本。まだ動く
- **深さ 10** … 59049 本。**ドローコールが 6 万回**になり、描画が止まる
- **深さ 12** … 53 万本。作る途中でメモリが尽きる

だから再帰で形を作るときは、**深さの上限を必ず決めます。**
そして「深さ 1 につき何倍か」を意識してください。

2 倍なら深さ 15 でも 3 万ですが、4 倍なら深さ 8 で 6 万です。
**同じ深さでも、分かれる数が 1 違うだけで桁が変わります。**
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '深さの上限を、引数で持ち回る',
      text: `
「長さが 0.1 以下になったら止める」だけだと、
パラメータをいじったときに止まらなくなることがあります。

深さのカウンタも一緒に渡して、二重に守るのが安全です。

depth <= 0 || length < 0.1 のように、
どちらか一方でも満たしたら止まる形にしておけば、
値をどういじっても暴走しません。
`,
    },
  ],
  exercises: [
    {
      prompt: `1 本の枝から 2 本ずつ分かれる木で、深さ 12 まで作ると、
いちばん外側の枝は何本になりますか。また全部の枝を合わせると何本ですか。`,
      hint: '$2^{12}$ を求めます。合計は等比数列の和です。',
      answer: `**いちばん外側** … $2^{12} = 4096$ 本

**全部の合計** … $2^0 + 2^1 + \\dots + 2^{12} = 2^{13} - 1 = 8191$ 本

分かれる数が 2 なら、深さ 12 でも 8000 本ほどで収まります。**まだ描けます。**

ところが**分かれる数を 3 にすると** $3^{12} = 531441$ 本 ―
いきなり 53 万本です。合計は 80 万本近くになります。

**分かれる数が 1 違うだけで、桁が 2 つ変わります。**
再帰で形を作るときは、深さより先に「1 回で何本に分かれるか」を決めてください。
そのうえで、許せる本数から深さの上限を逆算するのが現実的です。`,
    },
    {
      prompt: `次の再帰関数には問題があります。何が起きますか。

\`grow(parent, length)\` は、\`makeStick(length)\` で作った Mesh を \`parent\` に足し、
そのあと \`grow(stick, length * 0.9)\` を 2 回呼びます。それだけの関数です。`,
      hint: '止まる条件はどこにありますか。',
      answer: `**止まる条件がありません。**

\`length\` は 0.9 倍ずつ減りますが、**0 にはなりません。**
浮動小数点では非常に小さい値まで下がり続けるだけで、
「もう十分小さい」と判断する処理が無いので止まりません。

そして 1 回ごとに \`Mesh\` を作っているので、
スタックが溢れる前に**メモリを食い尽くす**可能性が高いです。
タブが固まり、ページを閉じるしかなくなります。

**直し方** … 関数の 1 行目に止まる条件を書きます。
長さと深さの**両方**で守るのが安全です。

長さだけだと、係数を 0.99 に変えたときに深さが極端に増えます。
深さのカウンタがあれば、どんな係数でも止まります。`,
      answerCode: `function grow(parent, length, depth) {
  if (depth <= 0 || length < 0.05) return;   // 1 行目に書く

  const stick = makeStick(length);
  parent.add(stick);
  for (let i = 0; i < 2; i += 1) {
    grow(stick, length * 0.9, depth - 1);
  }
}

grow(tree, 3, 8);`,
    },
    {
      prompt: `シーンの中から、名前に「enemy」を含むメッシュを全部集めたい。
再帰で書く方法と、three の \`traverse\` を使う方法をそれぞれ示してください。`,
      hint: '`traverse` の中身は何でしたか。',
      answer: `**\`traverse\` を使うのが実務の答え**です。中身は再帰なので、やっていることは同じです。

自分で書くとしても、形は 3 行で済みます。

- 自分に対する処理をする
- 子それぞれに、同じ関数を呼ぶ
- 子がいなければ、ループが回らないので自然に止まる

**この場合、明示的な「止まる条件」は要りません。**
葉（子を持たない物体）に着いたら \`for\` が 0 回になり、そこで止まるからです。

**「小さくする」が構造の側に組み込まれている**再帰は、こういう形になります。
木を生やす場合と違って、階層は有限だと分かっているからです。

ただし**循環参照があると止まりません**（A の子が B、B の子が A）。
three のシーングラフでは起こりませんが、
自分でグラフを組む場合は訪問済みの印が要ります。`,
      answerCode: `// three に任せる
const enemies = [];
scene.traverse((obj) => {
  if (obj.isMesh && obj.name.includes('enemy')) enemies.push(obj);
});

// 自分で書く（traverse の中身と同じ）
function collect(obj, out) {
  if (obj.isMesh && obj.name.includes('enemy')) out.push(obj);
  for (const child of obj.children) collect(child, out);
}`,
    },
  ],
  quiz: [
    {
      q: '再帰の関数に必ず必要な 2 つの部品はどれですか。',
      choices: [
        '止まる条件と、小さくして自分を呼ぶ部分',
        'ループと配列',
        '引数と戻り値',
        '深さと幅',
      ],
      answer: 0,
      explain:
        '止まる条件が無いと永遠に呼び続けて落ちます。また「必ず小さくなっている」ことも重要で、小さくならなければ止まる条件に到達しません。',
    },
    {
      q: '3 本ずつ分かれる木を深さ 10 まで作ると、いちばん外側は何本ですか。',
      choices: ['約 6 万本', '30 本', '約 1000 本', '無限'],
      answer: 0,
      explain:
        '$3^{10} = 59049$ です。深さを 1 増やすたびに 3 倍になるので、11 にすれば 12 万本。再帰で形を作るときは深さ 5 か 6 で止めるのが現実的です。',
    },
    {
      q: '`scene.traverse()` の中身は何ですか。',
      choices: [
        '再帰（自分を処理して、子それぞれに同じことをする）',
        '二重ループ',
        '配列の並べ替え',
        '行列の掛け算',
      ],
      answer: 0,
      explain:
        '階層をたどる処理は、構造そのものが再帰的です。この場合「子がいなければループが 0 回になる」ので、明示的な止まる条件は要りません。',
    },
  ],
};
