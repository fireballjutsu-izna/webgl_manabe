import type { Chapter } from '../types.ts';

export const chapter03: Chapter = {
  slug: '03-dot',
  part: 'basics',
  number: 19,
  title: '内積 ― 角度を測る',
  goal: '2つの向きがどれくらい揃っているかを1つの数で判定できるようになり、「前にいるか後ろにいるか」や面の明るさを自力で計算できるようになります。',
  requires: ['b17-normalize', '05-trig'],
  threeApis: ['Vector3.dot', 'Vector3.angleTo', 'Vector3.projectOnVector', 'Object3D.getWorldDirection'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- 敵はプレイヤーの**視界に入っている**のか、それとも背後にいるのか
- この面には光が**当たっている**のか、影になっているのか
- カメラは目標のほうを**向いている**のか

どれも「2 つの向きがどれくらい揃っているか」を知りたい、という同じ問いです。
そしてこの問いには、たった 1 つの計算で答えが出ます。それが{{内積}}です。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '追い風か向かい風か',
      text: `
自転車で走っているところを想像してください。風が真後ろから吹いていれば楽で（プラス）、
真横なら進みに関係がなく（ゼロ）、正面からなら押し戻されます（マイナス）。
内積は「進行方向に対して、その力がどれだけ味方しているか」を数にしたものです。
`,
    },
    {
      kind: 'md',
      text: `
## 計算そのものは拍子抜けするほど簡単

成分どうしを掛けて、全部足すだけです。結果はベクトルではなく**ただの数 1 つ**になります。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} \\cdot \\mathbf{b} = a_x b_x + a_y b_y + a_z b_z',
      readAloud:
        'a と b のあいだの中黒（・）が内積の記号で、「a ドット b」と読みます。x どうし、y どうし、z どうしを掛けて、3 つを足し合わせるだけです。答えは矢印ではなく、ただの数字が 1 つ出てきます。',
      worked: {
        given: '$\\mathbf{a} = (2,\\; 0,\\; 0)$（右向き）、$\\mathbf{b} = (0,\\; 3,\\; 0)$（上向き）のとき。',
        steps: [
          { calc: 'x どうし :  2 x 0 = 0' },
          { calc: 'y どうし :  0 x 3 = 0' },
          { calc: 'z どうし :  0 x 0 = 0' },
          { calc: '合計      :  0 + 0 + 0 = 0', note: '最後に 3 つを足す。出てくるのは矢印ではなく数字 1 つ' },
        ],
        result: '$\\mathbf{a} \\cdot \\mathbf{b} = 0$。右向きと上向きは**直角**なので 0 になりました。この 0 を、次の式でも別のやり方で出してみます。',
      },
    },
    {
      kind: 'md',
      text: `
## 本当に大事なのは、こちらの式

内積には、角度と結びつくもう 1 つの顔があります。こちらが 3D での主役です。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} \\cdot \\mathbf{b} = \\|\\mathbf{a}\\|\\,\\|\\mathbf{b}\\|\\cos\\theta',
      readAloud:
        'a ドット b は、a の長さ・b の長さ・そして 2 本のなす角のコサインを、3 つ掛け合わせた値に等しい、という意味です。θ（シータ）は角度を表すギリシャ文字です。',
      worked: {
        given: 'さっきと**まったく同じ** $\\mathbf{a} = (2,\\; 0,\\; 0)$、$\\mathbf{b} = (0,\\; 3,\\; 0)$ を、こんどは長さと角度から計算します。',
        steps: [
          { calc: 'a の長さ  = 2' },
          { calc: 'b の長さ  = 3' },
          { calc: 'なす角    = 90 度' },
          { calc: 'cos 90 度 = 0', note: 'ここが 0 になるので、掛けた結果も 0 になる' },
          { calc: '2 x 3 x 0 = 0' },
        ],
        result: '**さっきと同じ 0 が出ました。** 別々に見えた 2 つの式が、同じ答えを返すことをいま自分で確かめたことになります。片方は成分から、もう片方は角度から ― どちらから入っても同じ場所に着きます。',
      },
    },
    {
      kind: 'md',
      text: `
コサインは[](#/ch/05-trig)でやったばかりです。ここで効いてくるのは
「**なす角が 0°のとき 1、90°のとき 0、180°のとき −1 になる、なめらかな値**」という性質で、
単位円の横位置がそのまま出てきていると思ってください。

ここから、内積の**符号**だけを見れば位置関係が分かる、という強力な道具が手に入ります。

- 内積が**プラス** … 2 本は同じ側を向いている（なす角は 90°未満）
- 内積が**ゼロ** … ぴったり{{直交}}している（90°）
- 内積が**マイナス** … 逆側を向いている（90°より大きい）
`,
    },
    {
      kind: 'demo',
      id: 'dot-angle',
      caption:
        '角度のスライダーを 0°から 180°までゆっくり動かして、内積の符号が切り替わる瞬間を見てください。ちょうど 90°で 0 を通過します。「長さを 1 に揃える」を入れると、内積の値がそのまま cosθ になります。',
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '長さ 1 に揃えてから使うのが定石',
      text: `
両方を{{正規化}}しておくと、内積はそのまま cosθ になります（長さ 1 × 長さ 1 × cosθ なので）。
つまり **必ず −1 から 1 のあいだに収まる**扱いやすい値になります。
向きだけを比べたいときは、まず正規化。これを習慣にしてください。
`,
    },
    {
      kind: 'md',
      text: `
## 使い道その1 ― 前か後ろかを判定する

プレイヤーの視線の向きを $\\mathbf{f}$、プレイヤーから敵へ向かうベクトルを $\\mathbf{d}$ とします。
両方を正規化して内積を取れば、それだけで判定が終わります。

- $\\mathbf{f}\\cdot\\mathbf{d} > 0$ なら敵は前方
- $\\mathbf{f}\\cdot\\mathbf{d} > 0.7$ くらいなら、だいたい正面 45°以内
- マイナスなら背後

しきい値を変えるだけで視界の広さを調整できるのが気持ちのいいところです。
`,
    },
    {
      kind: 'code',
      title: '視界に入っているかを判定する',
      code: `import * as THREE from 'three';

// プレイヤーが向いている方向（長さ1で返ってくる）
const forward = new THREE.Vector3();
player.getWorldDirection(forward);

// プレイヤーから敵へ向かう方向
const toEnemy = enemy.position.clone().sub(player.position).normalize();

const d = forward.dot(toEnemy);

if (d > 0.7) {
  // 正面およそ45度の内側にいる
} else if (d > 0) {
  // 前方ではあるが、視界の端
} else {
  // 背後
}

// 角度そのものが欲しいならこちら（ラジアンで返る）
const radians = forward.angleTo(toEnemy);`,
    },
    {
      kind: 'md',
      text: `
## 使い道その2 ― 面の明るさ

もう 1 つの定番が、光の当たり方です。面が向いている向き（{{法線}}）と、光が来る向きの内積が、
そのまま明るさになります。真正面から光が当たれば 1、斜めになるほど小さくなり、
裏を向いたらマイナス——マイナスの明るさはないので、0 で止めます。

この単純きわまりない仕組みが{{ランバート反射}}で、あらゆる 3D の陰影の出発点です。
`,
    },
    {
      kind: 'demo',
      id: 'dot-light',
      caption:
        '面の傾きと光の向きを別々に動かせます。パネルの色は three のライトではなく、内積の値から直接作っています。「マイナスを 0 で止める」を外すと、裏面がうっすら光る不自然な状態になります。',
    },
    {
      kind: 'md',
      text: `
## 使い道その3 ― 影を落とす（射影）

デモに出ていた「b の影」は、b を a の向きに落としたものでした。これを射影と呼びます。
壁に沿って滑る移動や、坂道での速度の分解など、応用範囲がとても広い操作です。

いまは「内積は、片方をもう片方の向きに落としたときの長さでもある」とだけ覚えておけば十分です。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '正規化を忘れると、しきい値が意味を失います',
      text: `
正規化していないベクトルの内積は、長さが変わるだけで値が跳ね上がります。
「0.7 以上なら正面」といった判定は、**両方が長さ 1 のときにだけ**成り立ちます。
判定がうまく動かないときは、まず正規化を疑ってください。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「両方の長さを 1 に揃える」を入れてから、角度を 0 度・60 度・90 度・180 度にしてください。
そのときの a·b の値を 4 つ書き出し、**cosθ の値と見比べて**ください。`,
      hint: '長さを両方 1 にすると、内積の式から長さの掛け算が消えます。',
      answer: `0 度で 1、60 度で 0.5、90 度で 0、180 度で −1 になり、**cosθ そのもの**と一致します。
長さが両方 1 なら、内積は $1 \\times 1 \\times \\cos\\theta$ だからです。
だから「単位ベクトルどうしの内積は cos」と覚えてしまってよく、ライティングの計算はこれを使っています。`,
    },
    {
      prompt: '光のデモで「マイナスを 0 で止める」を**外して**、面が光に背を向けるところまで傾けてください。何が起きますか。止める必要があるのはなぜでしょう。',
      hint: '明るさがマイナスになると、その面はどう塗られるべきでしょうか。',
      answer: `n·l がマイナスになり、明るさもマイナスになります。
現実には「マイナスの明るさ」はありません。背を向けた面は、ただ暗いだけです。
放っておくと色の計算が壊れる（暗くするどころか、ほかの光のぶんまで引いてしまう）ので、\`Math.max(0, ...)\` で 0 で止めます。
GLSL では \`max(dot(n, l), 0.0)\` と書き、これが Lambert のライティングそのものです。`,
    },
    {
      prompt: '敵がプレイヤーの**正面 60 度の範囲**（左右に 30 度ずつ）にいるかどうかを判定してください。プレイヤーの向き \`forward\` は長さ 1 とします。',
      hint: '角度に戻してから比べるより、cos のまま比べたほうが速くて素直です。cos は角度が小さいほど大きい値になります。',
      answer: `プレイヤーから敵へのベクトルを正規化して \`forward\` と内積を取り、**cos 30 度と比べます**。
\`Math.acos\` で角度に戻してから比べても正しいのですが、逆三角関数は重いうえ、
丸め誤差で値がわずかに 1 を超えると \`NaN\` になります。cos のまま比べるほうが安全です。
不等号の向きに注意してください。**角度が小さいほど内積は大きい**ので、範囲内の条件は「以上」です。`,
      answerCode: `const toEnemy = new THREE.Vector3().subVectors(enemy.position, player.position).normalize();
const limit = Math.cos(THREE.MathUtils.degToRad(30)); // 半分の角度で比べる

if (toEnemy.dot(forward) >= limit) {
  // 正面 60 度の中にいる
}`,
    },
  ],
  quiz: [
    {
      q: '2 本のベクトルの内積が **0** でした。2 本の関係はどれですか。',
      choices: ['直角に交わっている', '同じ向きを向いている', '正反対を向いている', 'どちらかの長さが必ず 0'],
      answer: 0,
      explain:
        '内積は長さ × 長さ × cosθ で、cos90° は 0 です。どちらかの長さが 0 でも内積は 0 になりますが、両方に長さがある場合は「直交している」ことを意味します。',
    },
    {
      q: '長さ 1 に揃えた 2 本のベクトルの内積が **−1** でした。なす角は何度ですか。',
      choices: ['180°', '0°', '90°', '45°'],
      answer: 0,
      explain:
        '両方が長さ 1 なら内積はそのまま cosθ です。cosθ が −1 になるのは θ が 180°のとき、つまり正反対を向いているときです。',
    },
    {
      q: '正規化していない長いベクトルどうしで内積を取ると、何が起きますか。',
      choices: [
        '長さのぶんだけ値が大きくなり、しきい値による判定が壊れる',
        '常に 0 になる',
        '常に 1 と −1 のあいだに収まる',
        'エラーになって計算できない',
      ],
      answer: 0,
      explain:
        '内積には長さが掛かっています。−1〜1 に収まるのは両方が長さ 1 のときだけなので、角度で判定したいなら先に正規化が必要です。',
    },
  ],
};
