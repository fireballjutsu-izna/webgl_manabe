import type { Chapter } from '../types.ts';

export const chapter03: Chapter = {
  slug: '03-dot',
  part: 'basics',
  number: 24,
  title: '内積 ― 成分を掛けて、足す',
  goal: '内積の計算ができるようになり、返るのがベクトルではなく数 1 つであること、順番を入れ替えても同じであることが分かります。',
  requires: ['b17-normalize'],
  threeApis: ['Vector3.dot'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- 敵はプレイヤーの**視界に入っている**のか、それとも背後にいるのか
- この面には光が**当たっている**のか、影になっているのか
- カメラは目標のほうを**向いている**のか

どれも「2 つの向きがどれくらい揃っているか」を知りたい、という同じ問いです。
そしてこの問いには、たった 1 つの計算で答えが出ます。それが{{内積}}です。

これから 5 章かけて内積を扱います。**それだけの価値がある道具**で、
3D の陰影も、視界の判定も、壁ずりの移動も、全部これでできています。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '追い風か向かい風か',
      text: `
自転車で走っているところを想像してください。風が真後ろから吹いていれば楽で（プラス）、
真横なら進みに関係がなく（ゼロ）、正面からなら押し戻されます（マイナス）。
内積は「進行方向に対して、その力がどれだけ味方しているか」を数にしたものです。
`,
    },
    {
      kind: 'md',
      text: `
## 計算そのものは拍子抜けするほど簡単

成分どうしを掛けて、全部足すだけです。
足し算のときと同じく、**x は x とだけ、y は y とだけ**掛けます。混ざりません。

違うのは最後です。足し算は 3 つの結果を並べてベクトルにしましたが、
内積は**3 つを足してしまいます**。だから返るのはベクトルではなく、**ただの数 1 つ**です。

ここが最初の引っかかりどころで、「ベクトル同士の掛け算」と思って読むと
「なぜ矢印が返らないのか」で止まります。**内積は、2 本の矢印から数を 1 つ取り出す装置**です。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} \\cdot \\mathbf{b} = a_x b_x + a_y b_y + a_z b_z',
      readAloud:
        'a と b のあいだの中黒（・）が内積の記号で、「a ドット b」と読みます。x どうし、y どうし、z どうしを掛けて、3 つを足し合わせるだけです。答えは矢印ではなく、ただの数字が 1 つ出てきます。',
      worked: {
        given: '$\\mathbf{a} = (2,\\; 0,\\; 0)$（右向き）、$\\mathbf{b} = (0,\\; 3,\\; 0)$（上向き）のとき。',
        steps: [
          { calc: 'x どうし :  2 x 0 = 0' },
          { calc: 'y どうし :  0 x 3 = 0' },
          { calc: 'z どうし :  0 x 0 = 0' },
          {
            calc: '合計      :  0 + 0 + 0 = 0',
            note: '最後に 3 つを足す。出てくるのは矢印ではなく数字 1 つ',
          },
        ],
        result:
          '$\\mathbf{a} \\cdot \\mathbf{b} = 0$。右向きと上向きは**直角**なので 0 になりました。なぜ直角だと 0 になるのかは、次の章で分かります。',
      },
    },
    {
      kind: 'md',
      text: `
## 順番を入れ替えても同じ

$\\mathbf{a} \\cdot \\mathbf{b}$ と $\\mathbf{b} \\cdot \\mathbf{a}$ は同じ値です。
成分どうしの掛け算をして足しているだけなので、ふつうの数の掛け算と同じ性質を引き継ぎます。

**これは[](#/ch/04-cross)と対照的**です。外積は順番を入れ替えると真逆を向きます。
「内積は順不同、外積は順番が命」と対にして覚えておくと、あとで迷いません。
`,
    },
    {
      kind: 'md',
      text: `
## 自分自身との内積は、長さの二乗

$\\mathbf{a} \\cdot \\mathbf{a}$ を計算してみると、$a_x^2 + a_y^2 + a_z^2$ になります。
これは[](#/ch/b16-vector-length)で見た「長さの二乗」そのものです。

つまり \`v.lengthSq()\` は \`v.dot(v)\` と同じ値を返します。
**長さの計算は、内積の特別な場合だった**ということです。

ここから、内積が「掛け算」の一種であることが少し見えてきます。
自分自身に掛けると二乗になる ― ふつうの数と同じふるまいです。
`,
    },
    {
      kind: 'code',
      title: '内積を計算する',
      code: `import * as THREE from 'three';

const a = new THREE.Vector3(2, 0, 0);
const b = new THREE.Vector3(0, 3, 0);

a.dot(b);    // 0 ― 返るのは数。ベクトルではない
b.dot(a);    // 0 ― 順番を入れ替えても同じ

// 自分自身との内積は長さの二乗
const v = new THREE.Vector3(3, 4, 0);
v.dot(v);       // 25
v.lengthSq();   // 25 ― 同じもの

// dot は読み取るだけ。a も b も書き換わらない
a.x;  // 2 のまま`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '内積は「掛け算」ではありません',
      text: `
記号が中黒（・）なので掛け算に見えますが、返るものが違います。

- ベクトル × 数（スカラー倍）… 返るのは**ベクトル**
- ベクトル・ベクトル（内積）… 返るのは**数**

コードで \`a.dot(b).x\` のように書こうとして「x なんて無い」と怒られるのは、
この取り違えが原因です。
`,
    },
  ],
  exercises: [
    {
      prompt: `手で計算してください。

1. $(1,\\; 2,\\; 3) \\cdot (4,\\; 5,\\; 6)$
2. $(1,\\; 0,\\; 0) \\cdot (0,\\; 0,\\; 1)$
3. $(2,\\; 1,\\; 0) \\cdot (-2,\\; 4,\\; 0)$`,
      hint: '成分どうしを掛けて、最後に全部足します。',
      answer: `1. $4 + 10 + 18 = $ **32**
2. $0 + 0 + 0 = $ **0**。x 軸と z 軸は直角です
3. $-4 + 4 + 0 = $ **0**。こちらも直角

3 が 0 になるのは意外に見えるかもしれません。
$(2, 1, 0)$ と $(-2, 4, 0)$ は、図に描くとちょうど直角に交わっています。
**内積が 0 なら直角**という判定は、成分を見ただけでは分からない関係を教えてくれます。`,
    },
    {
      prompt: `$\\mathbf{v} = (1,\\; -2,\\; 2)$ について、$\\mathbf{v} \\cdot \\mathbf{v}$ を手で計算し、
そのあと $\\mathbf{v}$ の長さを求めてください。2 つの関係を説明してください。`,
      hint: '自分自身との内積は何になるのでしたか。',
      answer: `$\\mathbf{v} \\cdot \\mathbf{v} = 1 + 4 + 4 = $ **9**

長さは $\\sqrt{9} = $ **3**

自分自身との内積は**長さの二乗**です。だから内積のほうを開けば長さになります。

three では \`v.dot(v)\` と \`v.lengthSq()\` が同じ値を返します。
[](#/ch/b11-distance)で「比べるだけなら開かなくていい」と言ったのは、
この二乗の値のことでした。`,
    },
    {
      prompt: `つぎのコードはエラーになります。理由を説明し、直してください。

\`const dir = a.dot(b).normalize();\``,
      hint: '`dot` が返すのは何でしたか。',
      answer: `\`dot\` が返すのは**数**であって、ベクトルではありません。
数には \`normalize()\` というメソッドが無いので、
「normalize is not a function」というエラーになります。

内積は「2 本の矢印から数を 1 つ取り出す」操作です。
向きが欲しいなら、内積ではなく引き算や外積を使う場面です。

**エラーメッセージが出るぶん、まだ親切な間違い**です。
このあと出てくる「正規化を忘れる」ほうは、エラーが出ないまま
判定だけが静かに狂うので、そちらのほうが厄介です。`,
      answerCode: `const value = a.dot(b);   // 数（例：0.87）

// 向きが欲しいなら別の計算
const dir = new THREE.Vector3().subVectors(b, a).normalize();`,
    },
  ],
  quiz: [
    {
      q: '$(3,\\; 1,\\; 2) \\cdot (2,\\; -1,\\; 0)$ はいくつですか。',
      choices: ['5', '11', '7', '$(6,\\; -1,\\; 0)$'],
      answer: 0,
      explain:
        '$6 + (-1) + 0 = 5$ です。成分ごとに掛けて、最後に全部足します。返るのは数 1 つで、ベクトルではありません。',
    },
    {
      q: '`a.dot(b)` が返すものはどれですか。',
      choices: ['数（スカラー）', 'ベクトル', '角度（度）', '真偽値'],
      answer: 0,
      explain:
        '内積は 2 本のベクトルから数を 1 つ取り出す計算です。ベクトルが返るのは外積のほうで、こちらはこの部の後半に出てきます。',
    },
    {
      q: '`v.dot(v)` と同じ値を返すのはどれですか。',
      choices: ['`v.lengthSq()`', '`v.length()`', '`v.normalize()`', '`v.clone()`'],
      answer: 0,
      explain:
        '自分自身との内積は $x^2 + y^2 + z^2$ で、長さの二乗そのものです。長さが欲しければ、そこから平方根を取ります。',
    },
  ],
};
