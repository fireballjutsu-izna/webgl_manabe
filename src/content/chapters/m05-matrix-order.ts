import type { Chapter } from '../types.ts';

export const chapterM05: Chapter = {
  slug: 'm05-matrix-order',
  part: 'math3d',
  number: 5,
  title: '掛ける順番 ― 入れ替えると別の変換になる',
  goal: '行列の掛け算が順番を入れ替えられない理由を図で説明できるようになり、式の「右から効く」規則が読めるようになります。',
  requires: ['m04-homogeneous'],
  threeApis: ['Matrix4.multiply', 'Matrix4.premultiply', 'Matrix4.multiplyMatrices'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

ふつうの掛け算は $3 \\times 5 = 5 \\times 3$ です。
ところが**行列のかけ算は、順番を変えると結果が変わります**。

これは行列の欠陥ではありません。**現実がそうなっている**だけです。

「その場で回ってから 3 歩前へ」と「3 歩前へ出てから回る」は、
着く場所が違います。当たり前ですが、この当たり前が行列にも反映されています。
`,
    },
    {
      kind: 'demo',
      id: 'transform-playground',
      caption:
        '「順番を入れ替えた箱も表示」を入れてください。移動 x を 3、y 軸まわりの回転を 90 度にすると、2 つの箱がまったく違う場所に行きます。片方はその場で回ってから移動し、もう片方は移動してから原点を中心に振り回されています。',
    },
    {
      kind: 'md',
      text: `
## 回転は、いつでも原点を中心に起きる

なぜ順番で結果が変わるのか ― 鍵は**回転が原点を中心に起きる**ことです。

- **回してから移動** … 原点でくるっと回り、それから横にずれる。
  **その場で向きだけ変わった**ように見える
- **移動してから回転** … 横にずれた状態のまま、原点を中心にぐるっと回される。
  **腕を伸ばしたまま体ごと回る**のに似ている

後者は、移動した距離がそのまま**回転の半径**になります。
だから 3 だけ動かしてから 90 度回すと、半径 3 の円弧を描いて飛んでいきます。

**「先に動かすと、その距離が回転半径になる」** ―
これが順番の違いを説明する 1 行です。
`,
    },
    {
      kind: 'formula',
      tex: 'M = T \\cdot R \\qquad (\\text{点には } R \\text{ が先に効く})',
      readAloud:
        '行列を並べて書くと、いちばん右のものが点に最初に効きます。T かける R と書けば、まず R（回転）が効き、そのあと T（移動）が効きます。書いた順と逆から効く、と覚えてください。',
      worked: {
        given:
          '$T$ を「x に 3 動かす」、$R$ を「y 軸まわりに 90 度回す」として、点 $(1,\\; 0,\\; 0)$ に $T \\cdot R$ と $R \\cdot T$ を適用します。',
        steps: [
          { calc: 'T・R : まず R  (1,0,0) → (0,0,-1)', note: '90 度回って、奥を向いた' },
          { calc: '       つぎに T  (0,0,-1) → (3,0,-1)', note: '横へ 3 ずれた' },
          { calc: 'R・T : まず T  (1,0,0) → (4,0,0)', note: '先に横へ 3 ずれた' },
          { calc: '       つぎに R  (4,0,0) → (0,0,-4)', note: '原点を中心に 90 度。半径 4 で振られた' },
        ],
        result:
          '$(3,\\; 0,\\; -1)$ と $(0,\\; 0,\\; -4)$ ― **まったく別の場所**です。$R \\cdot T$ のほうは、先にずれた 4 がそのまま回転半径になっています。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '式は「右から」適用されます',
      text: `
$M = T \\times R \\times S$ と書いたとき、点に最初に効くのは**いちばん右の S（拡大）**です。
次に R（回転）、最後に T（移動）。書いた順と逆から効く、と覚えてください。

理由は $M\\mathbf{v}$ の形にあります。$T R S \\mathbf{v}$ は
$T(R(S\\mathbf{v}))$ ― ベクトルにいちばん近いものから順に効きます。
**紙の上での近さが、そのまま適用の順番**です。
`,
    },
    {
      kind: 'md',
      text: `
## three での書き方

three の \`multiply\` は「右から掛ける」です。

- \`a.multiply(b)\` … $a \\leftarrow a \\cdot b$。**b があとに書かれるので、b が先に効く**
- \`a.premultiply(b)\` … $a \\leftarrow b \\cdot a$。**b があとから効く**

名前が紛らわしいので、迷ったら**「点に近いほうが先」**に戻ってください。

なお \`multiply\` は呼ばれた側を書き換えます。
ベクトルのときと同じで、もとを残したいならコピーしてから使います。
`,
    },
    {
      kind: 'code',
      title: '順番を意識して合成する',
      code: `import * as THREE from 'three';

const T = new THREE.Matrix4().makeTranslation(3, 0, 0);
const R = new THREE.Matrix4().makeRotationY(Math.PI / 2);

// 「回してから移動」＝ T・R。R が右なので先に効く
const a = new THREE.Matrix4().multiplyMatrices(T, R);

// 「移動してから回転」＝ R・T
const b = new THREE.Matrix4().multiplyMatrices(R, T);

// メソッドチェーンで書くと、書いた順に右へ伸びていく
const c = new THREE.Matrix4()
  .multiply(T)   // 最後に効く
  .multiply(R);  // 先に効く

// premultiply は左から掛ける（あとから効かせたいとき）
const d = R.clone().premultiply(T);   // T・R と同じ`,
    },
    {
      kind: 'md',
      text: `
## 順番を入れ替えても同じ場合もある

いつでも変わるわけではありません。

- **移動どうし** … 順番を変えても同じ（足し算なので）
- **同じ軸まわりの回転どうし** … 同じ（角度が足されるだけ）
- **一様な拡大と回転** … 同じ（拡大が向きを変えないので）

変わるのは**種類の違う変換を混ぜたとき**と、
**違う軸まわりの回転を重ねたとき**です。

後者は回転の章で「オイラー角の順番」として、もう一度出てきます。
**x → y → z の順で回すのと、z → y → x の順で回すのは別物**です。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '迷ったら、手で 2 回試す',
      text: `
実務では、順番を理屈で詰めるより**両方書いて見比べる**ほうが速いことが多いです。
選択肢は 2 つしかないので、片方が正しい。

ただし「なぜそうなるか」を知っておくと、
**うまくいかないときに次の一手が決まります**。
「回転の中心がずれている」のか「順番が逆」なのかを切り分けられるからです。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「順番を入れ替えた箱も表示」を入れ、移動 x を 3、y 軸まわりの回転を 90 度にしてください。
2 つの箱はどこにいますか。**なぜ違う場所になるのか**を、原点との関係で説明してください。`,
      hint: '回転はいつでも原点を中心に起きます。',
      answer: `Three.js の順（拡大 → 回転 → 移動）の箱は、原点で 90 度回ってから x に 3 動くので、
**x = 3 の場所で向きだけが変わって**います。

入れ替えたほう（移動 → 回転）は、x = 3 に動いてから原点を中心に 90 度回るので、
**振り回されて**別の場所に行きます。

回転はつねに原点を中心に起きるので、
「先に動かすと、その距離がそのまま回転の半径になる」わけです。`,
    },
    {
      prompt: `$T$ を「y に 2 動かす」、$S$ を「全体を 3 倍に拡大する」として、
点 $(0,\\; 1,\\; 0)$ に対する $T \\cdot S$ と $S \\cdot T$ の結果を手で計算してください。`,
      hint: '右にあるものが先に効きます。',
      answer: `**$T \\cdot S$**（拡大してから移動）

1. $S$: $(0, 1, 0) \\to (0, 3, 0)$
2. $T$: $(0, 3, 0) \\to (0, 5, 0)$ … **$(0,\\; 5,\\; 0)$**

**$S \\cdot T$**（移動してから拡大）

1. $T$: $(0, 1, 0) \\to (0, 3, 0)$
2. $S$: $(0, 3, 0) \\to (0, 9, 0)$ … **$(0,\\; 9,\\; 0)$**

後者では、**移動した距離まで 3 倍されて**しまいました。
拡大は原点からの距離を倍にするので、先に遠ざけると拡大の影響も大きくなります。

これが「拡大 → 回転 → 移動」の順が選ばれている理由です。
移動を最後にすれば、移動量が拡大や回転の影響を受けません。`,
    },
    {
      prompt: `\`matrix.multiply(A).multiply(B)\` と書いたとき、点には A と B のどちらが先に効きますか。
また、この式を \`multiplyMatrices\` を使った 1 行で書き直してください。`,
      hint: '`multiply` は右から掛けます。点に近いほうが先に効きます。',
      answer: `**B が先**に効きます。

\`multiply\` は右から掛けるので、式としては $\\text{matrix} \\cdot A \\cdot B$ になります。
点に近いのは右端の B なので、B → A → matrix の順に効きます。

書き直すと \`multiplyMatrices(A, B)\` です（matrix が単位行列だった場合）。

**メソッドチェーンは、書いた順に右へ伸びていく** ―
つまり**あとに書いたものほど先に効く**、と覚えておくと読み違えません。`,
      answerCode: `// この 2 つは同じ
matrix.multiply(A).multiply(B);
matrix.multiplyMatrices(A, B);   // matrix が単位行列のとき

// 点には B が先に効く`,
    },
  ],
  quiz: [
    {
      q: '「その場で回転してから、横に 3 だけ移動する」変換を作りたいとき、行列のかけ算の並びはどれですか。',
      choices: [
        '$T \\times R$（移動を左、回転を右に書く）',
        '$R \\times T$（回転を左、移動を右に書く）',
        'どちらでも同じ結果になる',
        '行列では表せない',
      ],
      answer: 0,
      explain:
        '適用は右から順に起きます。回転を先に効かせたいなら回転を右に置きます。順番を入れ替えると、横にずれた状態のまま原点を中心に振り回される動きになります。',
    },
    {
      q: '移動してから回転すると、物体が大きく振り回されるのはなぜですか。',
      choices: [
        '回転はつねに原点を中心に起きるので、移動した距離が回転半径になるから',
        '移動が回転より強いから',
        '行列の順番が逆だとエラーになるから',
        '回転行列が拡大も含んでいるから',
      ],
      answer: 0,
      explain:
        '回転は原点を動かしません。だから先に遠ざけておくと、その距離を半径とする円弧を描きます。「先に動かすと、その距離が回転半径になる」と覚えてください。',
    },
    {
      q: '順番を入れ替えても結果が変わらないのはどれですか。',
      choices: [
        '平行移動どうし',
        '違う軸まわりの回転どうし',
        '移動と回転',
        '移動と拡大',
      ],
      answer: 0,
      explain:
        '平行移動は足し算なので順不同です。同じ軸まわりの回転どうしや、一様な拡大と回転の組み合わせも入れ替えられます。それ以外は基本的に変わります。',
    },
  ],
};
