import type { Chapter } from '../types.ts';

export const chapter04: Chapter = {
  slug: '04-cross',
  part: 'basics',
  number: 29,
  title: '外積 ― 両方に垂直な向きを作る',
  goal: '2 本のベクトルから両方に垂直な向きを作れるようになり、順番を入れ替えると真逆を向くことが分かります。',
  requires: ['b17-normalize', 'b28-projection'],
  threeApis: ['Vector3.cross', 'Vector3.crossVectors'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- 三角形が**どちらを向いているか**を知りたい（表か裏か、光がどう当たるか）
- 目標がプレイヤーの**右にいるか左にいるか**を知りたい
- 何かの軸のまわりに回したいが、その**回転軸**をどう作ればいいか分からない

内積は「1 つの数」を返す計算でした。今度は「新しい 1 本の矢印」を返す計算、{{外積}}です。

内積が 5 章あったのに対し、外積は 4 章です。使う場面は内積より狭いのですが、
**その場面では代わりがありません。**
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: 'ネジを回すところを思い浮かべてください',
      text: `
右ネジを時計回りに回すと、ネジは奥へ進みます。手を回した向き（面）に対して、
ネジが進む向き（垂直）が決まる——外積がやっているのは、まさにこれです。
2 本の矢印が張る「面」を入力すると、その面に垂直な 1 本が出てきます。
`,
    },
    {
      kind: 'md',
      text: `
## 外積が返すもの

$\\mathbf{a} \\times \\mathbf{b}$ には 3 つの性質があります。

- **向き** … a にも b にも垂直
- **長さ** … a と b が作る平行四辺形の面積と同じ
- **順番** … a×b と b×a は正反対になる（入れ替えてはいけない）

3 つ目が内積との決定的な違いです。内積は順番を入れ替えても同じ値でしたが、
外積は入れ替えると真逆を向きます。

なぜ 2 つの答えがあり得るのかというと、**面に垂直な向きは 2 つある**からです。
上向きと下向き、どちらも「垂直」です。そのどちらを返すかを、順番で決めています。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} \\times \\mathbf{b} = (a_y b_z - a_z b_y,\\; a_z b_x - a_x b_z,\\; a_x b_y - a_y b_x)',
      readAloud:
        'バツ印が外積の記号で、「a クロス b」と読みます。式は覚えなくて構いません。x 成分を作るのに y と z を使い、y 成分を作るのに z と x を使う、というたすきがけの形になっている、とだけ眺めておいてください。実際に手で書くことはまずありません。',
      worked: {
        given: '$\\mathbf{a} = (1,\\; 0,\\; 0)$（x 軸）、$\\mathbf{b} = (0,\\; 1,\\; 0)$（y 軸）のとき。',
        steps: [
          { calc: 'x 成分 :  0 x 0 - 0 x 1 = 0', note: 'a の y,z と b の y,z だけを使う' },
          { calc: 'y 成分 :  0 x 0 - 1 x 0 = 0' },
          { calc: 'z 成分 :  1 x 1 - 0 x 0 = 1' },
        ],
        result:
          '$\\mathbf{a} \\times \\mathbf{b} = (0,\\; 0,\\; 1)$ ― **z 軸そのもの**が出てきました。x 軸と y 軸に両方とも垂直な向きは z 軸しかないので、当然そうなります。順番を逆にして $\\mathbf{b} \\times \\mathbf{a}$ を計算すると $(0, 0, -1)$ になります。手で 1 度やってみてください。',
      },
    },
    {
      kind: 'demo',
      id: 'cross-normal',
      caption:
        '「(a×b)·a」と「(a×b)·b」の読み出しに注目してください。a と b をどう動かしても、どちらもほぼ 0 のままです。つまり外積の結果は常に両方と直交しています。',
    },
    {
      kind: 'md',
      text: `
## 式は覚えなくてよい

上の式を暗記する必要はありません。**手で書く機会はまずない**からです。
three なら 1 行で済みます。

覚えておくのは 3 つの性質だけです。

- 両方に垂直
- 長さは平行四辺形の面積
- 順番を変えると逆向き

もし手で確かめたくなったときは、**まず $x \\times y = z$ を試してください。**
軸どうしの外積は答えが分かっているので、式の使い方を確かめる検算になります。
`,
    },
    {
      kind: 'md',
      text: `
## 直交していることを、自分で確かめる

外積が本当に両方と垂直かは、**内積で確かめられます**。
直交していれば内積は 0 になるはずでした。

デモの読み出しがまさにそれをやっています。
$(\\mathbf{a}\\times\\mathbf{b})\\cdot\\mathbf{a}$ と
$(\\mathbf{a}\\times\\mathbf{b})\\cdot\\mathbf{b}$ が、どちらも 0 のまま動きません。

**内積と外積が、ここでつながります。**
片方で作ったものを、もう片方で検算する ― コードでも同じことができ、
法線がおかしいときの切り分けに使えます。
`,
    },
    {
      kind: 'code',
      title: '外積を計算する',
      code: `import * as THREE from 'three';

const a = new THREE.Vector3(1, 0, 0);
const b = new THREE.Vector3(0, 1, 0);

// 結果を新しいベクトルに入れる（a も b も壊さない）
const n = new THREE.Vector3().crossVectors(a, b);   // (0, 0, 1)

// 順番を入れ替えると真逆
new THREE.Vector3().crossVectors(b, a);             // (0, 0, -1)

// a 自身を書き換える形もある（クローンしないと a が壊れる）
a.clone().cross(b);

// 直交していることを内積で確かめる
n.dot(a);   // 0
n.dot(b);   // 0`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '内積と外積の使い分け',
      text: `
**「どれくらい揃っているか」を数で知りたいなら内積。**
**「両方に垂直な向きが欲しい」「どちら側かを知りたい」なら外積。**
迷ったら、答えが数 1 つで足りるのか、それとも矢印が要るのかを考えてください。
`,
    },
  ],
  exercises: [
    {
      prompt: `手で計算してください（軸どうしの外積です）。

1. $(1,\\; 0,\\; 0) \\times (0,\\; 1,\\; 0)$
2. $(0,\\; 1,\\; 0) \\times (0,\\; 0,\\; 1)$
3. $(0,\\; 0,\\; 1) \\times (1,\\; 0,\\; 0)$`,
      hint: 'x 成分は $a_y b_z - a_z b_y$、y 成分は $a_z b_x - a_x b_z$、z 成分は $a_x b_y - a_y b_x$ です。',
      answer: `1. **$(0,\\; 0,\\; 1)$** … x × y = z
2. **$(1,\\; 0,\\; 0)$** … y × z = x
3. **$(0,\\; 1,\\; 0)$** … z × x = y

**x → y → z → x → … と循環しています。**
この並びで隣り合う 2 つを掛けると次のものが出る、と覚えておくと、
符号を間違えたときの手がかりになります。

逆順（y × x など）にすると、すべてマイナスが付きます。`,
    },
    {
      prompt: `$\\mathbf{a} = (2,\\; 0,\\; 0)$、$\\mathbf{b} = (0,\\; 3,\\; 0)$ の外積を手で計算し、
その**長さ**を求めてください。長さが何を表しているかも説明してください。`,
      hint: '2 本が作る平行四辺形を図に描いてみてください。',
      answer: `外積は $(0 \\times 0 - 0 \\times 3,\\; 0 \\times 0 - 2 \\times 0,\\; 2 \\times 3 - 0 \\times 0) = $ **$(0,\\; 0,\\; 6)$**

長さは **6** です。

$\\mathbf{a}$ と $\\mathbf{b}$ は直交していて、長さがそれぞれ 2 と 3。
2 本が作る平行四辺形は**辺 2 と辺 3 の長方形**なので、面積は $2 \\times 3 = 6$。
外積の長さと一致しました。

内積が 0 だった（直交していた）のに対し、外積は最大になっています。
**内積と外積は、揃っているときと直交しているときで、役割が入れ替わります。**`,
    },
    {
      prompt: `つぎのコードを実行したあと、\`a\` の値はどうなっていますか。
また、その結果としてどんなバグが起きうるか答えてください。

\`const n1 = a.cross(b);\`
\`const n2 = a.cross(c);\``,
      hint: '`cross` は呼ばれた側を書き換えます。',
      answer: `1 行目で \`a\` 自身が $\\mathbf{a}\\times\\mathbf{b}$ に書き換わっています。
なので 2 行目の \`n2\` は $\\mathbf{a}\\times\\mathbf{c}$ ではなく、
**$(\\mathbf{a}\\times\\mathbf{b})\\times\\mathbf{c}$** になります。

まったく別の向きが返るので、たとえば「2 つの面の法線を求める」つもりで書くと、
2 つ目だけが謎の向きを指します。しかも**エラーは出ません。**

\`crossVectors\` を使えば、どちらも壊さずに結果だけを新しく作れます。`,
      answerCode: `// 危ない：a が書き換わる
const n1 = a.cross(b);
const n2 = a.cross(c);   // もう a は元の a ではない

// 安全
const n1 = new THREE.Vector3().crossVectors(a, b);
const n2 = new THREE.Vector3().crossVectors(a, c);`,
    },
  ],
  quiz: [
    {
      q: '$\\mathbf{a} \\times \\mathbf{b}$ の結果はどんなものですか。',
      choices: [
        'a にも b にも垂直なベクトル',
        'a と b のなす角を表す数',
        'a と b を足したベクトル',
        'a を b の向きに落とした長さ',
      ],
      answer: 0,
      explain:
        '外積が返すのはベクトルで、その向きは入力した 2 本の両方に垂直です。数 1 つを返すのは内積のほうです。',
    },
    {
      q: '$\\mathbf{b} \\times \\mathbf{a}$ は $\\mathbf{a} \\times \\mathbf{b}$ と比べてどうなりますか。',
      choices: ['正反対の向きになる', 'まったく同じになる', '長さだけが 2 倍になる', '常に 0 になる'],
      answer: 0,
      explain:
        '面に垂直な向きは 2 つあり、そのどちらを返すかを順番が決めています（内積は入れ替えても同じでした）。法線の向きが裏返ってしまうときは、まずこの順番を疑ってください。',
    },
    {
      q: '外積の結果が本当に両方と垂直かを確かめるには、どうしますか。',
      choices: [
        '元の 2 本それぞれと内積を取り、0 になるか見る',
        '長さが 1 になるか見る',
        'もう一度外積を取る',
        '確かめる方法はない',
      ],
      answer: 0,
      explain:
        '直交していれば内積は 0 です。デモの読み出しがこれをやっています。片方の計算をもう片方で検算できるので、法線がおかしいときの切り分けに使えます。',
    },
  ],
};
