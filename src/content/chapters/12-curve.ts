import type { Chapter } from '../types.ts';

export const chapter12: Chapter = {
  slug: '12-curve',
  part: 'math3d',
  number: 35,
  title: 'ベジェ曲線 ― 補間を、補間する',
  goal: 'ベジェ曲線が lerp の入れ子でできていることが分かり、制御点で形を狙って作れるようになります。',
  requires: ['08-interp', 'b13-vector-add'],
  threeApis: ['QuadraticBezierCurve3', 'CubicBezierCurve3', 'Curve.getPoint', 'CurvePath'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

カメラを A から B へ動かすだけなら[](#/ch/08-interp)の lerp で足ります。
ところが「建物のまわりを回り込みながら寄る」となると、途端に手が止まります。

- 途中に 1 点足して 2 回 lerp する → **その点で折れる**
- 点を増やす → **折れ目が増えるだけ**

**直線をいくら繋いでも、曲線にはなりません。**
必要なのは「折れずに曲がる」仕組みです。
`,
    },
    {
      kind: 'md',
      text: `
## 補間を、もう一度補間する

答えは驚くほど単純です。**lerp の結果を、また lerp する。**

3 つの点 $P_0$、$P_1$、$P_2$ で考えます。

1. $P_0 \\to P_1$ を $t$ で補間して、点 $A$ を作る
2. $P_1 \\to P_2$ を**同じ $t$** で補間して、点 $B$ を作る
3. $A \\to B$ を**また同じ $t$** で補間する

$t$ を 0 から 1 まで動かすと、3 つ目の点が描く軌跡が**曲線**になります。
折れ目はどこにもありません。1 段目の 2 点が連続して動くので、結果も連続します。

この曲線を{{ベジェ曲線}}と呼びます。
制御点を 4 つにすれば段が 1 つ増え、**3 次ベジェ曲線**になります。
実務でいちばんよく使う形です。
`,
    },
    {
      kind: 'demo',
      id: 'bezier-steps',
      caption:
        '「途中の段を見せる」を入れて、進み具合をゆっくり動かしてください。灰色の制御点を結ぶ線の上をピンクの点が滑り、そのピンクを結んだ線の上を白い点が滑り、さらにその上をアンバーの点が滑ります。3 段の入れ子が、そのまま曲線になっています。',
    },
    {
      kind: 'formula',
      tex: 'B(t) = (1-t)^2 P_0 + 2(1-t)t\\, P_1 + t^2 P_2',
      readAloud:
        '2 次ベジェ曲線を展開した形です。3 つの制御点それぞれに重みが付いていて、その重みは t で決まります。3 つの重みを足すと必ず 1 になるので、曲線は制御点で囲まれた範囲から外に出ません。',
      worked: {
        given:
          '$P_0 = (0,\\; 0)$、$P_1 = (4,\\; 4)$、$P_2 = (8,\\; 0)$ の曲線で、$t = 0.5$ の点を 2 通りに求めます。',
        steps: [
          { calc: '【段を追う】A = lerp(P0, P1, 0.5) = (2, 2)' },
          { calc: '        B = lerp(P1, P2, 0.5) = (6, 2)' },
          { calc: '        P = lerp(A, B, 0.5)  = (4, 2)' },
          { calc: '【式に入れる】重み : 0.25 / 0.5 / 0.25' },
          { calc: '  x = 0.25x0 + 0.5x4 + 0.25x8 = 4' },
          { calc: '  y = 0.25x0 + 0.5x4 + 0.25x0 = 2' },
        ],
        result:
          'どちらも **$(4,\\; 2)$**。**$P_1$ の高さ 4 に対して、曲線は 2 までしか上がりません。** 制御点は「引き寄せる目印」であって、曲線が通る点ではないからです。重みの $0.25 + 0.5 + 0.25 = 1$ も確かめておいてください。',
      },
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '制御点は「通る点」ではありません',
      text: `
ベジェ曲線が必ず通るのは、いちばん最初と最後の点だけです。
途中の制御点は曲線を引っぱる磁石のようなもので、曲線はそこに届きません。

デザインツールでハンドルを引っぱる操作は、この制御点を動かしています。
「思ったところを通ってくれない」と感じたら、それは仕様です。

全部の点を通したいなら、次の章の Catmull-Rom を使います。
`,
    },
    {
      kind: 'md',
      text: `
## 曲線は、制御点の箱から出ない

重みを全部足すと必ず 1 になる、という性質には実用的な意味があります。

**曲線は、制御点をすべて囲む最小の凸多角形の中から出られません。**

だから当たり判定を粗く済ませたいとき、
**制御点の境界箱で調べるだけで「絶対に当たらない」が判定できます**。
曲線そのものを細かく調べるより、ずっと軽く済みます。

もう 1 つ、**制御点を全部同じだけ動かすと、曲線も同じだけ動きます。**
重みの合計が 1 なので当然ですが、形を保ったまま平行移動できるということです。
`,
    },
    {
      kind: 'code',
      title: 'ベジェ曲線を使う',
      code: `import * as THREE from 'three';

// 2 次（制御点 1 つ）
const quad = new THREE.QuadraticBezierCurve3(
  new THREE.Vector3(0, 0, 0),
  new THREE.Vector3(4, 4, 0),     // 引き寄せる目印。通らない
  new THREE.Vector3(8, 0, 0),
);
console.log(quad.getPoint(0.5));   // (4, 2, 0)

// 3 次（制御点 2 つ）。実務でいちばん使う
const cubic = new THREE.CubicBezierCurve3(
  new THREE.Vector3(0, 0, 0),
  new THREE.Vector3(2, 5, 0),
  new THREE.Vector3(6, -3, 0),
  new THREE.Vector3(8, 0, 0),
);

// 自分で書くとこうなる（段を追う書き方）
function bezier2(p0, p1, p2, t) {
  const a = new THREE.Vector3().lerpVectors(p0, p1, t);
  const b = new THREE.Vector3().lerpVectors(p1, p2, t);
  return a.lerp(b, t);
}

// 何本かを繋いで 1 本の道にする
const path = new THREE.CurvePath();
path.add(quad);
path.add(cubic);`,
    },
    {
      kind: 'md',
      text: `
## 繋ぎ目で折れないようにする

長い道は、ベジェ曲線を何本か繋いで作ります。
このとき**繋ぎ目で折れないための条件**があります。

- **位置が合っている** … 前の終点と次の始点が同じ。これは最低条件
- **向きも合っている** … **前の最後の制御点・繋ぎ目・次の最初の制御点が、一直線に並ぶ**

2 つ目を満たさないと、位置は繋がっていても**そこで急に向きが変わります**。
車が走れば、その瞬間だけハンドルを切ることになります。

デザインツールでハンドルが「反対側と連動する」設定があるのは、
この条件を自動で守るためです。

**繋ぐ本数が増えるほど、この管理は面倒になります。**
だから「全部の点を通り、繋ぎ目を気にしなくていい曲線」が欲しくなる ―
それが次の章です。
`,
    },
  ],
  exercises: [
    {
      prompt: `$P_0 = (0,\\; 0)$、$P_1 = (6,\\; 6)$、$P_2 = (12,\\; 0)$ のベジェ曲線で、
$t = 0.5$ の点を、段を追う方法で求めてください。`,
      hint: '同じ $t$ で 3 回 lerp します。',
      answer: `**1 段目**
$A = \\mathrm{lerp}(P_0, P_1, 0.5) = (3,\\; 3)$
$B = \\mathrm{lerp}(P_1, P_2, 0.5) = (9,\\; 3)$

**2 段目**
$P = \\mathrm{lerp}(A, B, 0.5) = (6,\\; 3)$

答えは **$(6,\\; 3)$** です。

$P_1$ の高さは 6 なのに、曲線は **3** までしか上がりません。**ちょうど半分**です。

これは偶然ではなく、$t = 0.5$ での重みが $0.25 : 0.5 : 0.25$ だからです。
$P_0$ と $P_2$ の高さが 0 なので、$0.5 \\times 6 = 3$ になります。

**「制御点の高さの半分までしか上がらない」**は、
2 次ベジェの $t = 0.5$ で常に成り立つ関係です。山の高さを狙うときに使えます。`,
    },
    {
      prompt: `2 本のベジェ曲線を繋いだところ、繋ぎ目で道がカクッと折れました。
位置は正しく一致しています。何が足りませんか。`,
      hint: '繋ぎ目の前後で、曲線はどちらを向いていますか。',
      answer: `**向きが合っていません。**

ベジェ曲線の始点での進行方向は「始点 → 1 つ目の制御点」の向き、
終点での進行方向は「最後の制御点 → 終点」の向きです。

だから折れないためには、

**前の曲線の最後の制御点・繋ぎ目・次の曲線の最初の制御点が、一直線に並ぶ**

必要があります。位置が合っているだけでは足りません。

**直し方** … 次の曲線の最初の制御点を、
「繋ぎ目から見て、前の最後の制御点の**ちょうど反対側**」に置きます。

距離まで揃えると、曲がり方の変化までなめらかになります。
向きだけ揃えれば、見た目には十分なことが多いです。`,
      answerCode: `// 繋ぎ目 j、前の曲線の最後の制御点 prev
const j = new THREE.Vector3(6, 2, 0);
const prev = new THREE.Vector3(4, 3, 0);

// 反対側へ折り返した点を、次の曲線の最初の制御点にする
const next = j.clone().multiplyScalar(2).sub(prev);   // j + (j - prev)

const curveB = new THREE.CubicBezierCurve3(j, next, c2, end);`,
    },
    {
      prompt: `弾道（打ち上げて落ちる軌道）をベジェ曲線で作りたい。
地面の $(0,\\;0,\\;0)$ から $(20,\\;0,\\;0)$ へ飛ばし、**最高点を高さ 8** にしたい。
制御点をどこに置きますか。`,
      hint: '2 次ベジェの $t = 0.5$ では、制御点の高さの半分になります。',
      answer: `**$(10,\\; 16,\\; 0)$** に置きます。

2 次ベジェの $t = 0.5$ での高さは、制御点の高さの**半分**でした
（$0.25 \\times 0 + 0.5 \\times h + 0.25 \\times 0 = 0.5h$）。

最高点を 8 にしたいなら、制御点は $8 \\times 2 = 16$ です。

横位置を $10$（中間）にすれば左右対称の弧になり、実際の弾道らしく見えます。

**なお本物の放物運動とは別物です。** 形は放物線になりますが、
$t$ を等速で進めても**水平方向が等速になるだけ**で、重力で加速する動きにはなりません。
落下の加速まで欲しいなら、$t$ の進め方に手を入れるか、素直に物理式で書きます。`,
    },
  ],
  quiz: [
    {
      q: 'ベジェ曲線は、何からできていますか。',
      choices: [
        'lerp の入れ子（補間の結果を、また補間する）',
        '円弧の集まり',
        '三角関数',
        '直線の集まり',
      ],
      answer: 0,
      explain:
        '同じ $t$ で段を重ねて補間するだけです。1 段目の点が連続して動くので、結果も折れずに繋がります。de Casteljau のアルゴリズムと呼ばれる手順です。',
    },
    {
      q: 'ベジェ曲線が必ず通る点はどれですか。',
      choices: [
        '最初と最後の点だけ',
        'すべての制御点',
        '中間の制御点だけ',
        'どの点も通らない',
      ],
      answer: 0,
      explain:
        '途中の制御点は「引き寄せる磁石」で、曲線はそこに届きません。全部の点を通したいなら Catmull-Rom を使います。',
    },
    {
      q: '2 本のベジェ曲線を折れずに繋ぐ条件はどれですか。',
      choices: [
        '前の最後の制御点・繋ぎ目・次の最初の制御点が一直線に並ぶ',
        '位置が一致していればよい',
        '制御点の数が同じ',
        '長さが同じ',
      ],
      answer: 0,
      explain:
        '始点での進行方向は「始点 → 1 つ目の制御点」の向きなので、位置だけ合わせても向きが食い違えばそこで折れます。デザインツールのハンドル連動は、この条件を自動で守る機能です。',
    },
  ],
};
