import type { Chapter } from '../types.ts';

export const chapter13: Chapter = {
  slug: '13-random',
  part: 'basics',
  number: 38,
  title: '乱数 ― 毎回違う数を作る',
  goal: '0〜1 の乱数を好きな範囲に移せるようになり、位置・大きさ・向きをばらつかせて量産感を消せるようになります。',
  requires: ['b34-inverse-lerp'],
  threeApis: [
    'Math.random',
    'MathUtils.randFloat',
    'MathUtils.randFloatSpread',
    'MathUtils.randInt',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D で「たくさんのもの」を作ろうとすると、必ずこの壁に当たります。

- 木を 200 本置きたい。でも 1 本ずつ座標を書くのは無理
- 星を 5000 個ばらまきたい
- 敵の出現位置を、毎回変えたい

答えは乱数ですが、素朴に使うと**びっくりするほど不自然**になります。
この 5 章で、乱数の扱い方と、その限界（そしてノイズという別の道具）を身につけます。
`,
    },
    {
      kind: 'md',
      text: `
## 0〜1 が返る、それだけ

\`Math.random()\` が返すのは **0 以上 1 未満**の数です。それだけです。
0.5 になる確率も 0.9 になる確率も同じで、**どの値も等しく出ます**（一様乱数）。

「1 未満」なのがポイントで、**ちょうど 1 は絶対に返りません**。
配列の添字を作るときにこの性質が効きます。

そして 0〜1 と言えば、[](#/ch/b05-ratio)でやった割合です。
つまり **lerp にそのまま渡せます**。
`,
    },
    {
      kind: 'formula',
      tex: 'v = a + (b - a)\\,\\mathrm{rand}()',
      readAloud:
        '0〜1 の乱数に幅を掛けて、下限を足します。lerp の t のところに乱数を入れただけの形です。',
      worked: {
        given: '$-40$ から $40$ の範囲に散らしたいとき、3 つの乱数値がどこに移るかを見ます。',
        steps: [
          { calc: 'rand = 0.00 → -40 + 80 x 0.00 = -40', note: '下限' },
          { calc: 'rand = 0.25 → -40 + 80 x 0.25 = -20' },
          { calc: 'rand = 0.50 → -40 + 80 x 0.50 =   0', note: 'まんなか' },
          { calc: 'rand = 0.99 → -40 + 80 x 0.99 =  39.2', note: '上限には届かない' },
        ],
        result:
          '**lerp そのもの**です。新しい計算は 1 つも出てきません。$\\mathrm{rand}()$ が 1 を返さないので、上限にちょうど当たることはありません（実用上は問題になりません）。',
      },
    },
    {
      kind: 'md',
      text: `
## 整数が欲しいとき

配列から 1 つ選びたい、といった場面では整数が要ります。
**掛けてから切り捨てる**のが定石です。

- \`Math.floor(Math.random() * 5)\` … 0, 1, 2, 3, 4 のどれか

なぜ \`Math.round\` ではなく \`Math.floor\` なのか。
\`round\` を使うと**両端だけ確率が半分になる**からです。
0〜4.99 を四捨五入すると、0 になるのは 0〜0.49 の範囲だけで、
1 になるのは 0.5〜1.49 の範囲 ― **端だけ狭い**わけです。

\`floor\` なら全部が等しい幅になります。
**「掛けて、切り捨てる」と覚えてください。**
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '配列の添字は、長さを掛けて切り捨てる',
      text: `
\`arr[Math.floor(Math.random() * arr.length)]\` が定型です。

\`Math.random() * (arr.length - 1)\` と書きたくなりますが、これだと
**最後の要素が選ばれる確率が下がります**（切り捨てで最後だけ幅が狭くなる）。
\`length\` をそのまま掛けて切り捨てるのが正解です。
`,
    },
    {
      kind: 'md',
      text: `
## ばらつかせる先を増やす

位置だけをランダムにすると、**同じ形が並んでいるのが目立ちます**。
これが「量産感」の正体です。

大きさ・向き・色味も少しずつばらつかせると、途端に自然になります。

- **位置** … 大きくばらつかせる
- **向き** … 一周ぶん（$0$〜$2\\pi$）まるごと
- **大きさ** … 10〜30% 程度。振りすぎると散らかる
- **色味** … ほんの少し。明度を数パーセント動かすだけでも効く

**振り幅の目安は、大きさと色は控えめに**です。
向きは全部ばらつかせて構いません（木や岩は、どちらを向いていても不自然でないので）。
`,
    },
    {
      kind: 'code',
      title: '乱数の使い方',
      code: `import * as THREE from 'three';

Math.random();                      // 0 以上 1 未満

// 範囲を指定する（lerp と同じこと）
THREE.MathUtils.randFloat(-40, 40);      // -40〜40
THREE.MathUtils.randFloatSpread(80);     // -40〜40（中心 0 の幅指定）

// 整数（掛けて切り捨てる）
Math.floor(Math.random() * 5);           // 0〜4
THREE.MathUtils.randInt(1, 6);           // 1〜6（サイコロ）

// 配列から 1 つ選ぶ
const pick = arr[Math.floor(Math.random() * arr.length)];

// 木をばらまく。位置だけでなく、向きと大きさも振る
for (let i = 0; i < 200; i += 1) {
  tree.position.set(
    THREE.MathUtils.randFloatSpread(80),
    0,
    THREE.MathUtils.randFloatSpread(80),
  );
  tree.rotation.y = Math.random() * Math.PI * 2;   // 向きは全部
  tree.scale.setScalar(0.85 + Math.random() * 0.3); // 大きさは控えめ
}`,
    },
    {
      kind: 'md',
      text: `
## 一様乱数でよい場面、そうでない場面

このあと 4 章かけて、乱数の**足りないところ**を埋めていきます。
先に地図を出しておきます。

- **毎回違ってしまって困る** → シードつきの乱数（次の章）
- **思ったように散らばらない** → 分布の偏り
- **隣どうしが無関係でトゲトゲになる** → ノイズ
- **ノイズ 1 枚では作り物っぽい** → 重ね合わせ

素の \`Math.random()\` でよいのは、
**「毎回違ってよく、隣との関係も要らない」**場面だけです。
星をばらまく、パーティクルの初速を振る、といったところに限られます。
`,
    },
  ],
  exercises: [
    {
      prompt: `\`Math.random()\` が 0.75 を返したとして、つぎの値を手で計算してください。

1. $-10$ 〜 $10$ の範囲に移した値
2. $5$ 〜 $8$ の範囲に移した値
3. \`Math.floor(0.75 * 4)\``,
      hint: '1 と 2 は lerp です。3 は掛けてから切り捨てます。',
      answer: `1. $-10 + 20 \\times 0.75 = -10 + 15 = $ **5**
2. $5 + 3 \\times 0.75 = 5 + 2.25 = $ **7.25**
3. $0.75 \\times 4 = 3$ なので、切り捨てて **3**

3 で選ばれる整数は 0〜3 の 4 通りです。
\`Math.random()\` が 1 を返さないので、**4 が出ることはありません**。
だから長さ 4 の配列の添字として、そのまま安全に使えます。`,
    },
    {
      prompt: `\`Math.round(Math.random() * 2)\` で 0・1・2 のどれかを選ぼうとしました。
**3 つが等確率になりません。** それぞれの確率を求めてください。`,
      hint: '0〜2 のどの範囲が、四捨五入でどの整数になるかを図に描いてください。',
      answer: `- **0 になる** … $0 \\le x < 0.5$ の範囲。幅 0.5 → **25%**
- **1 になる** … $0.5 \\le x < 1.5$ の範囲。幅 1.0 → **50%**
- **2 になる** … $1.5 \\le x < 2$ の範囲。幅 0.5 → **25%**

**真ん中だけ 2 倍出ます。** 両端は「片側にしか広がれない」ので幅が半分になるからです。

正しくは \`Math.floor(Math.random() * 3)\` です。
0〜1、1〜2、2〜3 と幅が均等なので、それぞれ 3 分の 1 になります。`,
    },
    {
      prompt: `木を 200 本ばらまいたところ、**明らかに同じ形の繰り返しに見えます**。
位置はきちんとランダムです。何をばらつかせれば自然になるか、3 つ挙げてください。`,
      hint: '人間が「同じもの」と認識する手がかりは、位置以外にもあります。',
      answer: `**向き** … \`rotation.y\` を $0$〜$2\\pi$ で全部振ります。いちばん効きます

**大きさ** … 10〜30% 程度。振りすぎると木のサイズ感が壊れます

**色味** … 葉の色をほんの少しずつ変えます。明度を数パーセント動かすだけでも効きます

大事なのは**振り幅の配分**です。向きは全部振ってよく、大きさと色は控えめにします。
向きが揃っていると、人間は一瞬で「同じものが並んでいる」と気づきます。
逆に大きさを振りすぎると、遠近感が壊れて奇妙に見えます。`,
    },
  ],
  quiz: [
    {
      q: '`Math.random()` が返す値の範囲はどれですか。',
      choices: ['0 以上 1 未満', '0 より大きく 1 未満', '0 以上 1 以下', '−1 以上 1 以下'],
      answer: 0,
      explain:
        '0 は返り得ますが、1 は返りません。この「1 未満」という性質のおかげで、長さを掛けて切り捨てれば配列の添字としてそのまま使えます。',
    },
    {
      q: '0〜4 の整数を等確率で得たいとき、正しいのはどれですか。',
      choices: [
        '`Math.floor(Math.random() * 5)`',
        '`Math.round(Math.random() * 4)`',
        '`Math.floor(Math.random() * 4)`',
        '`Math.ceil(Math.random() * 5)`',
      ],
      answer: 0,
      explain:
        '掛けて切り捨てるのが定石です。round を使うと両端の確率が半分になります。5 ではなく 4 を掛けると、4 が一度も出ません。',
    },
    {
      q: '同じ形の物体を大量に置いたとき、量産感をいちばん減らせるのはどれですか。',
      choices: [
        '向きをばらつかせる',
        '色を大きく変える',
        '大きさを 2 倍から 5 倍まで振る',
        '数を減らす',
      ],
      answer: 0,
      explain:
        '向きが揃っていると、人間は一瞬で「同じものが並んでいる」と気づきます。向きは全部振ってよく、大きさと色は控えめにするのが配分の目安です。',
    },
  ],
};
