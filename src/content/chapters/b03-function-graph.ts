import type { Chapter } from '../types.ts';

export const chapterB03: Chapter = {
  slug: 'b03-function-graph',
  part: 'basics',
  number: 3,
  title: '関数とグラフ ― 入れると出てくる箱',
  goal: '「入力を 1 つ入れると出力が 1 つ決まる」という関数の見方が身につき、グラフを「あり得る入出力を全部描いた絵」として読めるようになります。',
  requires: ['b02-variable'],
  threeApis: ['Math.sin', 'Math.sqrt', 'MathUtils.lerp'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D のコードは、突き詰めると**関数の詰め合わせ**です。

- 時刻を入れると、位置が出てくる
- 角度を入れると、高さが出てくる（\`Math.sin\`）
- 0 から 1 の値を入れると、途中の色が出てくる

ところが「関数」という言葉は、数学では $f(x)$ の顔で、
プログラムでは \`function\` の顔で出てきます。
**この 2 つが同じものだと分かっていないと、数式とコードのあいだで毎回、頭の中で翻訳が要ります。**

この章で、その 2 つを 1 つにしておきます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '自動販売機だと思ってください',
      text: `
ボタンを 1 つ押すと、飲み物が 1 本出てきます。同じボタンを押せば、いつでも同じものが出ます。
これが関数です。**入力が決まれば出力が 1 つに決まる**、それだけが約束事です。
「同じボタンを押したのに違うものが出る」ものは関数とは呼びません。
`,
    },
    {
      kind: 'md',
      text: `
## $f(x)$ の読み方

$f(x) = 2x + 3$ と書いてあったら、こう読みます。

- $f$ … 箱の名前。「f という装置がある」
- $(x)$ … その箱に入れるもの。入力
- $= 2x + 3$ … 中でやっていること

$f(4)$ は「その箱に 4 を入れたときに出てくるもの」で、$2 \\times 4 + 3 = 11$ です。
**$f(4)$ は掛け算ではありません。** $f$ とかっこが並んでいたら「箱に入れる」の意味です。

コードで書けばそのままです。

- 数式 … $f(x) = 2x + 3$
- コード … \`const f = (x) => 2 * x + 3;\`
- 呼び出し … $f(4)$ ↔ \`f(4)\`

同じものに 2 つの書き方があるだけ、と思ってください。
`,
    },
    {
      kind: 'md',
      text: `
## グラフは「あり得る入出力を全部描いた絵」

関数は、入力を 1 つ入れると出力が 1 つ出ます。
入力を少しずつ変えながら、その組を 1 つずつ点として置いていくとどうなるか。

- 横方向に**入力 $x$** を取る
- 縦方向に**出力 $f(x)$** を取る
- 組 $(x,\\; f(x))$ の場所に点を打つ

これを $x$ について全部やると、点が並んで 1 本の線になります。これが{{グラフ}}です。

つまりグラフは、**関数の答えを全部いっぺんに見せた表**です。
「$x$ が 2 のときの答えは？」と思ったら、横軸の 2 の真上（または真下）を見ればいい。
`,
    },
    {
      kind: 'demo',
      id: 'graph-plot',
      caption:
        '「入力 x」のつまみを動かすと、点が曲線の上を走ります。破線が「入力を縦に上げて、出力を横に読む」道すじです。式を切り替えると、同じ x でも出力が変わることが見えます。',
    },
    {
      kind: 'md',
      text: `
## 一次関数 ― まっすぐな線

$f(x) = ax + b$ の形をした関数を{{一次関数}}と呼びます。グラフは必ず直線になります。

- **$a$ は傾き。** $x$ が 1 増えたときに、出力がいくつ増えるか。
  $a$ が大きいほど急な坂、マイナスなら下り坂
- **$b$ は切片。** $x = 0$ のときの高さ。線が縦軸を横切る場所

デモで $a$ と $b$ を動かして、この 2 つの役割を分けて確かめてください。
$a$ を変えると傾きだけが変わり、$b$ を変えると線が上下に平行移動します。
`,
    },
    {
      kind: 'formula',
      tex: 'a = \\frac{f(x_2) - f(x_1)}{x_2 - x_1}',
      readAloud:
        '傾き a は、出力の増えたぶんを、入力の増えたぶんで割ったもの、と言っています。「縦にいくつ上がったか ÷ 横にいくつ進んだか」です。',
      worked: {
        given: '$f(x) = 2x + 3$ のグラフから、$x_1 = 1$ と $x_2 = 4$ の 2 点を拾って傾きを求めます。',
        steps: [
          { calc: 'f(1) = 2×1 + 3 = 5', note: '1 点目の出力' },
          { calc: 'f(4) = 2×4 + 3 = 11', note: '2 点目の出力' },
          { calc: '縦の増え = 11 - 5 = 6' },
          { calc: '横の増え =  4 - 1 = 3' },
          { calc: 'a = 6 / 3 = 2' },
        ],
        result:
          '式に書いてあった **2** が戻ってきました。傾きは「どの 2 点で測っても同じ」なのが直線の性質です。曲線ではこうはなりません。',
      },
    },
    {
      kind: 'md',
      text: `
## 曲がる関数もある

デモの式を「二次」に切り替えてください。$f(x) = ax^2 + b$ のグラフは
お椀の形になります（$x^2$ は次の章で扱います）。

大事なのは**曲線でも約束は同じ**だということです。入力を 1 つ決めれば、出力が 1 つ決まる。
違うのは「$x$ が 1 増えたときの増え方が、場所によって変わる」ところだけです。

一次関数の傾きは、どこで測っても同じでした。二次関数はそうではありません。
$x$ が 0 の近くではほとんど平ら、遠くへ行くほど急になります。
デモの点を左右に走らせて、坂の急さが変わることを見てください。
**この「場所によって増え方が変わる」性質が、あとでイージング（動きの緩急）の正体になります。**
`,
    },
    {
      kind: 'code',
      title: '数式とコードは同じもの',
      code: `// f(x) = 2x + 3
const f = (x) => 2 * x + 3;

f(0);  // 3
f(1);  // 5
f(4);  // 11

// three が用意している関数も、入れると出てくる箱でしかない
import * as THREE from 'three';

// 0 から 1 の t を入れると、10 から 20 のあいだの値が出てくる
THREE.MathUtils.lerp(10, 20, 0.0);  // 10
THREE.MathUtils.lerp(10, 20, 0.5);  // 15
THREE.MathUtils.lerp(10, 20, 1.0);  // 20

// 角度を入れると -1〜1 が出てくる（波の章で主役になる）
Math.sin(0);  // 0`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '3D で「時間の関数」を書くと、アニメーションになる',
      text: `
毎フレーム、経過時間 $t$ を関数に入れて、出てきた値を位置や角度に入れる。
これがアニメーションのすべてです。
$y = \\sin(t)$ なら上下に揺れ、$y = t$ なら等速で上がっていきます。
**どんな動きにしたいかは、どんなグラフにしたいかと同じ問い**です。
`,
    },
  ],
  exercises: [
    {
      prompt: `$f(x) = -3x + 6$ について、手で計算してください。

1. $f(0)$
2. $f(2)$
3. グラフが横軸（$f(x) = 0$）と交わるのは $x$ がいくつのときですか`,
      hint: '3 は「出力が 0 になる入力」を探します。$-3x + 6 = 0$ を解いてください。',
      answer: `1. $f(0) = -3 \\times 0 + 6 = $ **6**。これが切片です
2. $f(2) = -3 \\times 2 + 6 = -6 + 6 = $ **0**
3. **$x = 2$**。2 の答えがそのまま使えます。$-3x + 6 = 0$ を移項して $3x = 6$、$x = 2$

$a$ がマイナスなので下り坂で、$x$ が 1 増えるごとに出力が 3 ずつ減っていきます。`,
    },
    {
      prompt: `デモを「一次」にし、$a = 1$、$b = 0$ にしてください（$f(x) = x$）。
このとき入力と出力の関係はどうなっていますか。
つぎに $a$ だけを $2$ にして、入力 $x = 3$ の出力を**先に手で予想してから**確かめてください。`,
      hint: '$f(x) = x$ は「入れたものがそのまま出てくる」箱です。',
      answer: `$f(x) = x$ は入力と出力が常に等しく、グラフは 45 度の直線になります
（何もしない関数、という意味で**恒等関数**と呼びます）。

$a = 2$ にすると $f(3) = 2 \\times 3 = $ **6**。
グラフの傾きが 2 倍になり、同じ $x$ でも出力が 2 倍の高さになります。
「$a$ を掛ける」は、グラフを縦に引き伸ばす操作だと見ることもできます。`,
    },
    {
      prompt: `つぎの 2 つの表のうち、関数と呼べるのはどちらですか。理由も書いてください。

- 表A … 入力 1 → 出力 5、入力 2 → 出力 7、入力 3 → 出力 5
- 表B … 入力 1 → 出力 5、入力 1 → 出力 8、入力 2 → 出力 7`,
      hint: '約束は「入力が決まれば出力が 1 つに決まる」でした。出力が重なるのは問題でしょうか。',
      answer: `**表A だけが関数です。**

表B は入力 1 に対して 5 と 8 の 2 つの出力があり、
「同じボタンを押したのに違うものが出る」状態なので関数ではありません。

表A は入力 1 と 3 がどちらも出力 5 ですが、これは問題ありません。
**別の入力から同じ出力が出るのは構わない**（自動販売機で 2 つのボタンに同じ飲み物を入れてもよい）。
禁じられているのは、1 つの入力から 2 つの出力が出ることだけです。`,
    },
  ],
  quiz: [
    {
      q: '$f(x) = 5x - 2$ のとき、$f(3)$ はいくつですか。',
      choices: ['13', '15', '10', '3'],
      answer: 0,
      explain:
        '$f(3)$ は「x のところに 3 を入れる」という意味です。$5 \\times 3 - 2 = 15 - 2 = 13$。$f(3)$ を「f と 3 の掛け算」と読まないことがポイントです。',
    },
    {
      q: '一次関数 $f(x) = ax + b$ の $b$ を大きくすると、グラフはどうなりますか。',
      choices: [
        '形は変わらず、上に平行移動する',
        '傾きが急になる',
        '左右が反転する',
        'お椀の形に曲がる',
      ],
      answer: 0,
      explain:
        '$b$ は「x が 0 のときの高さ」なので、変えると線全体が上下に動きます。傾きを変えるのは $a$ のほうです。デモで 2 つのつまみを別々に動かすと、役割の違いがはっきりします。',
    },
    {
      q: 'グラフとは何を描いた絵ですか。',
      choices: [
        'あり得る「入力と出力の組」を全部並べたもの',
        '関数の中でいちばん大きい出力',
        '入力の一覧',
        '関数を実行するのにかかる時間',
      ],
      answer: 0,
      explain:
        '横に入力、縦に出力を取り、組 (x, f(x)) を点として置いたものがグラフです。答えの一覧表を絵にしたもの、と思ってください。',
    },
  ],
};
