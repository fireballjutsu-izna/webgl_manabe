import type { Chapter } from '../types.ts';

export const chapterM29: Chapter = {
  slug: 'm29-ortho',
  part: 'math3d',
  number: 29,
  title: '正射影 ― 大きさが変わらない写し方',
  goal: '正射影と透視投影を目的で選び分けられるようになり、影の描画で正射影が使われる理由も分かります。',
  requires: ['m28-ndc', 'm26-perspective'],
  threeApis: [
    'OrthographicCamera',
    'OrthographicCamera.zoom',
    'DirectionalLight.shadow',
    'Camera.updateProjectionMatrix',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 割り算をやめると、どうなるか

透視投影の遠近感は「奥行きで割る」ことから生まれるのでした。

では**割るのをやめたら**どうなるか。

**遠くのものも、近くのものも、同じ大きさで写ります。**
これが{{正射影}}です。写る範囲は、四角い箱になります。

視錐台がピラミッドの形をしていたのに対し、正射影の範囲は**直方体**です。
斜めに開いていないので、どの奥行きでも写る幅が同じ ―
だから大きさが変わりません。
`,
    },
    {
      kind: 'formula',
      tex: 'x_{\\text{ndc}} = \\frac{x}{(\\text{right} - \\text{left})/2}',
      readAloud:
        '正射影では、横の座標を「写す範囲の半分の幅」で割るだけです。奥行きは一切出てきません。だからどれだけ遠くても、大きさが変わりません。',
      worked: {
        given:
          '横に $-5$ から $+5$ を写す正射影カメラで、$(2,\\; 0,\\; -4)$ と $(2,\\; 0,\\; -40)$ の 2 点を写します。',
        steps: [
          { calc: '写す範囲の半分の幅 : (5 - (-5)) / 2 = 5' },
          { calc: '手前の点 : 2 / 5 = 0.4' },
          { calc: '奥の点  : 2 / 5 = 0.4', note: '奥行きが 10 倍でも同じ' },
          { calc: '【比較】透視投影（画角90度）なら' },
          { calc: '  手前 : 2 / 4 = 0.5' },
          { calc: '  奥  : 2 / 40 = 0.05', note: '10分の1 になる' },
        ],
        result:
          '正射影では**どちらも 0.4** です。透視投影では 0.5 と 0.05 で 10 倍違いました。**式に奥行きが出てこない**のが、正射影のすべてです。',
      },
    },
    {
      kind: 'demo',
      id: 'frustum-viewer',
      caption:
        '「投影のしかた」を正射影に切り替えてください。視錐台がピラミッドから直方体に変わり、子画面の映像では奥の箱も手前の箱も同じ大きさで写ります。画角のスライダーが効かなくなるのも、式に画角が出てこないからです。',
    },
    {
      kind: 'md',
      text: `
## どこで使うか

見た目が不自然なので使いどころが無さそうに思えますが、**むしろ必須の場面があります。**

**① 図面・設計・CAD**

大きさを比べたいものに、遠近感は邪魔です。
奥にある部品が小さく写ったら、寸法が読めません。

**② 2D の UI・スプライト**

画面に重ねる HP バーやアイコンは、遠近感が要りません。
正射影のカメラを別に用意して、2 回目の描画で重ねるのが定番です。

**③ アイソメトリック風のゲーム**

斜め上から見下ろす、あの見た目です。
遠近感が無いおかげで**タイルがどこでも同じ形**になり、格子が組みやすくなります。

**④ 平行光源の影**

これが実は、いちばん多くの人が知らずに使っている用途です。
`,
    },
    {
      kind: 'md',
      text: `
## 影が正射影で描かれる理由

\`DirectionalLight\`（太陽のような平行光源）の影は、**正射影のカメラで描かれています。**

理由は光の性質そのものです。
平行光源は「無限に遠くから、平行に降ってくる光」でした。
点から放射状に広がるのではなく、**どこでも同じ向き**です。

これはまさに「割り算をしない写し方」―
つまり**正射影と同じ形**をしています。

だから three の \`DirectionalLight.shadow.camera\` は \`OrthographicCamera\` です。
そして**この範囲設定が、影のよくある不具合の原因になります。**

- **範囲が広すぎる** … 影の解像度が足りず、ギザギザになる
- **範囲が狭すぎる** … 範囲の外の物体に影が出ない
- **near / far が合っていない** … 影が途中で切れる

**「影が出ない」「影がガタガタ」のときは、まずこのカメラの範囲を疑ってください。**
`,
    },
    {
      kind: 'code',
      title: '正射影を使う',
      code: `import * as THREE from 'three';

// (left, right, top, bottom, near, far)
const aspect = window.innerWidth / window.innerHeight;
const size = 5;                       // 縦に写す範囲の半分
const camera = new THREE.OrthographicCamera(
  -size * aspect, size * aspect, size, -size, 0.1, 100,
);

// ズームは zoom で（画角は無いので）
camera.zoom = 1.5;
camera.updateProjectionMatrix();

// リサイズ：left/right を縦横比に合わせて作り直す
window.addEventListener('resize', () => {
  const a = window.innerWidth / window.innerHeight;
  camera.left = -size * a;
  camera.right = size * a;
  camera.updateProjectionMatrix();
});

// 平行光源の影のカメラ（これも正射影）
light.castShadow = true;
light.shadow.camera.left = -20;       // 影を落としたい範囲だけに絞る
light.shadow.camera.right = 20;
light.shadow.camera.top = 20;
light.shadow.camera.bottom = -20;
light.shadow.mapSize.set(2048, 2048);
light.shadow.camera.updateProjectionMatrix();

// 範囲を目で確かめる
scene.add(new THREE.CameraHelper(light.shadow.camera));`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '正射影に画角はありません',
      text: `
OrthographicCamera には fov がありません。写す範囲を直接指定するからです。

ズームしたいときは zoom を使うか、left / right / top / bottom を作り直します。
zoom を 2 にすると、写る範囲が半分になって拡大されます。

透視投影の感覚で fov を探すと見つからず戸惑いますが、
「割り算をしないのだから、画角という概念が要らない」と考えると納得できます。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでがカメラです

5 章で分かったことを並べます。

- **カメラ** … 世界を逆に動かす装置。ビュー行列はカメラの逆行列
- **透視投影** … 奥行きで割る。それだけで遠近感が出る
- **視錐台** … near と far が切り取る。精度は near でほぼ決まる
- **NDC とピクセル** … 割合で持っておき、最後に実寸へ
- **正射影** … 割らない写し方。図面・UI・影

**「なぜ映らないのか」を切り分ける道具が、これで一通りそろいました。**
位置か、向きか、切り取りか、光か ― 順に確かめれば必ず見つかります。

次は、その「光」の話です。
`,
    },
  ],
  exercises: [
    {
      prompt: `横に $-8$ から $+8$ を写す正射影カメラで、
$(4,\\; 0,\\; -10)$ と $(4,\\; 0,\\; -100)$ の 2 点はそれぞれ画面のどこに写りますか。`,
      hint: '式に奥行きは出てきません。',
      answer: `写す範囲の半分の幅は $\\frac{8 - (-8)}{2} = 8$ です。

- $(4, 0, -10)$ … $4 \\div 8 = 0.5$
- $(4, 0, -100)$ … $4 \\div 8 = 0.5$

**どちらも 0.5** ― まったく同じ場所に写ります。

奥行きが 10 倍違っても、写る位置は 1 ミリも変わりません。
式に $z$ が出てこないので、当然です。

**重なって見えるので、どちらが手前かは深度でしか分かりません。**
だから正射影でも深度バッファは必要です（near / far も指定します）。`,
    },
    {
      prompt: `影がギザギザで、しかもシーンの端では影がまったく出ません。
\`DirectionalLight\` を使っています。原因を 2 つ挙げ、直し方を答えてください。`,
      hint: '影は、どんなカメラで描かれていましたか。',
      answer: `どちらも**影用の正射影カメラの範囲**が原因です。

**ギザギザ** … 範囲が広すぎます。
影の解像度（\`shadow.mapSize\`）は固定なので、広い範囲を写すほど 1 ピクセルが担当する
実際の面積が大きくなり、階段状になります。
範囲を半分にすれば、同じ解像度で 2 倍細かくなります。

**端で影が出ない** … 範囲が狭すぎます。
影用カメラの外にある物体は、そもそも影の計算に入りません。

**両立させる方法**は 2 つあります。

- **範囲を必要最小限に絞る** … 影が要る場所だけ。カメラに追従させる手もある
- **\`mapSize\` を上げる** … 2048 や 4096 に。ただしメモリと描画時間が増える

順番としては**まず範囲を絞り、それでも足りなければ解像度を上げる**のが定石です。
範囲を半分にすれば、解像度を 4 倍にしたのと同じ効果が、負荷ゼロで得られます。

なお \`CameraHelper\` を足せば範囲が線で見えるので、調整が一気に楽になります。`,
      answerCode: `// 範囲を目で見ながら詰める
scene.add(new THREE.CameraHelper(light.shadow.camera));

const d = 20;                    // 影が要る範囲だけに絞る
light.shadow.camera.left = -d;
light.shadow.camera.right = d;
light.shadow.camera.top = d;
light.shadow.camera.bottom = -d;
light.shadow.camera.near = 1;
light.shadow.camera.far = 60;
light.shadow.camera.updateProjectionMatrix();

light.shadow.mapSize.set(2048, 2048);   // 足りなければ上げる`,
    },
    {
      prompt: `次の 4 つで、正射影と透視投影のどちらが向いているか答えてください。理由も添えてください。

1. 建築の外観ビューアー（人が見て回る）
2. 家具の寸法を確かめる図面ビュー
3. 画面の隅に出す 3D のミニマップ
4. 一人称の探索ゲーム`,
      hint: '大きさを比べたいか、その場にいる感じが欲しいか。',
      answer: `**1. 透視投影。** 人が見て回る体験なので、その場にいる感じが要ります。
遠近感が無いと、空間の広がりが伝わりません。

**2. 正射影。** 大きさを比べるのが目的なので、遠近感は邪魔です。
奥の部品が小さく写ったら寸法が読めません。図面が正射影なのはこのためです。

**3. 正射影。** 真上から見下ろす地図に遠近感は不要です。
画面の端と中央で縮尺が変わってしまうと、位置関係が読めなくなります。

**4. 透視投影。** 一人称視点では、遠近感が距離の手がかりそのものです。
正射影にすると、どれだけ遠くても同じ大きさなので、**距離感が完全に失われます。**

**見分ける問いは 1 つ** ―「大きさを比べたいか、その場にいる感じが欲しいか」。
前者なら正射影、後者なら透視投影です。`,
    },
  ],
  quiz: [
    {
      q: '正射影で、遠くのものが小さくならないのはなぜですか。',
      choices: [
        '式に奥行きが出てこない（割り算をしない）から',
        '画角が 0 だから',
        'near と far が等しいから',
        '深度バッファを使わないから',
      ],
      answer: 0,
      explain:
        '透視投影の遠近感は「奥行きで割る」ことから生まれていました。割るのをやめれば、どの奥行きでも同じ大きさで写ります。写る範囲もピラミッドではなく直方体になります。',
    },
    {
      q: '`DirectionalLight` の影が正射影のカメラで描かれるのはなぜですか。',
      choices: [
        '平行光源は「どこでも同じ向き」で、それが正射影と同じ形だから',
        '正射影のほうが速いから',
        '影は 2D だから',
        '深度が要らないから',
      ],
      answer: 0,
      explain:
        '平行光源は無限に遠くから平行に降ってくる光です。点から放射状に広がらないので、割り算をしない写し方と一致します。だから `shadow.camera` は `OrthographicCamera` です。',
    },
    {
      q: '影がギザギザなときに、まず試すべきはどれですか。',
      choices: [
        '影用カメラの範囲を狭くする',
        'mapSize を 512 に下げる',
        'near を 0.001 にする',
        'ライトの色を変える',
      ],
      answer: 0,
      explain:
        '範囲を半分にすれば、同じ解像度で 2 倍細かくなります。負荷はまったく増えません。それでも足りなければ `mapSize` を上げますが、そちらはメモリと描画時間が増えます。',
    },
  ],
};
