import type { Chapter } from '../types.ts';

export const chapterB02: Chapter = {
  slug: 'b02-variable',
  part: 'basics',
  number: 2,
  title: '文字と式 ― 数学の = と、コードの =',
  goal: '数式の中の文字が何を表しているのかを読み下せるようになり、数学の等号とプログラムの代入を混同しなくなります。',
  requires: ['b01-number-line'],
  threeApis: ['Math.PI'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

このサイトの後半には、$\\mathbf{a} \\cdot \\mathbf{b} = a_x b_x + a_y b_y + a_z b_z$
のような式が出てきます。

数式アレルギーの正体は、たいてい**内容ではなく読み方**です。
$a_x$ が何なのか、$a_x b_x$ のあいだの見えない演算子が何なのかを
教わらないまま出てくるので、目が滑ります。

この章で、数式に出てくる文字の読み方を決めておきます。
これは 3D の話ではありませんが、**ここを飛ばすと以降の全章で毎回つまずきます。**
`,
    },
    {
      kind: 'md',
      text: `
## 文字は「まだ決めていない数」の置き場所

$a$ や $x$ という文字は、**あとで数が入る場所**です。
「2 でも 100 でもいい。とにかく 1 つの数が入る」という意味で名前を置いています。

なぜ名前を付けるかというと、**同じものが 2 回以上出てくるとき**に必要だからです。

- 「ある数を 2 倍して 3 を足す」→ $2x + 3$
- 「ある数に、その数自身を掛ける」→ $x \\times x$

1 つ目の $x$ と 2 つ目の $x$ は**必ず同じ数**です。これが文字を使う理由のほとんどです。

プログラムの変数もまったく同じ考え方で、\`const x = 4\` と決めておけば、
そのあとに出てくる \`x\` はすべて 4 になります。
`,
    },
    {
      kind: 'md',
      text: `
## 数式の省略ルール ― 3 つだけ覚える

数式はとにかく書くのを省略します。省略の規則は 3 つで足ります。

- **掛け算の記号は書かない。** $2x$ は $2 \\times x$、$ab$ は $a \\times b$。
  文字と文字、数と文字が隣り合っていたら掛け算です
- **割り算は分数で書く。** $a \\div b$ ではなく $\\dfrac{a}{b}$
- **右下の小さい文字（添字）は、名前の一部。** $a_x$ は「a の x 成分」という 1 つの名前で、
  a と x を掛けているのでは**ありません**

3 つ目がいちばん誤読を生みます。$a_x b_x$ は
「a の x 成分」と「b の x 成分」を掛け算したもの、と読みます。
コードで書けば \`a.x * b.x\` です。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '数式は、声に出して読むと急に易しくなる',
      text: `
$\\mathbf{a} \\cdot \\mathbf{b} = a_x b_x + a_y b_y + a_z b_z$ は、
「a と b の内積は、x 同士を掛けて、y 同士を掛けて、z 同士を掛けて、全部足したもの」と読みます。
このサイトの数式にはすべて「日本語で言うと」を添えてあります。
**式を目で追うより先に、その 1 行を読んでください。**
`,
    },
    {
      kind: 'md',
      text: `
## 計算する順番

省略が効いてくると、順番の約束も要ります。上から順に強い（先に計算する）ものです。

1. かっこの中
2. 累乗（$x^2$ など。次の次の章で扱います）
3. 掛け算・割り算
4. 足し算・引き算

$2 + 3 \\times 4$ は 20 ではなく **14**。掛け算が先だからです。
これはプログラムでもまったく同じで、\`2 + 3 * 4\` は 14 になります。

**迷ったらかっこを付けてください。** 数式でもコードでも、
かっこが多くて怒られることはありません。
`,
    },
    {
      kind: 'formula',
      tex: 'y = a x + b',
      readAloud:
        'y は、x を a 倍して b を足したもの、と言っています。a と b は先に決めておく数（傾きと切片）で、x が入ってくる数、y が出ていく数です。',
      worked: {
        given: '$a = 2$、$b = 3$ と決めたうえで、$x$ に 3 つの値を入れてみます。',
        steps: [
          { calc: 'x = 0 → y = 2×0 + 3 = 3', note: '掛け算が先。0 を掛けたので a が消える' },
          { calc: 'x = 1 → y = 2×1 + 3 = 5', note: 'x が 1 増えると y は 2 増えた' },
          { calc: 'x = 4 → y = 2×4 + 3 = 11', note: 'x が 3 増えたので y は 6 増えた' },
        ],
        result:
          '**a は「x が 1 増えたときに y がいくつ増えるか」**でした。b は x が 0 のときの値です。この 2 つの役割は次の章のグラフでそのまま形になります。',
      },
    },
    {
      kind: 'md',
      text: `
## 数学の = と、コードの = はまったく別物

ここが、独学でいちばん静かに人をつまずかせるところです。

**数学の $=$ は「左と右は同じものだ」という宣言**です。時間の流れがありません。
$y = 2x + 3$ と書いたら、それはずっと成り立っています。

**コードの \`=\` は「右を計算して、左に入れろ」という命令**です。時間の流れがあります。
だから \`let x = 5;\` のあとに \`x = x + 1;\` と書けて、x は 6 になります。

数学として読むと $x = x + 1$ はあり得ない式です（両辺から $x$ を引くと $0 = 1$ になってしまう）。
コードとしては「いまの x に 1 を足したものを、あらためて x に入れろ」という
**まっとうな命令**です。3D のアニメーションはこれの塊で、毎フレーム
\`mesh.rotation.y = mesh.rotation.y + 0.01\` を実行して少しずつ回します。
**式ではなく手順**として読んでください。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'このサイトの表記の約束',
      text: `
数式の中の $=$ は「同じもの」の意味、コードブロックの中の \`=\` は「入れろ」の意味です。
コードで「同じかどうか」を確かめたいときは \`===\` を使います
（\`==\` ではなく 3 つ重ねるほうを使ってください）。
`,
    },
    {
      kind: 'code',
      title: '同じ式を、数式とコードで並べる',
      code: `// y = a x + b
const a = 2;
const b = 3;
const x = 4;
const y = a * x + b;   // 11 ― 掛け算が先に計算される

// 添字は「名前の一部」。a_x はコードでは a.x
const va = { x: 1, y: 2, z: 3 };
const vb = { x: 4, y: 5, z: 6 };

// a_x b_x + a_y b_y + a_z b_z
const dot = va.x * vb.x + va.y * vb.y + va.z * vb.z;  // 32

// three が用意している定数もある
Math.PI;  // 3.14159...（円周率。角度の章で主役になる）`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの式を、かっこを補って計算してください。答えを出す前に「どれが先か」を口に出してみてください。

1. $2 + 3 \\times 4$
2. $(2 + 3) \\times 4$
3. $10 - 2 \\times 3 + 1$`,
      hint: '掛け算・割り算が、足し算・引き算より先です。かっこがあれば、かっこが最優先。',
      answer: `1. **14**。$3 \\times 4 = 12$ が先で、そこに 2 を足します
2. **20**。かっこが最優先なので $5 \\times 4$
3. **5**。$2 \\times 3 = 6$ を先に出し、$10 - 6 + 1$ を左から順に計算します

3 番で 10 − (6 + 1) = 3 としてしまう間違いが多いところです。
足し算と引き算は**同じ強さ**なので、左から順に処理します。`,
    },
    {
      prompt: `$a_x b_x + a_y b_y$ を、$a = (2, 5)$、$b = (3, -1)$ として手で計算してください。
そのあと、同じ計算を JavaScript の 1 行で書いてください。`,
      hint: '添字は名前の一部です。$a_x$ は「a の x 成分」＝ 2 のこと。',
      answer: `$2 \\times 3 + 5 \\times (-1) = 6 - 5 = $ **1** です。

添字を「a と x の掛け算」と読んでしまうと、この式は永久に読めません。
$a_x$ でひとかたまりの名前だと決めておけば、コードとの対応も一対一になります。`,
      answerCode: `const a = { x: 2, y: 5 };
const b = { x: 3, y: -1 };

const result = a.x * b.x + a.y * b.y;  // 1`,
    },
    {
      prompt: `\`let n = 3;\` のあとに \`n = n * 2 + 1;\` を実行すると n はいくつになりますか。
また、これを**数学の等式として**読むと、なぜおかしいのかを説明してください。`,
      hint: 'コードの = は「右を計算して左に入れる」でした。',
      answer: `n は **7** になります（$3 \\times 2 + 1 = 7$ を計算して、あらためて n に入れる）。

数学の等式として $n = 2n + 1$ を読むと、これは「n はこういう数だ」という条件になり、
解くと $n = -1$ という**特定の数**に決まってしまいます。3 ではありません。
つまり同じ見た目の 1 行が、**数学では条件、コードでは手順**という別のものを表しています。
アニメーションのコードは全部この「手順」の側です。`,
    },
  ],
  quiz: [
    {
      q: '数式 $3ab$ が意味するものはどれですか。',
      choices: [
        '3 と a と b を掛け合わせたもの',
        '3 と a と b を足し合わせたもの',
        '3 の a 乗の b 倍',
        '3 番目の a と b',
      ],
      answer: 0,
      explain:
        '数と文字、文字と文字が隣り合っていたら掛け算です。記号を省略するのが数式の約束で、コードでは `3 * a * b` と書きます。',
    },
    {
      q: '$a_y$ の読み方として正しいものはどれですか。',
      choices: [
        'a の y 成分（これでひとかたまりの名前）',
        'a と y を掛けたもの',
        'a の y 乗',
        'a を y で割ったもの',
      ],
      answer: 0,
      explain:
        '右下の小さい文字（添字）は名前の一部で、掛け算ではありません。コードでは `a.y` にあたります。ここを掛け算と読むと、内積や外積の式が読めなくなります。',
    },
    {
      q: 'JavaScript で `let x = 2; x = x + 3;` を実行したあと、x はいくつですか。',
      choices: ['5', '2', '3', 'エラーになる'],
      answer: 0,
      explain:
        'コードの `=` は「右を計算して左に入れろ」という命令です。右辺 `2 + 3` を計算した 5 が、あらためて x に入ります。数学の等号とは別物として読んでください。',
    },
  ],
};
