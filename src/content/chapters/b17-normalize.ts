import type { Chapter } from '../types.ts';

export const chapterB17: Chapter = {
  slug: 'b17-normalize',
  part: 'basics',
  number: 17,
  title: '正規化 ― 「どっち」だけを取り出す',
  goal: 'ベクトルの長さを 1 に揃えられるようになり、「向きだけが欲しい」場面で正しく書けるようになります。長さ 0 で壊れる理由も分かります。',
  requires: ['b16-vector-length'],
  threeApis: ['Vector3.normalize', 'Vector3.setLength', 'Vector3.lengthSq'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

「敵をプレイヤーのほうに、毎秒 3 の速さで歩かせたい」。

引き算で向かう矢印は作れました。でもそれをそのまま足すと、
**遠いときは超高速、近いときはのろのろ**になってしまいます。
矢印の長さが「距離」そのものだからです。

欲しいのは向きだけです。長さの情報は邪魔なので、いったん捨てます。
そのために長さを 1 に揃える ― これが{{正規化}}で、
できあがったものを{{単位ベクトル}}と呼びます。

前の章が「向きを捨てて大きさを取り出す」でした。この章はその**裏返し**です。
`,
    },
    {
      kind: 'md',
      text: `
## 自分の長さで割るだけ

長さ 5 の矢印を長さ 1 にしたいなら、5 で割ればいい。それだけです。

割合の章でやった「幅で割ると 0〜1 になる」と同じ発想で、
**自分の大きさで割ると 1 になる**、という当たり前の操作をしています。

割るのは全成分に対してなので、これは前の章のスカラー倍（$\\frac{1}{5}$ 倍）でもあります。
だから**向きは変わりません**。変わるのは長さだけです。
`,
    },
    {
      kind: 'formula',
      tex: '\\hat{\\mathbf{v}} = \\dfrac{\\mathbf{v}}{\\|\\mathbf{v}\\|}',
      readAloud:
        'v を、v 自身の長さで割ります。上の帽子（ハット）は「長さ 1 に揃えたもの」という印です。長さ 5 の矢印を 5 で割れば長さ 1 になる、というだけの話です。',
      worked: {
        given: 'おなじみの $\\mathbf{v} = (3,\\; 4,\\; 0)$（長さ 5）を正規化します。',
        steps: [
          { calc: 'x :  3 / 5 = 0.6' },
          { calc: 'y :  4 / 5 = 0.8' },
          { calc: 'z :  0 / 5 = 0' },
          {
            calc: '確かめ : 0.6の2乗 + 0.8の2乗 = 0.36 + 0.64 = 1',
            note: '長さが 1 になったか、その場で検算する',
          },
        ],
        result:
          '$\\hat{\\mathbf{v}} = (0.6,\\; 0.8,\\; 0)$ で、長さはちょうど 1。**成分の比 $3 : 4$ は変わっていません**。向きだけを残して、長さを捨てた形です。',
      },
    },
    {
      kind: 'demo',
      id: 'vector-length',
      caption:
        'こんどは正規化した矢印（アンバー）に注目してください。v をどれだけ長くしても、その先端は半径 1 の球から出られません。破線が「切り落とされた残り」です。すべての成分を 0 に近づけると、向きが決められなくなることも確かめてください。',
    },
    {
      kind: 'md',
      text: `
## 向きが手に入ったら、速さを掛ける

これで冒頭の要求に答えられます。手順は 3 つです。

1. **引く** … 敵からプレイヤーへ向かうベクトルを作る
2. **正規化する** … 長さを 1 にして、向きだけにする
3. **掛ける** … 進みたい距離（速さ × 経過時間）を掛ける

3 つとも、この 5 章でやってきたことです。
**引き算・正規化・スカラー倍を並べるだけで、追いかける動きが書けます。**

なお 2 と 3 はまとめられます。「長さ 1 にしてから $s$ を掛ける」は
「長さを $s$ にする」と同じなので、\`setLength(s)\` の 1 行で済みます。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '長さ 0 は正規化できません',
      text: `
0 で割ることになるためです。Three.js では \`NaN\`（数ではない値）が入り込み、
その物体は画面から消えます。

理屈のうえでも当然で、**向きを持たないものから向きは取り出せません**。
差を取ったベクトルを正規化する前には、長さが 0 でないかを確かめる癖をつけてください。
2 つの物体がぴったり重なった瞬間に、これが起きます。
`,
    },
    {
      kind: 'md',
      text: `
## 単位ベクトルは、これから至るところに出てきます

長さ 1 のベクトルは「純粋な向き」なので、このサイトの残り全部で使われます。

- **法線** … 面がどちらを向いているか（長さ 1）
- **光の向き** … どちらから照らしているか（長さ 1）
- **視線** … カメラがどちらを見ているか（長さ 1）
- **軸** … 何を中心に回すか（長さ 1）

なぜ長さ 1 に揃えておくのかというと、**次の章から始まる計算が、それを前提にしているから**です。
とくに内積は、長さ 1 どうしなら結果がそのまま角度の情報になります。
長さが揃っていないと、角度なのか長さなのか分からない値が出てきます。

**「向きを表す値は長さ 1 にしておく」** ― この習慣が、この先の章をずっと楽にします。
`,
    },
    {
      kind: 'code',
      title: '敵をプレイヤーへ向かって歩かせる',
      code: `import * as THREE from 'three';

function stepTowards(enemy, player, speed, dt) {
  // 1. 「行き先 引く 出発点」で、敵からプレイヤーへ向かう矢印を作る
  const toPlayer = new THREE.Vector3().subVectors(player.position, enemy.position);

  // 重なっているときは向きが決められない（0 で割ると NaN になる）
  if (toPlayer.lengthSq() < 1e-8) return;

  // 2 と 3 をまとめて：長さを「このフレームで進む距離」にする
  toPlayer.setLength(speed * dt);

  enemy.position.add(toPlayer);
}

// 段階を分けて書くなら、こう
const dir = new THREE.Vector3().subVectors(b, a).normalize();  // 長さ 1
const step = dir.clone().multiplyScalar(speed * dt);

// 距離だけが欲しいときは専用のメソッドがある
const d = enemy.position.distanceTo(player.position);`,
    },
    {
      kind: 'md',
      text: `
## ここまでで、ベクトルはひととおり終わりです

足す・引く・伸ばす・測る・揃える。この 5 つで、位置と向きの操作はほぼ書けます。

次からは**角度**に進みます。「どちらを向いているか」を矢印ではなく数（角度）で表せるようになると、
回転が扱えるようになり、円や波が作れるようになります。

そしてそのあと、**ベクトルと角度をつなぐ計算**が出てきます。
「2 本の矢印が、どれくらい同じ方向を向いているか」を 1 つの数で答えるもので、
そこで単位ベクトルが主役になります。
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎのベクトルを正規化してください。**まず長さを出してから**、各成分を割ります。

1. $(0,\\; 0,\\; 4)$
2. $(6,\\; 8,\\; 0)$
3. $(1,\\; 2,\\; 2)$`,
      hint: '3 の長さは $\\sqrt{1 + 4 + 4}$ です。整数になります。',
      answer: `1. 長さ 4 なので $(0,\\; 0,\\; 1)$
2. 長さ 10 なので $(0.6,\\; 0.8,\\; 0)$
3. 長さ 3 なので $\\left(\\frac{1}{3},\\; \\frac{2}{3},\\; \\frac{2}{3}\\right) \\approx (0.333,\\; 0.667,\\; 0.667)$

2 の答えが $(3, 4, 0)$ を正規化したものと同じであることに注目してください。
$(6, 8, 0)$ は $(3, 4, 0)$ の 2 倍で、**向きが同じだから正規化すると一致します**。
正規化は「長さの情報を完全に捨てる」操作なので、これは当然の結果です。`,
    },
    {
      prompt: `点 $A = (1,\\; 1,\\; 1)$ から点 $B = (1,\\; 5,\\; 4)$ へ向かって、**2 だけ進んだ点**を手で求めてください。
引く・測る・正規化する・掛ける・足す、を順に実行します。`,
      hint: '$B - A$ の長さは 5 になります。',
      answer: `1. **引く** … $B - A = (0,\\; 4,\\; 3)$
2. **測る** … $\\sqrt{0 + 16 + 9} = \\sqrt{25} = 5$
3. **正規化する** … $(0,\\; 0.8,\\; 0.6)$
4. **掛ける** … 2 倍して $(0,\\; 1.6,\\; 1.2)$
5. **足す** … $A + (0,\\; 1.6,\\; 1.2) = (1,\\; 2.6,\\; 2.2)$

答えは **$(1,\\; 2.6,\\; 2.2)$** です。

この 5 段が、3D で「A から B の方向に決まった距離だけ進む」の全部です。
three では \`A.clone().addScaledVector(dir, 2)\` の 1 行になりますが、
中でやっているのはこれと同じことです。`,
    },
    {
      prompt: `つぎのコードは、2 つの物体が**ぴったり重なった瞬間だけ**物体を消します。
理由と、直し方を答えてください。

\`const dir = new THREE.Vector3().subVectors(a.position, b.position).normalize();\`
\`a.position.addScaledVector(dir, 0.1);\``,
      hint: '重なっているとき、引き算の結果はどうなりますか。それを正規化すると？',
      answer: `重なっていると引き算の結果が $(0,\\; 0,\\; 0)$ になり、
それを正規化すると **0 で割る**ことになって、成分が \`NaN\` になります。

\`NaN\` を足された \`a.position\` も \`NaN\` になり、three はエラーを出さずに
**その物体を描画しなくなります**（画面から消えます）。しかも一度 \`NaN\` が入ると
以後の計算もすべて \`NaN\` のままなので、離れても戻ってきません。

直し方は、**正規化の前に長さを確かめる**ことです。
比較は \`lengthSq()\` で十分で、平方根を計算する必要はありません。`,
      answerCode: `const diff = new THREE.Vector3().subVectors(a.position, b.position);

// 重なっているときは何もしない
if (diff.lengthSq() > 1e-8) {
  a.position.addScaledVector(diff.normalize(), 0.1);
}`,
    },
  ],
  quiz: [
    {
      q: '長さ 6 のベクトルを正規化すると、長さはいくつになりますか。',
      choices: ['1', '6', '0', 'もとの長さによる'],
      answer: 0,
      explain:
        '正規化は「自分の長さで割る」ことなので、もとが何であっても結果の長さは必ず 1 になります。向きは変わりません。',
    },
    {
      q: '$(0,\\; 0,\\; 0)$ を正規化すると何が起きますか。',
      choices: [
        '0 で割ることになり、成分が NaN になって物体が消える',
        '$(0, 0, 1)$ が返る',
        'そのまま $(0, 0, 0)$ が返る',
        '例外が投げられて処理が止まる',
      ],
      answer: 0,
      explain:
        '向きを持たないものから向きは取り出せません。差を取ったベクトルを正規化する前には、`lengthSq()` が 0 でないかを確かめてください。2 つの物体が重なった瞬間に踏みます。',
    },
    {
      q: '「A から B の向きに、速さ × 経過時間ぶん進む」を書くとき、正しい順番はどれですか。',
      choices: [
        '引く → 正規化する → 速さと経過時間を掛ける → 足す',
        '足す → 正規化する → 引く',
        '正規化する → 引く → 掛ける',
        '掛ける → 引く → 正規化する',
      ],
      answer: 0,
      explain:
        '向かう矢印を作り（引く）、長さの情報を捨て（正規化）、進みたい距離にして（掛ける）、いまの位置に足します。中の 2 つは `setLength(speed * dt)` の 1 行にまとめられます。',
    },
  ],
};
