import type { Chapter } from '../types.ts';

export const chapter06: Chapter = {
  slug: '06-matrix',
  part: 'math3d',
  number: 1,
  title: '行列と変換',
  goal: '移動・回転・拡大が1つの表にまとまることを理解し、「適用する順番で結果が変わる」理由を説明できるようになります。',
  requires: ['02-vector', '05-trig'],
  threeApis: [
    'Matrix4',
    'Object3D.matrix',
    'Object3D.scale',
    'Matrix4.makeRotationY',
    'Matrix4.multiply',
    'Vector3.applyMatrix4',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

Three.js では \`position\`・\`rotation\`・\`scale\` を書けば物体が動きます。
それで済むうちは、{{行列}}を知らなくても困りません。

困りはじめるのはこの辺からです。

- 「回してから移動」と「移動してから回転」で結果が違う理由が分からない
- 親を回したら子が予想外のところへ飛んでいった
- カメラの \`lookAt\` や \`matrixWorld\` が何をしているのか読めない

これらはすべて、**変換が行列のかけ算でできている**ことを知ると一気に見通せます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '行列は「操作の指示書」です',
      text: `
行列を「数を並べた表」として覚えようとすると苦しくなります。
**「点を入れると、動かした後の点を返してくれる装置」** だと思ってください。
装置の中身が数の表になっているだけで、使う側にとっては入口と出口がすべてです。
`,
    },
    {
      kind: 'md',
      text: `
## なぜ表にまとめるのか

移動・回転・拡大——この 3 つをまとめて{{変換}}と呼びます。別々に書くこともできますが、3D では、
**1つの物体に何十回も変換が重なる**のがふつうです（親の親の親…と辿っていくため）。

そこで「複数の変換をあらかじめ 1 つにまとめておく」仕組みが要ります。
行列のかけ算は、まさにその合成をやってくれます。10 個の変換を 1 つの表に潰しておけば、
頂点 1 万個に対して装置を 1 回通すだけで済みます。
`,
    },
    {
      kind: 'md',
      text: `
## なぜ 4x4 なのか ― 同次座標

3D なのに行列は 4 行 4 列です。3x3 でも回転と拡大は表せますが、**平行移動だけが表せません**。

回転や拡大は「原点を動かさない」変換です。行列のかけ算はこの性質を持っているので、
どうやっても「全体をずらす」ことができないのです。

そこで座標に 4 つ目の数（ふつうは 1）を足して $(x, y, z, 1)$ という 4 個組にします。
これが{{同次座標}}です。この 1 が席を作ってくれるおかげで、
平行移動も回転と同じ「行列を掛ける」操作で扱えるようになります。
`,
    },
    {
      kind: 'formula',
      tex: '\\begin{pmatrix} 1 & 0 & 0 & t_x \\\\ 0 & 1 & 0 & t_y \\\\ 0 & 0 & 1 & t_z \\\\ 0 & 0 & 0 & 1 \\end{pmatrix}',
      readAloud:
        '平行移動の行列です。対角線に 1 が並び、いちばん右の列に移動量が入っているだけ、という素直な形をしています。デモで行列の中身を見ると、右の列が移動量そのものになっているのが確認できます。',
      worked: {
        given: '移動量を $t = (2,\; 1,\; 0)$ にして、点 $(1,\; 0,\; 0)$ に掛けます。同次座標なので、点は $(1, 0, 0, 1)$ の 4 個組として扱います。',
        steps: [
          { calc: '1 行目 : 1x1 + 0x0 + 0x0 + 2x1 = 3', note: '最後の 2x1 が移動量。掛ける相手の 4 つ目の 1 が効いている' },
          { calc: '2 行目 : 0x1 + 1x0 + 0x0 + 1x1 = 1' },
          { calc: '3 行目 : 0x1 + 0x0 + 1x0 + 0x1 = 0' },
          { calc: '4 行目 : 0x1 + 0x0 + 0x0 + 1x1 = 1', note: '4 つ目は 1 のまま。次の掛け算でも使える' },
        ],
        result: '$(1, 0, 0)$ が $(3,\; 1,\; 0)$ へ動きました。**4 つ目の 1 が無ければ、右端の列は 0 と掛けられて消えてしまいます。** 同次座標が要るのはこのためです。',
      },
    },
    {
      kind: 'demo',
      id: 'transform-playground',
      caption:
        '移動・回転・拡大のスライダーを動かすと、下に並んだ 16 個の数がどう変わるかが見えます。まず「移動 x」だけを動かして、右端の列だけが変わることを確かめてください。',
    },
    {
      kind: 'md',
      text: `
## 順番がすべて ― かけ算は入れ替えられない

ふつうの掛け算は $3 \\times 5 = 5 \\times 3$ ですが、**行列のかけ算は順番を変えると結果が変わります**。
これは行列の欠陥ではなく、現実がそうなっているだけです。

デモの 2 つ目の箱（色の違う半透明のほう）が、順番を入れ替えた結果です。

- **回してから移動** … その場でくるっと回り、それから横にずれる
- **移動してから回転** … 横にずれた状態のまま、原点を中心にぐるっと振り回される

後者は、腕を伸ばしたまま体ごと回るのに似ています。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '式は「右から」適用されます',
      text: `
$M = T \\times R \\times S$ と書いたとき、点に最初に効くのは**いちばん右の S（拡大）**です。
次に R（回転）、最後に T（移動）。書いた順と逆から効く、と覚えてください。
Three.js が \`position\`・\`rotation\`・\`scale\` から行列を作るときも、この
「拡大 → 回転 → 移動」の順で合成しています。
`,
    },
    {
      kind: 'md',
      text: `
## Three.js での実際

ふだんは \`position\` などを書くだけで、Three.js が毎フレーム内部で行列を組み立ててくれます
（\`updateMatrix\` が呼ばれています）。

自分で行列を触るのは、次のような場面です。

- 変換を自分で合成して、まとめて適用したいとき
- ある点が別の座標空間ではどこになるかを知りたいとき
- インスタンス描画（\`InstancedMesh\`）で、1つ1つの配置を行列で渡すとき
`,
    },
    {
      kind: 'code',
      title: '行列を自分で組み立てる',
      code: `import * as THREE from 'three';

const translation = new THREE.Matrix4().makeTranslation(2, 0.5, 0);
const rotation = new THREE.Matrix4().makeRotationY(Math.PI / 4);
const scale = new THREE.Matrix4().makeScale(1.5, 1.5, 1.5);

// 拡大 → 回転 → 移動 の順で効く（右から適用される）
const matrix = new THREE.Matrix4()
  .multiply(translation)
  .multiply(rotation)
  .multiply(scale);

// 点に適用する
const point = new THREE.Vector3(1, 0, 0);
point.applyMatrix4(matrix);

// 物体に直接使うこともできる（自動更新は切っておく）
mesh.matrixAutoUpdate = false;
mesh.matrix.copy(matrix);

// 逆に、行列から位置・回転・拡大を取り出すこともできる
const p = new THREE.Vector3();
const q = new THREE.Quaternion();
const s = new THREE.Vector3();
matrix.decompose(p, q, s);`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '中身の並び順に注意',
      text: `
\`Matrix4.elements\` は**列優先**で並んでいます。人が読むときの並び（行優先）とは
順番が違うので、直接ログに出すと混乱します。デモの表示では行優先に並べ替えています。
`,
    },
    {
      kind: 'md',
      text: `
## 逆行列 ― 変換を巻き戻す

行列には「逆をやる行列」があります。移動した行列の逆行列は同じ量を戻す行列、
回転の逆行列は逆回りの回転です。

これが効くのが、次の章で出てくる「ワールド座標からローカル座標への変換」です。
親の変換行列の逆を掛ければ、世界の座標を親から見た座標に翻訳できます。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「順番を入れ替えた箱も表示」を入れ、移動 x を 3、y 軸まわりの回転を 90 度にしてください。
2 つの箱はどこにいますか。**なぜ違う場所になるのか**を、原点との関係で説明してください。`,
      hint: '回転はいつでも原点を中心に起きます。',
      answer: `Three.js の順（拡大 → 回転 → 移動）の箱は、原点で 90 度回ってから x に 3 動くので、**x = 3 の場所で向きだけが変わって**います。
入れ替えたほう（移動 → 回転）は、x = 3 に動いてから原点を中心に 90 度回るので、**振り回されて**別の場所に行きます。
回転はつねに原点を中心に起きるので、「先に動かすと、その距離がそのまま回転の半径になる」わけです。`,
    },
    {
      prompt: '\`mesh.position\` \`mesh.rotation\` \`mesh.scale\` を設定したとき、Three.js はそれをいつ 1 つの行列にまとめていますか。自分で今すぐ反映させたいときはどうしますか。',
      hint: '毎フレームの描画の直前に、シーン全体をたどって計算されています。',
      answer: `描画のたびに \`Scene\` を上からたどって \`updateMatrixWorld()\` が呼ばれ、そこで 3 つが 1 つの行列に合成されます。
つまり**設定した直後の \`matrixWorld\` は、まだ 1 フレーム前のまま**ということです。
描画を待たずにワールド座標を知りたいときは、自分で \`updateMatrixWorld()\` を呼びます
（第9章で、親子関係があるときにこれが効いてきます）。`,
      answerCode: `mesh.position.set(3, 0, 0);
mesh.rotation.y = Math.PI / 2;

// この時点の matrixWorld はまだ更新されていない
mesh.updateMatrixWorld(true);

const world = new THREE.Vector3();
mesh.getWorldPosition(world); // 中で matrixWorld を読んでいる`,
    },
  ],
  quiz: [
    {
      q: '3D の変換行列が 3x3 ではなく 4x4 になっているのはなぜですか。',
      choices: [
        '平行移動を、回転や拡大と同じかけ算で扱えるようにするため',
        '色の情報を持たせるため',
        '計算を速くするため',
        '時間の情報を持たせるため',
      ],
      answer: 0,
      explain:
        '3x3 の行列では原点が動かせず、平行移動を表せません。座標に 4 つ目の値を足した同次座標にすることで、移動も同じ枠組みに乗ります。',
    },
    {
      q: '「その場で回転してから、横に 3 だけ移動する」変換を作りたいとき、行列のかけ算の並びはどれですか。',
      choices: [
        '$T \\times R$（移動を左、回転を右に書く）',
        '$R \\times T$（回転を左、移動を右に書く）',
        'どちらでも同じ結果になる',
        '行列では表せない',
      ],
      answer: 0,
      explain:
        '適用は右から順に起きます。回転を先に効かせたいなら回転を右に置きます。順番を入れ替えると、横にずれた状態のまま原点を中心に振り回される動きになります。',
    },
    {
      q: 'Three.js が `position`・`rotation`・`scale` から行列を組み立てるとき、変換が効く順番はどれですか。',
      choices: ['拡大 → 回転 → 移動', '移動 → 回転 → 拡大', '回転 → 拡大 → 移動', '毎回ランダム'],
      answer: 0,
      explain:
        'まず原点で大きさを決め、次に向きを決め、最後に置きたい場所へ運ぶ、という順です。この順番だと、拡大しても回転の中心がずれないので直感どおりに動きます。',
    },
  ],
};
