import type { Chapter } from '../types.ts';

export const chapter06: Chapter = {
  slug: '06-matrix',
  part: 'math3d',
  number: 1,
  title: '行列 ― 点を入れると、動いた点が返る装置',
  goal: '行列を「数の表」ではなく「変換をひとまとめにした装置」として見られるようになり、なぜ変換を 1 つにまとめたいのかが分かります。',
  requires: ['b17-normalize', 'b20-sin-cos'],
  threeApis: ['Matrix4', 'Object3D.matrix', 'Vector3.applyMatrix4'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

Three.js では \`position\`・\`rotation\`・\`scale\` を書けば物体が動きます。
それで済むうちは、{{行列}}を知らなくても困りません。

困りはじめるのはこの辺からです。

- 「回してから移動」と「移動してから回転」で結果が違う理由が分からない
- 親を回したら子が予想外のところへ飛んでいった
- カメラの \`lookAt\` や \`matrixWorld\` が何をしているのか読めない
- 拡大したら、なぜか陰影がおかしくなった

これらはすべて、**変換が行列のかけ算でできている**ことを知ると一気に見通せます。
**この 8 章で、行列を道具として使えるようにします。**
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '行列は「操作の指示書」です',
      text: `
行列を「数を並べた表」として覚えようとすると苦しくなります。
**「点を入れると、動かした後の点を返してくれる装置」** だと思ってください。
装置の中身が数の表になっているだけで、使う側にとっては入口と出口がすべてです。
`,
    },
    {
      kind: 'md',
      text: `
## 変換とは、点を別の点へ移すこと

移動・回転・拡大——この 3 つをまとめて{{変換}}と呼びます。
どれも「ある点を、別の点へ移す」ことをしています。

- **移動** … すべての点を、同じ向きに同じだけずらす
- **回転** … 原点を中心に、すべての点をまわす
- **拡大** … 原点からの距離を、すべての点で倍にする

**3 つとも「すべての点に、同じ規則を当てる」**という形をしています。
だから 1 つの装置にまとめられます。

[](#/ch/b03-function-graph)でやった「入れると出てくる箱」の、
**入口と出口が点（ベクトル）になったもの**、と思ってください。
`,
    },
    {
      kind: 'md',
      text: `
## なぜ表にまとめるのか

別々に書くこともできます。実際 Three.js の \`position\` や \`rotation\` は別々です。
それでも行列にまとめる理由が 2 つあります。

**1 つ目 ― 変換が積み重なるから。**
3D では 1 つの物体に何十回も変換が重なります（親の親の親…と辿っていくため）。
指の位置を知るには、指→手→腕→体→世界と、変換を全部たどる必要があります。

**2 つ目 ― 頂点が大量にあるから。**
10 個の変換を 1 つの表に潰しておけば、頂点 1 万個に対して装置を 1 回通すだけで済みます。
潰さなければ、1 万 × 10 回の計算になります。

**「変換を合成できる」** ― これが行列を使ういちばんの理由です。
`,
    },
    {
      kind: 'demo',
      id: 'transform-playground',
      caption:
        '移動・回転・拡大のスライダーを動かすと、下に並んだ 16 個の数がどう変わるかが見えます。いまは数の意味が分からなくて構いません。「スライダーを動かすと表の数が変わり、その表が箱を動かしている」という関係だけ見てください。',
    },
    {
      kind: 'md',
      text: `
## 数の並びは 4 行 4 列

3D の変換行列は 4 行 4 列、つまり **16 個の数**でできています。
なぜ 3×3 ではなく 4×4 なのかは、[](#/ch/m04-homogeneous)で扱います。

いまの段階では、**16 個の数それぞれに意味がある**とだけ知っておいてください。
デモで「移動 x」だけを動かすと、変わるのは 16 個のうち 1 個だけです。
回転を動かすと、別の場所の数がまとまって変わります。

**表のどこに何が入っているか**は、このあとの章で少しずつ明かしていきます。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '中身の並び順に注意',
      text: `
\`Matrix4.elements\` は**列優先**で並んでいます。人が読むときの並び（行優先）とは
順番が違うので、直接ログに出すと混乱します。デモの表示では行優先に並べ替えています。

three で行列を組み立てるときも、\`set()\` に渡す順番は**行優先**です
（\`elements\` の並びとは違う）。ここは何度でも間違えるところなので、
「読むときと書くときで順番が違う」とだけ覚えておいてください。
`,
    },
    {
      kind: 'md',
      text: `
## Three.js での実際

ふだんは \`position\` などを書くだけで、Three.js が毎フレーム内部で行列を組み立ててくれます
（\`updateMatrix\` が呼ばれています）。

自分で行列を触るのは、次のような場面です。

- 変換を自分で合成して、まとめて適用したいとき
- ある点が別の座標空間ではどこになるかを知りたいとき
- インスタンス描画（\`InstancedMesh\`）で、1 つ 1 つの配置を行列で渡すとき

**逆に言えば、それ以外では触らなくて構いません。**
ただし「触らないが、何をしているかは分かる」状態にはしておく価値があります。
それが分かっていないと、思ったとおりに動かないときに手が止まります。
`,
    },
    {
      kind: 'code',
      title: '行列を作って、点に適用する',
      code: `import * as THREE from 'three';

// 移動の行列
const m = new THREE.Matrix4().makeTranslation(2, 0.5, 0);

// 点に適用する（点のほうが書き換わる）
const p = new THREE.Vector3(1, 0, 0);
p.applyMatrix4(m);      // (3, 0.5, 0)

// 物体が持っている行列を見る
mesh.position.set(1, 2, 3);
mesh.updateMatrix();
mesh.matrix.elements;   // 16 個の数（列優先の並び）

// 行列から位置・回転・拡大を取り出すこともできる
const pos = new THREE.Vector3();
const quat = new THREE.Quaternion();
const scl = new THREE.Vector3();
mesh.matrix.decompose(pos, quat, scl);`,
    },
    {
      kind: 'md',
      text: `
## この 8 章で扱うこと

- **行列とベクトルの掛け算** … 装置の中で何が起きているか
- **列は新しい軸** … 行列をいちばん直感的に読む方法
- **なぜ 4×4 か** … 同次座標
- **掛ける順番** … 入れ替えると結果が変わる
- **移動・回転・拡大の合成** … Three.js が内部でやっていること
- **逆行列** … 変換を巻き戻す
- **法線行列** … 拡大したときに陰影が壊れる理由

**どれも第1部の道具でできています。** 新しく覚える数学はほとんどありません。
行列は、これまでのベクトルの操作を**まとめて書くための記法**です。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「移動 x」だけを動かしてください。16 個の数のうち、いくつが変わりますか。
つぎに「y 軸まわりの回転」だけを動かして、同じことを見てください。`,
      hint: '数の表のどこが変わるかに注目してください。',
      answer: `**移動 x** … 変わるのは **1 個だけ**です（右端の列の 1 つ目）。

**回転** … **4 個**が同時に変わります（左上の 3×3 のうち、x と z に関わる 4 か所）。

移動が 1 個で済むのに、回転が 4 個も要るのはなぜか ―
回転は「x と z を混ぜ合わせる」操作だからです。
新しい x は古い x と古い z の両方から作られ、新しい z も同じ。
だから 2×2 = 4 個の数が要ります。

**移動は足し算、回転は混ぜ合わせ**。表の形の違いが、その違いを映しています。`,
    },
    {
      prompt: `変換を行列にまとめる利点を、**頂点 10000 個の物体に 5 段の親子関係がある**場合で説明してください。
まとめない場合と、まとめた場合で、計算量がどう変わりますか。`,
      hint: '「先に合成する」と「頂点ごとに全部たどる」のどちらが少ないでしょう。',
      answer: `**まとめない場合** … 頂点 1 つにつき 5 回の変換。$10000 \\times 5 = $ **5 万回**

**まとめる場合** … まず 5 つの行列を 1 つに合成（**4 回**の行列の掛け算）。
そのあと頂点ごとに 1 回ずつ適用して **1 万回**。合わせて約 1 万回

**5 分の 1 になりました。** しかも親子関係が深くなるほど差が開きます。
10 段なら 10 万回 対 1 万回で 10 分の 1 です。

これが「変換を合成できる」ことの実利です。
GPU に渡すときも、合成済みの行列を 1 つ渡すだけで済みます。`,
    },
    {
      prompt: `行列を「数の表」ではなく「装置」として見る、という言い方をしました。
この見方だと、**行列どうしの掛け算**は何を表すことになりますか。`,
      hint: '装置を 2 つ並べたら、何ができますか。',
      answer: `**2 つの装置をつないで、1 つの装置にすること**です。

装置 A に点を入れて、出てきた点を装置 B に入れる ― この一連の流れを、
最初から 1 つの装置でやってしまえるようにするのが行列の掛け算です。

だから「$B \\times A$」と書いたものは「A を通してから B を通す装置」になります。
**書いた順と、効く順が逆**になるのがここから来ています。
この話は[](#/ch/m05-matrix-order)で正面から扱います。

なお、つなぐ順番を変えれば別の装置になります。
だから行列の掛け算は、順番を入れ替えられません。`,
    },
  ],
  quiz: [
    {
      q: '行列をいちばん実用的に捉える言い方はどれですか。',
      choices: [
        '点を入れると、変換された点が返る装置',
        '数を 16 個並べた表',
        '3D 専用の特殊な数',
        '座標を保存しておく入れ物',
      ],
      answer: 0,
      explain:
        '中身が数の表なのは事実ですが、使う側にとっては入口と出口がすべてです。「表を覚える」ではなく「何をする装置か」で捉えると、以降の話が全部つながります。',
    },
    {
      q: '複数の変換を 1 つの行列にまとめる、いちばんの利点はどれですか。',
      choices: [
        '頂点ごとに変換を何度も適用せずに済む',
        '数値の精度が上がる',
        'メモリ使用量が減る',
        '回転の順番を気にせずに済む',
      ],
      answer: 0,
      explain:
        '親子関係が深いほど効きます。5 段の階層と 1 万頂点なら、合成しておけば計算量が 5 分の 1 になります。GPU に渡す行列も 1 つで済みます。',
    },
    {
      q: '`Matrix4.elements` の並び順について、正しいのはどれですか。',
      choices: [
        '列優先で並んでいるので、読むときに注意が要る',
        '行優先で、見た目のまま並んでいる',
        '並び順は実行環境によって変わる',
        '並び順は決まっていない',
      ],
      answer: 0,
      explain:
        '`elements` は列優先ですが、`set()` に渡すときは行優先です。読むときと書くときで順番が違うので、直接ログに出すと混乱します。',
    },
  ],
};
