import type { Chapter } from '../types.ts';

export const chapterB16: Chapter = {
  slug: 'b16-vector-length',
  part: 'basics',
  number: 16,
  title: 'ベクトルの長さ ― 「どれだけ」を取り出す',
  goal: 'ベクトルから大きさだけを取り出せるようになり、速さ・距離・強さがすべて同じ 1 つの式で出せることが分かります。',
  requires: ['b15-vector-scale', 'b11-distance'],
  threeApis: ['Vector3.length', 'Vector3.lengthSq', 'Vector3.manhattanLength'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

ベクトルは「向き」と「大きさ」でできています。
この章と次の章で、その**片方ずつを取り出す**方法を手に入れます。まずは大きさから。

大きさが欲しくなる場面は、思っているより多いです。

- 2 点の距離（引き算してから長さを測る）
- 速さ（速度ベクトルの長さ。「時速何キロか」にあたる）
- 光の届き具合（距離が遠いほど暗い）
- 当たり判定（半径より近いか）

計算は[](#/ch/b11-distance)でやったものと**まったく同じ**です。
新しく覚えることはありません。呼び名と記号だけ確認します。
`,
    },
    {
      kind: 'md',
      text: `
## 記号と呼び名

矢印から向きの情報を捨てて、「どれだけ」だけを取り出した値を{{大きさ}}、
または長さ・ノルムと呼びます。書き方は縦棒 2 本ではさみます。

$\\|\\mathbf{v}\\|$ と書いて「v の長さ」「v のノルム」と読みます。

数の絶対値 $|a|$ が縦棒 1 本だったのを思い出してください。
**あれを数の組に広げたものが、この記号**です。役割も同じで、

- 絶対値 … 数から符号（向き）を落として、隔たりだけを残す
- 長さ … ベクトルから向きを落として、大きさだけを残す

どちらも**結果は必ず 0 以上**になります。
`,
    },
    {
      kind: 'formula',
      tex: '\\|\\mathbf{v}\\| = \\sqrt{x^2 + y^2 + z^2}',
      readAloud:
        'v を縦棒 2 本ではさんだ記号は「v の長さ」と読みます。x・y・z をそれぞれ 2 乗して足し、最後に平方根を取ると長さになります。',
      worked: {
        given: '足し算の章で作った $\\mathbf{v} = (3,\\; 4,\\; 0)$ の長さを求めます。',
        steps: [
          { calc: '3 の 2 乗 = 9' },
          { calc: '4 の 2 乗 = 16' },
          { calc: '0 の 2 乗 = 0' },
          { calc: '9 + 16 + 0 = 25', note: '3 つを足す' },
          { calc: '25 の平方根 = 5', note: '最後に平方根を取る' },
        ],
        result:
          '長さは **5**。3 と 4 で 7 にならないのが要点で、[](#/ch/b10-pythagoras)で見た「斜めに突っ切ると得をする」がそのまま出ています。',
      },
    },
    {
      kind: 'demo',
      id: 'vector-length',
      caption:
        'v の成分を動かして、読み出しの「長さ」がどう変わるかを見てください。1 つの成分だけを増やすと長さも同じだけ増えますが、2 つを同時に増やすと増え方が足し算より緩やかになります。',
    },
    {
      kind: 'md',
      text: `
## 速さは、速度ベクトルの長さ

日本語では区別が曖昧ですが、3D では分けて考えると混乱が減ります。

- **速度** … 向きと大きさを持つベクトル。$(3,\\; 0,\\; 4)$ のような形
- **速さ** … その長さ。ただの数。この例なら 5

「毎秒 5 進む」と言えるのは長さを測ったからです。
だから「速さを一定にしたい」という要求は、**長さを一定にしたい**という要求になります。
その方法が次の章です。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '長さくらべなら、ルートは省ける',
      text: `
平方根の計算は他より重い処理です。「どちらが近いか」を比べたいだけなら、
ルートを取る前の値（\`lengthSq()\`）どうしを比べれば十分で、答えは変わりません。
当たり判定などで大量に距離を測るときの定番の手です。
比べる相手も二乗しておくのを忘れないでください。
`,
    },
    {
      kind: 'md',
      text: `
## もう 1 つの「長さ」

参考までに、three には \`manhattanLength()\` というメソッドもあります。
成分の絶対値をそのまま足したもので、$|x| + |y| + |z|$ です。

$(3,\\; 4,\\; 0)$ なら **7**。まさに「横に 3 進んでから縦に 4 進んだ道のり」で、
碁盤の目の街を歩く距離にあたります（名前の由来もそこです）。

ふだんの 3D で使うのは、まっすぐの距離を返す \`length()\` のほうです。
ただ「二乗も平方根も使わないので速い」という利点があり、
おおざっぱな足切りに使われることがあります。
**7 と 5 の違いが、この章でずっと言ってきたこと**でもあります。
`,
    },
    {
      kind: 'code',
      title: '長さを測る',
      code: `import * as THREE from 'three';

const v = new THREE.Vector3(3, 4, 0);

v.length();           // 5      ― まっすぐの長さ
v.lengthSq();         // 25     ― その二乗（平方根を取っていない）
v.manhattanLength();  // 7      ― 成分の絶対値の合計（道のり）

// 速度ベクトルから「速さ」を出す
const speed = velocity.length();

// 当たり判定は二乗のまま比べるのが定石
const RADIUS = 3;
if (a.distanceToSquared(b) < RADIUS * RADIUS) {
  // 当たっている
}

// length() は読み取るだけで、v は書き換わらない
v.x;  // 3 のまま`,
    },
  ],
  exercises: [
    {
      prompt: `長さを手で計算してください。1〜3 は整数になります。

1. $(0,\\; 0,\\; 7)$
2. $(6,\\; 8,\\; 0)$
3. $(2,\\; -3,\\; 6)$
4. $(1,\\; -1,\\; 0)$（小数第 3 位まで）`,
      hint: '2 は 3・4・5 の 2 倍です。3 は $4 + 9 + 36$ を足してみてください。',
      answer: `1. **7**。1 つの軸だけに伸びているベクトルの長さは、その成分の絶対値そのものです
2. $\\sqrt{36 + 64} = \\sqrt{100} = $ **10**
3. $\\sqrt{4 + 9 + 36} = \\sqrt{49} = $ **7**。マイナスは二乗で消えました
4. $\\sqrt{1 + 1} = \\sqrt{2} \\approx $ **1.414**

1 と 3 が同じ 7 になったことに注目してください。
**成分の並びが違っても長さは同じ**になり得ます。長さは向きの情報を捨てているからです。`,
    },
    {
      prompt: `速度が $(3,\\; 0,\\; 4)$ のとき、速さはいくつですか。
また、この物体を**同じ向きのまま速さ 10 にする**には、速度ベクトルをどう変えればよいですか。`,
      hint: 'まず長さを出します。それを 10 にするには何倍でしょう。',
      answer: `速さは $\\sqrt{9 + 0 + 16} = \\sqrt{25} = $ **5** です。

速さ 10 にするには **2 倍**します。$(6,\\; 0,\\; 8)$ で、長さは
$\\sqrt{36 + 64} = 10$ になります。

一般には「欲しい長さ ÷ いまの長さ」を掛けます。
three なら \`velocity.setLength(10)\` の 1 行で済み、
中では「長さで割ってから 10 を掛ける」ことをしています。`,
    },
    {
      prompt: `\`lengthSq()\` を使って「半径 4 の中にあるか」を判定するコードを書きたい。
比べる相手をどう書けばよいですか。また、うっかり \`< 4\` と書いてしまったとき、
**実際には半径いくつの判定**になりますか。`,
      hint: '左辺が二乗の値なら、右辺も二乗しておく必要があります。',
      answer: `比べる相手も二乗して \`< 4 * 4\`（つまり 16）と書きます。

うっかり \`< 4\` と書くと、$\\sqrt{4} = $ **半径 2** の判定になります。
半分です。「当たり判定が思ったより小さい」という形で表に出ますが、
コードを見ても間違いに見えないので、見つけるのに時間がかかります。

半径を変数に取り、\`RADIUS * RADIUS\` と書く習慣にしておくと、
半径を変えたときの直し忘れも同時に防げます。`,
      answerCode: `const RADIUS = 4;

// 良い（半径を変えても壊れない）
if (p.lengthSq() < RADIUS * RADIUS) { /* ... */ }

// 危ない（実際には半径 2 の判定）
if (p.lengthSq() < RADIUS) { /* ... */ }`,
    },
  ],
  quiz: [
    {
      q: '$(0,\\; -5,\\; 0)$ の長さはいくつですか。',
      choices: ['5', '-5', '25', '0'],
      answer: 0,
      explain:
        '二乗した時点で符号が消えるので、$\\sqrt{25} = 5$ です。長さは向きの情報を捨てた値なので、決して負になりません。',
    },
    {
      q: '$\\|\\mathbf{v}\\|$ という記号は何を表していますか。',
      choices: [
        'v の長さ（大きさ）',
        'v の x 成分',
        'v を 2 倍したもの',
        'v の向き',
      ],
      answer: 0,
      explain:
        '縦棒 2 本ではさむのが長さの記号です。数の絶対値 $|a|$ を、数の組に広げたものだと思ってください。どちらも「向きを捨てて大きさだけ残す」働きをします。',
    },
    {
      q: '`manhattanLength()` が $(3, 4, 0)$ に対して返す値はどれですか。',
      choices: ['7', '5', '12', '25'],
      answer: 0,
      explain:
        '成分の絶対値をそのまま足すので $3 + 4 + 0 = 7$ です。これは「横に 3 進んでから縦に 4 進んだ道のり」で、まっすぐの距離 5 とは別物です。ふだん使うのは `length()` のほうです。',
    },
  ],
};
