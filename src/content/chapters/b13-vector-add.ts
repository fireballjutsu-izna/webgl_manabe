import type { Chapter } from '../types.ts';

export const chapterB13: Chapter = {
  slug: 'b13-vector-add',
  part: 'basics',
  number: 13,
  title: 'ベクトルの足し算 ― 矢印を継ぎ足す',
  goal: 'ベクトルの足し算を成分ごとの計算としても矢印の継ぎ足しとしても説明できるようになり、「いまの位置からこのぶん動かす」を自分で書けるようになります。',
  requires: ['02-vector', 'b01-number-line'],
  threeApis: ['Vector3.add', 'Vector3.addVectors', 'Vector3.addScaledVector'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

物を動かすコードは、突き詰めると全部これです。

- いまの位置に、**進むぶん**を足す
- いまの速度に、**加速するぶん**を足す
- 親の位置に、**子のずれ**を足す

数直線の章で「足し算は、いまいる場所からその数のぶん進むこと」と決めました。
ベクトルの足し算は、**それを 3 方向いっぺんにやるだけ**です。
新しい考え方は 1 つも出てきません。
`,
    },
    {
      kind: 'md',
      text: `
## 矢印を継ぎ足す

$\\mathbf{a} + \\mathbf{b}$ は、「a の矢印の先端から、b の矢印を継ぎ足す」ことです。
着いた先が答えになります。

数直線でやったこととまったく同じ形です。あのときは線が 1 本だったので
右か左かしかありませんでしたが、こんどは 3 方向に進めるようになった、それだけです。

計算のほうは拍子抜けするほど単純で、**成分ごとに足す**だけです。
x は x とだけ、y は y とだけ足します。**混ざりません。**
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} + \\mathbf{b} = (a_x + b_x,\\; a_y + b_y,\\; a_z + b_z)',
      readAloud:
        'a と b を足すには、x どうし・y どうし・z どうしをそれぞれ足すだけです。太字の a はベクトル、右下の小さな x は「その x 成分」を意味します。',
      worked: {
        given: '$\\mathbf{a} = (3,\\; 0,\\; 0)$、$\\mathbf{b} = (0,\\; 4,\\; 0)$ のとき。',
        steps: [
          { calc: 'x :  3 + 0 = 3' },
          { calc: 'y :  0 + 4 = 4' },
          { calc: 'z :  0 + 0 = 0' },
        ],
        result:
          '$\\mathbf{a} + \\mathbf{b} = (3,\\; 4,\\; 0)$。**成分ごとに足すだけ**で、x が y に混ざることはありません。この $(3, 4, 0)$ は、長さの章でもう一度使います。',
      },
    },
    {
      kind: 'demo',
      id: 'vector-add',
      caption:
        'a の先端から b が継ぎ足され、その終点に太い矢印が届いているのが見えます。「平行四辺形を表示」を入れると、a と b の順番を入れ替えても同じ場所に着くことが分かります。',
    },
    {
      kind: 'md',
      text: `
## 順番を入れ替えても同じ

$\\mathbf{a} + \\mathbf{b}$ と $\\mathbf{b} + \\mathbf{a}$ は同じ結果になります。
成分ごとの足し算なので、ふつうの数の足し算と同じ性質を引き継ぐわけです。

デモの平行四辺形が、そのことを絵にしています。
「a を進んでから b」でも「b を進んでから a」でも、平行四辺形の向かいの角に着きます。

**この性質は引き算にはありません。** 次の章で扱いますが、
$\\mathbf{a} - \\mathbf{b}$ と $\\mathbf{b} - \\mathbf{a}$ は真逆を向きます。
「足し算は順不同、引き算は順番が命」と覚えておいてください。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '0 を足しても何も起きない',
      text: `
$(0,\\; 0,\\; 0)$ は、足しても相手を変えないベクトルです（**ゼロベクトル**と呼びます）。
数の 0 とまったく同じ役目で、「動かない」を表すのに使えます。
毎フレーム速度を足す作りにしておけば、速度をゼロベクトルにするだけで止められます。
`,
    },
    {
      kind: 'code',
      title: '足し算の 3 つの書き方',
      code: `import * as THREE from 'three';

const a = new THREE.Vector3(3, 0, 0);
const b = new THREE.Vector3(0, 4, 0);

// 1. a を書き換える（いちばん短いが、a が壊れる）
a.add(b);                    // a は (3, 4, 0) になった

// 2. 結果を別のベクトルに入れる（a も b も壊れない）
const c = new THREE.Vector3().addVectors(a, b);

// 3. コピーしてから足す（2 と同じ意味。読みやすいほうを選ぶ）
const d = a.clone().add(b);

// 「向きに沿って、この距離だけ進む」は 1 つでできる
mesh.position.addScaledVector(velocity, dt);
// ↑ これは position.add(velocity.clone().multiplyScalar(dt)) と同じ`,
    },
    {
      kind: 'md',
      text: `
## 位置に「進むぶん」を足す

ここまでを 1 行にすると、3D のアニメーションの中心が書けます。

- 位置ベクトル $\\mathbf{p}$ に、
- 「このフレームで進むぶん」のベクトルを足す

進むぶんは、たいてい**向き × 速さ × 経過時間**で作ります。
向きの作り方は次の章、長さの揃え方は[](#/ch/b17-normalize)で手に入りますが、
**足す部分だけは、もう書けます。**
`,
    },
  ],
  exercises: [
    {
      prompt: `手で計算してください。

1. $(1,\\; 2,\\; 3) + (4,\\; 5,\\; 6)$
2. $(2,\\; -1,\\; 0) + (-2,\\; 1,\\; 0)$
3. $(0,\\; 5,\\; 0) + (0,\\; 0,\\; 0)$`,
      hint: '成分ごとに足すだけです。2 は符号に注意してください。',
      answer: `1. $(5,\\; 7,\\; 9)$
2. $(0,\\; 0,\\; 0)$。**ちょうど打ち消し合いました。** 2 つは向きが真逆で長さが同じベクトルです
3. $(0,\\; 5,\\; 0)$。ゼロベクトルを足しても何も変わりません

2 のように「足すと 0 になる相手」を**逆ベクトル**と呼びます。
次の章の引き算は、この逆ベクトルを足すことだと分かります。`,
    },
    {
      prompt: `箱が $(2,\\; 0,\\; 0)$ にあります。毎フレーム $(0.1,\\; 0,\\; 0)$ を足していくとき、
3 フレーム後の位置を手で計算してください。
また、これを \`mesh.position.add(step)\` と書いたとき、\`step\` は毎フレーム変化しますか。`,
      hint: '足すベクトルそのものは書き換えていません。書き換わるのはどちらでしょう。',
      answer: `3 フレーム後は $(2.3,\\; 0,\\; 0)$ です。

\`step\` は**変化しません。** \`add\` が書き換えるのは呼ばれた側、つまり \`mesh.position\` のほうです。
だから同じ \`step\` を毎フレーム使い回せます。

ただし \`step.multiplyScalar(dt)\` のように \`step\` 自身を書き換える処理を挟むと、
**そのフレームから \`step\` が別物になります。**
毎フレーム掛けていくと、あっという間に 0 に近づいて動かなくなります。`,
    },
    {
      prompt: `$\\mathbf{a} + \\mathbf{b}$ を計算するのに、つぎの 2 つのコードがあります。
どちらも結果は同じですが、**片方だけが持っている副作用**を答えてください。

- コードA … \`const c = a.clone().add(b);\`
- コードB … \`const c = a.add(b);\``,
      hint: '`add` は呼ばれた側を書き換えます。',
      answer: `**コードB だけ、\`a\` が書き換わります。** 実行後の \`a\` は $\\mathbf{a} + \\mathbf{b}$ になっており、
しかも \`c\` と \`a\` は同じものを指しています。

そのあと \`a\` を使い回すと、意図しない値で計算が進みます。
ループの中でこれをやると 1 フレームごとに足し込まれ、
**値がどんどん大きくなって物体が飛んでいく**という形で表に出ます。

なお \`new THREE.Vector3().addVectors(a, b)\` と書けば、
どちらも壊さずに結果だけを新しく作れます。`,
    },
  ],
  quiz: [
    {
      q: '$(1,\\; -2,\\; 3) + (2,\\; 2,\\; -3)$ はどれですか。',
      choices: [
        '$(3,\\; 0,\\; 0)$',
        '$(3,\\; 4,\\; 6)$',
        '$(-1,\\; -4,\\; 6)$',
        '$(2,\\; -4,\\; -9)$',
      ],
      answer: 0,
      explain:
        '成分ごとに足すだけです。y は $-2 + 2 = 0$、z は $3 + (-3) = 0$ で打ち消し合います。x どうし・y どうし・z どうしが混ざることはありません。',
    },
    {
      q: '$\\mathbf{a} + \\mathbf{b}$ と $\\mathbf{b} + \\mathbf{a}$ の関係はどれですか。',
      choices: [
        '常に同じ結果になる',
        '常に向きが真逆になる',
        '長さだけが同じで向きは違う',
        'a と b の長さが等しいときだけ同じ',
      ],
      answer: 0,
      explain:
        '成分ごとの足し算なので、ふつうの数と同じく順番を入れ替えられます。デモの平行四辺形が、どちらの道を通っても同じ角に着くことを示しています。引き算にはこの性質がありません。',
    },
    {
      q: '`mesh.position.addScaledVector(velocity, dt)` は何をしていますか。',
      choices: [
        'velocity を dt 倍したものを position に足す',
        'position を dt 倍してから velocity を足す',
        'velocity 自身を dt 倍する',
        'position と velocity を入れ替える',
      ],
      answer: 0,
      explain:
        '「向きと速さ」を持つ velocity に、経過時間 dt を掛けたぶんだけ進めます。velocity 自身は書き換わらないので、毎フレーム使い回せます。',
    },
  ],
};
