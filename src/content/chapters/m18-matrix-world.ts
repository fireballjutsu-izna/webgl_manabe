import type { Chapter } from '../types.ts';

export const chapterM18: Chapter = {
  slug: 'm18-matrix-world',
  part: 'math3d',
  number: 18,
  title: 'matrixWorld ― 親をさかのぼって積み上げる',
  goal: '深い階層でも物体の最終的な位置を行列の積として説明でき、階層が深いときの落とし穴を避けられるようになります。',
  requires: ['m17-local-world', 'm05-matrix-order'],
  threeApis: [
    'Object3D.matrix',
    'Object3D.matrixWorld',
    'Object3D.updateMatrixWorld',
    'Object3D.matrixAutoUpdate',
    'Object3D.traverse',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 親の親の、そのまた親

前の章で「ローカル → ワールドは \`matrixWorld\` を掛ける」と書きました。
では、その \`matrixWorld\` はどこから来るのか。

答えは単純で、**親のワールド行列に、自分のローカル行列を掛けたもの**です。
親も同じ規則で作られているので、たどっていくと世界の原点に着きます。

手の位置は「前腕の中の手」で、前腕は「上腕の中の前腕」で、
上腕は「胴体の中の上腕」で、胴体は「世界の中の胴体」―
その入れ子が、そのまま行列の積になります。
`,
    },
    {
      kind: 'formula',
      tex: 'M_{\\text{world}} = M_{\\text{parent}}^{\\text{world}} \\cdot M_{\\text{local}}',
      readAloud:
        'ある物体のワールド行列は、親のワールド行列に、自分のローカルな変換行列を掛けたものです。親の親がいれば、それがさらに左に積み重なっていきます。',
      worked: {
        given:
          '親を $x$ に 5 動かし、y 軸まわりに 90 度回します。子は親の中の $(2,\\; 0,\\; 0)$ にいます。子のワールド座標を求めます。',
        steps: [
          { calc: '子のローカル位置 : (2, 0, 0)' },
          { calc: '親の回転が先に効く : y 軸 90度 で (0, 0, -2)', note: '右から効く' },
          { calc: '親の移動を足す   : (0+5, 0, -2+0)' },
          { calc: '               = (5, 0, -2)' },
          { calc: '【比較】足し算だと : (5+2, 0, 0) = (7, 0, 0)', note: '合わない' },
        ],
        result:
          'ワールドでは **$(5,\\; 0,\\; -2)$**。**単純な足し算 $(7, 0, 0)$ にはなりません。** 親が回っていると、子のローカル座標そのものが向きを変えて運ばれるからです。[](#/ch/09-hierarchy)の足し算が破れるのが、まさにここです。',
      },
    },
    {
      kind: 'md',
      text: `
## 右から効く、が階層でも同じ

$M_{\\text{parent}} \\cdot M_{\\text{local}}$ の並びを、[](#/ch/m05-matrix-order)の読み方で読みます。

**掛け算は右から効く**ので、

1. まず自分のローカルな変換が効く（親の中で、ここに置かれる）
2. そのあと親の変換が丸ごとかぶさる（親ごと運ばれる）

「船の中を 3 歩進んでから、船ごと運ばれる」の順です。
**逆にはなりません。**先に船が動いてから中を歩いても、着く場所は同じに見えますが、
行列の並びとしては右が先です。

階層が深くなると、こう積み上がります。

$M_{\\text{手}} = M_{\\text{胴体}} \\cdot M_{\\text{上腕}} \\cdot M_{\\text{前腕}} \\cdot M_{\\text{手}}^{\\text{local}}$

**左へ行くほど、上位の親**です。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '深い階層で位置がずれるときの調べ方',
      text: `
どこで狂ったか分からないときは、上から順にワールド座標を出力してください。

胴体、上腕、前腕、手 ― 順に getWorldPosition を見ていけば、
最初に「思っていた場所と違う」段が見つかります。そこが原因です。

行列全体を眺めるより、この方法のほうが速く済みます。
階層のバグは「どこか 1 段」で起きていることがほとんどだからです。
`,
    },
    {
      kind: 'md',
      text: `
## いつ計算されるのか

three は毎フレーム、描画の直前に \`scene.updateMatrixWorld()\` を呼びます。
そこで**シーン全体を上から下へたどりながら**、各物体の行列を作ります。

各物体でやることは 2 段階です。

1. \`position\` \`quaternion\` \`scale\` から、自分の \`matrix\` を組む（[](#/ch/m06-trs)の TRS）
2. 親の \`matrixWorld\` に掛けて、自分の \`matrixWorld\` にする

**上から下へたどるのが要点**です。親を先に確定させないと、子を計算できません。
`,
    },
    {
      kind: 'code',
      title: '行列の更新を自分で握る',
      code: `import * as THREE from 'three';

// 毎フレーム自動で計算される（既定）
mesh.matrixAutoUpdate = true;

// まったく動かない物体は、切ると計算を減らせる
staticMesh.matrixAutoUpdate = false;
staticMesh.position.set(1, 2, 3);
staticMesh.updateMatrix();          // 切ったら、変えたときに自分で呼ぶ

// その場で必要になったとき
mesh.updateWorldMatrix(true, false);  // 自分と親だけ
scene.updateMatrixWorld(true);        // 全体（重いので多用しない）

// 階層をたどって全部に触る
scene.traverse((obj) => {
  if (obj.isMesh) obj.castShadow = true;
});

// 行列から中身を取り出す
const pos = new THREE.Vector3();
const quat = new THREE.Quaternion();
const scale = new THREE.Vector3();
mesh.matrixWorld.decompose(pos, quat, scale);`,
    },
    {
      kind: 'md',
      text: `
## 階層は深くしすぎない

行列の積は 1 回が軽い計算ですが、**物体の数だけ毎フレーム走ります**。

1 万個の物体が 10 段の階層にぶら下がっていると、毎フレーム 10 万回の行列積です。
描画そのものより、この更新のほうが重くなることがあります。

対策は 2 つ。

- **動かないものは \`matrixAutoUpdate = false\`** … 地形、建物、背景
- **階層を浅くする** … 意味のないネストは畳む

とくに 1 つ目は効きます。街ひとつぶんの建物が動かないなら、
その計算は初回の 1 回で足ります。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '親を消しても、子は消えません',
      text: `
scene.remove(parent) としても、子は parent にぶら下がったまま残ります。
シーンから外れるので描画はされませんが、メモリは解放されません。

後片付けは traverse で全部たどって、geometry と material を dispose する必要があります。
「章を移るたびにメモリが増える」の原因はここです。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでで、行列の話は完結します

[](#/ch/06-matrix)から数えて、行列で分かったことを並べます。

- 行列は点を運ぶ装置
- 列は軸の行き先
- 4×4 なのは移動を含めるため
- 掛ける順番は右から効く
- TRS が three の内部
- 逆行列で巻き戻せる
- 法線だけは逆転置が要る
- **そして階層は、その積み上げ**

残りは「回転をどう持つか」と「どんな座標で場所を指すか」です。
後者を次の 4 章でやります。
`,
    },
  ],
  exercises: [
    {
      prompt: `親を $(0,\\; 0,\\; 4)$ に置き、y 軸まわりに 90 度回しました。
子は親の中の $(3,\\; 0,\\; 0)$ にいます。子のワールド座標を手で計算してください。`,
      hint: '回転を先に、移動をあとに適用します。',
      answer: `**回転** … $(3, 0, 0)$ を y 軸まわりに 90 度回すと、
$x' = 3 \\times 0 + 0 \\times 1 = 0$、$z' = -3 \\times 1 + 0 \\times 0 = -3$
→ $(0,\\; 0,\\; -3)$

**移動** … $(0, 0, -3) + (0, 0, 4) = (0,\\; 0,\\; 1)$

答えは **$(0,\\; 0,\\; 1)$** です。

親は $z$ の 4 の位置にいるのに、子は $z = 1$ ― **原点に近づいています。**
親が 90 度回った結果、子は親から見て「手前」ではなく「奥」に置かれ直したからです。

足し算で済ませると $(3, 0, 4)$ になり、**まったく別の場所**を指します。`,
    },
    {
      prompt: `$M_{\\text{手}} = M_{\\text{胴体}} \\cdot M_{\\text{上腕}} \\cdot M_{\\text{前腕}} \\cdot M_{\\text{手}}^{\\text{local}}$ で、
**肘を曲げる**とき（前腕を回すとき）、手はどう動きますか。また胴体は動きますか。`,
      hint: '右から効くと、どの範囲に影響が及びますか。',
      answer: `**手は動き、胴体は動きません。**

前腕の行列を変えると、それより**右にあるもの（手）**が影響を受けます。
左にあるもの（胴体・上腕）は、掛け算の中で先に確定しているので変わりません。

これが階層の便利さの正体です。
**ある段を変えると、その下の枝が丸ごと付いてくる。上には波及しない。**

肘を曲げれば手も付いてくる ― 現実の腕と同じ振る舞いが、
行列の並びからそのまま出てきます。

逆に言えば、**上位の親を変えると下が全部動く**ので、
影響範囲を絞りたいときは階層の浅いところを触らないことです。`,
    },
    {
      prompt: `街に建物が 8000 個あり、どれも一度置いたら動きません。
毎フレームの負荷を減らす方法を 1 つ挙げ、注意点も答えてください。`,
      hint: '行列は毎フレーム計算し直されます。',
      answer: `**\`matrixAutoUpdate = false\` にします。**

three は毎フレーム、全物体の \`matrix\` と \`matrixWorld\` を計算し直します。
8000 個ぶんの TRS 合成と行列積が、動かないのに毎フレーム走っているわけです。

これを切ると、その計算がゼロになります。

**注意点** … 切ったあとに \`position\` などを変えたら、
自分で \`updateMatrix()\` を呼ばないと反映されません。
「動かしたのに動かない」という症状で出るので、忘れると原因が分かりにくいです。

なお建物が本当に大量なら、[](#/ch/t11-performance)の \`InstancedMesh\` のほうが効きます。
行列の更新だけでなく、描画の呼び出し回数そのものが減るからです。`,
      answerCode: `for (const building of buildings) {
  building.position.set(x, 0, z);
  building.updateMatrix();          // 1 回だけ計算する
  building.matrixAutoUpdate = false;  // 以後は毎フレームの計算を止める
}`,
    },
  ],
  quiz: [
    {
      q: '`matrixWorld` はどう作られますか。',
      choices: [
        '親の matrixWorld に、自分のローカル行列を掛ける',
        '自分の position をそのまま入れる',
        'すべての子の行列を足す',
        'カメラの行列を掛ける',
      ],
      answer: 0,
      explain:
        '親も同じ規則で作られているので、たどると世界の原点まで積み上がります。腕の階層なら、胴体・上腕・前腕・手の行列が左から順に並びます。',
    },
    {
      q: '$M_{\\text{胴体}} \\cdot M_{\\text{上腕}} \\cdot M_{\\text{前腕}}$ で、上腕を回すと影響を受けるのはどれですか。',
      choices: [
        '前腕（右にあるもの）だけ',
        '胴体（左にあるもの）だけ',
        '全部',
        'どれも影響しない',
      ],
      answer: 0,
      explain:
        '掛け算は右から効くので、ある段を変えるとその下の枝が丸ごと付いてきます。上には波及しません。肘を曲げれば手が付いてくる、という現実の腕と同じ振る舞いです。',
    },
    {
      q: '一度置いたら動かない物体を大量に置くとき、負荷を下げる設定はどれですか。',
      choices: [
        '`matrixAutoUpdate = false`（変えたときだけ `updateMatrix()`）',
        '`visible = false`',
        '`frustumCulled = false`',
        '`castShadow = false`',
      ],
      answer: 0,
      explain:
        '毎フレームの TRS 合成と行列積が止まります。ただし切ったあとに position を変えたときは、自分で `updateMatrix()` を呼ぶ必要があります。',
    },
  ],
};
