import type { Chapter } from '../types.ts';

export const chapterM37: Chapter = {
  slug: 'm37-arclength',
  part: 'math3d',
  number: 37,
  title: '弧長 ― 曲線の上を、等速で進む',
  goal: '$t$ と道のりが別物だと分かり、曲線の上を一定の速さで走らせられるようになります。',
  requires: ['m36-catmull', 'b34-inverse-lerp'],
  threeApis: [
    'Curve.getPointAt',
    'Curve.getLength',
    'Curve.getLengths',
    'Curve.getSpacedPoints',
    'Curve.getTangentAt',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## $t$ は、道のりではない

曲線に沿って球を走らせます。$t$ を毎フレーム同じだけ増やせば、等速で進むはずです。

**ところが、進みません。**

曲がりのきついところで遅くなり、まっすぐなところで速くなる。
コードのどこにも速さを変える処理は書いていないのに、そう見えます。

原因は、$t$ の意味を取り違えていることです。

**$t$ は「道のりの割合」ではありません。**
「何番目の区間の、どのあたりか」でしかありません。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '駅の数と、距離は違う',
      text: `
路線の駅を順に 0、1、2、3 と数えるとします。
「2.5 番目の駅」と言えば、2 番と 3 番の中間だと分かります。

けれど駅の間隔は一定ではありません。
都市部では隣の駅まで 800 メートル、郊外では 5 キロあります。

「駅番号を一定の速さで増やす」電車は、
都市部でのろのろ、郊外で猛スピードになります。

t を等速で進めるとは、そういうことです。
`,
    },
    {
      kind: 'demo',
      id: 'arc-length',
      caption:
        '2 つの球が同じ曲線を走ります。ピンクは $t$ をそのまま進めた球、アンバーは距離で等分した球。曲がりのきついところでピンクだけが遅くなり、2 つが大きく離れます。目盛りを出すと、ピンクの点が密なところと疎なところに分かれているのが見えます。',
    },
    {
      kind: 'md',
      text: `
## 直して使う ― 弧長パラメータ化

解決の方針は単純です。

**「道のりの割合」と「$t$」の対応表を、あらかじめ作っておく。**

1. 曲線を細かく（たとえば 200 分割）刻む
2. 各点までの**累積の道のり**を記録する
3. 「道のりの 30% の地点」を聞かれたら、表を引いて対応する $t$ を返す

3 の「表を引く」が[](#/ch/b34-inverse-lerp)の逆補間です。
求めたい道のりが表のどの 2 つのあいだにあるかを見て、そのあいだを比で割ります。

曲線に沿って測った道のりを{{弧長}}と呼び、この手当てを**弧長パラメータ化**と言います。
three では \`getPointAt()\` を呼ぶだけで、中でこれをやってくれます。
`,
    },
    {
      kind: 'formula',
      tex: 's(t) = \\sum_{i} \\left| P(t_i) - P(t_{i-1}) \\right|',
      readAloud:
        '曲線の長さは、細かく刻んだ線分の長さを足し合わせて近似します。刻みを細かくするほど本当の長さに近づきますが、完全には一致しません。',
      worked: {
        given:
          '曲線を 4 分割したら、各区間の長さが $2,\\; 5,\\; 1,\\; 2$ でした。**道のりの半分**の地点は、$t$ でいくつでしょう。',
        steps: [
          { calc: '累積の道のり : 0, 2, 7, 8, 10' },
          { calc: '全長 10 の半分 = 5' },
          { calc: '5 は 2 と 7 のあいだ ＝ 2 番目の区間' },
          { calc: '区間内の割合 : (5 - 2) / (7 - 2) = 0.6' },
          { calc: 't = (1 + 0.6) / 4 = 0.4', note: '区間 1 の 60% 地点' },
        ],
        result:
          '**$t = 0.4$** です。**$t = 0.5$ ではありません。** 2 番目の区間が長い（全体の半分）ので、道のりの中間はそこに来ます。$t$ を半分にすると $t = 0.5$、つまり区間 2 の頭になり、道のりでは 70% の地点になってしまいます。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '長さは、あくまで近似です',
      text: `
getLength() が返すのは、細かく刻んだ線分の合計です。
本当の長さ（積分）ではありません。

刻みの数は arcLengthDivisions（既定 200）で決まっており、
曲がりの激しい曲線では足りないことがあります。

長さそのものを表示するような用途では、この数を上げてください。
上げると初回の計算だけ重くなり、以後は表が再利用されます。
`,
    },
    {
      kind: 'code',
      title: '等速で走らせる',
      code: `import * as THREE from 'three';

const curve = new THREE.CatmullRomCurve3(points);

// 速さがばらつく（t は道のりではない）
const bad = curve.getPoint(t);

// 道のりで測るので、速さが一定になる
const good = curve.getPointAt(t);

// 進行方向も同じ違いがある
const dir = curve.getTangentAt(t);

// 曲線の長さと、区間ごとの累積
console.log(curve.getLength());
console.log(curve.getLengths(10));   // 11 個の累積値

// 刻みを細かくする（曲がりが激しいとき）
curve.arcLengthDivisions = 1000;
curve.updateArcLengths();

// 「秒速 5 で走る」を書く
let traveled = 0;
const total = curve.getLength();

function animate(dt) {
  traveled = (traveled + 5 * dt) % total;
  const p = curve.getPointAt(traveled / total);
  mesh.position.copy(p);

  const forward = curve.getTangentAt(traveled / total);
  mesh.lookAt(p.clone().add(forward));   // 進行方向を向く
}`,
    },
    {
      kind: 'md',
      text: `
## 「秒速いくつ」で書けるようになる

弧長で扱う最大の利点は、**速さを実際の単位で指定できる**ことです。

$t$ で書くと「毎秒 0.1 ずつ $t$ を増やす」としか言えず、
それが秒速いくつなのかは曲線の長さ次第です。曲線を編集すると速さも変わってしまいます。

弧長なら「秒速 5」と書けます。
**曲線の形を変えても、速さは変わりません。**

これは実務でかなり効きます。

- 車を制限速度どおりに走らせる
- 弾を一定の速さで飛ばす
- カメラの移動時間を「何秒かける」で指定する

どれも $t$ のままでは書けません。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '弧長とイージングは、両立できます',
      text: `
等速にしたうえで、始まりと終わりだけ緩めたいことがあります。

そのときは、まずイージングで 0 から 1 の進み具合を作り、
その値を getPointAt に渡してください。

順番が逆だと、イージングの効きが曲線の形に引きずられます。
「道のりで測ってから、緩急をつける」の順です。
`,
    },
  ],
  exercises: [
    {
      prompt: `曲線を 5 分割したら、各区間の長さが $1,\\; 1,\\; 6,\\; 1,\\; 1$ でした。
**道のりの半分**の地点は $t$ でいくつですか。手で計算してください。`,
      hint: 'まず累積の道のりを並べます。',
      answer: `**累積の道のり** … $0,\\; 1,\\; 2,\\; 8,\\; 9,\\; 10$

全長 10 の半分は **5**。これは 2 と 8 のあいだ、つまり**3 番目の区間**（添字 2）です。

**区間内の割合** … $\\frac{5 - 2}{8 - 2} = \\frac{3}{6} = 0.5$

**$t$** … $\\frac{2 + 0.5}{5} = 0.5$

答えは **$t = 0.5$** です。

この場合はたまたま一致しました。長い区間が真ん中にあり、左右対称だからです。

**対称でなければ一致しません。** たとえば長さが $6,\\;1,\\;1,\\;1,\\;1$ なら、
道のりの半分（5）は最初の区間の中にあり、$t = \\frac{0 + 5/6}{5} = 0.167$ になります。
$t = 0.5$ とは大違いです。`,
    },
    {
      prompt: `車を「秒速 8」でコースの上を走らせたい。
コースは \`CatmullRomCurve3\` で、輪になっています。どう書きますか。`,
      hint: '走った距離を持っておいて、全長で割ります。',
      answer: `**走った距離そのものを状態として持ち**、それを全長で割って \`getPointAt()\` に渡します。

要点は 3 つです。

- **距離を積む** … \`traveled += 8 * dt\`。$t$ ではなく距離を持つ
- **全長で割る** … \`getPointAt\` は 0 から 1 を受け取るので、割合に直す
- **輪なら剰余** … \`% total\` で、一周したら先頭へ戻る

こう書けば、**あとでコースの形を変えても速さは変わりません。**
$t$ で書いていると、コースを延ばした瞬間に車が速くなってしまいます。

なお \`getLength()\` は近似なので、非常に長いコースでは
一周したときにわずかな誤差が出ます。気になる場合は
\`arcLengthDivisions\` を上げてください。`,
      answerCode: `const total = course.getLength();
let traveled = 0;

function animate(dt) {
  traveled = (traveled + 8 * dt) % total;   // 秒速 8
  const u = traveled / total;

  car.position.copy(course.getPointAt(u));
  const forward = course.getTangentAt(u);
  car.lookAt(car.position.clone().add(forward));
}`,
    },
    {
      prompt: `カメラを曲線に沿って動かすとき、
「**等速で、ただし始まりと終わりだけ緩やかに**」したい。
弧長とイージングをどの順で組み合わせますか。`,
      hint: 'どちらを先に通しますか。',
      answer: `**イージングを先、弧長をあと**です。

1. 経過時間から $0 \\to 1$ の進み具合を作る
2. その値に**イージングを掛ける**（[](#/ch/b35-easing)）
3. **その結果を \`getPointAt()\` に渡す**

こうすると「道のりに対して緩急がつく」ので、意図どおりになります。

**逆にすると狂います。** \`getPoint()\` にイージングを掛けた値を渡すと、
イージングの効きが曲線の形（区間の長さのばらつき）に引きずられ、
「ゆっくり始めたいのに、たまたま短い区間があって速く動く」といったことが起きます。

**「道のりで測ってから、緩急をつける」** と覚えてください。
2 つの変換の順番が意味を持つ、という点では[](#/ch/m05-matrix-order)と同じ話です。`,
      answerCode: `const duration = 4;                    // 4 秒かける
let time = 0;

function animate(dt) {
  time = Math.min(time + dt, duration);
  const raw = time / duration;         // 0 → 1

  const eased = raw * raw * (3 - 2 * raw);   // smoothstep
  camera.position.copy(path.getPointAt(eased));   // 弧長はあと
}`,
    },
  ],
  quiz: [
    {
      q: '`getPoint(t)` の $t$ は何を表していますか。',
      choices: [
        '何番目の区間の、どのあたりか（道のりではない）',
        '道のりの割合',
        '経過時間',
        '曲線の長さ',
      ],
      answer: 0,
      explain:
        '駅番号のようなものです。駅の間隔がばらつけば、番号を等速で増やしても実際の速さはばらつきます。道のりで進めたいなら `getPointAt()` を使います。',
    },
    {
      q: '弧長パラメータ化は、何をしていますか。',
      choices: [
        '道のりと $t$ の対応表を作り、逆に引けるようにする',
        '曲線をなめらかにする',
        '制御点を増やす',
        '曲線を短くする',
      ],
      answer: 0,
      explain:
        '細かく刻んで累積の道のりを記録し、「道のりの 30%」を聞かれたら表から $t$ を逆に引きます。逆補間がそのまま使われています。',
    },
    {
      q: '「等速で、始まりと終わりだけ緩やかに」したいとき、正しい順番はどれですか。',
      choices: [
        'イージングを掛けてから `getPointAt()` に渡す',
        '`getPoint()` の結果にイージングを掛ける',
        '`getPointAt()` の結果にイージングを掛ける',
        'どちらでも同じ',
      ],
      answer: 0,
      explain:
        '「道のりで測ってから、緩急をつける」順です。逆にすると、イージングの効きが曲線の区間の長さのばらつきに引きずられます。',
    },
  ],
};
