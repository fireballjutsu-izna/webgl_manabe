import type { Chapter } from '../types.ts';

export const chapterM21: Chapter = {
  slug: 'm21-polar',
  part: 'math3d',
  number: 21,
  title: '極座標 ― 角度と距離で場所を指す',
  goal: '「どっち向きに、どれだけ」で場所を表せるようになり、円運動や渦を素直に書けるようになります。',
  requires: ['b23-atan2', 'b21-circular-motion'],
  threeApis: ['Math.atan2', 'Math.hypot', 'Vector2.setFromPolarCoordinates', 'Vector2.angle'],
  blocks: [
    {
      kind: 'md',
      text: `
## 場所の指し方は、1 つではない

ここまで場所はずっと $(x,\\; y,\\; z)$ で指してきました。
「右に 3、上に 2」という指し方です。

けれど、こう言ったほうが自然な場面があります。

- 「**北東の向きに、5 メートル**」
- 「中心から **30 度の方向に、半径 2**」
- 「**まっすぐ前に 10**」

どちらも同じ場所を指せますが、**書きやすさがまったく違います。**
前者を直交座標、後者を**極座標**と呼びます。
`,
    },
    {
      kind: 'md',
      text: `
## 極座標が向いている場面

見分け方は簡単です。**円が出てきたら極座標**です。

- **円を描いて回る** … 角度だけを増やせばよい。$x$ と $y$ を別々に考えなくて済む
- **中心からの距離が意味を持つ** … 爆風の減衰、レーダーの範囲、渦
- **向きを保ったまま距離だけ変える** … カメラの寄り引き

逆に、直交座標が向いている場面もはっきりしています。

- **格子に並べる** … 街のブロック、タイル
- **軸ごとに独立して動かす** … 上下と左右を別々に扱いたいとき

**どちらか一方に統一する必要はありません。**
書きやすいほうで考えて、必要なときに変換するのが実務です。
`,
    },
    {
      kind: 'formula',
      tex: 'x = r\\cos\\theta, \\quad y = r\\sin\\theta \\qquad r = \\sqrt{x^2+y^2}, \\quad \\theta = \\mathrm{atan2}(y,\\, x)',
      readAloud:
        '角度と距離から x と y を作るには、コサインとサインに距離を掛けます。逆に x と y から角度と距離を求めるには、ピタゴラスの定理と atan2 を使います。行きと帰りの 2 組の式です。',
      worked: {
        given: '中心から 60 度の方向、距離 4 の点を直交座標に直し、そこから戻します。',
        steps: [
          { calc: 'cos 60度 = 0.5 / sin 60度 = 0.866' },
          { calc: 'x = 4 x 0.5   = 2' },
          { calc: 'y = 4 x 0.866 = 3.464' },
          { calc: '【戻す】r = √(2² + 3.464²) = √(4 + 12) = 4', note: '距離が戻った' },
          { calc: '     θ = atan2(3.464, 2) = 60度', note: '角度も戻った' },
        ],
        result:
          '$(2,\\; 3.464)$ です。往復して元に戻ることも確かめました。$r$ の計算に出てくる $\\sqrt{x^2+y^2}$ は[](#/ch/b10-pythagoras)そのもので、$\\theta$ の `atan2` は[](#/ch/b23-atan2)そのものです。**新しい道具は 1 つも使っていません。**',
      },
    },
    {
      kind: 'demo',
      id: 'atan2-angle',
      caption:
        '点を動かすと角度が求まるデモです。ここでは逆向きに見てください ― 読み出しの角度と距離の 2 つが決まれば、点の位置が決まります。それが極座標です。x と y を別々に指定するのではなく、「向きと距離」で指しています。',
    },
    {
      kind: 'md',
      text: `
## 円運動が、1 行になる

極座標のありがたみが出るのは、**動かすとき**です。

円を描いて回る点を直交座標で書こうとすると、$x$ と $y$ をどう連動させるか悩みます。
極座標なら、**角度を増やすだけ**です。半径には触りません。

$\\theta$ を毎フレーム少し増やして、そのつど $x = r\\cos\\theta$、$y = r\\sin\\theta$ に直す ―
[](#/ch/b21-circular-motion)でやったことは、まさにこれでした。

**渦を描きたければ、角度と一緒に半径も増やします。** 式は 1 文字増えるだけです。
`,
    },
    {
      kind: 'code',
      title: '極座標を使う',
      code: `// 角度と距離から位置を作る
const angle = t * 0.8;          // 毎フレーム増やす
const radius = 5;
mesh.position.x = Math.cos(angle) * radius;
mesh.position.z = Math.sin(angle) * radius;

// 渦：角度と一緒に半径も増やす
const spiralR = 0.5 + t * 0.3;
mesh.position.x = Math.cos(angle) * spiralR;
mesh.position.z = Math.sin(angle) * spiralR;

// 位置から角度と距離を求める
const r = Math.hypot(mesh.position.x, mesh.position.z);
const th = Math.atan2(mesh.position.z, mesh.position.x);

// 向きを保ったまま、距離だけ変える（極座標が効く典型）
const keepAngle = Math.atan2(p.z, p.x);
p.x = Math.cos(keepAngle) * newRadius;
p.z = Math.sin(keepAngle) * newRadius;

// 同じことは正規化でも書ける。こちらは角度を経由しない
p.normalize().multiplyScalar(newRadius);`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '「向きを保って距離だけ変える」なら、正規化のほうが速い',
      text: `
角度を求めて、また cos と sin に戻す ―
往復するくらいなら、正規化して掛けるほうが短くて速く済みます。

三角関数を 3 回呼ぶ代わりに、平方根 1 回で終わります。

角度そのものが必要なとき（表示する、角度で判定する、角度を補間する）だけ、
atan2 を通してください。
`,
    },
    {
      kind: 'md',
      text: `
## 3D では、どの平面か

3D で極座標を使うときは、**どの平面の上で考えているか**をはっきりさせます。

three で地面に沿って回るなら、使うのは **xz 平面**です（y が上なので）。

$x = r\\cos\\theta$、$z = r\\sin\\theta$ ― 教科書の $y$ が $z$ に変わるだけです。
$y$ には触らないので、高さは別に決められます。

**これが「円柱座標」**です。極座標に高さを足しただけのもので、
らせん階段、円形の柱の並び、渦を巻きながら上がる煙 ― どれも素直に書けます。

そして高さのかわりに「もう 1 つの角度」を使うと、球面座標になります。**それが次の章です。**
`,
    },
  ],
  exercises: [
    {
      prompt: `中心から 30 度の方向、距離 6 の点を直交座標に直してください。
$\\cos 30^\\circ = 0.866$、$\\sin 30^\\circ = 0.5$ です。`,
      hint: 'それぞれに距離を掛けるだけです。',
      answer: `$x = 6 \\times 0.866 = 5.196$
$y = 6 \\times 0.5 = 3$

答えは **$(5.196,\\; 3)$** です。

検算しておきます。$\\sqrt{5.196^2 + 3^2} = \\sqrt{27 + 9} = \\sqrt{36} = 6$ ―
**距離が 6 に戻りました。**

$\\cos^2\\theta + \\sin^2\\theta = 1$ なので、
$r\\cos\\theta$ と $r\\sin\\theta$ から長さを測ると必ず $r$ に戻ります。
極座標の計算では、これが手軽な検算になります。`,
    },
    {
      prompt: `点 $(-3,\\; 3)$ の極座標（距離と角度）を求めてください。
角度は $-180$ 度から $180$ 度の範囲で答えてください。`,
      hint: '角度は `atan2(y, x)` です。符号の組み合わせから、どの象限にありますか。',
      answer: `**距離** … $\\sqrt{(-3)^2 + 3^2} = \\sqrt{18} = 4.243$

**角度** … $x$ が負で $y$ が正なので、左上（第 2 象限）です。
$\\mathrm{atan2}(3,\\; -3) = 135^\\circ$

答えは **距離 4.243、角度 135 度** です。

ここで \`atan2\` を使うのが要点です。
$\\arctan(y/x) = \\arctan(-1) = -45^\\circ$ となり、**まったく逆の向き**を指してしまいます。

割り算をすると符号の情報が 1 つに潰れるからで、
[](#/ch/b23-atan2)で見たとおり、\`atan2\` は 2 つの符号を別々に見て象限を判定します。`,
    },
    {
      prompt: `半径 5 の円の上に、点を 8 個等間隔で並べたい。
$i$ 番目（0 から 7）の点の座標を求める式を書き、$i = 2$ のときの値を手で計算してください。`,
      hint: '一周は 360 度。8 等分すると 1 つぶんは何度ですか。',
      answer: `一周を 8 等分するので、$i$ 番目の角度は $\\theta_i = \\frac{2\\pi i}{8}$ ＝ **45 度ずつ**です。

$x_i = 5\\cos\\theta_i$、$z_i = 5\\sin\\theta_i$

**$i = 2$ のとき** … $\\theta = 90^\\circ$ なので
$x = 5 \\times 0 = 0$、$z = 5 \\times 1 = 5$ → **$(0,\\; 5)$**

「等間隔に並べる」は極座標の独壇場です。
直交座標で 8 点の位置を直接書こうとすると、$0.707$ のような数を手で並べることになり、
個数を変えた瞬間に全部書き直しです。

角度で書いておけば、**8 を 12 に変えるだけ**で 12 個並びます。`,
      answerCode: `const count = 8;
const radius = 5;

for (let i = 0; i < count; i += 1) {
  const angle = (i / count) * Math.PI * 2;
  const mesh = new THREE.Mesh(geometry, material);
  mesh.position.set(Math.cos(angle) * radius, 0, Math.sin(angle) * radius);
  scene.add(mesh);
}`,
    },
  ],
  quiz: [
    {
      q: '極座標が向いているのは、どんな場面ですか。',
      choices: [
        '円を描いて回る・中心からの距離が意味を持つ',
        '格子に並べる',
        '軸ごとに独立して動かす',
        '直方体の頂点を並べる',
      ],
      answer: 0,
      explain:
        '円が出てきたら極座標です。角度を増やすだけで回るので、x と y をどう連動させるか悩まずに済みます。格子や軸ごとの動きは直交座標のほうが素直です。',
    },
    {
      q: '$(x, y)$ から角度を求めるとき、`atan2(y, x)` を使う理由はどれですか。',
      choices: [
        '割り算では符号の情報が潰れ、逆向きの角度が返ることがあるため',
        '計算が速いため',
        '角度がラジアンで返るため',
        '距離も同時に求まるため',
      ],
      answer: 0,
      explain:
        '$\\arctan(y/x)$ は $y$ と $x$ の符号を別々に見られないので、$(-3, 3)$ と $(3, -3)$ を区別できません。`atan2` は 2 つを別々の引数で受け取り、象限まで正しく判定します。',
    },
    {
      q: 'three で地面に沿って円を描くとき、使う 2 つの軸はどれですか。',
      choices: ['x と z', 'x と y', 'y と z', 'x だけ'],
      answer: 0,
      explain:
        'three は y が上なので、水平面は xz 平面です。教科書の $y = r\\sin\\theta$ が $z = r\\sin\\theta$ に変わるだけで、式の形は同じです。',
    },
  ],
};
