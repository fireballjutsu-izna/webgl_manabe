import type { Chapter } from '../types.ts';

export const chapterB31: Chapter = {
  slug: 'b31-triangle-normal',
  part: 'basics',
  number: 31,
  title: '三角形の法線 ― 頂点の順番が表裏を決める',
  goal: '3 頂点から面の法線を作れるようになり、モデルが裏返って見えないときに何を疑えばよいか分かります。',
  requires: ['b30-cross-area', 'b09-handedness'],
  threeApis: [
    'Vector3.crossVectors',
    'Triangle.getNormal',
    'BufferGeometry.computeVertexNormals',
    'Material.side',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D のモデルはすべて三角形の集まりです。そして三角形 1 枚ごとに
「どちらを向いているか」の情報 ―{{法線}}― が必要になります。

法線が無ければ、[](#/ch/b27-lambert)でやった明るさの計算ができません。
法線が**逆を向いていれば**、その面は真っ暗になるか、裏側からしか見えなくなります。

Three.js の標準ジオメトリには最初から法線が入っています。
問題になるのは、**自分で頂点を並べて形を作ったとき**と、
**他のツールから持ち込んだとき**です。
`,
    },
    {
      kind: 'md',
      text: `
## 2 辺の外積が法線になる

三角形の 3 頂点を $P_0$、$P_1$、$P_2$ とします。手順は 3 つです。

1. **1 点を基準にして、2 本の辺ベクトルを作る** … $P_1 - P_0$ と $P_2 - P_0$
2. **外積を取る** … 2 辺の両方に垂直なので、面に垂直
3. **正規化する** … 外積の長さは面積なので、向きだけにする

これだけです。**引き算・外積・正規化** ― どれもこの部でやってきた操作です。

なぜ 2 辺の外積が面に垂直になるかというと、
2 辺はどちらも三角形の面の上に乗っているからです。
その両方に垂直な向きは、面そのものに垂直です。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{n} = \\dfrac{(P_1 - P_0) \\times (P_2 - P_0)}{\\|(P_1 - P_0) \\times (P_2 - P_0)\\|}',
      readAloud:
        '法線 n は、1 点目から 2 点目へのベクトルと、1 点目から 3 点目へのベクトルの外積を、その長さで割ったものです。割るのは長さを 1 にするため、つまり正規化です。',
      worked: {
        given: '$P_0 = (0,\\; 0,\\; 0)$、$P_1 = (2,\\; 0,\\; 0)$、$P_2 = (0,\\; 0,\\; -2)$ の三角形（床の上）。',
        steps: [
          { calc: '辺1 = P1 - P0 = (2, 0,  0)' },
          { calc: '辺2 = P2 - P0 = (0, 0, -2)' },
          { calc: '外積 x 成分 : 0x(-2) - 0x0 = 0' },
          { calc: '外積 y 成分 : 0x0 - 2x(-2) = 4' },
          { calc: '外積 z 成分 : 2x0 - 0x0    = 0' },
          { calc: '長さ 4 で割る → (0, 1, 0)', note: '真上を向いた' },
        ],
        result:
          '**法線は $(0,\\; 1,\\; 0)$**、つまり真上です。床に置いた三角形なので、正しい答えです。$P_1$ と $P_2$ を入れ替えて計算すると $(0,\\; -1,\\; 0)$ になり、**真下を向きます**。',
      },
    },
    {
      kind: 'demo',
      id: 'triangle-normal',
      caption:
        'P2 を動かして、法線が常に面に垂直であることを確かめてください。「P1 と P2 を入れ替える」を入れると、法線が反転し、面の色も表（アンバー）から裏（バイオレット）に変わります。3 点を一直線に並べると、法線が決まらなくなります。',
    },
    {
      kind: 'md',
      text: `
## 表と裏は、頂点を並べた順で決まる

デモで見たとおり、**$P_1$ と $P_2$ を入れ替えると法線が裏返ります**。
これは外積の「順番を入れ替えると逆向き」がそのまま出たものです。

Three.js（と WebGL）は、**手前から見て反時計回りに並んでいるほうを表**と決めています。
だから頂点を書く順番が、そのまま面の表裏になります。

そして既定では、**表だけを描いて裏は描きません**（背面カリング）。
見えない面を描かないぶん速くなるからです。
順番を間違えた面は、**正面からは消えて、裏から見ると見える**という状態になります。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '「両面描く」は応急処置',
      text: `
面が裏返って見えないとき、\`side: THREE.DoubleSide\` にすれば見えるようになります。
ただし描く量が増え、影の付き方もおかしくなることがあります。

**まず「なぜ裏返っているか」を疑ってください。**
ほとんどは書き出し側の流儀（[](#/ch/b09-handedness)）か、頂点を並べた順番が原因です。
`,
    },
    {
      kind: 'md',
      text: `
## 面の法線と、頂点の法線

ここまでは「三角形 1 枚の法線」を作ってきました。これを**面法線**と呼びます。
面法線を使うと、隣り合う面の境目がくっきり出ます ― 宝石やローポリの見た目です。

なめらかな曲面に見せたいときは、**頂点ごとに法線を持たせます**。
その頂点を共有している面の法線を平均したもので、**頂点法線**と呼びます。
面と面の境目で向きが連続するので、陰影がなめらかにつながります。

Three.js の \`computeVertexNormals()\` がこの平均を計算してくれます。
球や円柱がなめらかに見えるのは、これが入っているからです。

**どちらが正しいということはありません。** 意図で選びます。
「ローポリにしたいのになめらかになる」ときは、頂点を共有しないように分ければ解決します。
`,
    },
    {
      kind: 'code',
      title: '三角形の法線を求める',
      code: `import * as THREE from 'three';

function triangleNormal(p0, p1, p2) {
  const e1 = new THREE.Vector3().subVectors(p1, p0);
  const e2 = new THREE.Vector3().subVectors(p2, p0);

  const n = new THREE.Vector3().crossVectors(e1, e2);

  // 3 点が一直線だと長さ 0 になる。正規化する前に確かめる
  if (n.lengthSq() < 1e-10) return null;

  return n.normalize();
}

// three が用意しているもの
const tri = new THREE.Triangle(p0, p1, p2);
tri.getNormal(new THREE.Vector3());

// 自分で組んだジオメトリなら、まとめて計算させられる
geometry.computeVertexNormals();   // 頂点法線（なめらか）

// 面の表裏の扱い
material.side = THREE.FrontSide;   // 既定。裏は描かない
material.side = THREE.DoubleSide;  // 応急処置`,
    },
  ],
  exercises: [
    {
      prompt: `3 つの頂点 \`p0\` \`p1\` \`p2\` からなる三角形の、**長さ 1 の法線**を求めてください。`,
      hint: '同じ点から出る 2 本の辺ベクトルを作ります。',
      answer: `\`p0\` を起点に 2 本の辺を作り、外積を取って正規化します。

\`p1 - p0\` と \`p2 - p0\` の順で掛けると、頂点を**反時計回り**に並べたときに手前を向く法線になります。
入れ替えると裏返るので、法線が逆だったときは、まずここを疑ってください。

3 点が一直線に並んでいると外積の長さが 0 になり、
正規化で \`NaN\` が生まれます。**必ず確かめてから正規化してください。**`,
      answerCode: `const e1 = new THREE.Vector3().subVectors(p1, p0);
const e2 = new THREE.Vector3().subVectors(p2, p0);
const normal = new THREE.Vector3().crossVectors(e1, e2);

// 3 点が一直線に並んでいると長さ 0 になり、normalize が NaN を生む
if (normal.lengthSq() < 1e-10) {
  console.warn('三角形が潰れています');
} else {
  normal.normalize();
}`,
    },
    {
      prompt: `$P_0 = (0,\\; 0,\\; 0)$、$P_1 = (0,\\; 0,\\; 3)$、$P_2 = (3,\\; 0,\\; 0)$ の三角形について、
法線を手で計算してください。真上と真下のどちらを向きますか。`,
      hint: '辺1 = $P_1 - P_0$、辺2 = $P_2 - P_0$ の順で外積を取ります。',
      answer: `辺1 $= (0, 0, 3)$、辺2 $= (3, 0, 0)$

外積の y 成分は $a_z b_x - a_x b_z = 3 \\times 3 - 0 \\times 0 = 9$、
x 成分と z 成分は 0 なので、外積は $(0,\\; 9,\\; 0)$。

正規化して **$(0,\\; 1,\\; 0)$ ― 真上**を向きます。

前の計算例（$P_1$ と $P_2$ が逆順）では真下を向いていました。
**同じ 3 点でも、書く順番で表裏が変わります。**
モデルを自分で組むときは、順番を決めて一貫させてください。`,
    },
    {
      prompt: `三角形を 1 枚作ったら、正面からは見えず、裏に回り込むと見えました。
考えられる原因を 2 つ挙げ、それぞれの直し方を書いてください。`,
      hint: '「表」がどちらかは、頂点を並べた順番で決まります。',
      answer: `**原因 1 ― 頂点を並べた順番が逆。**
three は「反時計回りに見えるほうが表」と決めています。
時計回りに並べると裏面になります。直すには頂点の順番を入れ替えます
（3 つのうち 2 つを入れ替えるだけで反転します）。

**原因 2 ― 法線が逆を向いている。**
読み込んだモデルで、面の向きの情報そのものが反対になっている場合です。
書き出し側で法線を再計算するか、\`geometry.computeVertexNormals()\` を呼びます。

どちらも \`side: THREE.DoubleSide\` で「見える」ようにはできますが、
それは症状を隠しただけで、影やライティングは正しくならないままです。`,
    },
  ],
  quiz: [
    {
      q: '三角形の法線を作る手順として正しいのはどれですか。',
      choices: [
        '2 辺のベクトルを作って外積を取り、正規化する',
        '3 頂点を足して 3 で割る',
        '3 辺の長さを掛け合わせる',
        '2 辺の内積を取る',
      ],
      answer: 0,
      explain:
        '2 辺はどちらも面の上にあるので、その両方に垂直な向き（外積）が面に垂直になります。外積の長さは面積なので、向きだけ欲しいときは正規化します。',
    },
    {
      q: '$P_1$ と $P_2$ を入れ替えて法線を計算すると、どうなりますか。',
      choices: [
        '法線が正反対を向き、面の表裏が入れ替わる',
        '法線の長さが 2 倍になる',
        '何も変わらない',
        '計算できなくなる',
      ],
      answer: 0,
      explain:
        '外積は順番を入れ替えると逆向きになります。だから頂点を書く順番が、そのまま面の表裏を決めます。Three.js は既定で表しか描かないので、順番を間違えた面は消えて見えます。',
    },
    {
      q: '`computeVertexNormals()` が計算するのはどれですか。',
      choices: [
        '頂点を共有する面の法線を平均した、なめらかな法線',
        '面ごとの法線だけ',
        '頂点の位置',
        '三角形の面積',
      ],
      answer: 0,
      explain:
        '頂点法線です。面と面の境目で向きが連続するので、陰影がなめらかにつながります。ローポリのくっきりした見た目が欲しいときは、頂点を共有しないように分けます。',
    },
  ],
};
