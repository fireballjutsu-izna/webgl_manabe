import type { Chapter } from '../types.ts';

export const chapterM19: Chapter = {
  slug: 'm19-pivot',
  part: 'math3d',
  number: 19,
  title: '回転の中心 ― ピボットをずらす',
  goal: '物体が必ずローカル原点まわりに回ることが分かり、ドアの蝶番のように任意の点を中心に回せるようになります。',
  requires: ['m18-matrix-world', '07-rotation'],
  threeApis: [
    'BufferGeometry.translate',
    'BufferGeometry.center',
    'Group',
    'Object3D.rotation',
    'Box3.setFromObject',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- ドアを開けたいのに、**真ん中で回って**しまう
- 読み込んだモデルを回したら、**遠くをぐるぐる周回**しはじめた
- 車輪を回したら、車体ごと変な軌道を描いた

原因はひとつで、しかも例外がありません。

**物体は必ず、自分のローカル原点のまわりに回ります。**

回転の中心を選ぶ設定はありません。「回したい中心」に原点が無いなら、
**原点のほうを動かす**しかありません。
`,
    },
    {
      kind: 'demo',
      id: 'pivot-offset',
      caption:
        '「回転の中心の位置」を動かすと、ドアの中でローカル原点が移動します。中央のままだとその場で回り、端に寄せるとドアのように開きます。白い点がつねに回転の中心（ローカル原点）で、そこから右端までの距離が、右端が描く円の半径になります。',
    },
    {
      kind: 'md',
      text: `
## 中心をずらす ＝ 物体をずらす

回転の中心を右に 1 動かしたい ― そのためには、**物体を左に 1 動かします。**

逆向きなのが引っかかりますが、絵で考えれば当たり前です。
原点は動かせないので、原点に「来てほしい点」のほうを原点へ持ってくるわけです。

ドアの左端を蝶番にしたいなら、左端が原点に来るように、ドア全体を右へずらします。
`,
    },
    {
      kind: 'formula',
      tex: "p' = R\\,(p - c) + c",
      readAloud:
        '点 c を中心に回すには、まず c が原点に来るようにずらし、原点まわりに回して、最後に元の場所へ戻します。ずらして、回して、戻す。この 3 段が回転の中心をずらす操作のすべてです。',
      worked: {
        given:
          '幅 4 のドアが $x = -2$ から $2$ にあります。左端 $c = (-2,\\; 0,\\; 0)$ を中心に、y 軸まわりに 90 度回したときの**右端** $p = (2,\\; 0,\\; 0)$ の行き先を求めます。',
        steps: [
          { calc: 'ずらす : p - c = (2,0,0) - (-2,0,0) = (4, 0, 0)' },
          { calc: '回す  : y 軸 90度 で (4,0,0) → (0, 0, -4)' },
          { calc: '戻す  : (0,0,-4) + (-2,0,0) = (-2, 0, -4)' },
          { calc: '【比較】中心をずらさない場合 : (2,0,0) → (0,0,-2)', note: '別の場所' },
        ],
        result:
          '右端は **$(-2,\\; 0,\\; -4)$** へ行きます。左端からの距離は $\\sqrt{0^2 + 4^2} = 4$ で、ドアの幅そのまま ― **蝶番から見た距離は変わっていません**。中心をずらさなければ $(0, 0, -2)$ で、半分の半径でしか回りません。',
      },
    },
    {
      kind: 'md',
      text: `
## 手段は 2 つある

「ずらして、回して、戻す」を three で書く方法は 2 通りあります。**結果は同じですが、向く場面が違います。**

**① ジオメトリをずらす（\`geometry.translate()\`）**

頂点そのものを移動させます。作った直後に 1 回だけ呼びます。

- **速い** … 実行時のコストがゼロ。行列が 1 段増えない
- **向く場面** … 自分で作った形。回転の中心が最初から決まっているもの
- **注意** … そのジオメトリを共有している他の物体にも効きます

**② 入れ物をかぶせる（\`Group\`）**

親を 1 つ足して、その中で物体をずらして置きます。

- **柔軟** … 中心をあとから変えられる。元のモデルに触らない
- **向く場面** … 読み込んだモデル。中心を動かしたい場合
- **注意** … 階層が 1 段深くなります

**読み込んだモデルには、ほぼ ② を使います。**
他人が作ったジオメトリを書き換えるのは、副作用が読めないからです。
`,
    },
    {
      kind: 'code',
      title: '回転の中心をずらす 2 つの方法',
      code: `import * as THREE from 'three';

// ① ジオメトリをずらす（作った直後に 1 回だけ）
const geometry = new THREE.BoxGeometry(4, 3, 0.2);
geometry.translate(2, 0, 0);        // 左端が原点に来る
const door = new THREE.Mesh(geometry, material);
door.rotation.y = Math.PI / 4;      // 蝶番のように開く

// 中心に戻したいとき
geometry.center();                  // 境界箱の中心を原点へ

// ② 入れ物をかぶせる（読み込んだモデル向き）
const pivot = new THREE.Group();
pivot.position.set(hingeX, hingeY, hingeZ);   // 中心にしたい場所へ
scene.add(pivot);

model.position.set(-hingeX, -hingeY, -hingeZ);  // 逆向きにずらす
pivot.add(model);

pivot.rotation.y = Math.PI / 4;     // pivot を回す

// モデルの中心がどこにあるか調べる
const box = new THREE.Box3().setFromObject(model);
const center = box.getCenter(new THREE.Vector3());
console.log(center);                // 0 から離れていたら、原点がずれている`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '「遠くを周回する」モデルの正体',
      text: `
読み込んだモデルを回したら、はるか彼方を中心にぐるぐる回った ―
これはモデルの原点が、形からずっと離れた場所にあるためです。

制作ソフト側で原点を動かさずに書き出すと、こうなります。
たとえばシーンの隅で作ったモデルが、その位置のまま出力される場合です。

Box3 で中心を測れば、どれだけずれているかがすぐ分かります。
そのぶんだけ逆向きにずらせば直ります。
`,
    },
    {
      kind: 'md',
      text: `
## 拡大も、同じ話です

回転の中心について書いてきましたが、**拡大の中心もまったく同じ**です。

物体は自分のローカル原点を中心に拡大されます。
原点が端にあれば、その端を固定したまま反対側へ伸びます。

- **炎が下から伸びる** … 原点を炎の根元に置いて、y を伸ばす
- **棒グラフが下から育つ** … 同じ
- **その場で膨らむ** … 原点を中心に置く

$p' = s(p - c) + c$ ― 回転の $R$ が $s$ に変わっただけで、構造は同じです。
`,
    },
  ],
  exercises: [
    {
      prompt: `幅 6 のドアが $x = -3$ から $3$ にあります。
右端 $(3,\\; 0,\\; 0)$ を蝶番にして y 軸まわりに 90 度回したとき、**左端** $(-3,\\; 0,\\; 0)$ はどこへ行きますか。手で計算してください。`,
      hint: 'ずらして、回して、戻すの 3 段です。',
      answer: `**ずらす** … $(-3, 0, 0) - (3, 0, 0) = (-6,\\; 0,\\; 0)$

**回す** … y 軸まわり 90 度で $x' = -6 \\times 0 + 0 \\times 1 = 0$、$z' = -(-6) \\times 1 + 0 = 6$
→ $(0,\\; 0,\\; 6)$

**戻す** … $(0, 0, 6) + (3, 0, 0) = (3,\\; 0,\\; 6)$

答えは **$(3,\\; 0,\\; 6)$** です。

蝶番からの距離を確かめると $\\sqrt{0^2 + 6^2} = 6$ ― ドアの幅と一致します。
**回転は長さを変えない**（[](#/ch/07-rotation)）ので、これは必ず合うはずの検算です。`,
    },
    {
      prompt: `読み込んだモデルを回したら、モデルからかなり離れた場所を中心に周回しました。
原因を調べる方法と、直す方法を答えてください。`,
      hint: 'モデルの形は、原点からどれくらい離れたところにありますか。',
      answer: `**モデルの原点が、形からずれた場所にある**からです。
制作ソフトでシーンの隅に置いたまま書き出すと、こうなります。

**調べる** … \`Box3.setFromObject()\` で境界箱を取り、その中心を見ます。
$(0,0,0)$ から離れていたら、そのぶん原点がずれています。

**直す** … 入れ物をかぶせて、中心のぶんだけ逆向きにずらします。

ジオメトリを直接ずらす手もありますが、読み込んだモデルは
**複数のメッシュの集まり**であることが多く、それぞれを直すのは面倒です。
入れ物をかぶせるほうが 1 か所で済みます。`,
      answerCode: `const box = new THREE.Box3().setFromObject(model);
const center = box.getCenter(new THREE.Vector3());

const pivot = new THREE.Group();
model.position.sub(center);   // 中心が原点に来るようにずらす
pivot.add(model);
scene.add(pivot);

pivot.rotation.y = angle;     // これでその場で回る`,
    },
    {
      prompt: `炎のエフェクトを「根元を固定したまま、上へ伸びる」ようにしたい。
\`scale.y\` を大きくすると、上下**両方**に伸びてしまいます。どう直しますか。`,
      hint: '拡大の中心も、ローカル原点です。',
      answer: `**ジオメトリの底面が原点に来るようにずらします。**

高さ 2 の円柱なら、既定では $y = -1$ から $1$ にあり、原点は真ん中です。
だから \`scale.y\` を上げると上下に伸びます。

\`geometry.translate(0, 1, 0)\` で $y = 0$ から $2$ に移せば、
底面が原点に来るので、**上へだけ伸びます**。

同じ手が使える場面は多いです ―
棒グラフが下から育つ、ドアが端から開く、ロープが上から垂れる。
どれも「原点を、動かしたくない側に置く」だけの話です。

**「拡大や回転が変な向きに効く」と思ったら、まず原点の場所を疑ってください。**`,
      answerCode: `const geometry = new THREE.CylinderGeometry(0.3, 0.5, 2, 12);
geometry.translate(0, 1, 0);     // 底面を原点へ

const flame = new THREE.Mesh(geometry, flameMaterial);
flame.scale.y = 1 + Math.sin(t) * 0.3;   // 上へだけ伸びる`,
    },
  ],
  quiz: [
    {
      q: '物体はどこを中心に回りますか。',
      choices: [
        '必ず自分のローカル原点',
        '形の重心',
        '境界箱の中心',
        '親の位置',
      ],
      answer: 0,
      explain:
        '回転の中心を選ぶ設定はありません。中心を変えたければ、物体のほうをずらして、原点に「中心にしたい点」を持ってくる必要があります。',
    },
    {
      q: '回転の中心を右に 1 ずらしたいとき、物体はどちらに動かしますか。',
      choices: ['左に 1', '右に 1', '動かさない', '上に 1'],
      answer: 0,
      explain:
        '原点は動かせないので、原点に来てほしい点のほうを原点へ持ってきます。だから逆向きです。$p - c$ で「ずらす」のが、まさにこの操作です。',
    },
    {
      q: '読み込んだモデルの回転の中心を変えるとき、向いているのはどちらですか。',
      choices: [
        '入れ物（Group）をかぶせる',
        'geometry.translate() で頂点を動かす',
        'position を毎フレーム補正する',
        'rotation.order を変える',
      ],
      answer: 0,
      explain:
        '読み込んだモデルは複数メッシュの集まりであることが多く、ジオメトリを直接書き換えると副作用が読めません。入れ物なら 1 か所で済み、中心をあとから変えることもできます。',
    },
  ],
};
