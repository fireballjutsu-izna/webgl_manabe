import type { Chapter } from '../types.ts';

export const chapterB26: Chapter = {
  slug: 'b26-dot-facing',
  part: 'basics',
  number: 26,
  title: '視界の判定 ― 前か後ろか、正面何度以内か',
  goal: '内積のしきい値で「正面◯度以内」を判定できるようになり、角度に戻さずに済ませる書き方が身につきます。',
  requires: ['b25-dot-angle'],
  threeApis: ['Vector3.dot', 'Object3D.getWorldDirection', 'MathUtils.degToRad'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

内積の符号が使えるようになりました。ここからが実際の使いどころです。

- 敵はプレイヤーの前方にいるか
- **正面 60 度の視界に入っているか**
- 背後から近づかれていないか

1 つ目は符号だけで済みます。2 つ目からは「どれくらい前か」が要るので、
**しきい値**を決めることになります。その決め方を、この章で身につけます。
`,
    },
    {
      kind: 'md',
      text: `
## 前か後ろかは、符号だけ

プレイヤーの視線の向きを $\\mathbf{f}$、プレイヤーから敵へ向かうベクトルを $\\mathbf{d}$ とします。
両方を正規化して内積を取れば、それだけで判定が終わります。

- $\\mathbf{f}\\cdot\\mathbf{d} > 0$ なら敵は前方
- $\\mathbf{f}\\cdot\\mathbf{d} < 0$ なら背後

$\\mathbf{d}$ は「行き先 引く 出発点」で作ります（[](#/ch/b14-vector-sub)）。
$\\mathbf{f}$ は three が \`getWorldDirection\` で長さ 1 のまま返してくれます。

**この判定に角度は 1 度も出てきません。** 掛け算と足し算だけです。
`,
    },
    {
      kind: 'md',
      text: `
## 「正面 60 度以内」は、cos で比べる

視界を絞りたいときは、しきい値を 0 より大きくします。
ではいくつにすればよいか ― ここが要点です。

**「正面 60 度」は、左右にそれぞれ 30 度**という意味です。
なので比べる相手は $\\cos 60^\\circ$ ではなく **$\\cos 30^\\circ$**（約 0.866）になります。
半分の角度で比べる、と覚えてください。

そして不等号の向きにも注意が要ります。
**角度が小さいほど cos は大きい**ので、「範囲内」の条件は**以上**です。
`,
    },
    {
      kind: 'formula',
      tex: '\\hat{\\mathbf{f}} \\cdot \\hat{\\mathbf{d}} \\ge \\cos\\left(\\dfrac{\\alpha}{2}\\right)',
      readAloud:
        '正規化した視線と、正規化した相手への向きの内積が、視野角の半分のコサイン以上なら、その相手は視界の中にいる、と言っています。α（アルファ）が視野角の全体です。',
      worked: {
        given: '視野角 $\\alpha = 60^\\circ$ のとき、3 つの位置関係で判定します。',
        steps: [
          { calc: 'しきい値 = cos(60/2) = cos 30 度 = 0.866' },
          { calc: '真正面（0 度）   : 内積 1.000 ≥ 0.866 → 見えている' },
          { calc: '斜め 20 度       : 内積 0.940 ≥ 0.866 → 見えている' },
          { calc: '斜め 40 度       : 内積 0.766 < 0.866 → 見えていない' },
        ],
        result:
          '**30 度を境に切り替わりました。** 視野角を広げたいならしきい値を下げます。$\\cos 90^\\circ = 0$ なので、**0 と比べるのは「視野角 180 度」**、つまり前方かどうかの判定と同じでした。前後の判定は、この式の特別な場合だったわけです。',
      },
    },
    {
      kind: 'md',
      text: `
## しきい値の目安

よく使う値を並べておきます。暗記の必要はなく、\`Math.cos\` に半分の角度を渡せば出ます。

- 視野角 30 度 … しきい値 0.966（かなり狭い。狙撃の照準など）
- 視野角 60 度 … しきい値 0.866（正面という感じ）
- 視野角 90 度 … しきい値 0.707（人の集中視野に近い）
- 視野角 120 度 … しきい値 0.5（かなり広い）
- 視野角 180 度 … しきい値 0（前方かどうか）

**数字を直接書かず、角度から作ってください。**
\`Math.cos(THREE.MathUtils.degToRad(30))\` と書いておけば、
あとで視野角を変えるときに、コードを読んだ人が意図を追えます。
`,
    },
    {
      kind: 'code',
      title: '視界に入っているかを判定する',
      code: `import * as THREE from 'three';

// プレイヤーが向いている方向（長さ1で返ってくる）
const forward = new THREE.Vector3();
player.getWorldDirection(forward);

// プレイヤーから敵へ向かう方向
const toEnemy = new THREE.Vector3()
  .subVectors(enemy.position, player.position)
  .normalize();

const d = forward.dot(toEnemy);

// 視野角 60 度 ＝ 片側 30 度
const FOV = THREE.MathUtils.degToRad(60);
const threshold = Math.cos(FOV / 2);

if (d >= threshold) {
  // 視界の中
} else if (d > 0) {
  // 前方ではあるが、視界の外
} else {
  // 背後
}`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '距離は内積では分かりません',
      text: `
内積で分かるのは**向きの関係だけ**です。正規化した時点で長さを捨てているので、
遠くにいる敵も近くにいる敵も同じ値になります。

「視界に入っていて、かつ 20 以内」を判定したいなら、
距離は別に測ってください（[](#/ch/b11-distance)の二乗のまま比べる手が使えます）。
`,
    },
    {
      kind: 'md',
      text: `
## 上下を無視したいとき

3D で視界を判定すると、**真上や真下にいる相手が視界から外れます**。
現実的にはそれで正しいのですが、床の上のゲームでは
「高さは無視して、平面での向きだけ見たい」ことがよくあります。

そのときは、**内積を取る前に y 成分を 0 にして正規化し直します**。
床に落とした影どうしで判定している、と考えてください。

この「ある方向の成分を消す」操作は、次の章の射影と地続きです。
`,
    },
  ],
  exercises: [
    {
      prompt: `視野角がつぎのとき、内積のしきい値を手で計算してください。
$\\cos 45^\\circ = 0.707$、$\\cos 60^\\circ = 0.5$、$\\cos 90^\\circ = 0$ を使います。

1. 視野角 90 度
2. 視野角 120 度
3. 視野角 180 度`,
      hint: '比べる相手は「視野角の半分」の cos でした。',
      answer: `1. $\\cos 45^\\circ = $ **0.707**
2. $\\cos 60^\\circ = $ **0.5**
3. $\\cos 90^\\circ = $ **0**

3 が 0 になるのが要点です。**「前方かどうか」の判定は、視野角 180 度の場合**でした。
最初に習った「符号を見るだけ」は、この式の特別な場合だったことになります。

視野角を広げるほどしきい値は下がり、狭めるほど 1 に近づきます。`,
    },
    {
      prompt: `敵がプレイヤーの**正面 60 度の範囲**（左右に 30 度ずつ）にいるかどうかを判定してください。
プレイヤーの向き \`forward\` は長さ 1 とします。`,
      hint: '角度に戻してから比べるより、cos のまま比べたほうが速くて素直です。cos は角度が小さいほど大きい値になります。',
      answer: `プレイヤーから敵へのベクトルを正規化して \`forward\` と内積を取り、**cos 30 度と比べます**。

\`Math.acos\` で角度に戻してから比べても正しいのですが、逆三角関数は重いうえ、
丸め誤差で値がわずかに 1 を超えると \`NaN\` になります。cos のまま比べるほうが安全です。

不等号の向きに注意してください。**角度が小さいほど内積は大きい**ので、範囲内の条件は「以上」です。`,
      answerCode: `const toEnemy = new THREE.Vector3().subVectors(enemy.position, player.position).normalize();
const limit = Math.cos(THREE.MathUtils.degToRad(30)); // 半分の角度で比べる

if (toEnemy.dot(forward) >= limit) {
  // 正面 60 度の中にいる
}`,
    },
    {
      prompt: `視界の判定を書いたところ、**どこにいても「見えている」と判定されます。**
コードは \`forward.dot(enemy.position.clone().sub(player.position)) > 0.866\` です。
何が抜けているか答えてください。`,
      hint: 'しきい値 0.866 が意味を持つのは、どういうときでしたか。',
      answer: `**正規化が抜けています。** 引き算で作ったベクトルの長さは 2 点の距離なので、
敵が 20 離れていれば内積は 20 倍のスケールになり、
少し前方にいるだけで簡単に 0.866 を超えます。

しきい値による判定は、**両方が長さ 1 のときにだけ**意味を持ちます。
\`.normalize()\` を足せば直ります。

なおこの間違いは**遠くにいるほど当たりやすくなる**ので、
「近いと判定されないのに、遠いと判定される」という奇妙な症状になります。
逆に見えるので原因にたどり着きにくいところです。`,
      answerCode: `// 誤り：長さが混ざっていて、しきい値が意味を持たない
forward.dot(enemy.position.clone().sub(player.position)) > 0.866;

// 正しい：正規化してから比べる
const toEnemy = new THREE.Vector3()
  .subVectors(enemy.position, player.position)
  .normalize();
forward.dot(toEnemy) > 0.866;`,
    },
  ],
  quiz: [
    {
      q: '視野角 90 度の判定に使うしきい値はどれですか（$\\cos 45^\\circ = 0.707$）。',
      choices: ['0.707', '0', '0.5', '1'],
      answer: 0,
      explain:
        '視野角の半分（45 度）の cos と比べます。視野角そのものの cos を使うと、意図の半分の広さになってしまいます。',
    },
    {
      q: '内積のしきい値による視界判定で、**距離**はどう扱われますか。',
      choices: [
        '分からない。距離は別に測る必要がある',
        '内積の大きさが距離を表す',
        '正規化すれば距離も含まれる',
        '内積が 1 に近いほど近い',
      ],
      answer: 0,
      explain:
        '正規化した時点で長さの情報は捨てています。内積で分かるのは向きの関係だけなので、距離は `distanceToSquared` などで別に測ってください。',
    },
    {
      q: '視野角を**広げたい**とき、しきい値はどうしますか。',
      choices: ['小さくする', '大きくする', '変えない', '負の数にはできない'],
      answer: 0,
      explain:
        '角度が大きいほど cos は小さくなるので、しきい値を下げると視界が広がります。0 まで下げると視野角 180 度（前方かどうか）、負にすると背後まで含まれます。',
    },
  ],
};
