import type { Chapter } from '../types.ts';

export const chapterM04: Chapter = {
  slug: 'm04-homogeneous',
  part: 'math3d',
  number: 4,
  title: 'なぜ 4×4 なのか ― 同次座標',
  goal: '3D なのに行列が 4 行 4 列である理由が分かり、点と方向で 4 つ目の値を使い分けられるようになります。',
  requires: ['m03-basis'],
  threeApis: ['Vector4', 'Matrix4.makeTranslation', 'Vector3.applyMatrix4', 'Vector3.transformDirection'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D なのに行列は 4 行 4 列です。ここで一度立ち止まる人が多い。

理由ははっきりしています。**3×3 では平行移動が表せない**からです。
そして、その解決策には**思わぬ副作用**があります ―
「点」と「方向」を区別して扱えるようになる、という利点です。
`,
    },
    {
      kind: 'md',
      text: `
## 3×3 では原点が動かせない

前の章で「行列の列は軸の行き先」と見ました。
このとき、**原点はどこへ行くでしょうか**。

原点 $(0, 0, 0)$ を掛けると、すべての列が 0 と掛かって消えます。
つまり**原点は必ず原点のまま**です。

回転も拡大もせん断も、原点を動かしません。だから 3×3 の行列では、
**どうやっても「全体をずらす」ことができない**のです。

平行移動だけが、他の変換と違う性質を持っています。
`,
    },
    {
      kind: 'md',
      text: `
## 4 つ目の数を足す

そこで座標に 4 つ目の数を足して $(x,\\; y,\\; z,\\; 1)$ という 4 個組にします。
これが{{同次座標}}です。

この 1 が席を作ってくれるおかげで、平行移動も「行列を掛ける」操作で扱えます。
行列の**4 列目**に移動量を置いておけば、その列が「1」と掛かって足される、という仕掛けです。

**4 つ目の 1 が無ければ、4 列目は 0 と掛けられて消えます。**
そこが要点です。
`,
    },
    {
      kind: 'formula',
      tex: '\\begin{pmatrix} 1 & 0 & 0 & t_x \\\\ 0 & 1 & 0 & t_y \\\\ 0 & 0 & 1 & t_z \\\\ 0 & 0 & 0 & 1 \\end{pmatrix}',
      readAloud:
        '平行移動の行列です。対角線に 1 が並び、いちばん右の列に移動量が入っているだけ、という素直な形をしています。デモで行列の中身を見ると、右の列が移動量そのものになっているのが確認できます。',
      worked: {
        given:
          '移動量を $t = (2,\\; 1,\\; 0)$ にして、点 $(1,\\; 0,\\; 0)$ に掛けます。同次座標なので、点は $(1, 0, 0, 1)$ の 4 個組として扱います。',
        steps: [
          {
            calc: '1 行目 : 1x1 + 0x0 + 0x0 + 2x1 = 3',
            note: '最後の 2x1 が移動量。掛ける相手の 4 つ目の 1 が効いている',
          },
          { calc: '2 行目 : 0x1 + 1x0 + 0x0 + 1x1 = 1' },
          { calc: '3 行目 : 0x1 + 0x0 + 1x0 + 0x1 = 0' },
          { calc: '4 行目 : 0x1 + 0x0 + 0x0 + 1x1 = 1', note: '4 つ目は 1 のまま。次の掛け算でも使える' },
        ],
        result:
          '$(1, 0, 0)$ が $(3,\\; 1,\\; 0)$ へ動きました。**4 つ目の 1 が無ければ、右端の列は 0 と掛けられて消えてしまいます。** 同次座標が要るのはこのためです。',
      },
    },
    {
      kind: 'demo',
      id: 'transform-playground',
      caption:
        'こんどは「移動 x」を動かして、右端の列だけが変わることを確かめてください。回転や拡大では左上の 3×3 だけが変わり、右端の列は動きません。「移動は 4 列目、それ以外は左上の 3×3」という住み分けが見えます。',
    },
    {
      kind: 'md',
      text: `
## 4 つ目が 0 なら「方向」になる

ここからが同次座標の面白いところです。

4 つ目を **0** にすると $(x,\\; y,\\; z,\\; 0)$ になり、
**平行移動が効かなくなります**（4 列目が 0 と掛かって消えるので）。

これがちょうど、**方向ベクトルの扱いとして正しい**のです。

- **点**（4 つ目が 1）… 場所なので、平行移動で動く
- **方向**（4 つ目が 0）… 向きなので、平行移動しても変わらない

[](#/ch/02-vector)で「位置ベクトルと方向ベクトルは型で区別されない」と言いました。
実は**同次座標の 4 つ目が、その区別を持っている**のです。

「北を向いている」という向きは、100 m 移動しても北のままです。
法線や光の向きを変換するときは、必ず 4 つ目を 0 にします。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '方向に平行移動を掛けてはいけません',
      text: `
法線や光の向きを、点と同じように \`applyMatrix4\` で変換すると、
**平行移動のぶんだけずれます**。物体を遠くに置くほど、向きが狂っていきます。

three には方向専用の \`transformDirection()\` があり、
こちらは平行移動を無視して、正規化もしてくれます。
向きを変換するときは、必ずこちらを使ってください。
`,
    },
    {
      kind: 'md',
      text: `
## 4 つ目は「1」でなくてもよい

同次座標のもう 1 つの働きが、**遠近感**です。

4 つ目の値 $w$ は、最後に **x・y・z を $w$ で割る**という約束になっています。
$w = 1$ なら何も起きませんが、$w = 2$ なら座標が半分になります。

透視投影の行列は、**奥行きを $w$ に入れる**ように作られています。
だから最後に割ると、遠いものほど小さくなる ― それが遠近感の正体です。

この話はカメラの章で正面から扱います。
いまは「4 つ目には、平行移動を成立させる以外の役目もある」とだけ知っておいてください。
`,
    },
    {
      kind: 'code',
      title: '点と方向を区別する',
      code: `import * as THREE from 'three';

const point = new THREE.Vector3(1, 0, 0);
const dir = new THREE.Vector3(0, 0, -1);

// 点は平行移動を受ける（4 つ目を 1 として扱う）
point.applyMatrix4(matrix);

// 方向は平行移動を受けない（4 つ目を 0 として扱う）
dir.transformDirection(matrix);   // 正規化もしてくれる

// 4 つ組を明示的に扱いたいとき
const v = new THREE.Vector4(1, 0, 0, 1);   // 点
const d = new THREE.Vector4(0, 0, -1, 0);  // 方向
v.applyMatrix4(matrix);

// 誤り：向きに平行移動が掛かってしまう
// dir.applyMatrix4(matrix);`,
    },
  ],
  exercises: [
    {
      prompt: `移動量 $(5,\\; 0,\\; 0)$ の行列を、つぎの 4 個組に掛けたときの結果を手で計算してください。

1. $(1,\\; 2,\\; 3,\\; 1)$
2. $(1,\\; 2,\\; 3,\\; 0)$`,
      hint: '4 つ目が 4 列目（移動量）に掛かります。',
      answer: `1. $(1 + 5,\\; 2,\\; 3,\\; 1) = $ **$(6,\\; 2,\\; 3,\\; 1)$**。移動した
2. $(1 + 0,\\; 2,\\; 3,\\; 0) = $ **$(1,\\; 2,\\; 3,\\; 0)$**。移動しなかった

**同じ行列、同じ 3 成分なのに、結果が違います。**
違いは 4 つ目だけ。1 なら点として、0 なら方向として扱われた、ということです。

だから「点か方向か」は、その値が持っている情報ではなく、
**こちらが決めて渡すもの**です。`,
    },
    {
      prompt: `光の向きを表すベクトルを、物体の変換行列で変換したい。
\`applyMatrix4\` を使ってはいけない理由と、正しい書き方を答えてください。`,
      hint: '光の向きは、物体を遠くに動かしても変わるべきでしょうか。',
      answer: `\`applyMatrix4\` は 4 つ目を 1 として扱うので、**平行移動が掛かります**。
物体を $(100, 0, 0)$ に置いていたら、光の向きに 100 が足されて、まったく別の向きになります。

しかも**物体を遠くに置くほど狂う**ので、
「近くでは正しいのに、遠ざけると陰影が壊れる」という分かりにくい症状になります。

正しくは \`transformDirection()\` です。平行移動を無視し、
さらに正規化もしてくれるので、[](#/ch/b17-normalize)で決めた
「向きは長さ 1 に」も守られます。`,
      answerCode: `// 誤り：平行移動のぶんだけ向きが狂う
lightDir.applyMatrix4(matrix);

// 正しい：平行移動を無視し、正規化もする
lightDir.transformDirection(matrix);`,
    },
    {
      prompt: `3×3 の行列では平行移動が表せない、と説明しました。
その理由を、**原点がどこへ移るか**という観点で説明してください。`,
      hint: '原点 $(0, 0, 0)$ を行列に掛けると、何が起きますか。',
      answer: `原点を掛けると、**すべての列が 0 と掛かって消えます**。
だから 3×3 の行列は、**必ず原点を原点に移します**。

平行移動は「原点を別の場所へ動かす」変換なので、
原点が動かせない仕組みでは表せません。

回転も拡大もせん断も原点を動かさないので、3×3 で表せます。
**平行移動だけが仲間外れ**で、そのために次元を 1 つ足しているわけです。

なお 4 つ目を足したあとも、**4 次元の意味では原点は動いていません**。
$(0,0,0,1)$ という「4 次元では原点でない点」を使うことで、
3 次元の見かけ上の平行移動を作り出しています。`,
    },
  ],
  quiz: [
    {
      q: '3D の変換行列が 3×3 ではなく 4×4 になっているのはなぜですか。',
      choices: [
        '平行移動を、回転や拡大と同じかけ算で扱えるようにするため',
        '色の情報を持たせるため',
        '計算を速くするため',
        '時間の情報を持たせるため',
      ],
      answer: 0,
      explain:
        '3×3 の行列では原点が動かせず、平行移動を表せません。座標に 4 つ目の値を足した同次座標にすることで、移動も同じ枠組みに乗ります。',
    },
    {
      q: '同次座標の 4 つ目を **0** にすると、どう扱われますか。',
      choices: [
        '方向として扱われ、平行移動が効かなくなる',
        '点が原点に移動する',
        '計算がエラーになる',
        '拡大率が 0 になる',
      ],
      answer: 0,
      explain:
        '4 列目（移動量）が 0 と掛かって消えるためです。法線や光の向きは平行移動で変わってはいけないので、0 にするのが正しい扱いです。',
    },
    {
      q: '光の向きを変換するとき、three で使うべきメソッドはどれですか。',
      choices: [
        '`transformDirection()`',
        '`applyMatrix4()`',
        '`applyQuaternion()` のみ',
        '`multiplyScalar()`',
      ],
      answer: 0,
      explain:
        '`applyMatrix4` は 4 つ目を 1 として扱うので平行移動が掛かり、物体を遠くに置くほど向きが狂います。`transformDirection` は平行移動を無視し、正規化もしてくれます。',
    },
  ],
};
