import type { Chapter } from '../types.ts';

export const chapterM41: Chapter = {
  slug: 'm41-fractal',
  part: 'math3d',
  number: 41,
  title: '自己相似 ― 拡大しても同じ形',
  goal: '木・雲・海岸線に共通する性質が分かり、少ない規則で豊かな形を生み出せるようになります。',
  requires: ['m40-subdivision', 'b42-fbm'],
  threeApis: ['Group', 'Object3D.scale', 'InstancedMesh', 'Matrix4.compose'],
  blocks: [
    {
      kind: 'md',
      text: `
## 一部を拡大すると、全体と同じ

木の枝を 1 本折り取って、写真に撮ります。
それを木全体の写真と並べると ― **見分けがつきません。**

シダの葉も、海岸線も、雲も、稲妻も同じです。
**一部を拡大すると、全体と同じ形が現れる。**
この性質を{{自己相似}}と呼び、そういう形を**フラクタル**と言います。

そして前の 2 章で作ってきたものは、**すべてこれ**です。
再帰は自己相似を作る手順そのものだからです。
`,
    },
    {
      kind: 'demo',
      id: 'subdivision',
      caption:
        '「真ん中を三角に折る」を選び、深さを上げてください。どの部分を拡大しても、同じ形が現れます。読み出しの「全体の長さ」が深さごとに $\\frac{4}{3}$ 倍ずつ増え続けるのも見てください。囲む範囲は有限なのに、線の長さは無限に伸びます。',
    },
    {
      kind: 'md',
      text: `
## 長さが定まらない ― 海岸線の話

コッホ曲線では、深さを 1 増やすたびに全体の長さが $\\frac{4}{3}$ 倍になります。
3 等分した真ん中を、2 辺で置き換えるからです。

**深さを無限にすると、長さも無限**になります。
それでいて、曲線は有限の範囲から出ません。

これは絵空事ではありません。**海岸線の長さは、測る物差しの長さで変わります。**

- 100 km の物差し … 入り江を無視して直線的に測る。短い
- 1 km の物差し … 湾のかたちを拾う。長い
- 1 m の物差し … 岩の凹凸まで拾う。もっと長い

だから「イギリスの海岸線の長さ」には**唯一の答えがありません。**
どこまで細かく測るかを決めない限り、値が定まらないからです。
`,
    },
    {
      kind: 'formula',
      tex: 'L(d) = L_0 \\left(\\frac{4}{3}\\right)^{d}',
      readAloud:
        'コッホ曲線の長さです。深さを 1 増やすたびに 3 分の 4 倍になります。1 より大きい数を掛け続けるので、深さを上げれば長さはいくらでも大きくなります。',
      worked: {
        given: '長さ 9 の線分から始めて、コッホ曲線の長さを深さごとに追います。',
        steps: [
          { calc: '深さ 0 : 9' },
          { calc: '深さ 1 : 9 x 4/3 = 12' },
          { calc: '深さ 2 : 12 x 4/3 = 16' },
          { calc: '深さ 3 : 16 x 4/3 = 21.33' },
          { calc: '深さ 10 : 9 x (4/3)^10 = 159.8' },
          { calc: '深さ 30 : 9 x (4/3)^30 = 50397', note: '5600 倍になる' },
        ],
        result:
          '**深さ 30 で 5 万を超えます。** それでも曲線は、最初の線分を囲む有限の範囲から出ません。「**有限の場所に、無限の長さが収まっている**」― これがフラクタルのいちばん有名な性質です。',
      },
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '完全な自己相似は、自然界には無い',
      text: `
本物の木は、枝を拡大しても幹と完全には同じになりません。
太さの比も、分かれる角度も、少しずつ違います。

自然に見せたいなら、この「ゆらぎ」を足してください。
分かれる角度と長さの比に、毎回わずかな乱数を混ぜるだけで、
一気に生き物らしくなります。

完全に規則的なフラクタルは、人工物のように見えます。
それが欲しい場合（結晶、建築）は、むしろ揺らさないでください。
`,
    },
    {
      kind: 'md',
      text: `
## 少ない規則から、豊かな形が出る

自己相似がありがたいのは、**書く量に対して得られる形が豊か**なことです。

木を生やす関数は 15 行ほどです。
そこにパラメータを 4 つ（分かれる数、角度、長さの比、深さ）持たせるだけで、

- 針葉樹（角度を小さく、長さの比を大きく）
- 広葉樹（角度を大きく、分かれる数を増やす）
- 枯れ木（角度をばらつかせ、長さの比を小さく）

が、**同じ関数から**出てきます。
そこに乱数の種を足せば、**同じ種類の木が無数に**作れます。

**データを持たなくてよい**のが最大の利点です。
モデルを 100 種類用意するかわりに、関数 1 つと種 100 個で足ります。
`,
    },
    {
      kind: 'code',
      title: 'ゆらぎのある木',
      code: `import * as THREE from 'three';

function branch(parent, length, radius, depth, rand, opts) {
  if (depth <= 0 || length < 0.05) return;

  const stick = new THREE.Mesh(
    new THREE.CylinderGeometry(radius * 0.7, radius, length, 5),
    barkMaterial,
  );
  stick.position.y = length / 2;
  parent.add(stick);

  const count = depth > 2 ? opts.count : 2;      // 先のほうは 2 本に減らす
  for (let i = 0; i < count; i += 1) {
    const child = new THREE.Group();
    child.position.y = length * (0.75 + rand() * 0.25);   // 出る高さもばらす
    child.rotation.y = (i / count) * Math.PI * 2 + rand();
    // 角度と長さの比に、毎回わずかな乱数を混ぜる
    child.rotation.z = opts.angle * (0.7 + rand() * 0.6);
    parent.add(child);

    branch(child, length * opts.shrink * (0.85 + rand() * 0.3), radius * 0.7, depth - 1, rand, opts);
  }
}

// 同じ関数から、違う木を作る
const pine = { count: 5, angle: THREE.MathUtils.degToRad(20), shrink: 0.8 };
const oak = { count: 3, angle: THREE.MathUtils.degToRad(40), shrink: 0.68 };

for (let i = 0; i < 30; i += 1) {
  const tree = new THREE.Group();
  branch(tree, 3, 0.12, 5, mulberry32(i), oak);   // 種を変えれば別の木
  tree.position.set(rand() * 40 - 20, 0, rand() * 40 - 20);
  scene.add(tree);
}`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '木 1 本が数百の Mesh になります',
      text: `
深さ 5・分かれる数 3 なら、枝は 364 本。
これを Mesh 1 つずつで作ると、木 1 本でドローコールが 364 回です。

30 本並べたら 1 万回を超え、描画が止まります。

対策は 2 つ。
枝のジオメトリを 1 つにまとめる（mergeGeometries）か、
InstancedMesh で同じ形をまとめて描くかです。

実践部ではこれを扱います。
「作れること」と「並べられること」は別だと覚えておいてください。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでが第2部です

**41 章で、Three.js を書くための数学がそろいました。** 大きく 5 つに分けて振り返ります。

- **行列**（8 章）… 点を運ぶ装置。列は軸の行き先。順番は右から効く
- **回転**（7 章）… オイラー角の限界と、クォータニオンという答え
- **階層と座標**（9 章）… 座標系の入れ子と、球面という別の指し方
- **カメラと光**（10 章）… 世界を平らな画面へ。なぜそう見えるのか
- **曲線と再帰**（7 章）… なめらかな道と、繰り返して生やす形

**これ以降、「なぜそう書くのか」が分からないまま進む場面はありません。**

\`position\` を読んだら親を疑う。金属が黒ければ環境マップを疑う。
影がギザギザなら正射影カメラの範囲を疑う。
**症状から原因を名指しできる**ようになっているはずです。

次の部からは、いよいよ three を書きます。
`,
    },
  ],
  exercises: [
    {
      prompt: `コッホ曲線を長さ 27 の線分から始めます。
深さ 4 での全体の長さを手で計算してください。`,
      hint: '1 段ごとに $\\frac{4}{3}$ 倍です。',
      answer: `$27 \\times \\left(\\frac{4}{3}\\right)^4 = 27 \\times \\frac{256}{81} = \\frac{6912}{81} = 85.33$

答えは約 **85.33** です。

段を追うと $27 \\to 36 \\to 48 \\to 64 \\to 85.33$。

**線分は 3 倍以上に伸びています。** それでいて、曲線は最初の線分を囲む
狭い範囲から出ていません。

$\\frac{4}{3}$ という比は、
「3 等分した真ん中 1 つを、2 つの辺で置き換える」（3 本 → 4 本）から来ています。
**1 より大きい数を掛け続けるので、深さを上げれば長さはいくらでも大きくなります。**`,
    },
    {
      prompt: `再帰で作った木を 50 本並べたら、フレームレートが 5 fps まで落ちました。
1 本あたり枝は 364 本です。何が起きていますか。対策を 2 つ挙げてください。`,
      hint: '枝 1 本が何になっていますか。',
      answer: `**ドローコールが 18200 回**（$364 \\times 50$）になっています。

枝 1 本を \`Mesh\` 1 つで作ると、GPU への描画命令が枝の数だけ発生します。
1 万回を超えると、GPU の性能に関係なく **CPU 側が命令を出すだけで手一杯**になります。

**対策 1 ― ジオメトリを 1 つにまとめる。**
\`BufferGeometryUtils.mergeGeometries()\` で、木 1 本を 1 つのジオメトリにします。
ドローコールは 50 回になります。**50 本が別々の形でも使えます。**

**対策 2 ― \`InstancedMesh\` を使う。**
枝の形が同じなら（太さと長さを scale で吸収できるなら）、
全部の枝を 1 回の描画でまとめられます。ドローコールは 1 回です。

**どちらを選ぶか** … 木の形がすべて違うなら 1、
同じ木を並べるだけなら 2 が有利です。組み合わせることもできます。

**「作れること」と「並べられること」は別**だと覚えておいてください。`,
    },
    {
      prompt: `完全に規則的なフラクタルで木を作ったら、人工的に見えました。
自然に見せるには、どこに乱数を足しますか。3 つ挙げてください。`,
      hint: '本物の木で、完全に同じになっていない部分はどこですか。',
      answer: `**1. 分かれる角度。**
毎回まったく同じ角度だと、規則性が目に付きます。
$\\pm 30\\%$ ほどばらつかせるだけで、かなり自然になります。

**2. 長さの比。**
「必ず 0.7 倍」ではなく、$0.6 \\sim 0.8$ の範囲でばらつかせます。
枝の長さがそろっていないほうが、生き物らしく見えます。

**3. 枝が出る位置。**
親の枝の先端からだけでなく、**途中からも出す**ようにします。
本物の木は、枝の付け根から先端まで、いろいろな高さから枝が出ています。

**さらに足すなら** … 分かれる本数そのものをばらつかせる（2 本のときと 3 本のときがある）、
先のほうほど本数を減らす、重力で少し下向きに垂れさせる。

**逆に、揺らさないほうがよい場合もあります。**
結晶、建築物、人工的な装飾 ― これらは規則性そのものが持ち味です。
何を作りたいかで決めてください。`,
    },
  ],
  quiz: [
    {
      q: '自己相似とは何ですか。',
      choices: [
        '一部を拡大すると、全体と同じ形が現れる性質',
        '左右対称であること',
        '同じ大きさの形が並ぶこと',
        '色が同じであること',
      ],
      answer: 0,
      explain:
        '木の枝は木全体と、海岸線の一部は海岸線全体と似た形をしています。再帰は、この性質を作る手順そのものです。',
    },
    {
      q: 'コッホ曲線の長さは、深さを 1 増やすと何倍になりますか。',
      choices: ['4/3 倍', '2 倍', '変わらない', '3/4 倍'],
      answer: 0,
      explain:
        '3 等分した真ん中 1 つを 2 つの辺で置き換えるので、3 本が 4 本になります。1 より大きい数を掛け続けるため、深さを上げれば長さはいくらでも大きくなります。それでも囲む範囲は有限のままです。',
    },
    {
      q: '再帰で作った木を自然に見せるには、どうしますか。',
      choices: [
        '角度・長さの比・枝の出る位置に、わずかな乱数を混ぜる',
        '深さをもっと上げる',
        '分かれる数を増やす',
        '色を変える',
      ],
      answer: 0,
      explain:
        '本物の木は完全な自己相似ではありません。ゆらぎを足すと一気に生き物らしくなります。逆に結晶や建築物では、規則性そのものが持ち味になります。',
    },
  ],
};
