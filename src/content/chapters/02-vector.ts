import type { Chapter } from '../types.ts';

export const chapter02: Chapter = {
  slug: '02-vector',
  part: 'basics',
  number: 12,
  title: 'ベクトル ― 位置と向き',
  goal: '同じ 3 つ組を「場所」としても「向きと距離」としても読めるようになり、Three.js のベクトル演算がもとの値を書き換えることを知って使えるようになります。',
  requires: ['01-space', 'b11-distance'],
  threeApis: ['Vector3', 'Vector3.clone', 'Vector3.copy', 'Vector3.set'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

「敵をプレイヤーのほうに歩かせたい」――これができないと、3D は置物の展示で終わります。

必要なのは 2 つです。**どっちに向かえばいいのか**と、**どれだけ進むのか**。
この 2 つをひとまとめにしたものが{{ベクトル}}で、Three.js のほぼすべての計算がこれで動いています。

ここまでの章では 3 つの数字を「位置」として読んできました。
この章では、まったく同じ 3 つの数字を「向きと距離」としても読めることを見ます。
そして次の 5 章で、それを足す・引く・伸ばす・測る・揃える、を 1 つずつ手に入れます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '矢印だと思ってください',
      text: `
ベクトルは矢印です。矢印には「どっちを指しているか」と「どれくらい長いか」しかありません。
どこに置かれているかは問いません。同じ向き・同じ長さの矢印は、画面の右上にあっても左下にあっても
**同じベクトル**です。この「置き場所を問わない」性質が、あとで効いてきます。
`,
    },
    {
      kind: 'md',
      text: `
## 位置ベクトルと方向ベクトル

同じ $(2,\\; 0,\\; 1)$ でも、使い方が 2 通りあります。

- **位置として使う** … 原点から生えた矢印だと思って、その先端を「場所」とみなす
- **向きとして使う** … 矢印の根元は気にせず、「右に 2、手前に 1 のほうへ」という指示とみなす

前者を**位置ベクトル**、後者を**方向ベクトル**と呼び分けることがあります。
ただし Three.js はどちらも \`Vector3\` で表します。**型では区別されません。**

これは不便ではなく、むしろ利点です。「敵の位置」から「プレイヤーへ向かう向き」を作り、
それを足して新しい位置にする、といった行き来が何の変換もなしにできます。
そのかわり、**いま自分がどちらのつもりで持っているのか**は自分で意識する必要があります。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{v} = (x,\\; y,\\; z)',
      readAloud:
        'ベクトル v は 3 つの成分の組で書ける、と言っているだけです。太字にするのが「これはただの数ではなく、向きと大きさを持つもの」という印です。手書きでは上に矢印を書くこともあります。',
      worked: {
        given: '$\\mathbf{v} = (2,\\; 0,\\; 1)$ を、2 通りに読み下します。',
        steps: [
          { calc: '位置として : 原点から右へ 2、高さはそのまま、手前へ 1', note: 'そこに「ある」' },
          { calc: '向きとして : 右へ 2 進むあいだに、手前へ 1 進む向き', note: 'そちらへ「行く」' },
          { calc: 'どちらも長さは √(4 + 0 + 1) = √5 ≈ 2.236', note: '数としては同じもの' },
        ],
        result:
          '**同じ 3 つの数**です。違うのは読み方だけで、three もこの 2 つを区別しません。「原点から生えている」と思えば場所、「どこに置いてもいい」と思えば向きになります。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'Three.js の計算はもとの値を書き換えます',
      text: `
これから 5 章ぶん、ベクトルの計算が続きます。その全部に関わる注意を先に 1 つ。

\`a.add(b)\` は「a と b を足した新しいベクトルを返す」のではなく、**a 自身を書き換えます**。
もとの値を残したいときは \`a.clone().add(b)\` のようにコピーしてから計算してください。
知らずに使うと、位置がじわじわ壊れていく厄介なバグになります。
`,
    },
    {
      kind: 'code',
      title: 'ベクトルを作る・写す・書き換える',
      code: `import * as THREE from 'three';

const v = new THREE.Vector3(2, 0, 1);

v.x;          // 2 ― 成分は素直に読める
v.set(0, 5, 0);  // 3 つまとめて入れ替える

// 位置としても、向きとしても、同じ型で扱う
mesh.position.copy(v);        // 場所として使った
mesh.position.add(v);         // 向きとして使った（v のぶん動かした）

// もとを壊したくないときは、必ずコピーしてから
const moved = mesh.position.clone().add(v);

// copy は「中身を写す」。= で代入すると同じものを 2 つの名前で指すことになる
const alias = mesh.position;        // 危ない：alias を変えると mesh も動く
const snapshot = mesh.position.clone();  // 安全：いまの値を写し取った`,
    },
    {
      kind: 'md',
      text: `
## これから 5 章でやること

ベクトルにできることは、意外と少数です。この 5 つで、3D のほとんどが書けます。

- **足す** … 矢印を継ぎ足す。「いまの位置から、このぶん動く」
- **引く** … 2 点をつなぐ矢印を作る。「ここからあそこへ向かう向き」
- **スカラー倍する** … 向きはそのまま、長さだけ変える
- **長さを測る** … 向きを捨てて「どれだけ」だけを取り出す
- **正規化する** … 長さを捨てて「どっち」だけを取り出す

最後の 2 つが対になっているのが分かるでしょうか。
**ベクトルは「向き」と「大きさ」の 2 つでできていて、その片方ずつを取り出す操作**です。
`,
    },
  ],
  exercises: [
    {
      prompt: `$\\mathbf{v} = (0,\\; 3,\\; -4)$ を、位置として読んだ場合と向きとして読んだ場合で、
それぞれ日本語で説明してください。また、長さを手で計算してください。`,
      hint: '長さは、成分を二乗して足して開きます。3 と 4 の組み合わせです。',
      answer: `**位置として** … 原点から真上に 3、奥に 4 の場所（x は 0 なので左右にはずれていない）

**向きとして** … 上に 3 進むあいだに奥へ 4 進む向き。「斜め上の奥」を指す指示

**長さ** … $\\sqrt{0 + 9 + 16} = \\sqrt{25} = $ **5**

どちらの読み方でも長さは同じ 5 です。位置として読めば「原点からの距離」、
向きとして読めば「そのぶん進んだときの移動量」を表します。`,
    },
    {
      prompt: `つぎのコードを実行したあと、\`mesh.position\` はどうなっていますか。

\`const p = mesh.position;\`
\`p.y = 10;\`

また、\`mesh\` を動かさずに現在位置を控えておきたいときは、どう書くべきですか。`,
      hint: 'JavaScript の `=` はコピーではなく、同じものに別の名前を付ける操作です。',
      answer: `\`mesh.position.y\` も **10 になります**。\`p\` と \`mesh.position\` は同じものを指しているからです。

控えておきたいときは \`clone()\` で写し取ります。

これは Three.js に限った話ではなく、JavaScript でオブジェクトを \`=\` で渡したときに
常に起きることです。ただ 3D では「なぜか物体が動く」という形で症状が出るので、
原因にたどり着きにくくなります。`,
      answerCode: `const alias = mesh.position;         // 別名。書き換えると mesh も動く
const snapshot = mesh.position.clone();  // 写し。書き換えても mesh は動かない`,
    },
    {
      prompt: `「ベクトルは置き場所を問わない」と説明しました。
では、$(1,\\; 0,\\; 0)$ という**方向**ベクトルを、点 $(5,\\; 2,\\; 5)$ に置いた場合と
原点に置いた場合で、何か違いはありますか。`,
      hint: '向きと長さのどちらかが変わるでしょうか。',
      answer: `**違いはありません。** どちらも「右へ 1」を表す同じベクトルです。

矢印を描くときにどこから生やすかは、**人間が読むための都合**にすぎません。
実際、あとの章で「引き算で作った矢印は出発点から生やす」という描き方をしますが、
それは意味が伝わりやすいからで、ベクトルそのものは置き場所を持っていません。

一方、**位置**ベクトルとして読むときだけは根元が原点に固定されます。
「原点からのずれ」で場所を表す約束だからです。`,
    },
  ],
  quiz: [
    {
      q: 'ベクトルが持っている情報はどれですか。',
      choices: [
        '向きと大きさ',
        '向きと置き場所',
        '大きさと色',
        '始点と終点の名前',
      ],
      answer: 0,
      explain:
        'ベクトルは矢印で、持っているのは「どっちを指しているか」と「どれくらい長いか」だけです。どこに置かれているかは持っていません。位置ベクトルとして使うときだけ、根元を原点に固定して読みます。',
    },
    {
      q: '`const c = a.add(b);` を実行したあと、`a` はどうなっていますか。',
      choices: [
        '`a` 自身が `a + b` に書き換わっている',
        '`a` はもとのまま変わらない',
        '`a` は `0` にリセットされる',
        '`a` は `b` と同じ値になる',
      ],
      answer: 0,
      explain:
        'Three.js のベクトル演算は自分自身を書き換えます（`c` と `a` は同じものを指します）。もとの値を残したいときは `a.clone().add(b)` としてください。',
    },
    {
      q: '位置ベクトルと方向ベクトルは、Three.js ではどう区別されますか。',
      choices: [
        '区別されない。どちらも Vector3 で、使う側が意識する',
        'Position 型と Direction 型に分かれている',
        '方向ベクトルは必ず長さが 1 になる',
        '位置は Vector3、方向は Euler で表す',
      ],
      answer: 0,
      explain:
        '型では分かれていません。おかげで「位置から向きを作って、また位置に足す」が変換なしで書けます。そのかわり、いまどちらのつもりで持っているかは自分で把握しておく必要があります。',
    },
  ],
};
