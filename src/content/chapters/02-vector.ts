import type { Chapter } from '../types.ts';

export const chapter02: Chapter = {
  slug: '02-vector',
  part: 'math',
  number: 2,
  title: 'ベクトル ― 位置と向き',
  goal: '「位置」と「向き」を同じ道具で扱えるようになり、足し算・引き算・正規化が何をしているのかを図で説明できるようになります。',
  requires: ['01-space'],
  threeApis: [
    'Vector3.add',
    'Vector3.sub',
    'Vector3.multiplyScalar',
    'Vector3.length',
    'Vector3.normalize',
    'Vector3.distanceTo',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

「敵をプレイヤーのほうに歩かせたい」——これができないと、3D は置物の展示で終わります。

必要なのは 2 つです。**どっちに向かえばいいのか**と、**どれだけ進むのか**。
この 2 つをひとまとめにしたものが{{ベクトル}}で、Three.js のほぼすべての計算がこれで動いています。

前の章では 3 つの数字を「位置」として読みました。この章では、まったく同じ 3 つの数字を
「向きと距離」としても読めることを見ていきます。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '矢印だと思ってください',
      text: `
ベクトルは矢印です。矢印には「どっちを指しているか」と「どれくらい長いか」しかありません。
どこに置かれているかは問いません。同じ向き・同じ長さの矢印は、画面の右上にあっても左下にあっても
**同じベクトル**です。この「置き場所を問わない」性質が、あとで効いてきます。
`,
    },
    {
      kind: 'md',
      text: `
## 位置ベクトルと方向ベクトル

同じ $(2, 0, 1)$ でも、使い方が 2 通りあります。

- **位置として使う** … 原点から生えた矢印だと思って、その先端を「場所」とみなす
- **向きとして使う** … 矢印の根元は気にせず、「右に 2、手前に 1 のほうへ」という指示とみなす

Three.js はどちらも \`Vector3\` で表します。型で区別されないので、いまどちらのつもりで
使っているのかは自分で意識する必要があります。逆に言えば、両者を自由に行き来できるということです。
`,
    },
    {
      kind: 'md',
      text: `
## 足し算 ― 矢印を継ぎ足す

ベクトルの足し算は「1 本目の矢印の先端から、2 本目の矢印を継ぎ足す」ことです。
計算は成分ごとの足し算で、順番を入れ替えても結果は同じになります。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} + \\mathbf{b} = (a_x + b_x,\\; a_y + b_y,\\; a_z + b_z)',
      readAloud:
        'a と b を足すには、x どうし・y どうし・z どうしをそれぞれ足すだけです。太字の a はベクトル、右下の小さな x は「その x 成分」を意味します。',
    },
    {
      kind: 'demo',
      id: 'vector-add',
      caption:
        'a の先端から b が継ぎ足され、その終点に太い矢印が届いているのが見えます。「計算」を引き算に切り替えると、b が逆向きになるだけだと分かります。',
    },
    {
      kind: 'md',
      text: `
## 引き算 ― 「あそこまでの道のり」を作る

引き算は、逆向きにした矢印を足すのと同じです。デモで確かめたとおりですね。

そして 3D で圧倒的によく使うのがこの形です。

- 敵の位置を $\\mathbf{e}$、プレイヤーの位置を $\\mathbf{p}$ とすると、
- $\\mathbf{p} - \\mathbf{e}$ が「敵からプレイヤーへ向かう矢印」になります。

**引く順番が向きを決めます。**「行き先 引く 出発点」と覚えてください。逆にすると真後ろを向きます。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'Three.js の計算はもとの値を書き換えます',
      text: `
\`a.add(b)\` は「a と b を足した新しいベクトルを返す」のではなく、**a 自身を書き換えます**。
もとの値を残したいときは \`a.clone().add(b)\` のようにコピーしてから計算してください。
知らずに使うと、位置がじわじわ壊れていく厄介なバグになります。
`,
    },
    {
      kind: 'md',
      text: `
## スカラー倍 ― 向きはそのまま、長さだけ変える

ベクトルにただの数（{{スカラー}}）を掛けると、向きは変わらず長さだけが変わります。
2 を掛ければ 2 倍の長さ、0.5 なら半分、そして **マイナスを掛けると真逆を向きます**。

デモの「k 倍」スライダーをマイナスにすると、矢印がくるりと反転したはずです。
`,
    },
    {
      kind: 'md',
      text: `
## 長さ ― 矢印がどれだけ長いか

矢印から向きの情報を捨てて、「どれだけ」だけを取り出した値が{{大きさ}}（長さ）です。
2 本の直角な辺から斜辺を出す、あの計算を 3 回ぶんやると思ってください。
`,
    },
    {
      kind: 'formula',
      tex: '\\|\\mathbf{v}\\| = \\sqrt{x^2 + y^2 + z^2}',
      readAloud:
        'v を縦棒 2 本ではさんだ記号は「v の長さ」と読みます。x・y・z をそれぞれ 2 乗して足し、最後に平方根（ルート）を取ると長さになります。',
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '長さくらべなら、ルートは省ける',
      text: `
平方根の計算は他より重い処理です。「どちらが近いか」を比べたいだけなら、
ルートを取る前の値（\`lengthSq()\`）どうしを比べれば十分で、答えは変わりません。
当たり判定などで大量に距離を測るときの定番の手です。
`,
    },
    {
      kind: 'md',
      text: `
## 正規化 ― 「どっち」だけを取り出す

「敵をプレイヤーのほうに、毎秒 3 の速さで歩かせたい」。
このとき $\\mathbf{p} - \\mathbf{e}$ をそのまま足してはいけません。
遠いときは超高速、近いときはのろのろになってしまいます。

欲しいのは向きだけです。そこで長さを 1 に揃えます。これが{{正規化}}で、
できあがったものを{{単位ベクトル}}と呼びます。あとは進みたい速さを掛けるだけです。
`,
    },
    {
      kind: 'formula',
      tex: '\\hat{\\mathbf{v}} = \\dfrac{\\mathbf{v}}{\\|\\mathbf{v}\\|}',
      readAloud:
        'v を、v 自身の長さで割ります。上の帽子（ハット）は「長さ 1 に揃えたもの」という印です。長さ 5 の矢印を 5 で割れば長さ 1 になる、というだけの話です。',
    },
    {
      kind: 'demo',
      id: 'vector-length',
      caption:
        'v をどれだけ長くしても、正規化した矢印の先端は半径 1 の球から出られません。長さ 0 のときだけは向きが決められないことにも注意してください。',
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '長さ 0 は正規化できません',
      text: `
0 で割ることになるためです。Three.js では \`NaN\`（数ではない値）が入り込み、
その物体は画面から消えます。差を取ったベクトルを正規化する前には、
長さが 0 でないかを確かめる癖をつけてください。
`,
    },
    {
      kind: 'code',
      title: '敵をプレイヤーへ向かって歩かせる',
      code: `import * as THREE from 'three';

function stepTowards(enemy, player, speed, dt) {
  // 「行き先 引く 出発点」で、敵からプレイヤーへ向かう矢印を作る
  const toPlayer = player.position.clone().sub(enemy.position);

  const distance = toPlayer.length();
  if (distance < 0.001) return; // 重なっているときは向きが決められない

  // 向きだけを取り出し、進みたい距離を掛ける
  const step = toPlayer.normalize().multiplyScalar(speed * dt);

  enemy.position.add(step);
}

// 距離だけが欲しいときは専用のメソッドがある
const d = enemy.position.distanceTo(player.position);`,
    },
  ],
  quiz: [
    {
      q: '位置 $\\mathbf{a}$ にいる物体から、位置 $\\mathbf{b}$ にいる物体へ**向かう**ベクトルはどれですか。',
      choices: ['$\\mathbf{b} - \\mathbf{a}$', '$\\mathbf{a} - \\mathbf{b}$', '$\\mathbf{a} + \\mathbf{b}$', '$\\mathbf{a} \\times \\mathbf{b}$'],
      answer: 0,
      explain:
        '「行き先 引く 出発点」です。$\\mathbf{a} - \\mathbf{b}$ にすると、b から a へ向かう真逆のベクトルになります。',
    },
    {
      q: '長さ 6 のベクトルを正規化すると、長さはいくつになりますか。',
      choices: ['1', '6', '0', 'もとの長さによる'],
      answer: 0,
      explain:
        '正規化は「自分の長さで割る」ことなので、もとが何であっても結果の長さは必ず 1 になります。向きは変わりません。',
    },
    {
      q: '`const c = a.add(b);` を実行したあと、`a` はどうなっていますか。',
      choices: [
        '`a` 自身が `a + b` に書き換わっている',
        '`a` はもとのまま変わらない',
        '`a` は `0` にリセットされる',
        '`a` は `b` と同じ値になる',
      ],
      answer: 0,
      explain:
        'Three.js のベクトル演算は自分自身を書き換えます（`c` と `a` は同じものを指します）。もとの値を残したいときは `a.clone().add(b)` としてください。',
    },
  ],
};
