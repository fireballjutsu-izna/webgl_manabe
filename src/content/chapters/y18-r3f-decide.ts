import type { Chapter } from '../types.ts';

export const chapterY18: Chapter = {
  slug: 'y18-r3f-decide',
  part: 'polish',
  number: 18,
  title: '入れるか、入れないか ― 状態と 3 次元をつなぐ',
  goal: '$R3F$ が本当に効く場面を $1$ つに絞って言えるようになり、自分の作品に入れるべきかを $3$ つの問いで決められるようになります。',
  requires: ['y17-r3f-map'],
  threeApis: ['Mesh', 'InstancedMesh', 'Material'],
  mathRecall: [
    { slug: 'y17-r3f-map', note: '読み替えの規則。ここでは使い所の判断をします' },
    { slug: 'w44-gpu-cost', note: '毎フレーム走る仕事の値段' },
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## R3F が本当に効く場面

$1$ つだけです。

**$React$ の状態と $3$ 次元をつなぐとき。**

たとえば「サイドバーで選んだ色が、$3$ 次元の物体に反映される」を素の three で書くと、
$DOM$ のイベントを購読して、対応する \`Mesh\` を探して、\`material.color.set()\` を呼ぶ、
という**橋渡しのコードを自分で書き、しかも同期を保ち続ける**ことになります。

物が増えたり減ったりするなら、さらに面倒です。
**「いま何個あるか」を数えて、差分だけ作り、余ったぶんを \`dispose\` する** ―
これを手で書くのは、地味に骨が折れます。

$R3F$ なら、色や個数を持っているのは $React$ の状態ひとつだけで、
$3$ 次元側はそれを読むだけになります。
`,
    },
    {
      kind: 'code',
      title: 'UI の状態が、そのまま 3 次元になる',
      code: `function Scene() {
  const [color, setColor] = useState('#4fd6ff');
  const [count, setCount] = useState(3);

  return (
    <>
      {/* ふつうの HTML の UI */}
      <input type="color" value={color} onChange={(e) => setColor(e.target.value)} />
      <input type="range" min={1} max={20} value={count}
             onChange={(e) => setCount(Number(e.target.value))} />

      <Canvas>
        <directionalLight position={[3, 4, 5]} intensity={2.5} />
        {/* 数が変われば、増えた分の Mesh が作られ、減った分は解放される */}
        {Array.from({ length: count }, (_, i) => (
          <mesh key={i} position={[(i - (count - 1) / 2) * 1.4, 0, 0]}>
            <boxGeometry />
            <meshStandardMaterial color={color} />
          </mesh>
        ))}
      </Canvas>
    </>
  );
}`,
    },
    {
      kind: 'md',
      text: `
\`count\` を変えるだけで、\`Mesh\` の生成も破棄も起きます。
**差分を取るコードが $1$ 行も要りません。**

もう $1$ つの利点が**エコシステム**です。\`@react-three/drei\` には、
$\\mathrm{OrbitControls}$・環境マップ・テキスト・影・読み込み表示といった定型が揃っていて、
第4部で自分で組んだものの多くが $1$ 行で置き換わります。

ただし ― **上のコードには、[](#/ch/y17-r3f-map)で見た落とし穴が仕込んであります。**
\`count\` が $20$ なら、\`<meshStandardMaterial>\` も \`<boxGeometry>\` も $20$ 個できます。
$20$ 個くらいなら気になりませんが、**$1000$ 個で同じことをすると効いてきます。**
`,
    },
    {
      kind: 'md',
      text: `
## useFrame の中では、素の three を書く

$R3F$ を入れても、**毎フレームの動きは $React$ の外**でやります。

- **やってはいけない** … \`useFrame((state, dt) => setX(x + dt))\`
- **正しい** … \`useFrame((state, dt) => { ref.current.position.x += dt; })\`

上を書くと、**毎秒 $60$ 回、$React$ の再描画が走ります。**
木をたどって差分を取る仕事が、$1$ 秒に $60$ 回。

$3$ 次元の中だけで完結する動きは、$React$ に知らせる必要がありません。
\`ref\` を掴んで、**これまでとまったく同じように書きます。**

**目安**

- **状態にするのは、$UI$ に出す値だけ**（色、個数、選択中の物体、再生中かどうか）
- **位置・回転・スケールのように毎フレーム変わるものは、状態にしない**

つまり $R3F$ を入れても、**書くコードの中心は素の three のまま**です。
`,
    },
    {
      kind: 'md',
      text: `
## 効かない場面 ― 入れないほうがよいとき

$4$ つあります。

- **$React$ を使っていない。**
  $R3F$ は three を楽にする道具ではなく、**$React$ と three をつなぐ**道具です。
  $3$ 次元のために $React$ を持ち込むのは、順序が逆です
- **$UI$ がほとんど無い作品。** [](#/ch/x19-labels-finish)の惑星ビューアーや
  [](#/ch/x39-city-finish)のローポリの街のような。
  同期させる状態が無いので、得られるのは \`dispose\` の自動化くらいです
- **$1$ フレームに数千回、細かく物を触るもの。**
  上で見たとおり、そこは素の three を書きます。$R3F$ の取り分がありません
- **three の仕組みそのものを学んでいる最中。**
  \`<Canvas>\` は便利ですが、**便利さは中で何が起きているかを隠します**

最後の $1$ 点が、この $3$ 章を第5部の終わり近くに置いた理由です。
**先に $R3F$ から入ると、「何も映らない」ときに見るところが分かりません。**
シーンに \`add\` されているか、光はあるか、カメラは手前か ―
[逆引き](#/help)に並べたあの順番は、$R3F$ でもそのまま必要になります。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '自動車の運転と、整備',
      text: `
$R3F$ は「オートマ車」です。楽で、速く、日常の用途には明らかに向いています。

ただし、坂道で止まったときに何が起きているのかは、
**変速機がどう動いているかを知っている人にしか分かりません。**

このサイトが $173$ 章かけてやってきたのは、その中身のほうです。

だから順番はこれでよいのです。**まず中身、それから道具。** 逆はできません。

そして、**中身を知っている人にとっては、$R3F$ は本当に楽**です。
$265$ 行ぶんの定型を書かずに済むのですから。
`,
    },
    {
      kind: 'md',
      text: `
## 3 つの問いで決める

入れるかどうかは、これで決まります。

- **すでに $React$ を使っているか。**
  使っていないなら、$React$ ごと持ち込む理由がまず無い
- **$UI$ の状態と $3$ 次元を同期させる必要があるか。**
  無いなら、得るのは \`dispose\` の自動化くらい
- **中で何が起きているか説明できるか。**
  できないなら、詰まったときに調べる先が二重になる

$3$ つとも「はい」なら、入れる価値があります。
**$1$ つでも「いいえ」なら、素の three のままで困りません。**

この判断ができるようになったことが、この $3$ 章の成果です。
**道具を知ることと、道具を使うことは別**で、
「知っていて、使わないと決めた」は立派な結論です。
`,
    },
    {
      kind: 'md',
      text: `
## この 3 章で分かったこと

- $R3F$ は**新しい $3$ 次元の仕組みではない。** 中で動いているのは、これまでと同じ three
- 短くなるのは**定型**であって、$3$ 次元の記述ではない（$32$ 行 $\\to$ $22$ 行、$31\\%$）
- タグ名は three のクラス名を小文字始まりに。引数は \`args\`、値が変われば**作り直し**
- **入れ子が親子とは限らない。** \`Object3D\` を継承しているものだけが子になる
- \`useFrame\` の中身は、これまで書いてきた描画ループの中身そのまま
- **効くのは、$React$ の状態と $3$ 次元を同期させたいとき。** それが無いなら、入れる理由も無い
`,
    },
  ],
  exercises: [
    {
      prompt: `\`useFrame\` の中で \`setPosition(x + dt)\` と書いてあるコードを見つけました。

何が問題で、どう直しますか。`,
      hint: '状態を変えると、$React$ はその部分を作り直します。それが毎秒 $60$ 回起きると。',
      answer: `**毎フレーム $React$ の再描画が走ります。**

**何が起きるか**

$60\\ \\mathrm{fps}$ なら**毎秒 $60$ 回**、$React$ が木をたどって差分を取ります。

$3$ 次元の描画とは別に、その仕事が $CPU$ に乗ります。

**知らせる必要が無い**

位置は $UI$ に出していません。$React$ が知る必要のない値です。

\`ref\` を掴んで、**素の three と同じように直接書き換えます。**

**書き直し**

- **遅い** … \`useFrame((state, dt) => setX(x + dt));\`
- **速い** … \`useFrame((state, dt) => { ref.current.position.x += dt; });\`

下は[](#/ch/t06-loop-clock)で書いたコードと**まったく同じ**です。

**目安**

**状態にするのは、$UI$ に出す値だけ。**

- 状態にしてよい … 色、個数、選択中の物体、再生中かどうか
- 状態にしない … 位置、回転、スケール、経過時間

**つまり**

$R3F$ を入れても、**毎フレームのコードは素の three のまま**です。
$R3F$ が引き受けるのは、その外側の組み立てだけです。`,
      answerCode: `// 遅い: 毎フレーム React が作り直す
useFrame((state, dt) => setX(x + dt));

// 速い: three のオブジェクトを直接触る（これまでと同じ書き方）
useFrame((state, dt) => {
  ref.current.position.x += dt;
});`,
    },
    {
      prompt: `[](#/ch/x39-city-finish)のローポリの街を $R3F$ で書き直すと、何が良くなりますか。

**何が良くならない**でしょうか。`,
      hint: 'あの作品に、$UI$ の状態はいくつありますか。',
      answer: `**良くなるのは \`dispose\` の自動化だけ。ほとんど何も良くなりません。**

**あの作品にある状態**

時刻のスライダーくらいです。それも $3$ 次元の中の値で、$UI$ と同期する必要は薄い。

**$3$ つの問いに当てると**

- **$React$ を使っているか** … いいえ
- **$UI$ の状態と同期するか** … ほとんど無い
- **中身を説明できるか** … はい

**$1$ つめと $2$ つめが「いいえ」です。** 入れる理由がありません。

**良くならないもの**

- **描画の速さ** … 同じ three が動くので、まったく同じ
- **建物の生成** … シード付き乱数も分割も、そのまま自分で書きます
- **インスタンシング** … [](#/ch/x37-car-instancing)の \`setMatrixAt\` は
  \`useFrame\` の中で**そのまま**書きます
- **街の見た目** … $1$ ミリも変わりません

**むしろ悪くなりうるもの**

[](#/ch/y17-r3f-map)で見たマテリアルの共有です。

$JSX$ で素直に書くと、共有していたマテリアルが**建物の数だけ**できます。
気をつけて書けば防げますが、**気をつけないと防げない**のは、素の three より不利です。

**結論**

**$UI$ の無い作品に $R3F$ を入れる理由は、ほぼありません。**`,
    },
    {
      prompt: `「$R3F$ を勉強したから、次の作品では $R3F$ で書こう」と考えました。

この判断のどこが危ういですか。`,
      hint: '道具を選ぶ理由として、それは妥当ですか。',
      answer: `**道具ではなく、道具を覚えたことが理由になっています。**

**判断の順序が逆**

正しい順序は、

- $1$. **作るものを決める**
- $2$. **その作りに合う道具を選ぶ**

「覚えたから使う」は、$2$ を飛ばして道具から入っています。

**$3$ つの問いに戻る**

- $React$ を使っているか
- $UI$ の状態と $3$ 次元を同期させるか
- 中で何が起きているか説明できるか

**$3$ つめだけが「はい」の状態**で入れると、

- $React$ の依存が増える
- $JSX$ の変換工程が要る
- バンドルが数百 $KB$ 増える
- そして**得るものが \`dispose\` の自動化だけ**

**とはいえ、練習は別**

「$R3F$ を使ってみるための小さな作品を作る」なら、まったく正しい判断です。

**目的が「$R3F$ を試すこと」だと自覚していれば**問題ありません。

危ういのは、**道具の選択を、学習の記録で正当化してしまう**ときです。

**このサイト自身がそうしています**

$R3F$ を $3$ 章ぶん書いておきながら、**使っていません。**
$3$ つの問いのうち $2$ つが「いいえ」だからです。

**「知っていて、使わないと決めた」は立派な結論**です。`,
    },
  ],
  quiz: [
    {
      q: 'R3F を入れる理由として、いちばん強いものはどれですか。',
      choices: [
        'React の状態と 3 次元を同期させたいとき',
        'three より速く動くから',
        'three より少ない数学で書けるから',
        'シェーダを書かなくてよくなるから',
      ],
      answer: 0,
      explain:
        '速さも数学もシェーダも、R3F では何も変わりません。中で動いているのは同じ three です。変わるのは「React の世界と 3 次元の世界をつなぐ手間」で、そこに用が無いなら入れる理由もありません。R3F は three を楽にする道具ではなく、React と three をつなぐ道具です。',
    },
    {
      q: '`useFrame` の中で物体の位置を毎フレーム動かしたい。正しいのはどれですか。',
      choices: [
        '`ref.current.position.x += dt` のように、three のオブジェクトを直接触る',
        '`setPosition(x + dt)` で React の状態を更新する',
        '`<mesh position={...}>` の値を毎フレーム書き換える',
        '`useEffect` の中で動かす',
      ],
      answer: 0,
      explain:
        '毎フレーム変わる値を React の状態にすると、毎秒 60 回の再描画が起きます。位置は UI に出していないので、React が知る必要のない値です。ref を掴んで直接書き換える ― つまり、これまで書いてきたコードとまったく同じになります。状態にするのは UI に出す値だけ、が目安です。',
    },
    {
      q: 'UI がほとんど無い作品に R3F を入れると、何が良くなりますか。',
      choices: [
        'dispose の自動化くらい。描画も見た目も速さも変わらない',
        '描画が速くなる',
        'インスタンシングが自動になる',
        'シーンの構築が自動になる',
      ],
      answer: 0,
      explain:
        '同じ three が動くので、速さも見た目も変わりません。むしろ JSX で素直に書くとマテリアルの共有が消え、まとめて描けなくなる危険があります。3 つの問い ― React を使っているか、UI の状態と同期するか、中身を説明できるか ― のうち 1 つでも「いいえ」なら、素の three のままで困りません。',
    },
  ],
};
