import type { Chapter } from '../types.ts';

export const chapterB11: Chapter = {
  slug: 'b11-distance',
  part: 'basics',
  number: 11,
  title: '3D の距離と、二乗のままで比べる',
  goal: '3 つ組の距離を計算できるようになり、「どちらが近いか」を知りたいだけのときに平方根を省く定石が使えるようになります。',
  requires: ['b10-pythagoras', '01-space'],
  threeApis: [
    'Vector3.length',
    'Vector3.lengthSq',
    'Vector3.distanceTo',
    'Vector3.distanceToSquared',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

3D 空間と座標系の章で、デモの読み出しに「原点からの距離」がありました。
$(3,\\; 0,\\; 3)$ のとき 4.24 と出ていたはずです。あの数字を、この章で自分で出せるようにします。

そしてもう 1 つ、実務でいちばん効く小技を持ち帰ります。
**「どちらが近いか」を知りたいだけなら、平方根は要りません。**
これを知っているかどうかで、当たり判定や「いちばん近い物を探す」処理の速さが変わります。
`,
    },
    {
      kind: 'md',
      text: `
## 2 回使えば、3D になる

平面のときは $\\sqrt{x^2 + y^2}$ でした。奥行きが増えても、やることは同じです。

$(3,\\; 0,\\; 3)$ の距離を、2 段階で出してみます。

1. **まず床の上（x と z）だけ見る。** 高さを忘れて、真上から見下ろした影の長さを出す。
   $\\sqrt{3^2 + 3^2} = \\sqrt{18} \\approx 4.243$
2. **つぎに、その影と高さで直角三角形を作る。** 影が横、高さ y が縦。
   $y = 0$ なので、答えは影の長さのまま 4.243

高さが 0 でない場合も同じです。**ピタゴラスの定理を 2 回使うだけ**で、
それを 1 つの式にまとめると、根号の中に項が 1 つ増えた形になります。

これが「$\\sqrt{\\phantom{x}}$ の中に、成分の二乗を全部並べて足す」形の正体です。
4 つ組でも 10 個組でも、同じやり方で伸びていきます。
`,
    },
    {
      kind: 'formula',
      tex: '\\|P\\| = \\sqrt{x^2 + y^2 + z^2}',
      readAloud:
        '原点から点 P までの距離は、3 つの成分をそれぞれ二乗して足し、平方根を取ったもの、と言っています。二重の縦棒は「長さ」を表す記号で、絶対値の記号を数の組に広げたものです。',
      worked: {
        given: '$P = (3,\\; 0,\\; 3)$ の、原点からの距離を求めます。',
        steps: [
          { calc: 'x² = 3 × 3 = 9' },
          { calc: 'y² = 0 × 0 = 0', note: '高さが無いので効かない' },
          { calc: 'z² = 3 × 3 = 9' },
          { calc: '9 + 0 + 9 = 18' },
          { calc: '√18 ≈ 4.243' },
        ],
        result:
          '**約 4.24**。デモに出ていた数字と一致しました。道のりの $3 + 3 = 6$ より短く、長いほうの成分 3 より長い ― 斜めに突っ切ったぶん得をしています。',
      },
    },
    {
      kind: 'demo',
      id: 'axes-explorer',
      caption:
        'こんどは「原点からの距離」の読み出しに注目してください。x を 3、y を 0、z を 3 にすると 4.24 になります。x だけを大きくしていくと距離もそのまま増えますが、2 つの成分を同時に増やすと、増え方が足し算より緩やかになることが見えます。',
    },
    {
      kind: 'md',
      text: `
## 2 点のあいだも、引いてから測る

平面のときとまったく同じです。**差を取ってから、長さを測る。**

点 $A = (1,\\; 2,\\; 3)$ と点 $B = (4,\\; 6,\\; 3)$ なら、

- 差は $(4 - 1,\\; 6 - 2,\\; 3 - 3) = (3,\\; 4,\\; 0)$
- その長さは $\\sqrt{9 + 16 + 0} = 5$

「引いてから測る」の**引く**にあたる部分が、次の章のベクトルの引き算です。
つまりベクトルは、ここでやった手順に名前を付けて道具にしたもの、と言えます。
`,
    },
    {
      kind: 'md',
      text: `
## 二乗のままで比べる ― 実務でいちばん効く小技

「敵が半径 5 の中に入ったか」を判定したいとします。素直に書けばこうです。

- 距離を出す（**平方根を計算する**）
- 5 と比べる

ところが平方根は、掛け算や足し算に比べてずいぶん重い計算です。
そして**比べるだけなら、平方根は要りません。**

距離が大きいほうは、距離の二乗も必ず大きいからです（どちらも 0 以上なので、
大小関係がひっくり返りません）。だから

- 距離の二乗を出す（**平方根を計算しない**）
- $5^2 = 25$ と比べる

で同じ答えが出ます。物体が数百個あって、毎フレーム全部を調べるような場面では、
この差がそのまま体感の速さになります。
`,
    },
    {
      kind: 'formula',
      tex: '\\|P\\|^2 = x^2 + y^2 + z^2',
      readAloud:
        '距離の二乗は、成分の二乗を足しただけのもの、と言っています。平方根を取る手前で止めた形です。長さそのものではないので、表示に使ってはいけません。',
      worked: {
        given: '$A = (3,\\; 0,\\; 4)$ と $B = (0,\\; 5,\\; 1)$ の、どちらが原点に近いかを判定します。',
        steps: [
          { calc: 'A: 3² + 0² + 4² =  9 + 0 + 16 = 25' },
          { calc: 'B: 0² + 5² + 1² =  0 + 25 + 1 = 26' },
          { calc: '25 < 26 → A のほうが近い', note: '√ を一度も計算していない' },
        ],
        result:
          '**A のほうが近い。** 実際の距離は $\\sqrt{25} = 5$ と $\\sqrt{26} \\approx 5.099$ ですが、**どちらが近いかを知るだけなら開く必要がありません。** 距離そのものを画面に出すときだけ開いてください。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '二乗のままの値を、距離として表示しない',
      text: `
\`lengthSq()\` が返すのは**長さの二乗**であって、長さではありません。
これをそのまま「距離 25 m」と表示すると、実際は 5 m です。
**比べるためだけに使い、人に見せる数には使わない。** ここを混ぜると発見しにくいバグになります。
`,
    },
    {
      kind: 'code',
      title: '距離を扱う 4 つのメソッド',
      code: `import * as THREE from 'three';

const p = new THREE.Vector3(3, 0, 3);

p.length();     // 4.2426... ← 原点からの距離
p.lengthSq();   // 18        ← その二乗（開いていない）

const a = new THREE.Vector3(1, 2, 3);
const b = new THREE.Vector3(4, 6, 3);

a.distanceTo(b);         // 5   ← 2 点の距離
a.distanceToSquared(b);  // 25  ← その二乗

// 半径 5 の中に入ったかの判定は、こう書くのが定石
const RADIUS = 5;
if (a.distanceToSquared(b) < RADIUS * RADIUS) {
  // 当たっている
}

// 表示するときだけ開く
label.textContent = a.distanceTo(b).toFixed(2) + ' m';`,
    },
    {
      kind: 'md',
      text: `
## ここまでが「数と形の土台」の前半

ここまでで、次のことができるようになりました。

- 符号を「向き」として読める
- 数式の文字を読み下せる
- 関数とグラフを行き来できる
- 二乗と平方根を往復できる
- 割合（0〜1）で物事を言える
- 小数の誤差を避けて比較できる
- 平面と空間の座標を読める
- 長さと距離を計算できる

**次の章から、これらを 1 つの道具にまとめます。**
「3 つ組をまとめて 1 つのもの（矢印）として扱う」 ― それがベクトルです。
ここまでで手でやってきた「引いてから測る」が、そのまま 1 行になります。
`,
    },
  ],
  exercises: [
    {
      prompt: `原点からの距離を、手で計算してください。1 と 2 は整数になります。

1. $(2,\\; 3,\\; 6)$
2. $(0,\\; -4,\\; 3)$
3. $(1,\\; 1,\\; 1)$（小数で、小数第 3 位まで）`,
      hint: '2 は、マイナスも二乗すればプラスです。3 は $\\sqrt{3}$ になります。',
      answer: `1. $\\sqrt{4 + 9 + 36} = \\sqrt{49} = $ **7**
2. $\\sqrt{0 + 16 + 9} = \\sqrt{25} = $ **5**。符号は二乗で消えました
3. $\\sqrt{1 + 1 + 1} = \\sqrt{3} \\approx $ **1.732**

3 は「1 辺 1 の立方体の、対角線の長さ」です。
平面の対角線が約 1.414 だったのに対し、立体では約 1.732。
**次元が 1 つ増えるごとに、斜めの得が少しずつ大きくなります。**`,
    },
    {
      prompt: `点 $A = (2,\\; 1,\\; 2)$ と点 $B = (5,\\; 1,\\; 6)$ の距離を求めてください。
そのあと、**平方根を取らずに** A と B のどちらが原点に近いかを判定してください。`,
      hint: '距離は差を取ってから。近さの比較は二乗のまま比べます。',
      answer: `**距離** … 差は $(3,\\; 0,\\; 4)$ なので $\\sqrt{9 + 0 + 16} = \\sqrt{25} = $ **5**

**どちらが原点に近いか**

- A の距離の二乗 … $4 + 1 + 4 = 9$
- B の距離の二乗 … $25 + 1 + 36 = 62$
- $9 < 62$ なので **A のほうが近い**

実際の距離は 3 と約 7.87 ですが、それを出さずに答えが決まりました。
「近い順に並べる」「いちばん近いものを探す」も、この二乗のままの値で並べ替えれば済みます。`,
    },
    {
      prompt: `つぎの 2 つのコードは、どちらも「半径 3 の中にあるか」を判定しています。
**どちらが速いか**と、**どちらか一方だけが持っている落とし穴**を答えてください。

- コードA … \`if (p.length() < 3)\`
- コードB … \`if (p.lengthSq() < 9)\``,
      hint: '平方根の計算はどちらに入っていますか。そして 9 という数字はどこから来たものでしたか。',
      answer: `**速いのは B** です。A は平方根を計算しますが、B は掛け算と足し算だけで済みます。

**落とし穴は B のほう**にあります。比べる相手を**二乗し忘れる**と、
\`p.lengthSq() < 3\` と書いてしまい、実際には半径 $\\sqrt{3} \\approx 1.73$ の判定になります。
半径がほぼ半分になるので、「当たり判定がやたら小さい」という形で表に出ます。

対策は、半径を変数に取って \`RADIUS * RADIUS\` と書くことです。
数字の 9 を直接書くと、半径を変えたときに直し忘れます。`,
      answerCode: `// 危ない（9 の出どころが読めない）
if (p.lengthSq() < 9) { /* ... */ }

// 安全（半径を変えても壊れない）
const RADIUS = 3;
if (p.lengthSq() < RADIUS * RADIUS) { /* ... */ }`,
    },
  ],
  quiz: [
    {
      q: '$(1,\\; 2,\\; 2)$ の原点からの距離はいくつですか。',
      choices: ['3', '5', '4.24', '2.24'],
      answer: 0,
      explain:
        '$1 + 4 + 4 = 9$ で、$\\sqrt{9} = 3$ です。1・2・2・3 は答えが整数になる組で、確かめ算に便利です。',
    },
    {
      q: '`lengthSq()` を使う主な理由はどれですか。',
      choices: [
        '平方根の計算を省けるので、大小を比べるだけなら速い',
        '負の値も扱えるようになるから',
        '結果が必ず整数になるから',
        '距離をメートル単位で返してくれるから',
      ],
      answer: 0,
      explain:
        '距離が大きいほうは距離の二乗も大きいので、比べるだけなら開く必要がありません。ただし返る値は長さそのものではないので、表示には使わないでください。',
    },
    {
      q: '`if (p.lengthSq() < 5)` と書いたとき、実際には半径いくつの判定になっていますか。',
      choices: [
        '$\\sqrt{5}$（約 2.24）',
        '5',
        '25',
        '2.5',
      ],
      answer: 0,
      explain:
        '左辺が二乗の値なので、右辺も二乗した値でないと釣り合いません。半径 5 で判定したいなら `< 25` と書きます。「当たり判定が思ったより小さい」ときの典型的な原因です。',
    },
  ],
};
