import type { Chapter } from '../types.ts';

export const chapterM13: Chapter = {
  slug: 'm13-quaternion',
  part: 'math3d',
  number: 13,
  title: 'クォータニオン ― 4 つの数で姿勢を持つ',
  goal: 'クォータニオンが「軸と角度の詰め直し」だと分かり、中身を読まずに正しく使えるようになります。',
  requires: ['m12-axis-angle', 'b16-vector-length'],
  threeApis: [
    'Quaternion',
    'Quaternion.setFromAxisAngle',
    'Quaternion.multiply',
    'Object3D.quaternion',
    'Quaternion.normalize',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

軸と角度で姿勢を持てるようになりました。ジンバルロックも起きません。
**では、なぜさらに別の表し方が必要なのでしょう。**

困るのは**回転を重ねるとき**です。

「軸 A に 30 度」のあとに「軸 B に 45 度」を重ねたい。
結果もまた 1 本の軸と 1 つの角度で書けるはずですが、
その軸と角度を求める計算は、まったく素直ではありません。
毎フレームこれをやるのは現実的でない。

**{{クォータニオン}}は、この一点を解決するために形を変えたものです。**
`,
    },
    {
      kind: 'md',
      text: `
## 中身は、軸と角度そのまま

新しい概念ではありません。**前の章の「軸と角度」を、掛け算しやすい形に並べ替えただけ**です。

4 つの数のうち、

- **はじめの 3 つ** … 回転軸に、$\\sin\\frac{\\theta}{2}$ を掛けたもの
- **4 つ目**（$w$ と呼びます） … $\\cos\\frac{\\theta}{2}$

角度が**半分**になっているのが唯一の見慣れない点です。
これは「2 回掛けると 1 回ぶん回る」という構造から出てくるもので、
気持ち悪いなら**そういう入れ物だと思ってかまいません**。中身を読む機会は来ません。
`,
    },
    {
      kind: 'formula',
      tex: 'q = \\left(\\;\\hat{a}\\sin\\tfrac{\\theta}{2},\\;\\; \\cos\\tfrac{\\theta}{2}\\;\\right)',
      readAloud:
        '回転軸 a と回転角 θ から作られる、4 つの数の組です。はじめの 3 つは軸に「半分の角度のサイン」を掛けたもの、4 つ目は「半分の角度のコサイン」です。この式を自分で書くことはまずありません。軸と角度から作れることだけ押さえてください。',
      worked: {
        given:
          'y 軸 $\\hat{a} = (0,\\; 1,\\; 0)$ のまわりに $\\theta = 90$ 度回すクォータニオンを作ります。',
        steps: [
          { calc: 'まず角度を半分にする : 90 / 2 = 45 度' },
          { calc: 'sin 45度 = 0.7071 / cos 45度 = 0.7071' },
          { calc: 'x = 0 x 0.7071 = 0', note: '軸の各成分に sin(θ/2) を掛ける' },
          { calc: 'y = 1 x 0.7071 = 0.7071' },
          { calc: 'z = 0 x 0.7071 = 0' },
          { calc: 'w =     cos(θ/2) = 0.7071', note: '4 つ目だけは cos' },
          { calc: '長さ : √(0.7071² + 0.7071²) = √(0.5+0.5) = 1', note: '必ず 1 になる' },
        ],
        result:
          '$q = (0,\\; 0.7071,\\; 0,\\; 0.7071)$。**90 度回したいのに 45 度が出てくる**のが見慣れないところですが、覚える必要はありません。`setFromAxisAngle` が代わりにやってくれます。長さが 1 になるところだけ、次の節で効いてきます。',
      },
    },
    {
      kind: 'md',
      text: `
## 長さは、必ず 1

いま計算で出たとおり、**正しいクォータニオンの長さは必ず 1** です。
[](#/ch/b16-vector-length)の長さの計算を、4 つの成分に広げただけです。

$\\sin^2 + \\cos^2 = 1$ が効いているので、軸が長さ 1 でありさえすれば自動的にそうなります。

これは実用上とても重要です。**長さが 1 から外れたクォータニオンは、回転に拡大が混ざったもの**になり、
物体が伸び縮みしてしまいます。掛け算を繰り返すと誤差で少しずつずれていくので、
**定期的に正規化する**のが定石です。

これも[](#/ch/b17-normalize)と同じ操作で、three では \`normalize()\` の 1 行です。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '成分を直接書き換えないこと',
      text: `
q.x = 0.5 のように成分を直接いじると、長さが 1 でなくなり、姿勢が壊れます。
オイラー角と違って、4 つの数に個別の意味はありません。

クォータニオンに触るのは、次の 3 つの操作を通してだけにしてください。

軸と角度から作る（setFromAxisAngle）、掛けて重ねる（multiply）、
2 つのあいだを繋ぐ（slerp）。この 3 つで実務は足ります。
`,
    },
    {
      kind: 'md',
      text: `
## 重ねるのは、掛け算

冒頭の困りごとに戻ります。**回転を重ねるのは掛け算 1 回**です。

$q_2 q_1$ と書けば「$q_1$ で回してから $q_2$ で回した」姿勢になります。
[](#/ch/m05-matrix-order)の行列と同じで、**右から効きます**。順番も同じ規則です。

行列（16 個の数）に比べて 4 つで済むので、掛け算も軽い。
毎フレーム姿勢を積み上げていく処理には、これが向いています。

そして**掛けても長さは 1 のまま**なので、結果もまた正しい回転になります。
「回転を重ねたら回転になる」という当たり前のことが、構造として保証されているわけです。
`,
    },
    {
      kind: 'code',
      title: 'クォータニオンを使う',
      code: `import * as THREE from 'three';

// 軸と角度から作る（これが基本の入口）
const axis = new THREE.Vector3(0, 1, 0);
const q = new THREE.Quaternion().setFromAxisAngle(axis, Math.PI / 2);

// 物体に適用する
mesh.quaternion.copy(q);

// 回転を重ねる。右から効くので「いまの姿勢のあとに spin」
const spin = new THREE.Quaternion().setFromAxisAngle(axis, 0.01);
mesh.quaternion.multiply(spin);      // ローカル軸まわりに回す
mesh.quaternion.premultiply(spin);   // ワールド軸まわりに回す

// 逆回転は conjugate（長さ 1 なら逆行列と同じ）
const back = q.clone().conjugate();

// 誤差がたまるので、ときどき長さを 1 に戻す
mesh.quaternion.normalize();

// rotation と quaternion は同じ姿勢の 2 つの見方。混ぜても壊れない
console.log(mesh.rotation.y);`,
    },
    {
      kind: 'md',
      text: `
## 同じ姿勢を表す書き方が 2 つある

[](#/ch/m12-axis-angle)の演習で見た重複が、ここに形を変えて現れます。

**$q$ と $-q$（全成分の符号を反転したもの）は、まったく同じ姿勢を表します。**

軸を裏返して角度も裏返した、あの書き方に対応します。
姿勢として区別が付かないので、ふだんは気にしなくてかまいません。

ただし**次の章の補間では、これが実害になります。**
「同じ場所へ行くのに、遠回りの道と近道の 2 通りがある」という形で出てくるからです。
そこで初めて意識することになります。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '使い分けの結論',
      text: `
オイラー角 … 人が読み書きするところ。固定の向き、1 軸だけの回転、エディタの UI。

クォータニオン … プログラムが持ち回るところ。姿勢の保存、回転の積み上げ、補間。

Three.js では rotation と quaternion が自動で同期されるので、
「表示と入力はオイラー角、内部の計算はクォータニオン」と役割で分けるのが自然です。
どちらか一方に統一する必要はありません。
`,
    },
  ],
  exercises: [
    {
      prompt: `x 軸 $(1,\\; 0,\\; 0)$ のまわりに 180 度回すクォータニオンを、手で計算してください。`,
      hint: '角度を半分にしてから、$\\sin$ と $\\cos$ を出します。',
      answer: `半分にすると 90 度。$\\sin 90^\\circ = 1$、$\\cos 90^\\circ = 0$ です。

- $x = 1 \\times 1 = 1$
- $y = 0 \\times 1 = 0$
- $z = 0 \\times 1 = 0$
- $w = \\cos 90^\\circ = 0$

答えは **$q = (1,\\; 0,\\; 0,\\; 0)$** です。

長さは $\\sqrt{1^2} = 1$ で、ちゃんと 1 になっています。

**$w = 0$ が「半回転」の印**です。逆に $\\theta = 0$ のときは
$q = (0,\\; 0,\\; 0,\\; 1)$ ― これが「何もしない姿勢」で、
行列でいう単位行列にあたります（[](#/ch/m02-matrix-vector)）。`,
    },
    {
      prompt: `毎フレーム \`mesh.quaternion.multiply(spin)\` で回し続けたら、
数分後に物体がわずかに伸びていました。原因と対策を答えてください。`,
      hint: 'クォータニオンの長さは、いくつでなければなりませんでしたか。',
      answer: `**掛け算を繰り返すうちに、長さが 1 からずれた**からです。

[](#/ch/b06-float)で見たとおり、浮動小数点の計算には毎回わずかな誤差が乗ります。
1 回では無視できても、毎秒 60 回・数分ぶん積み重なると効いてきます。

長さが 1 でないクォータニオンは、**回転に拡大が混ざったもの**として働くので、
物体が伸び縮みします。

対策は 1 行です。

長さを毎フレーム測って条件分岐するより、**毎フレーム無条件に正規化する**ほうが単純で安全です。
コストもごくわずかです。`,
      answerCode: `function animate() {
  mesh.quaternion.multiply(spin);
  mesh.quaternion.normalize();   // これだけ
}`,
    },
    {
      prompt: `\`mesh.quaternion.multiply(q)\` と \`mesh.quaternion.premultiply(q)\` は、
何が違いますか。物体が既に横を向いている状態で、それぞれ「y 軸まわりに 10 度」を渡すと、どうなりますか。`,
      hint: '[](#/ch/m05-matrix-order)の「右から効く」を思い出してください。',
      answer: `**\`multiply\` は右から掛け、\`premultiply\` は左から掛けます。**

- \`multiply(q)\` … いまの姿勢**のあとに** q を適用 ＝ **物体自身の軸**（ローカル）まわりに回る
- \`premultiply(q)\` … q **のあとに**いまの姿勢 ＝ **世界の軸**（ワールド）まわりに回る

横を向いた物体に「y 軸まわり 10 度」を渡すと、

- \`multiply\` … **物体にとっての上**のまわりに回る。横倒しなら、世界から見ると横回転になる
- \`premultiply\` … **世界の上**のまわりに回る。物体の傾きに関係なく、水平に回る

飛行機の操縦桿はローカル（\`multiply\`）、
シーンを見回すカメラの左右振りはワールド（\`premultiply\`）が自然です。

**「思ったのと違う軸で回る」ときは、この 2 つを取り違えていないか疑ってください。**`,
    },
  ],
  quiz: [
    {
      q: 'クォータニオンの 4 つの数は、何からできていますか。',
      choices: [
        '回転軸に sin(θ/2) を掛けたもの 3 つと、cos(θ/2) が 1 つ',
        'x・y・z の 3 つの角度と、その合計',
        '回転行列の 1 行目',
        '位置と角度',
      ],
      answer: 0,
      explain:
        '前章の「軸と角度」を、掛け算しやすい形に詰め直したものです。新しい概念ではなく、同じ情報の別の入れ物だと捉えると読みやすくなります。',
    },
    {
      q: '正しいクォータニオンの長さは、いくつですか。',
      choices: ['必ず 1', '回転角に比例する', '軸の長さと同じ', '決まっていない'],
      answer: 0,
      explain:
        '$\\sin^2 + \\cos^2 = 1$ から自動的にそうなります。掛け算を繰り返すと誤差でずれ、回転に拡大が混ざって物体が伸びるので、ときどき `normalize()` します。',
    },
    {
      q: '回転を重ねるとき、クォータニオンでは何をしますか。',
      choices: [
        '掛け算する（順番は行列と同じで右から効く）',
        '成分どうしを足す',
        '角度を足す',
        '平均をとる',
      ],
      answer: 0,
      explain:
        '足し算ではありません。行列と同じ合成の規則が働き、`multiply` はローカル軸まわり、`premultiply` はワールド軸まわりの回転になります。',
    },
  ],
};
