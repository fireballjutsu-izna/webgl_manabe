import type { Chapter } from '../types.ts';

export const chapterM11: Chapter = {
  slug: 'm11-gimbal',
  part: 'math3d',
  number: 11,
  title: 'ジンバルロック ― 自由度が 1 つ消える',
  goal: 'ジンバルロックがなぜ起きるのかを軸の重なりとして説明でき、それを避ける設計を選べるようになります。',
  requires: ['m10-euler', '03-dot'],
  threeApis: ['Euler.order', 'Object3D.rotation', 'MathUtils.clamp', 'Vector3.dot'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

一人称カメラを作っていて、真上を見上げようとしたとき ―

- 上を向ききった瞬間、**左右の操作が効かなくなる**
- あるいは急に**画面がぐるりと回る**
- 少し下を向くと直る

バグを探しても見つかりません。**コードは正しく、仕組みのほうが壊れています。**
これが{{ジンバルロック}}です。
`,
    },
    {
      kind: 'md',
      text: `
## 3 つのリングで考える

オイラー角は「x → y → z の順に回す」でした。これを**入れ子になった 3 つのリング**だと考えます。

- いちばん外のリング … y 軸まわりに回る
- 真ん中のリング … 外のリングに取り付けられていて、x 軸まわりに回る
- いちばん内のリング … 真ん中に取り付けられていて、z 軸まわりに回る

ここが要点です ― **外のリングを回すと、内側のリングは丸ごと持っていかれます。**
リングは自分の軸を持っているのではなく、**親に載っている**からです
（[](#/ch/m05-matrix-order)で見た「後の回転が先の回転の軸ごと動かす」の、目に見える形です）。
`,
    },
    {
      kind: 'md',
      text: `
## 真ん中を 90 度回すと、外と内が重なる

真ん中のリング（x 軸まわり）を 90 度回してみます。

すると、いちばん内のリングの軸が**ちょうど倒れて、いちばん外のリングの軸と平行**になります。
2 本のリングが同じ向きを回すようになるのです。

**回せる方向が 3 つから 2 つに減りました。**
スライダーは 3 本あるのに、行ける姿勢が減っている。
これがジンバルロックの正体で、**計算の誤差でも実装のバグでもありません**。
`,
    },
    {
      kind: 'demo',
      id: 'euler-gimbal',
      caption:
        '「ジンバルロックの状態にする」を押すと、x 軸まわりが 90 度になります。この状態で y と z のスライダーを動かしてください。まったく同じ動きしか作れません。読み出しの「揃い具合」は 2 本の軸の内積の絶対値で、1 に近いほど重なっています。',
    },
    {
      kind: 'md',
      text: `
## 重なりを数で測る

「軸が重なった」は、[](#/ch/03-dot)の道具でそのまま測れます。

2 本の軸はどちらも長さ 1 なので、**内積の絶対値がそのまま揃い具合**になります。
1 に近ければ重なっている、0 なら直交していて安全です。

デモの読み出しがこの値です。x を 90 度に近づけるほど 1 に近づくのが見えます。
`,
    },
    {
      kind: 'formula',
      tex: '|\\hat{a} \\cdot \\hat{b}| \\to 1 \\;\\Longrightarrow\\; \\text{自由度が 1 つ失われる}',
      readAloud:
        '2 本の回転軸の内積の絶対値が 1 に近づくと、その 2 本は同じ向きを回すようになり、回せる方向が 1 つ減ります。0 なら直交していて、それぞれ独立に回せます。',
      worked: {
        given:
          'x 軸まわりの回転が $\\theta$ のとき、z 軸は $(0,\\; -\\sin\\theta,\\; \\cos\\theta)$ の向きに倒れます。y 軸 $(0,\\; 1,\\; 0)$ との揃い具合を測ります。',
        steps: [
          { calc: 'θ = 0度  : |(0,1,0)·(0, 0, 1)|    = 0', note: '直交。3 方向とも自由' },
          { calc: 'θ = 45度 : |(0,1,0)·(0,-0.707,0.707)| = 0.707' },
          { calc: 'θ = 80度 : |(0,1,0)·(0,-0.985,0.174)| = 0.985', note: 'かなり危ない' },
          { calc: 'θ = 90度 : |(0,1,0)·(0,   -1,    0)| = 1', note: '完全に重なった' },
        ],
        result:
          '**$\\theta = 90$ 度で 1 になります。** 内積は $|\\sin\\theta|$ そのもので、45 度で既に 0.707 ―  半分以上重なっています。ジンバルロックは 90 度で突然起きるのではなく、**近づくにつれて操作が鈍くなっていく**現象です。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'ロックの手前から、もう不自然になります',
      text: `
「90 度ちょうどでなければ大丈夫」ではありません。

80 度の時点で 2 本の軸は 0.985 まで揃っており、
残りの 1 方向へ動かすには**極端に大きな角度変化**が要ります。
利用者から見ると「上のほうを向くと、カメラの動きが急に速くなる」ように感じられます。

真上を向く可能性のある操作では、90 度に達しなくても既に体験が壊れています。
`,
    },
    {
      kind: 'md',
      text: `
## 3 つの避け方

**① そもそも 3 軸を使わない**

回る床、時計の針、観覧車 ― 1 軸で済むならオイラー角のままで完全に安全です。
「使わない」がいちばん確実な対策です。

**② 順番を選び、範囲を制限する**

一人称カメラなら \`order = 'YXZ'\` にして、上下の角度を $\\pm 89$ 度に制限します。
実際、多くのゲームが「真上をわずかに見上げられない」のはこのためです。
[](#/ch/b35-easing)で使った \`clamp\` がそのまま効きます。

**③ 姿勢そのものを持つ**

そもそも「3 つの角度を順に重ねる」のをやめて、姿勢を別の形で持つ ―
これが根本的な解決で、次の 2 章で扱います。
`,
    },
    {
      kind: 'code',
      title: '②の手当て（一人称カメラ）',
      code: `import * as THREE from 'three';

camera.rotation.order = 'YXZ';   // 左右を先に、上下をあとに

let yaw = 0;    // 左右。制限なし
let pitch = 0;  // 上下。ここを制限する

function onMouseMove(dx, dy) {
  yaw -= dx * 0.002;
  pitch -= dy * 0.002;

  // 真上・真下に達させない。89 度で止める
  const limit = THREE.MathUtils.degToRad(89);
  pitch = THREE.MathUtils.clamp(pitch, -limit, limit);

  camera.rotation.set(pitch, yaw, 0);
}

// order を YXZ にしないと、上を向いたとき地面が傾いて見える`,
    },
    {
      kind: 'md',
      text: `
## ロックは「表し方」の問題であって、回転の問題ではない

大事なところなので、はっきりさせておきます。

**回転そのものにジンバルロックはありません。**
どんな向きにも、なめらかに行けます。
行けなくなるのは、それを**3 つの角度で表そうとしたとき**だけです。

地球儀の経度で言えば、北極の真上では「東経何度」に意味がなくなるのと同じです。
北極という場所に問題があるのではなく、**経度という表し方**がそこで破綻しています。

だから解決策も「表し方を変える」になります。次の章に進みます。
`,
    },
  ],
  exercises: [
    {
      prompt: `x 軸まわりの回転が 60 度のとき、z 軸は $(0,\\; -\\sin 60^\\circ,\\; \\cos 60^\\circ)$ の向きに倒れています。
y 軸 $(0,\\; 1,\\; 0)$ との揃い具合（内積の絶対値）を手で計算してください。
$\\sin 60^\\circ = 0.866$、$\\cos 60^\\circ = 0.5$ です。`,
      hint: '内積は成分どうしを掛けて足すだけです（[](#/ch/03-dot)）。',
      answer: `$(0,\\; 1,\\; 0) \\cdot (0,\\; -0.866,\\; 0.5) = 0 \\times 0 + 1 \\times (-0.866) + 0 \\times 0.5 = -0.866$

絶対値をとって **0.866** です。

**86.6% 揃っています。** まだロックしていませんが、直交（0）からはかなり遠い。
この状態で y と z のスライダーを動かすと、動きの大半が重複します。

内積は結局 $|\\sin\\theta|$ になるので、覚えるべきは 1 つだけです ―
**上下の角度の $\\sin$ が、そのまま危険度**になります。`,
    },
    {
      prompt: `一人称カメラで上下の角度を $\\pm 89$ 度に制限しました。
このとき 2 本の軸の揃い具合はいくつになりますか。
$\\sin 89^\\circ = 0.9998$ です。制限として十分だと思いますか。`,
      hint: '数字を見てから判断してください。',
      answer: `揃い具合は **0.9998** です。**ほとんど完全に重なっています。**

つまり $\\pm 89$ 度の制限は、ジンバルロックを「起こさない」対策ではありません。
**発散を防ぐだけ**の対策です。

それでも実用になるのは、この状態で困る操作が
「真上を向いたまま、その場でぐるりと回る」だけだからです。
一人称視点でそれをする場面はほぼ無いので、**問題が体験に出ない**。

逆に言えば、**真上を向いたまま回る操作が必要なもの**
（飛行機、宇宙船、自由に飛び回るカメラ）では、この対策は使えません。
そこではクォータニオンが必要になります。`,
    },
    {
      prompt: `「クォータニオンを使えばジンバルロックは起きない」と言われます。
では、クォータニオンで持った姿勢を \`mesh.rotation.x\` で読み出したら、何が起きますか。`,
      hint: '読み出した瞬間、どの表し方に戻っていますか。',
      answer: `**読み出した時点でオイラー角に戻るので、そこで縮退します。**

three は \`quaternion\` と \`rotation\` を相互に同期させています。
内部の姿勢はクォータニオンで正しく保たれていますが、
\`rotation\` として読み出す瞬間に 3 つの角度へ変換され、
**ロックの位置では x と z の分け方が一意に決まりません**。

だからこう覚えてください ―
**ジンバルロックは「保存の仕方」ではなく「表現の仕方」の問題**です。
クォータニオンで持っていても、オイラー角として読み書きした瞬間に問題が戻ってきます。

姿勢をクォータニオンで扱うなら、**読み出しもクォータニオンのまま**にしてください。`,
    },
  ],
  quiz: [
    {
      q: 'ジンバルロックが起きる原因はどれですか。',
      choices: [
        '3 つの軸のうち 2 つが重なり、回せる方向が 1 つ減るため',
        '浮動小数点の誤差がたまるため',
        '回転の角度が大きすぎるため',
        'GPU の精度が足りないため',
      ],
      answer: 0,
      explain:
        '3 つの角度を順に重ねるという仕組みそのものから出てきます。前の回転が後の回転の軸ごと動かすので、特定の角度で 2 本の軸が平行になってしまいます。',
    },
    {
      q: '2 本の回転軸の揃い具合は、どう測れますか。',
      choices: [
        '長さ 1 の軸どうしの内積の絶対値',
        '2 本の外積の長さ',
        '角度の差をそのまま足す',
        '成分の合計',
      ],
      answer: 0,
      explain:
        '内積は 2 本の向きがどれだけ揃っているかを表す道具でした。1 に近いほど重なっており、0 なら直交していて独立に回せます。',
    },
    {
      q: 'ジンバルロックを根本的に避けるには、どうしますか。',
      choices: [
        '3 つの角度で表すのをやめて、姿勢そのものを持つ',
        '角度をもっと細かい刻みで指定する',
        '毎フレーム rotation を 0 に戻す',
        '計算を倍精度で行う',
      ],
      answer: 0,
      explain:
        '回転そのものにロックは存在せず、「3 つの角度で表す」という表現の側で起きています。だから表し方を変えるのが根本の解決で、次章の軸と角度、そしてクォータニオンがその答えです。',
    },
  ],
};
