import type { Chapter } from '../types.ts';

export const chapterM22: Chapter = {
  slug: 'm22-spherical',
  part: 'math3d',
  number: 22,
  title: '球面座標 ― 緯度と経度から x y z へ',
  goal: '緯度・経度・半径から直交座標を組み立てられるようになり、球の上に物を置く処理を自分で書けるようになります。',
  requires: ['m21-polar', 'b20-sin-cos'],
  threeApis: [
    'Spherical',
    'Vector3.setFromSpherical',
    'Vector3.setFromSphericalCoords',
    'Spherical.setFromVector3',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

惑星の表面に都市を置きたい。地球儀にピンを刺したい。星空を作りたい。

やりたいことは「**球の上のこの場所**」を指すことなのに、
$(x,\\; y,\\; z)$ で書こうとすると途端に難しくなります。
3 つの数が独立していないからです ―
$x^2 + y^2 + z^2 = r^2$ を満たさないと、球から浮くか、めり込みます。

**緯度と経度なら、この問題が起きません。**
どんな値を入れても、必ず球の上に乗ります。
`,
    },
    {
      kind: 'md',
      text: `
## 角度 2 つで、球の上が全部指せる

地球の住所と同じ考え方です。

- **緯度** … 赤道から北へ何度か。$-90$ 度（南極）から $+90$ 度（北極）まで
- **経度** … 赤道に沿って何度回ったか。一周 360 度
- **半径** … 中心からの距離

この 3 つを{{球面座標}}と呼びます。
**角度をどう組み合わせても、必ず半径 $r$ の球の上に来ます。**
`,
    },
    {
      kind: 'md',
      text: `
## 組み立て方 ― まず水平面へ潰す

式を丸暗記するとすぐ忘れます。**2 段階に分けて考える**と、二度と忘れません。

**① 緯度で、高さと「水平方向の半径」が決まる**

緯度 $\\phi$ の点は、球を輪切りにしたときの断面の輪の上にいます。

- 高さ … $y = r\\sin\\phi$
- その輪の半径 … $r\\cos\\phi$

赤道（緯度 0）では輪の半径は $r$ そのもの。北極（緯度 90 度）では 0 に潰れます。

**② 経度で、その輪の上を回る**

あとは半径 $r\\cos\\phi$ の円の上を、[](#/ch/m21-polar)の極座標で回るだけです。

**$\\cos$ が 2 回出てくるのは、この 2 段階のため**です。
`,
    },
    {
      kind: 'formula',
      tex: 'x = r\\cos\\phi\\sin\\theta, \\quad y = r\\sin\\phi, \\quad z = r\\cos\\phi\\cos\\theta',
      readAloud:
        '緯度ファイ、経度シータ、半径 r から直交座標を作る式です。y は緯度だけで決まります。x と z は、まず水平方向の半径 r かける コサイン ファイ を求めてから、経度で回して得られます。',
      worked: {
        given: '半径 10 の球の上で、緯度 30 度・経度 60 度の点を求めます。',
        steps: [
          { calc: '【1段目】緯度で高さと水平半径を出す' },
          { calc: 'y  = 10 x sin30度 = 10 x 0.5   = 5' },
          { calc: '水平半径 = 10 x cos30度 = 10 x 0.866 = 8.66' },
          { calc: '【2段目】その輪の上を経度で回す' },
          { calc: 'x = 8.66 x sin60度 = 8.66 x 0.866 = 7.5' },
          { calc: 'z = 8.66 x cos60度 = 8.66 x 0.5   = 4.33' },
          { calc: '【検算】√(7.5² + 5² + 4.33²) = √(56.25+25+18.75) = 10' },
        ],
        result:
          '**$(7.5,\\; 5,\\; 4.33)$**。検算で原点からの距離がちょうど 10 に戻りました。**この検算は必ず合う**ので、球面座標の計算では毎回やる価値があります。合わなければ、どこかで $\\sin$ と $\\cos$ を取り違えています。',
      },
    },
    {
      kind: 'demo',
      id: 'spherical-coords',
      caption:
        '緯度のスライダーを 90 度に近づけると、水平方向の半径が 0 に潰れていくのが見えます。そこでは経度をいくら動かしても点が動きません ― 北極では「東経何度」に意味がなくなる、という話がそのまま出ています。',
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'three の Spherical は「天頂角」を使います',
      text: `
three の Spherical クラスが持つ phi は、緯度ではありません。
北極から測った角度（天頂角）で、0 が真上、90 度が赤道、180 度が真下です。

緯度から直すには phi = 90度 − 緯度 とします。北緯 30 度なら phi は 60 度です。

theta のほうは経度に相当し、+z 軸から測ります。

つまり緯度で考えたいなら、渡す直前に一度変換してください。
ここを取り違えると、北半球と南半球が入れ替わります。
`,
    },
    {
      kind: 'code',
      title: '球面座標を使う',
      code: `import * as THREE from 'three';

// 自分で書く（緯度・経度でそのまま考えられる）
function fromLatLon(latDeg, lonDeg, radius) {
  const lat = THREE.MathUtils.degToRad(latDeg);
  const lon = THREE.MathUtils.degToRad(lonDeg);
  const flat = Math.cos(lat) * radius;   // 水平方向の半径
  return new THREE.Vector3(
    flat * Math.sin(lon),
    Math.sin(lat) * radius,
    flat * Math.cos(lon),
  );
}

const tokyo = fromLatLon(35.68, 139.77, 10);

// three に任せる（phi は天頂角なので変換が要る）
const phi = THREE.MathUtils.degToRad(90 - 35.68);
const theta = THREE.MathUtils.degToRad(139.77);
const p = new THREE.Vector3().setFromSphericalCoords(10, phi, theta);

// 逆向き：位置から球面座標へ
const s = new THREE.Spherical().setFromVector3(p);
console.log(s.radius, s.phi, s.theta);

// 球の上に置いた物を、表面に立たせる（法線は中心からの向き）
marker.position.copy(tokyo);
marker.lookAt(tokyo.clone().multiplyScalar(2));   // 外向きを向く`,
    },
    {
      kind: 'md',
      text: `
## 極では、経度の意味が消える

デモで見たとおり、**北極と南極では経度が意味を持ちません。**

緯度 90 度では水平方向の半径が 0 になるので、経度をどう変えても同じ 1 点です。
「北極点の東経 45 度」と「北極点の東経 120 度」は、同じ場所を指します。

これは[](#/ch/m11-gimbal)や[](#/ch/m15-lookat)で見たのとまったく同じ縮退です。
**球面を 2 つの角度で覆おうとすると、極で必ず破綻します。**

実害は主に 2 つ。

- **テクスチャが極で潰れる** … 正距円筒図法の画像を球に貼ると、極付近が押し潰されます
- **一様に散らそうとすると極に溜まる** … これは次の章の主題です
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '実は、テクスチャの UV も同じ式です',
      text: `
球にテクスチャを貼るときの UV は、経度を u に、緯度を v に対応させたものです。

three の SphereGeometry がまさにこれをやっているので、
世界地図（正距円筒図法）の画像をそのまま貼れば地球になります。

球面座標を理解しておくと、
「なぜ極付近だけ画像が伸びるのか」も同じ話として片付きます。
`,
    },
  ],
  exercises: [
    {
      prompt: `半径 8 の球の上で、緯度 60 度・経度 90 度の点を手で計算してください。
$\\sin 60^\\circ = 0.866$、$\\cos 60^\\circ = 0.5$ です。`,
      hint: 'まず高さと水平方向の半径を出してから、経度で回します。',
      answer: `**1 段目** … $y = 8 \\times 0.866 = 6.928$、水平半径 $= 8 \\times 0.5 = 4$

**2 段目** … 経度 90 度なので $\\sin 90^\\circ = 1$、$\\cos 90^\\circ = 0$
$x = 4 \\times 1 = 4$、$z = 4 \\times 0 = 0$

答えは **$(4,\\; 6.928,\\; 0)$** です。

**検算** … $\\sqrt{4^2 + 6.928^2 + 0^2} = \\sqrt{16 + 48} = \\sqrt{64} = 8$ ―
半径に戻りました。

緯度 60 度は北極寄りなので、高さ（6.93）が水平半径（4）より大きくなっています。
数字の大小が絵と合っているかを見るのも、有効な確認です。`,
    },
    {
      prompt: `three の \`setFromSphericalCoords(r, phi, theta)\` に、
北緯 45 度のつもりで \`phi = 45度\` を渡しました。実際にはどこの点になりますか。`,
      hint: 'three の `phi` は、どこから測った角度でしたか。',
      answer: `three の \`phi\` は**天頂角**（北極から測った角度）なので、
$45^\\circ$ を渡すと「北極から 45 度下がった点」 ＝ **北緯 45 度**になります。

**この場合はたまたま正解です。** $90 - 45 = 45$ なので一致します。

ただしこれは偶然で、**45 度以外では必ずずれます。**

- 北緯 30 度のつもりで $\\phi = 30^\\circ$ → 実際は**北緯 60 度**
- 北緯 0 度（赤道）のつもりで $\\phi = 0^\\circ$ → 実際は**北極**

正しくは $\\phi = 90^\\circ - \\text{緯度}$ です。

**「45 度で試したら合っていたので大丈夫」と判断すると、必ず後で刺されます。**
角度の変換を確かめるときは、0 度と 90 度のような**端の値**で試してください。`,
    },
    {
      prompt: `半径 20 の星空の球に、星を 200 個置きたい。
緯度と経度を \`Math.random()\` でそのまま選ぶコードを書くと、どんな見た目になりますか。`,
      hint: '緯度が 90 度に近いところでは、輪の大きさはどうなっていましたか。',
      answer: `**北極と南極のあたりだけ、星が密集します。**

緯度を一様に選ぶと、どの緯度にも同じ数の星が配られます。
ところが緯度 0 度（赤道）の輪は周囲 $2\\pi r$ と長く、
緯度 80 度の輪は周囲が $2\\pi r \\cos 80^\\circ$ ＝ **17% の長さ**しかありません。

**短い輪に同じ数だけ配れば、当然そこは混みます。**

見た目としては、真上と真下に星が集まった、明らかに不自然な空になります。
惑星の表面に木や街を配るときも、同じ症状が出ます。

直し方は次の章で扱います。**要点は「緯度ではなく高さを一様に選ぶ」**ことです。`,
    },
  ],
  quiz: [
    {
      q: '球面座標で、高さ $y$ を決めているのはどれですか。',
      choices: ['緯度だけ', '経度だけ', '緯度と経度の両方', '半径だけ'],
      answer: 0,
      explain:
        '$y = r\\sin\\phi$ で、経度は関係しません。経度は「その高さの輪の上を、どこまで回るか」を決めるだけです。この 2 段階の分け方が、式を忘れないコツです。',
    },
    {
      q: 'three の `Spherical` の `phi` は何を表しますか。',
      choices: [
        '北極から測った角度（天頂角）。緯度は 90 度 − phi',
        '緯度そのもの',
        '経度',
        '半径',
      ],
      answer: 0,
      explain:
        '0 が真上、90 度が赤道、180 度が真下です。緯度のつもりで渡すと北半球と南半球が入れ替わります。0 度や 90 度のような端の値で確かめると、取り違えに気づけます。',
    },
    {
      q: '緯度 90 度（北極）で起きることはどれですか。',
      choices: [
        '水平方向の半径が 0 になり、経度が意味を失う',
        '半径が 0 になる',
        '高さが 0 になる',
        '何も特別なことは起きない',
      ],
      answer: 0,
      explain:
        'ジンバルロックや lookAt の縮退と、まったく同じ構造です。球面を 2 つの角度で覆おうとすると、極で必ず破綻します。テクスチャが極で潰れるのも同じ理由です。',
    },
  ],
};
