import type { Chapter } from '../types.ts';

export const chapterM24: Chapter = {
  slug: 'm24-orbit',
  part: 'math3d',
  number: 24,
  title: '軌道 ― 何かのまわりを回る',
  goal: '公転や軌道カメラを 2 通りの方法で書けるようになり、場面に応じて階層と球面座標を選べるようになります。',
  requires: ['m23-sphere-uniform', '09-hierarchy'],
  threeApis: [
    'Spherical',
    'Vector3.setFromSpherical',
    'Object3D.lookAt',
    'MathUtils.clamp',
    'Group',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 同じことを、2 通りで書ける

「何かのまわりを回る」は、3D でいちばんよく書く動きです。
惑星、衛星、周回するカメラ、的のまわりを飛ぶ敵。

そして書き方が 2 通りあります。**どちらも正しく、向く場面が違います。**

- **階層で書く** … 入れ物を回す（[](#/ch/09-hierarchy)）
- **球面座標で書く** … 角度から位置を毎フレーム計算する（[](#/ch/m22-spherical)）

この章で使い分けを決めます。
`,
    },
    {
      kind: 'md',
      text: `
## 階層が向く場面 ― 入れ子になるとき

**回るものの上で、さらに何かが回る**なら、階層が圧倒的に楽です。

太陽 → 惑星 → 月と重なると、球面座標で書くには
「惑星の現在位置を求めてから、そこを中心にもう一度」と積み上げる必要があります。
階層なら**各段が \`rotation.y += dt\` の 1 行**で済みます。

- **向く** … 太陽系、ロボットの関節、観覧車のゴンドラ、車輪
- **向かない** … 軌道の形を細かく制御したいとき

階層で作れるのは**真円の等速回転だけ**です。
楕円にしたい、途中で速さを変えたい、傾いた面で回したい ―
そうなると入れ物をいくつも重ねることになり、かえって読みにくくなります。
`,
    },
    {
      kind: 'md',
      text: `
## 球面座標が向く場面 ― 位置を直接決めたいとき

**軌道の形を自由にしたい**なら、位置を毎フレーム計算するほうが素直です。

とくに**カメラ**はこちらが定番です。
「対象から距離 $r$、水平角 $\\theta$、仰角 $\\phi$」の 3 つを状態として持ち、
毎フレーム位置を組み立てて \`lookAt\` する ―
これが \`OrbitControls\` の中身そのものです。

利点は、**3 つの数を直接触れる**ことです。

- マウスの横ドラッグ → $\\theta$ を増やす
- 縦ドラッグ → $\\phi$ を増やす（範囲を制限する）
- ホイール → $r$ を増減する

階層でこれをやろうとすると、入れ物が 2 段必要なうえに、
制限をかけるのが面倒になります。
`,
    },
    {
      kind: 'formula',
      tex: 'p = c + r\\,\\big(\\cos\\phi\\sin\\theta,\\; \\sin\\phi,\\; \\cos\\phi\\cos\\theta\\big)',
      readAloud:
        '軌道上の位置は、中心 c から、球面座標で作った向きに距離 r だけ離れた点です。前の章の式に、中心を足しただけになっています。',
      worked: {
        given:
          '対象が $(0,\\; 2,\\; 0)$ にあります。距離 10、水平角 90 度、仰角 30 度のカメラ位置を求めます。',
        steps: [
          { calc: '仰角で高さと水平半径 : sin30度 = 0.5 / cos30度 = 0.866' },
          { calc: '高さ    : 10 x 0.5   = 5' },
          { calc: '水平半径 : 10 x 0.866 = 8.66' },
          { calc: '水平角 90度 : sin = 1 / cos = 0' },
          { calc: 'オフセット : (8.66 x 1, 5, 8.66 x 0) = (8.66, 5, 0)' },
          { calc: '中心を足す : (0,2,0) + (8.66,5,0) = (8.66, 7, 0)' },
        ],
        result:
          'カメラは **$(8.66,\\; 7,\\; 0)$** に置き、そこから $(0, 2, 0)$ を `lookAt` します。対象との距離を測ると $\\sqrt{8.66^2 + 5^2} = 10$ で、指定どおりです。**中心を足すのを忘れると、いつも原点のまわりを回るカメラ**になります。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '仰角は必ず制限してください',
      text: `
仰角を 90 度ちょうどにすると、カメラが真上から見下ろす形になり、
lookAt が破綻します。的への向きが up と平行になるからです。

lookAt の章で見た縮退がそのまま出るので、
仰角は ±89 度あたりで止めるか、もう少し内側（±85 度）に抑えます。

OrbitControls にも minPolarAngle と maxPolarAngle があり、
既定でわずかに内側に制限されています。
`,
    },
    {
      kind: 'code',
      title: '軌道カメラを自分で書く',
      code: `import * as THREE from 'three';

const target = new THREE.Vector3(0, 2, 0);
let radius = 10;
let theta = 0;                  // 水平角
let phi = THREE.MathUtils.degToRad(30);   // 仰角

function onDrag(dx, dy) {
  theta -= dx * 0.005;
  phi += dy * 0.005;

  // 真上・真下に達させない（lookAt が破綻する）
  const limit = THREE.MathUtils.degToRad(85);
  phi = THREE.MathUtils.clamp(phi, -limit, limit);
}

function onWheel(delta) {
  radius = THREE.MathUtils.clamp(radius * (1 + delta * 0.001), 2, 60);
}

function updateCamera() {
  const flat = Math.cos(phi) * radius;
  camera.position.set(
    target.x + flat * Math.sin(theta),
    target.y + Math.sin(phi) * radius,
    target.z + flat * Math.cos(theta),
  );
  camera.lookAt(target);
}

// three の Spherical を使うと短く書ける
const s = new THREE.Spherical(radius, Math.PI / 2 - phi, theta);
camera.position.setFromSpherical(s).add(target);
camera.lookAt(target);`,
    },
    {
      kind: 'md',
      text: `
## 傾いた軌道と、楕円

球面座標で書くと、**軌道の形をいじれる**のが利点でした。実際にやってみます。

**傾いた軌道** … 円を作ってから、[](#/ch/m12-axis-angle)で軸まわりに回します。
太陽系の惑星はそれぞれ少しずつ違う面を回っているので、これが要ります。

**楕円軌道** … 半径を角度によって変えます。
$r(\\theta) = a$ と $b$ を $\\cos$ と $\\sin$ に別々に掛ければ、簡単な楕円になります。

**速さを変える** … 角度の増分を一定にしなければ、遅くなったり速くなったりします。
実際の惑星は近いときほど速く動きます。

どれも**階層では書けない**ものです。入れ物は等速の真円しか作れません。
`,
    },
    {
      kind: 'code',
      title: '傾いた楕円軌道',
      code: `import * as THREE from 'three';

const a = 12;      // 長いほうの半径
const b = 8;       // 短いほうの半径
const tilt = THREE.MathUtils.degToRad(15);
const axis = new THREE.Vector3(0, 0, 1);

function orbitPosition(angle) {
  // まず水平面の上に楕円を作る
  const p = new THREE.Vector3(Math.cos(angle) * a, 0, Math.sin(angle) * b);
  // 軌道面を傾ける
  p.applyAxisAngle(axis, tilt);
  return p;
}

function animate(dt) {
  angle += dt * 0.4;
  planet.position.copy(orbitPosition(angle));
}

// 階層でも傾きは作れる（入れ物をもう 1 段かぶせる）
const tiltGroup = new THREE.Group();
tiltGroup.rotation.z = tilt;
scene.add(tiltGroup);
tiltGroup.add(orbitGroup);`,
    },
    {
      kind: 'md',
      text: `
## ここまでが第2部です

24 章で手に入れたものを、大きく 3 つに分けて振り返ります。

- **行列**（8 章）… 点を運ぶ装置。列は軸の行き先。順番は右から効く。逆行列で戻せる
- **回転**（7 章）… オイラー角の限界と、クォータニオンという答え。slerp と lookAt
- **階層と座標**（9 章）… 座標系の入れ子と、球面という別の指し方

**この 3 つが、Three.js の \`Object3D\` が持っている機能のほぼすべて**です。
\`position\` \`rotation\` \`scale\` \`parent\` \`matrixWorld\` ―
どれも、ここまでの章のどこかで正体を見ました。

**次の部からは、実際に three を書きます。**
もう「なぜそう書くのか」が分からないまま進むことはありません。
`,
    },
  ],
  exercises: [
    {
      prompt: `対象が $(4,\\; 0,\\; 0)$ にあります。距離 6、水平角 0 度、仰角 60 度のカメラ位置を手で計算してください。
$\\sin 60^\\circ = 0.866$、$\\cos 60^\\circ = 0.5$ です。`,
      hint: '仰角で高さと水平半径を出し、水平角で回してから、中心を足します。',
      answer: `**仰角で分ける** … 高さ $= 6 \\times 0.866 = 5.196$、水平半径 $= 6 \\times 0.5 = 3$

**水平角 0 度** … $\\sin 0^\\circ = 0$、$\\cos 0^\\circ = 1$ なので
オフセットは $(3 \\times 0,\\;\\; 5.196,\\;\\; 3 \\times 1) = (0,\\; 5.196,\\; 3)$

**中心を足す** … $(4, 0, 0) + (0, 5.196, 3) = (4,\\; 5.196,\\; 3)$

答えは **$(4,\\; 5.196,\\; 3)$** です。

**検算** … 対象との距離は $\\sqrt{0^2 + 5.196^2 + 3^2} = \\sqrt{27 + 9} = 6$ ― 指定どおりです。

仰角 60 度はかなり見下ろす角度なので、高さ（5.20）が水平距離（3）より大きくなっています。`,
    },
    {
      prompt: `軌道カメラを作ったところ、対象を動かしてもカメラが原点のまわりを回り続けました。
何が抜けていますか。`,
      hint: '式の最後に何を足すのでしたか。',
      answer: `**中心（対象の位置）を足していない**からです。

球面座標が返すのは「**原点から見たオフセット**」であって、位置そのものではありません。
$p = c + r(\\dots)$ の $c$ が抜けると、いつも原点のまわりを回ります。

対象が原点にいるあいだは正しく動いて見えるので、
**対象を動かした瞬間に初めて発覚する**種類のバグです。

なお \`lookAt(target)\` のほうは正しく対象を向くので、
「カメラは対象を見ているのに、位置だけ変な場所にいる」という
分かりにくい症状になります。`,
      answerCode: `// 誤り：原点のまわりを回る
camera.position.setFromSpherical(s);

// 正しい：対象からのオフセットとして置く
camera.position.setFromSpherical(s).add(target);
camera.lookAt(target);`,
    },
    {
      prompt: `次の 3 つを、階層と球面座標のどちらで書くべきか答えてください。理由も添えてください。

1. 観覧車のゴンドラ（水平を保ったまま、大車輪について回る）
2. マウスで自由に回せる商品ビューアーのカメラ
3. 太陽を回る惑星と、その惑星を回る月`,
      hint: '入れ子になるか、値を直接触りたいか。',
      answer: `**1. 階層。** 大車輪の入れ物を回し、ゴンドラをその子にします。
ただしゴンドラは水平を保ちたいので、**ゴンドラ自身に逆回転**をかけます
（\`gondola.rotation.z = -wheel.rotation.z\`）。
親の回転を打ち消す、という書き方は階層ならではです。

**2. 球面座標。** 距離・水平角・仰角の 3 つを状態として持ち、
マウスの入力をその 3 つに直接足します。
仰角の制限（$\\pm 85$ 度）も、値を直接持っているから 1 行で書けます。
階層でやると、制限をかける場所が分かりにくくなります。

**3. 階層。** 入れ子そのものなので、階層の独壇場です。
球面座標だと「惑星の現在位置を求めてから、そこを中心に月の位置を計算」と積み上がり、
段が増えるほど式が長くなります。階層なら各段が 1 行です。

**目安** … 入れ子なら階層、値を直接触りたいなら球面座標。`,
    },
  ],
  quiz: [
    {
      q: '入れ子の回転（太陽・惑星・月）を書くとき、向いているのはどちらですか。',
      choices: [
        '階層（入れ物を回す）',
        '球面座標（位置を毎フレーム計算する）',
        'どちらでも同じ手間',
        'どちらでも書けない',
      ],
      answer: 0,
      explain:
        '階層なら各段が `rotation.y += dt` の 1 行で済みます。球面座標では「親の現在位置を求めてから」と積み上がるので、段が増えるほど式が長くなります。',
    },
    {
      q: '軌道カメラを球面座標で書く利点はどれですか。',
      choices: [
        '距離・水平角・仰角を直接触れるので、入力の反映も制限も書きやすい',
        '計算が速い',
        'lookAt が要らない',
        '階層が深くならない',
      ],
      answer: 0,
      explain:
        '3 つの数を状態として持つので、マウスの入力をそのまま足せますし、仰角の制限も 1 行です。`OrbitControls` の中身もこの形です。',
    },
    {
      q: '軌道カメラで対象を動かしたら、カメラが原点のまわりを回りました。原因はどれですか。',
      choices: [
        '球面座標で作ったオフセットに、中心を足していない',
        '仰角を制限していない',
        '距離が小さすぎる',
        'lookAt を呼んでいない',
      ],
      answer: 0,
      explain:
        '球面座標が返すのは「原点から見たオフセット」で、位置そのものではありません。対象が原点にいるあいだは正しく見えるので、動かした瞬間に発覚します。',
    },
  ],
};
