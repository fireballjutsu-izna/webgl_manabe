import type { Chapter } from '../types.ts';

export const chapter09: Chapter = {
  slug: '09-hierarchy',
  part: 'math3d',
  number: 16,
  title: '親子関係 ― 座標系を入れ子にする',
  goal: '物体を親子にすると何が起きるのかが分かり、車体と車輪のようなまとまりを階層で組み立てられるようになります。',
  requires: ['m06-trs', '01-space'],
  threeApis: ['Object3D.add', 'Object3D.remove', 'Group', 'Object3D.children', 'Object3D.parent'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- 車体に車輪をくっつけたら、車体を動かしたときだけ車輪が置き去りになった
- 太陽のまわりを回る惑星を作りたいが、毎フレーム位置を計算するのが大変
- ロボットの腕を曲げたら、その先の手だけ元の場所に取り残された

どれも「**まとまりとして動かしたい**」という同じ願いです。
Three.js の答えは、物体を**親子にする**ことです。
`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '船の中を歩く人',
      text: `
船の甲板を、船首に向かって 3 歩進んだところに立っている人がいます。
その人の「船から見た位置」は、船がどこにいようと 3 歩のままです。
けれど「地球から見た位置」は、船が進めば刻々と変わります。

親子関係とは、この「船」を作ることです。
人は船の中の座標で暮らし、船が動けば一緒に運ばれます。
`,
    },
    {
      kind: 'md',
      text: `
## add は「座標系の中に入れる」

Three.js で \`parent.add(child)\` と書くと、子は**親の座標系の中に住む**ことになります。

親を動かせば、子の \`position\` の値はまったく変わらないまま、実際の位置だけが動きます。
子は「親から見て、ここ」としか言っていないので、親が動けば結果も動く ― それだけです。

しかも引き継がれるのは移動だけではありません。**親の回転と拡大も、丸ごとかぶさります。**

- 親が 2 倍になれば、子も 2 倍になる（子の \`scale\` は 1 のまま）
- 親が 45 度回れば、子は**親を中心に**振り回される

3 つとも[](#/ch/m06-trs)で見た TRS が、そのまま上から掛かっているだけです。
`,
    },
    {
      kind: 'demo',
      id: 'parent-child',
      caption:
        '「子のローカル位置」のスライダーは動かさず、親の位置・回転・拡大だけを動かしてみてください。読み出しの上段（ローカル）は固定されたまま、下段（ワールド）だけが変わります。子は一度も「動いた」と言っていないのに、世界での場所は変わっています。',
    },
    {
      kind: 'md',
      text: `
## 親が移動だけなら、ただの足し算

いちばん単純な場合から始めます。**親が移動しかしていないなら、世界での位置は足し算です。**
`,
    },
    {
      kind: 'formula',
      tex: 'p_{\\text{world}} = p_{\\text{parent}} + p_{\\text{local}} \\quad (\\text{親が移動だけのとき})',
      readAloud:
        '子の世界での位置は、親の位置に子のローカル位置を足したものです。ただしこれが成り立つのは、親が回っても拡大もしていない場合だけです。',
      worked: {
        given:
          '親を $(5,\\; 0,\\; 0)$ に置き、子を親の中の $(2,\\; 1,\\; 0)$ に置きます。親は回転も拡大もしていません。',
        steps: [
          { calc: '子のローカル位置 : (2, 1, 0)' },
          { calc: '親の位置       : (5, 0, 0)' },
          { calc: '足す           : (5+2, 0+1, 0+0)' },
          { calc: '              = (7, 1, 0)' },
        ],
        result:
          'ワールドでは $(7,\\; 1,\\; 0)$ です。**ここまでは直感どおり。** ところが親を回した瞬間、この足し算は成り立たなくなります。次の 2 章でそこを扱います。',
      },
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '親を回すと、足し算では合わなくなります',
      text: `
親を y 軸まわりに 90 度回すと、さっきの子は (7, 1, 0) には来ません。
子のローカル座標そのものが、親と一緒に向きを変えて運ばれるからです。

正しい答えは (5, 1, -2) です。
なぜそうなるのかは、matrixWorld の章で行列として片付けます。
ここでは「回転が入ると足し算では済まない」ことだけ覚えてください。
`,
    },
    {
      kind: 'md',
      text: `
## Group ― 形を持たない入れ物

\`THREE.Group\` は、**形を持たない親**です。画面には何も描かれません。

「まとまりを作るためだけの存在」で、使いどころははっきりしています。

- **部品をまとめる** … 車体・車輪・窓をまとめて 1 台の車として動かす
- **回転の中心をずらす** … 中で物体をずらして置き、入れ物のほうを回す
- **入れ子の回転を重ねる** … 公転する惑星の上で、月がさらに公転する

2 つ目は[](#/ch/m19-pivot)で、3 つ目はこの章の最後で扱います。

なお \`Mesh\` も親になれるので、Group は必須ではありません。
それでも Group を挟むほうがよいのは、**部品を差し替えても構造が壊れない**からです。
車体のモデルを別のものに替えても、Group の中身を入れ替えるだけで済みます。
`,
    },
    {
      kind: 'code',
      title: '階層を組み立てる',
      code: `import * as THREE from 'three';

// 車をひとかたまりにする
const car = new THREE.Group();
scene.add(car);

const body = new THREE.Mesh(bodyGeometry, bodyMaterial);
car.add(body);

// 車輪は車の中の座標で置く。車がどこにいても、この値は変わらない
for (const x of [-0.8, 0.8]) {
  for (const z of [-1.2, 1.2]) {
    const wheel = new THREE.Mesh(wheelGeometry, wheelMaterial);
    wheel.position.set(x, 0.3, z);
    car.add(wheel);
  }
}

// これだけで車輪ごと動く・回る
car.position.x = 10;
car.rotation.y = Math.PI / 4;

// 親子を確かめる
console.log(car.children.length);   // 5（車体 + 車輪 4 つ）
console.log(body.parent === car);   // true

// 外す。remove したものは自分で dispose する必要がある
car.remove(body);`,
    },
    {
      kind: 'md',
      text: `
## 入れ子にすると、動きが積み上がる

階層のいちばんおいしいところは、**難しい動きを、簡単な動きの入れ子で作れる**ことです。

惑星が太陽を回り、その惑星を月が回る ―
これを座標の計算で書くと三角関数が入り乱れますが、階層なら**それぞれが 1 行の回転**で済みます。

- 公転用の入れ物を回す → 惑星が太陽のまわりを回る
- 惑星自身を回す → 自転する。**月も一緒に運ばれる**

各段は「自分の原点まわりに回る」としか言っていません。
複雑さは、階層のほうが引き受けてくれます。
`,
    },
    {
      kind: 'code',
      title: '軌道運動を階層で作る',
      code: `import * as THREE from 'three';

const sun = new THREE.Mesh(sunGeometry, sunMaterial);
scene.add(sun);

// 公転用の入れ物。これ自体は形を持たない
const orbit = new THREE.Group();
sun.add(orbit);

// 入れ物の中で、中心から離して惑星を置く
const planet = new THREE.Mesh(planetGeometry, planetMaterial);
planet.position.x = 5;
orbit.add(planet);

// 月は惑星の子にする。惑星について回る
const moon = new THREE.Mesh(moonGeometry, moonMaterial);
moon.position.x = 1.2;
planet.add(moon);

function animate(dt) {
  orbit.rotation.y += dt * 0.5;   // 公転（惑星が太陽のまわりを回る）
  planet.rotation.y += dt * 2;    // 自転。月も一緒に回る
}`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '非一様な拡大は、階層と相性が悪い',
      text: `
親を軸ごとに違う倍率で拡大（x だけ 2 倍など）すると、回転した子が歪みます。

理由は法線行列の章と同じで、
「回してから伸ばす」と「伸ばしてから回す」が別物だからです。
親に scale を使うなら、3 軸とも同じ倍率にしておくのが安全です。

大きさを個別に変えたいなら、親ではなく子それぞれの scale を触ってください。
`,
    },
  ],
  exercises: [
    {
      prompt: `親を $(3,\\; 0,\\; 4)$ に置き、子を親の中の $(-1,\\; 2,\\; 0)$ に置きました。
親は回転も拡大もしていません。子のワールド座標を手で計算してください。`,
      hint: '親が移動だけなら、足し算で済みます。',
      answer: `$(3 + (-1),\\;\\; 0 + 2,\\;\\; 4 + 0) = (2,\\; 2,\\; 4)$

答えは **$(2,\\; 2,\\; 4)$** です。

念のため確かめておくと、**親からの距離**は
$\\sqrt{(-1)^2 + 2^2 + 0^2} = \\sqrt{5} \\approx 2.24$ で、
親をどこへ動かしてもこの距離は変わりません。

「子のローカル位置は、親からの相対的な置き場所」だからです。`,
    },
    {
      prompt: `車の Group に車輪を 4 つ入れました。
\`car.position.x = 10\` としたあと、\`wheel.position.x\` を読むと何が返りますか。`,
      hint: 'position はどちらの座標系の値でしたか。',
      answer: `**最初に設定した値のまま**です（$-0.8$ か $0.8$）。10 は足されていません。

\`position\` は**ローカル座標** ― 親から見た位置なので、
親がどこへ動こうと変わりません。車輪は「車の中の、この位置」としか言っていないからです。

世界での位置が欲しいときは、専用の取り出し方を使います。
それが次の章の主題です。

**この取り違えは、three でいちばん多いつまずき**です。
\`console.log(mesh.position)\` の値と画面上の位置が合わないときは、
まず親がいないかを疑ってください。`,
    },
    {
      prompt: `太陽系のデモで、月が惑星ではなく**太陽の子**になっていたとします。
見た目はどうなりますか。`,
      hint: '月は誰について回りますか。',
      answer: `**月が惑星について行かなくなります。**

太陽の子なので、月は太陽を中心に自分のペースで回るだけになり、
惑星が公転して離れていっても、月はその場に取り残されます。
ときどきすれ違う 2 つの天体、という絵になります。

階層は「**誰について回るか**」を決めるものです。
月は惑星について回るので、惑星の子でなければなりません。

逆に言えば、**親を間違えたときの症状は「一緒に動いてほしいものが動かない」**という形で出ます。
車輪が置き去りになるのも、腕の先の手が取り残されるのも、すべて同じ症状です。`,
    },
    {
      prompt: `親を 2 倍に拡大したところ、子の \`scale\` は 1 のままなのに、
子が 2 倍の大きさで描かれました。さらに子は親から 2 倍遠くへ移動していました。なぜですか。`,
      hint: '拡大は「原点からの距離を伸ばす」変換でした。',
      answer: `**拡大は、大きさだけでなく位置にも効く**からです。

親の座標系そのものが 2 倍に引き伸ばされるので、

- 子の**形**が 2 倍になる（ローカルの各頂点が 2 倍の位置へ運ばれる）
- 子の**位置**も 2 倍になる（ローカル位置 $(2,0,0)$ が、世界では $(4,0,0)$ 相当へ）

どちらも「親の座標系の目盛りが 2 倍になった」ことの現れです。
[](#/ch/m03-basis)の見方で言えば、**親の軸が 2 倍の長さになった**ということです。

だから「子だけ元の大きさに戻したい」と思って子の scale を 0.5 にすると、
形は戻りますが**位置は 2 倍のまま**です。位置も戻すには、子の position も半分にする必要があります。`,
    },
  ],
  quiz: [
    {
      q: '`parent.add(child)` としたあと、親を動かすと子の `position` はどうなりますか。',
      choices: [
        '変わらない（親から見た位置なので）',
        '親と同じ値になる',
        '親の移動ぶんが足される',
        '0 にリセットされる',
      ],
      answer: 0,
      explain:
        '`position` はローカル座標 ― 親から見た位置です。子は「親の中の、ここ」としか言っていないので、親がどこへ動いても値は変わりません。変わるのは世界での実際の位置だけです。',
    },
    {
      q: 'THREE.Group は何のためにありますか。',
      choices: [
        '形を持たない親として、まとまりや回転の中心を作るため',
        '描画を速くするため',
        'マテリアルを共有するため',
        '複数のシーンを作るため',
      ],
      answer: 0,
      explain:
        '画面には何も描かれません。部品をまとめる、回転の中心をずらす、入れ子の回転を重ねる ― この 3 つが主な使いどころです。',
    },
    {
      q: '親が移動だけしているとき、子のワールド座標はどう求まりますか。',
      choices: [
        '親の位置と子のローカル位置を足す',
        '掛ける',
        '子のローカル位置そのもの',
        '親の位置そのもの',
      ],
      answer: 0,
      explain:
        '移動だけなら足し算で済みます。ただし親が回転や拡大をしていると成り立たなくなり、行列の掛け算が必要になります。',
    },
  ],
};
