import type { Chapter } from '../types.ts';

export const chapterM08: Chapter = {
  slug: 'm08-normal-matrix',
  part: 'math3d',
  number: 8,
  title: '法線行列 ― 拡大すると陰影が壊れる',
  goal: '法線を頂点と同じ行列で変換してはいけない理由が分かり、非一様な拡大で陰影が崩れるときに何を疑えばよいか分かります。',
  requires: ['m07-inverse', 'b27-lambert'],
  threeApis: ['Matrix3.getNormalMatrix', 'Vector3.applyNormalMatrix', 'Object3D.scale'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

物体を横に伸ばしたら、**陰影がおかしくなった**。
明るさが変で、丸いはずのものが平たく見える。形は正しいのに、光り方だけが違う。

これは行列を扱ううえで、いちばん気づきにくい落とし穴です。
原因は 1 つ ― **法線を、頂点と同じ行列で変換してはいけない**からです。
`,
    },
    {
      kind: 'md',
      text: `
## 面を伸ばすと、法線は逆に傾く

なぜいけないのか。絵で考えるのがいちばん早いです。

斜めの面を、**横方向にだけ 2 倍**に伸ばすとします。

- **面そのもの** … 横に引き伸ばされるので、**寝る**方向に傾く
- **法線** … 面に垂直なので、面が寝たぶん**立つ**方向に傾くはず

ところが法線を同じ行列で変換すると、法線も横に 2 倍されて、
**面と同じく寝る**方向に傾いてしまいます。**逆です。**

結果として、法線が面に垂直でなくなります。
そのまま[](#/ch/b27-lambert)の明るさを計算するので、陰影が狂います。
`,
    },
    {
      kind: 'demo',
      id: 'normal-matrix',
      caption:
        '「横方向の拡大」を動かしてください。バイオレットの矢印（同じ行列で変換したもの）が面から傾いていくのに対し、白い矢印（法線行列で変換したもの）は常に面に垂直を保ちます。読み出しの内積が、それを数字で示しています。0 なら直交です。',
    },
    {
      kind: 'md',
      text: `
## 正しい変換 ― 逆行列の転置

法線に使うべきなのは、**元の行列の逆行列を転置したもの**です。
これを{{法線行列}}と呼びます。

なぜこれで直るのか。
「面に垂直」という関係は、**面の向きと法線の内積が 0** ということでした。
変換したあともこの 0 を保ちたい ― その条件を解くと、
逆行列の転置が出てきます。

拡大だけの単純な場合なら、答えは直感的です。
**頂点を $s$ 倍したなら、法線は $\\frac{1}{s}$ 倍する**。
逆数を掛ける、それだけです。
`,
    },
    {
      kind: 'formula',
      tex: 'N = (M^{-1})^{T}',
      readAloud:
        '法線行列 N は、もとの変換行列 M の逆行列を転置したものです。上付きの T が転置、マイナス 1 乗が逆行列を表します。',
      worked: {
        given:
          '横に 2 倍、縦は等倍の拡大 $\\begin{pmatrix} 2 & 0 \\\\ 0 & 1 \\end{pmatrix}$ で、45 度の面の法線 $(-0.707,\\; 0.707)$ を変換します。',
        steps: [
          { calc: '面の向き (0.707, 0.707) を変換 : (1.414, 0.707)', note: '横に伸びて寝た' },
          { calc: '誤り : 法線も同じ行列で  (-1.414, 0.707)', note: '法線も寝てしまった' },
          { calc: '  面との内積 : 1.414x(-1.414) + 0.707x0.707 = -1.5', note: '直交していない' },
          { calc: '正しい : 逆数を掛ける  (-0.354, 0.707)', note: '横を 1/2 倍にした' },
          { calc: '  面との内積 : 1.414x(-0.354) + 0.707x0.707 = 0', note: '直交を保った' },
        ],
        result:
          '**誤ったほうは内積が $-1.5$、正しいほうは 0。** 直交が保たれているかどうかが、数字ではっきり出ます。デモで拡大率を動かすと、この 2 つの値が別々に動くのが見えます。',
      },
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '一様な拡大なら、心配は要りません',
      text: `
縦・横・奥行きを**同じ倍率**で拡大するなら、法線は向きを変えません。
$\\frac{1}{s}$ 倍しても向きは同じで、正規化すれば元に戻るからです。

問題になるのは**非一様な拡大**（\`scale.set(2, 1, 1)\` のように、軸ごとに違う倍率）だけです。
回転と移動だけなら、そのままの行列で正しく変換できます
（回転行列は逆行列が転置なので、逆転置すると元に戻ります）。

**「陰影がおかしい」と思ったら、まず非一様な拡大を疑う。**
`,
    },
    {
      kind: 'md',
      text: `
## three は自動でやってくれる

安心してよいのは、**three が標準のマテリアルではこれを自動でやっている**ことです。
\`normalMatrix\` という uniform がシェーダに渡されており、
その中身がまさに逆転置です。

自分で気にする必要があるのは、次の場合です。

- **ShaderMaterial を自分で書くとき** … \`normalMatrix\` を使う
- **法線を CPU 側で計算するとき** … \`applyNormalMatrix()\` を使う
- **InstancedMesh で非一様に拡大するとき** … インスタンスごとの法線が狂うことがある

3 つ目は引っかかりやすいところです。
**インスタンスごとに違う非一様拡大をかけると、three も面倒を見きれません。**
`,
    },
    {
      kind: 'code',
      title: '法線行列を使う',
      code: `import * as THREE from 'three';

// 行列から法線行列を作る（逆転置をやってくれる）
const normalMatrix = new THREE.Matrix3().getNormalMatrix(mesh.matrixWorld);

// 法線に適用する（正規化もされる）
const n = new THREE.Vector3(0, 1, 0);
n.applyNormalMatrix(normalMatrix);

// 誤り：頂点と同じ行列を使う
// n.transformDirection(mesh.matrixWorld);   // 非一様拡大があると狂う

// シェーダの中では uniform として渡ってきている
//   vNormal = normalize(normalMatrix * normal);
//   ↑ three が自動で用意している`,
    },
    {
      kind: 'md',
      text: `
## ここまでが行列です

8 章で手に入れたものを並べます。

- **装置としての行列** … 点を入れると動いた点が返る
- **行ごとの内積** … 中でやっている計算
- **列は軸の行き先** … 行列を読むいちばんの見方
- **同次座標** … 4×4 の理由と、点と方向の区別
- **掛ける順番** … 右から効く
- **TRS** … three が内部でやっている合成
- **逆行列** … 巻き戻す。ワールドとローカルの行き来
- **法線行列** … 拡大したときの手当て

**次からは回転です。** 行列でも回転は表せますが、
「2 つの向きのあいだを補間する」といった操作には向きません。
そこで別の表し方（クォータニオン）が出てきます。
`,
    },
  ],
  exercises: [
    {
      prompt: `物体を \`scale.set(3, 1, 1)\` にしました。
法線に掛けるべき倍率を、軸ごとに答えてください。`,
      hint: '逆数を掛けます。',
      answer: `- x … $\\frac{1}{3}$ 倍
- y … 1 倍（そのまま）
- z … 1 倍（そのまま）

**頂点を伸ばした方向を、法線では縮めます。**
面が寝るぶん、法線は立つ ― その関係が逆数として出ています。

なお倍率を掛けたあとは**正規化が必要**です。
$\\frac{1}{3}$ 倍したことで長さが 1 でなくなるので、
そのまま明るさの計算に使うと暗くなりすぎます。`,
    },
    {
      prompt: `\`scale.setScalar(5)\`（一様に 5 倍）にした物体では、法線行列を使わなくても陰影が正しく出ます。
なぜでしょう。`,
      hint: '逆数を全成分に掛けると、向きはどうなりますか。',
      answer: `全成分に $\\frac{1}{5}$ を掛けても、**向きは変わらない**からです。
これはただのスカラー倍（[](#/ch/b15-vector-scale)）で、
長さが $\\frac{1}{5}$ になるだけです。

そして法線は使う前に正規化するので、**長さの違いも消えます**。
結果として、法線行列を使っても使わなくても同じ向きになります。

**問題は「軸ごとに倍率が違う」ときだけ**です。
そのときだけ、向きが実際に変わってしまいます。

逆に言えば、一様な拡大で済ませられるなら、この問題は起きません。
非一様な拡大を避けられるなら、避けるのがいちばん安全です。`,
    },
    {
      prompt: `シェーダを自分で書いていて、\`vNormal = normalize(modelMatrix * vec4(normal, 0.0)).xyz;\`
と書きました。何が問題で、どう直しますか。`,
      hint: '`modelMatrix` は頂点用の行列です。法線には何を使うべきでしたか。',
      answer: `**頂点用の行列をそのまま法線に使っています。**
非一様な拡大が入ると、法線が面に垂直でなくなり、陰影が狂います。

正しくは \`normalMatrix\` を使います。
three が自動で用意している uniform で、中身は
**モデルビュー行列の左上 3×3 の逆転置**です。

なお \`vec4(normal, 0.0)\` として 4 つ目を 0 にしているのは正しい判断です
（[](#/ch/m04-homogeneous)の「方向は 0」）。
平行移動は正しく無視できています。**間違っているのは拡大の扱いだけ**です。`,
      answerCode: `// 誤り：非一様な拡大で法線が面に垂直でなくなる
vNormal = normalize((modelMatrix * vec4(normal, 0.0)).xyz);

// 正しい：three が用意している normalMatrix を使う
vNormal = normalize(normalMatrix * normal);`,
    },
  ],
  quiz: [
    {
      q: '法線を変換するときに使うべき行列はどれですか。',
      choices: [
        '元の行列の逆行列を転置したもの',
        '元の行列そのまま',
        '元の行列の転置',
        '単位行列',
      ],
      answer: 0,
      explain:
        '「面と法線が直交している」という関係を保つ条件を解くと、逆転置が出てきます。拡大だけの場合は「頂点を s 倍したら法線は 1/s 倍」と直感的に言い換えられます。',
    },
    {
      q: '法線行列が必要になるのは、どんな変換のときですか。',
      choices: [
        '軸ごとに倍率が違う拡大（非一様な拡大）',
        '平行移動',
        '一様な拡大',
        '回転',
      ],
      answer: 0,
      explain:
        '回転と移動だけなら、そのままの行列で正しく変換できます。一様な拡大も、正規化すれば向きが変わりません。狂うのは非一様な拡大のときだけです。',
    },
    {
      q: '「形は正しいのに、陰影だけがおかしい」とき、まず疑うべきはどれですか。',
      choices: [
        '非一様な拡大がかかっていないか',
        'ライトの色',
        'カメラの位置',
        'ジオメトリの頂点数',
      ],
      answer: 0,
      explain:
        '頂点は正しい行列で変換されるので形は合いますが、法線の扱いを間違えると明るさだけが狂います。`scale` に軸ごとに違う値が入っていないか確かめてください。',
    },
  ],
};
