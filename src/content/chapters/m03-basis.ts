import type { Chapter } from '../types.ts';

export const chapterM03: Chapter = {
  slug: 'm03-basis',
  part: 'math3d',
  number: 3,
  title: '行列の列は「軸の行き先」',
  goal: '行列を見ただけで何をする変換か読めるようになり、行列式が面積（体積）の倍率であることが分かります。',
  requires: ['m02-matrix-vector'],
  threeApis: ['Matrix4.extractBasis', 'Matrix4.makeBasis', 'Matrix4.determinant'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章の演習で、こんなことが起きました。

- $(1,\\; 0)$ を掛けると、行列の **1 列目**が出てきた
- $(0,\\; 1)$ を掛けると、行列の **2 列目**が出てきた

これは偶然ではありません。**行列の列は、もとの軸が行く先そのもの**です。

この見方を手に入れると、行列が「16 個の数の羅列」から
「軸をどこへ運ぶかの指示」に変わります。**この章がいちばん効きます。**
`,
    },
    {
      kind: 'md',
      text: `
## なぜ列が出てくるのか

$(1,\\; 0)$ を掛けるとき、行ごとの内積を取ります。
2 つ目の成分が 0 なので、**2 列目は全部 0 と掛かって消えます**。
残るのは 1 列目に 1 を掛けたもの ― つまり 1 列目そのものです。

同じ理屈で、$(0,\\; 1)$ を掛ければ 2 列目が出ます。

つまり行列は、**「x 軸をここへ、y 軸をここへ、z 軸をここへ運びます」という表**です。
列を 3 本並べただけのもの、と読めます。
`,
    },
    {
      kind: 'formula',
      tex: 'M\\mathbf{v} = v_x\\,\\mathbf{i} + v_y\\,\\mathbf{j} + v_z\\,\\mathbf{k}',
      readAloud:
        'i、j、k は行列の 1 列目・2 列目・3 列目です。入力ベクトルの各成分を、対応する列の本数として足し合わせる ― それが行列を掛けるということです。',
      worked: {
        given: '$\\mathbf{i} = (1,\\; 0)$、$\\mathbf{j} = (0.6,\\; 1.4)$ という行列に、点 $(2,\\; 1)$ を掛けます。',
        steps: [
          { calc: '2 本ぶんの i : 2 x (1, 0)     = (2, 0)' },
          { calc: '1 本ぶんの j : 1 x (0.6, 1.4) = (0.6, 1.4)' },
          { calc: '足す         : (2 + 0.6, 0 + 1.4) = (2.6, 1.4)' },
        ],
        result:
          '$(2,\\; 1) \\to (2.6,\\; 1.4)$。**入力の成分は「その軸を何本ぶん進むか」**でした。軸そのものが動いたので、進む先も一緒にずれた ― それだけのことです。',
      },
    },
    {
      kind: 'demo',
      id: 'basis-vectors',
      caption:
        'i と j を動かすと、格子ごと変形します。薄い格子が変換前、明るい格子が変換後です。点 p が「i を px 本、j を py 本」で決まる場所へ運ばれることを確かめてください。j を i と同じ向きに重ねると、面積の倍率（行列式）が 0 になります。',
    },
    {
      kind: 'md',
      text: `
## 行列を読めるようになる

この見方で、行列の中身が読めるようになります。

- **列がもとの軸のまま** … 何もしない行列（単位行列）
- **列が長くなっただけ** … 拡大
- **列が回っただけ（長さは 1 のまま）** … 回転
- **列が斜めに倒れている** … せん断
- **2 本の列が同じ向き** … 面がつぶれる。**もとに戻せない**

最後のものが大事です。2 本の列が平行だと、
すべての点が 1 本の線の上に潰れてしまいます。
[](#/ch/b30-cross-area)で「平行だと外積が消える」と言ったのと同じ状況です。
`,
    },
    {
      kind: 'md',
      text: `
## 行列式 ― 面積が何倍になるか

2 本の列が張る平行四辺形の面積を{{行列式}}と呼びます。
これが「その変換で面積が何倍になるか」を表します。

- **2** … 面積が 2 倍になる変換
- **1** … 面積が変わらない（回転はこれ）
- **0** … つぶれる。**逆変換が存在しない**
- **マイナス** … 裏返る（鏡に映したような変換）

3D なら体積の倍率です。

**0 になるかどうかが、実用上いちばん大事**です。
0 の行列は「戻せない」ので、逆行列が作れません。
拡大率に 0 を渡してしまったとき（\`scale.set(1, 0, 1)\` など）にこれが起きます。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '外積との関係',
      text: `
2D の行列式は $i_x j_y - i_y j_x$ で、これは[](#/ch/b32-cross-side)で左右の判定に使った式そのものです。
外積の z 成分と同じ形をしています。

3D の行列式も、3 本の列で作った平行六面体の体積になります。
**「面積・体積・向き」を 1 つの数で表す**という点で、外積と地続きの道具です。
`,
    },
    {
      kind: 'code',
      title: '列を取り出す・列から作る',
      code: `import * as THREE from 'three';

// 行列から 3 本の軸（列）を取り出す
const i = new THREE.Vector3();
const j = new THREE.Vector3();
const k = new THREE.Vector3();
matrix.extractBasis(i, j, k);

// 逆に、3 本の軸から行列を作る
const m = new THREE.Matrix4().makeBasis(i, j, k);

// 体積の倍率
matrix.determinant();   // 0 なら逆行列が作れない

// 物体の「前」「上」「右」を知るのも、実は列を読むこと
const forward = new THREE.Vector3();
mesh.getWorldDirection(forward);   // 3 列目（の符号違い）に相当する`,
    },
    {
      kind: 'md',
      text: `
## 「新しい軸」で見ると、階層も読める

この見方は、親子関係を扱うときに効いてきます。

親の行列は「子の座標系の軸が、世界のどこを向いているか」を持っています。
だから子の座標に親の行列を掛けると、世界での位置になる ―
**軸ごと運ばれるから**です。

腕を上げると手も一緒に上がるのは、
「手が乗っている座標系の軸そのものが動いた」からだ、と読めます。

この話は階層の章で正面から扱いますが、
**その章の内容は、この「列は軸」という 1 つの見方で全部説明できます。**
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの行列が何をする変換か、**列を見て**答えてください。

1. $\\begin{pmatrix} 1 & 0 \\\\ 0 & 1 \\end{pmatrix}$
2. $\\begin{pmatrix} 0 & 1 \\\\ 1 & 0 \\end{pmatrix}$
3. $\\begin{pmatrix} -1 & 0 \\\\ 0 & 1 \\end{pmatrix}$`,
      hint: '1 列目が「x 軸の行き先」、2 列目が「y 軸の行き先」です。',
      answer: `1. x 軸が $(1,0)$、y 軸が $(0,1)$ ― **何も動かない**（単位行列）
2. x 軸が $(0,1)$、y 軸が $(1,0)$ ― **x と y が入れ替わる**。$y = x$ の線で鏡に映す変換
3. x 軸が $(-1,0)$、y 軸はそのまま ― **左右が反転する**（y 軸で鏡に映す）

2 と 3 はどちらも**鏡映**で、行列式がマイナスになります
（2 は $0 \\times 0 - 1 \\times 1 = -1$、3 は $-1 \\times 1 - 0 = -1$）。

行列式がマイナスの変換を適用すると、**面の表裏が入れ替わります**。
モデルを鏡映したときに裏返って見えるのは、これが理由です。`,
    },
    {
      prompt: `$\\mathbf{i} = (2,\\; 0)$、$\\mathbf{j} = (1,\\; 3)$ の行列について手で計算してください。

1. 点 $(1,\\; 1)$ の行き先
2. 行列式（面積の倍率）`,
      hint: '1 は「i を 1 本、j を 1 本」足します。2 は $i_x j_y - i_y j_x$ です。',
      answer: `1. $(2, 0) + (1, 3) = $ **$(3,\\; 3)$**
2. $2 \\times 3 - 0 \\times 1 = $ **6**

行列式が 6 なので、この変換は**面積を 6 倍にします**。
もとの単位正方形（面積 1）が、i と j の張る平行四辺形（面積 6）になった、ということです。

デモで同じ値を入れて、面の大きさが 6 マスぶんになることを確かめられます。`,
    },
    {
      prompt: `\`mesh.scale.set(1, 0, 1)\` と書いてしまいました。
行列式はいくつになりますか。そして、どんな不具合が起きますか。`,
      hint: '3D の行列式は 3 本の列が張る体積です。1 本が長さ 0 になったら？',
      answer: `行列式は **0** です。y 軸の行き先が長さ 0 になり、体積がつぶれます。

起きることは 3 つあります。

1. **物体が平面に潰れる**（見た目には「消えた」ように見えることも）
2. **逆行列が作れない** … 潰れた情報は戻せません。
   ワールド座標からローカル座標への変換が壊れます
3. **法線が壊れる** … 法線の計算に逆行列が要るので、陰影がおかしくなります
   （[](#/ch/m08-normal-matrix)で扱います）

three は行列式 0 のときに逆行列の計算で警告を出します。
**「scale に 0 を入れない」** ― 非表示にしたいなら \`visible = false\` を使ってください。`,
    },
  ],
  quiz: [
    {
      q: '行列の「1 列目」は何を表していますか。',
      choices: [
        'もとの x 軸が移る先',
        '出力の x 成分の作り方',
        '平行移動の量',
        '拡大率',
      ],
      answer: 0,
      explain:
        '$(1, 0, 0)$ を掛けると 1 列目がそのまま出てきます。行列は「各軸をどこへ運ぶか」を列として並べた表だ、と読めます。行は出力の成分の作り方に対応します。',
    },
    {
      q: '行列式が 0 になるのは、どんなときですか。',
      choices: [
        '列が平行になって、面（体積）がつぶれるとき',
        '回転しているとき',
        '平行移動が含まれるとき',
        '行列が単位行列のとき',
      ],
      answer: 0,
      explain:
        'つぶれた変換は情報を失うので、逆行列が存在しません。`scale` に 0 を渡したときに起き、逆変換と法線の計算が壊れます。単位行列の行列式は 1 です。',
    },
    {
      q: '行列式がマイナスの変換を適用すると、何が起きますか。',
      choices: [
        '鏡に映したように裏返り、面の表裏が入れ替わる',
        '物体が消える',
        '拡大率が負になる',
        '何も起きない',
      ],
      answer: 0,
      explain:
        '符号は「向きが保たれるか」を表します。マイナスは反転で、頂点の並び順が逆になるため面が裏返ります。モデルを鏡映したときに黒くなるのはこれが原因です。',
    },
  ],
};
