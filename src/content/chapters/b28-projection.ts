import type { Chapter } from '../types.ts';

export const chapterB28: Chapter = {
  slug: 'b28-projection',
  part: 'basics',
  number: 28,
  title: '射影 ― ある向きに落とした影の長さ',
  goal: 'ベクトルを別の向きに落とす計算ができるようになり、壁に沿って滑る移動や、成分を取り除く操作が書けるようになります。',
  requires: ['b25-dot-angle'],
  threeApis: ['Vector3.projectOnVector', 'Vector3.projectOnPlane', 'Vector3.dot'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

内積の最後の顔です。ここまで「揃い具合を測る道具」として使ってきましたが、
内積にはもう 1 つ、**長さを取り出す**という読み方があります。

これが分かると、こんなことが書けるようになります。

- 壁にぶつかったとき、**止まらずに壁に沿って滑る**
- 坂道での速度を「坂に沿う成分」と「坂に垂直な成分」に分ける
- 高さを無視して、床の上の向きだけで判定する

どれも「ベクトルから、ある向きの成分だけを取り出す（または取り除く）」操作です。
`,
    },
    {
      kind: 'md',
      text: `
## 影の長さとしての内積

長さ 1 のベクトル $\\hat{\\mathbf{u}}$ の向きに、
真上から光を当てて $\\mathbf{v}$ の影を落とすところを想像してください。
その影の長さが、**$\\mathbf{v} \\cdot \\hat{\\mathbf{u}}$** です。

$\\|\\mathbf{v}\\|\\|\\hat{\\mathbf{u}}\\|\\cos\\theta$ で $\\|\\hat{\\mathbf{u}}\\| = 1$ なので、
$\\|\\mathbf{v}\\|\\cos\\theta$ ― まさに直角三角形の「底辺の長さ」です。

- $\\theta$ が小さい（向きが近い）… 影は長い
- $\\theta$ が 90 度 … 影の長さは 0
- $\\theta$ が 90 度より大きい … 影は**反対側に伸びる**ので、値がマイナスになる

3 つ目が大事です。**射影の長さは負になり得ます**。
「反対向きに $\\|$値$\\|$ だけ」という意味で、符号に情報が入っています。
`,
    },
    {
      kind: 'formula',
      tex: '\\operatorname{proj}_{\\mathbf{u}}\\mathbf{v} = (\\mathbf{v} \\cdot \\hat{\\mathbf{u}})\\,\\hat{\\mathbf{u}}',
      readAloud:
        'v を u の向きに落としたベクトルは、影の長さ（v と単位ベクトル u の内積）に、単位ベクトル u を掛けたものです。まず長さを出して、それを向きに掛け直している、と読んでください。',
      worked: {
        given:
          '$\\mathbf{v} = (3,\\; 4,\\; 0)$ を、$\\hat{\\mathbf{u}} = (1,\\; 0,\\; 0)$（x 軸の向き）に落とします。',
        steps: [
          { calc: '影の長さ = v・u = 3x1 + 4x0 + 0x0 = 3', note: 'まず内積で長さを出す' },
          { calc: '向きに掛け直す = 3 x (1, 0, 0)' },
          { calc: '            = (3, 0, 0)', note: 'これが x 軸に落とした影' },
          { calc: '残り = v - (3, 0, 0) = (0, 4, 0)', note: '引くと、垂直な成分だけが残る' },
        ],
        result:
          '**$(3, 0, 0)$ が x 方向の成分、$(0, 4, 0)$ が残り**です。この 2 本を足すと元の $\\mathbf{v}$ に戻り、しかも**互いに直交**しています。1 本のベクトルを、指定した向きと、それに垂直な向きに分解できました。',
      },
    },
    {
      kind: 'demo',
      id: 'dot-angle',
      caption:
        'こんどは「b の影」に注目してください。b を a の向きに落としたものが射影です。角度を 90 度より大きくすると、影が反対側へ伸びて長さがマイナスになることを確かめてください。',
    },
    {
      kind: 'md',
      text: `
## 引くと、その成分が消える

射影のいちばん実用的な使い方は、**足すことではなく引くこと**です。

$\\mathbf{v} - \\operatorname{proj}_{\\mathbf{u}}\\mathbf{v}$ を計算すると、
**$\\mathbf{u}$ 方向の成分が完全に消えたベクトル**が残ります。

これが「壁に沿って滑る」の正体です。

1. 壁の{{法線}}（壁に垂直な向き）を $\\mathbf{n}$ とする
2. 移動しようとしたベクトル $\\mathbf{v}$ から、$\\mathbf{n}$ 方向の成分を引く
3. 残ったベクトルは、**壁にめり込まない方向**だけを持っている

壁に正面からぶつかれば残りは 0（止まる）、斜めにぶつかれば横成分が残って滑る ―
**場合分けを 1 つも書かずに、自然な動きになります。**
`,
    },
    {
      kind: 'code',
      title: '壁に沿って滑る',
      code: `import * as THREE from 'three';

// 壁の法線（長さ 1）
const n = new THREE.Vector3(1, 0, 0);

// 進もうとしていた移動量
const move = new THREE.Vector3(-2, 0, 1);

// 法線方向の成分を取り除く ＝ 壁にめり込む成分を消す
const slide = move.clone().projectOnPlane(n);   // (0, 0, 1)

player.position.add(slide);

// 手で書くとこうなる（projectOnPlane が中でやっていること）
const along = n.clone().multiplyScalar(move.dot(n));  // (-2, 0, 0)
const slide2 = move.clone().sub(along);               // (0, 0, 1)

// ある向きの成分だけを取り出したいとき
move.clone().projectOnVector(n);   // (-2, 0, 0)`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '落とす先は、必ず長さ 1 にする',
      text: `
$\\hat{\\mathbf{u}}$ が長さ 1 でないと、影の長さが $\\|\\mathbf{u}\\|$ 倍になります。
長さ 2 のベクトルに落とすと、影も 2 倍の長さで返ってきます。

three の \`projectOnVector\` は内部で長さを補正してくれますが、
自分で $(\\mathbf{v}\\cdot\\mathbf{u})\\mathbf{u}$ と書くときは正規化が必須です。
`,
    },
    {
      kind: 'md',
      text: `
## 高さを無視するのも、射影

前の章で「上下を無視して床の上の向きだけで判定したい」という話をしました。
あれもこの操作です。

$\\mathbf{v}$ から y 方向の成分（$(0, 1, 0)$ への射影）を引けば、
**床に落とした影**が残ります。あとはそれを正規化して内積を取ればいい。

コードで \`v.y = 0\` と書くのと同じ結果になりますが、
「y 方向の成分を取り除いた」と読めるぶん、
**任意の向きに一般化できる**のが射影の書き方の利点です。
床が傾いていても同じコードで動きます。
`,
    },
    {
      kind: 'md',
      text: `
## 内積の 5 つの顔

ここまでで内積を 5 章ぶん見てきました。まとめておきます。

- **成分を掛けて足す** … 計算のしかた
- **長さ × 長さ × cos** … 角度との関係
- **符号** … 前か後ろか、直交しているか
- **明るさ** … 面がどれだけ光を受けるか
- **影の長さ** … ある向きの成分を取り出す

**全部同じ 1 つの計算**です。掛けて足すだけ、という拍子抜けするほど単純な操作に、
これだけの読み方が詰まっています。

次からは外積に進みます。内積が「数を 1 つ返す」計算だったのに対し、
外積は「新しい矢印を返す」計算です。
`,
    },
  ],
  exercises: [
    {
      prompt: `$\\mathbf{v} = (5,\\; 2,\\; 0)$ を、$\\hat{\\mathbf{u}} = (0,\\; 1,\\; 0)$ に落としてください。

1. 影の長さ
2. 射影ベクトル
3. 残り（$\\mathbf{v}$ から射影を引いたもの）`,
      hint: 'まず内積で長さを出し、それを u に掛けます。',
      answer: `1. $5 \\times 0 + 2 \\times 1 + 0 = $ **2**
2. $2 \\times (0, 1, 0) = $ **$(0,\\; 2,\\; 0)$**
3. $(5, 2, 0) - (0, 2, 0) = $ **$(5,\\; 0,\\; 0)$**

2 と 3 を足すと元の $\\mathbf{v}$ に戻ります。そして
$(0, 2, 0) \\cdot (5, 0, 0) = 0$ なので、**この 2 本は直交**しています。

「y 方向の成分」と「それ以外」に、きれいに分かれました。`,
    },
    {
      prompt: `壁の法線が $\\mathbf{n} = (1,\\; 0,\\; 0)$（長さ 1）です。
移動量 $\\mathbf{v} = (-3,\\; 0,\\; 4)$ で壁に向かって進もうとしたとき、
**実際に進める移動量**を手で計算してください。`,
      hint: '法線方向の成分を取り除きます。',
      answer: `1. **影の長さ** … $\\mathbf{v} \\cdot \\mathbf{n} = -3$
2. **法線方向の成分** … $-3 \\times (1, 0, 0) = (-3, 0, 0)$
3. **引く** … $(-3, 0, 4) - (-3, 0, 0) = $ **$(0,\\; 0,\\; 4)$**

壁に向かう $-3$ のぶんが消え、**壁に沿う 4 のぶんだけが残りました**。
これが「壁ずり」の動きです。

もし真正面からぶつかっていたら（$\\mathbf{v} = (-3, 0, 0)$）、
残りは $(0, 0, 0)$ になって止まります。
**場合分けを書かなくても、正面なら止まり、斜めなら滑る**のがこの計算の良さです。`,
      answerCode: `const n = new THREE.Vector3(1, 0, 0);
const v = new THREE.Vector3(-3, 0, 4);

const slide = v.clone().projectOnPlane(n);   // (0, 0, 4)`,
    },
    {
      prompt: `射影を \`(v.dot(u)) * u\` の形で自分で書いたところ、
**結果が思ったより長く**なりました。\`u\` は $(0,\\; 3,\\; 0)$ です。
原因と直し方を答えてください。`,
      hint: '式が前提にしていたのは、どんな u でしたか。',
      answer: `\`u\` の長さが 3 で、**長さ 1 ではない**からです。

$(\\mathbf{v}\\cdot\\mathbf{u})\\mathbf{u}$ の式は $\\mathbf{u}$ が単位ベクトルであることを前提にしています。
長さ 3 だと、内積で 3 倍、掛け直しでもう 3 倍かかり、**合計 9 倍**の長さになります。

直し方は 2 つあります。

1. 先に正規化する … \`const uh = u.clone().normalize();\`
2. three に任せる … \`v.clone().projectOnVector(u)\` は長さを内部で補正してくれます

「向きを表す値は長さ 1 に」という習慣が、ここでも効いています。`,
      answerCode: `// 誤り：u の長さが 1 でないと 9 倍になる
const proj = u.clone().multiplyScalar(v.dot(u));

// 正しい（自分で書くなら）
const uh = u.clone().normalize();
const proj2 = uh.clone().multiplyScalar(v.dot(uh));

// three に任せる
const proj3 = v.clone().projectOnVector(u);`,
    },
  ],
  quiz: [
    {
      q: '$\\mathbf{v}$ を単位ベクトル $\\hat{\\mathbf{u}}$ に落としたときの「影の長さ」はどれですか。',
      choices: [
        '$\\mathbf{v} \\cdot \\hat{\\mathbf{u}}$',
        '$\\|\\mathbf{v}\\| \\times \\|\\hat{\\mathbf{u}}\\|$',
        '$\\mathbf{v} \\times \\hat{\\mathbf{u}}$',
        '$\\|\\mathbf{v}\\| - 1$',
      ],
      answer: 0,
      explain:
        '内積そのものです。$\\|v\\|\\|u\\|\\cos\\theta$ で $\\|u\\| = 1$ なので $\\|v\\|\\cos\\theta$ になり、これは直角三角形の底辺の長さにあたります。90 度を超えると負になります。',
    },
    {
      q: '壁に沿って滑らせるには、移動ベクトルから何を引きますか。',
      choices: [
        '壁の法線方向の成分',
        '壁に沿う方向の成分',
        '移動ベクトルの長さ',
        '重力の成分',
      ],
      answer: 0,
      explain:
        '法線方向の成分が「壁にめり込む」ぶんです。それを引くと壁に沿う成分だけが残ります。正面からぶつかれば残りは 0 になって止まり、斜めなら滑るので、場合分けが要りません。',
    },
    {
      q: '$(\\mathbf{v}\\cdot\\mathbf{u})\\mathbf{u}$ という式が正しく働くのはどんなときですか。',
      choices: [
        '$\\mathbf{u}$ の長さが 1 のとき',
        '$\\mathbf{v}$ の長さが 1 のとき',
        '2 本が直交しているとき',
        'いつでも正しい',
      ],
      answer: 0,
      explain:
        '$\\mathbf{u}$ が長さ 1 でないと、内積と掛け直しで二重に長さが掛かります。長さ 3 なら 9 倍になります。three の `projectOnVector` は内部で補正してくれます。',
    },
  ],
};
