import type { Chapter } from '../types.ts';

export const chapterX06: Chapter = {
  slug: 'x06-value-noise',
  part: 'project',
  number: 6,
  title: 'value noise ― 格子に乱数を置いて、あいだを埋める',
  goal: 'ノイズ関数を自分で書けるようになり、ハッシュ・補間・なめらかさの 3 つがそれぞれ何を担当しているかを説明できるようになります。',
  requires: ['p02-planet-surface', 'b41-noise'],
  threeApis: ['CanvasTexture', 'Texture.colorSpace', 'MeshBasicMaterial', 'PlaneGeometry', 'OrthographicCamera'],
  mathRecall: [
    { slug: 'b41-noise', note: '格子に乱数を置いて、あいだを補間する' },
    { slug: '08-interp', note: 'mix（lerp） ― 2 つの値を割合で混ぜる' },
    { slug: 'b36-smoothstep', note: '両端で傾きが 0 になる曲線' },
    { slug: 'b39-seed', note: '同じ入力なら、いつも同じ乱数' },
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## 高さを作る関数を、自分で書く

[](#/ch/p02-planet-surface)で、地表は「高さ $1$ つ」から導くと決めました。
その高さを作るのが**ノイズ**です。

[](#/ch/b41-noise)で、ノイズの作り方は $2$ 手だと言いました。

1. **格子の交点にだけ、乱数を置く**
2. **交点と交点のあいだは、なめらかに補間する**

**この章で、それを実際に書きます。** ライブラリは入れません ―
$40$ 行ほどで書けるものに依存を $1$ つ増やすのは割に合わないからです。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'value noise を選ぶ理由',
      text: `
ノイズにはいくつか種類があります。よく名前を聞くのは 3 つです。

- **value noise** … 格子点に**値**を置いて補間する。いちばん単純
- **Perlin noise** … 格子点に**勾配（向き）**を置く。value より自然だが、実装が長い
- **simplex noise** … 格子を三角形にしたもの。次元が上がっても速いが、いちばん難しい

**この作品では value noise で足ります。** 惑星の地表は
「重ねる」ほうで自然さを稼ぐので、1 枚ぶんの質の差は最後まで見えません。

いちばん短くて、いちばん読めて、いちばん直せるものを選びます。
足りなくなったら差し替えればよく、**呼び出し側は 1 文字も変わりません。**
`,
    },
    {
      kind: 'md',
      text: `
## 格子に乱数を置く ― 配列は持たない

「格子の交点に乱数を置く」と言われると、素直には**配列に入れて覚えておく**ことを考えます。

$2$ 次元の小さなノイズならそれでいいのですが、いまは $3$ 次元です。

いちばん細かい段では、格子が $1$ 辺 $71$ 個ぶんに広がります。
$71^3 = 357911$ 個。全部の段を合わせればもっと要ります。
しかも**あらかじめ範囲を決めておかないと配列が作れません。**

そこで、覚えるのをやめます。**整数の座標を入れると値が返る関数**を書けば、
配列は $1$ つも要らず、範囲の制限も消えます。

- 同じ $(x, y, z)$ を何度聞いても、**必ず同じ値**が返る
- 隣の格子点とは**まったく無関係**な値が返る
- 覚えておく必要がない（メモリ $0$）

これが{{ハッシュ}}です。[](#/ch/b39-seed)のシードつき乱数と同じ考え方で、
違うのは「何番目か」ではなく「どこか」を種にするところだけです。
`,
    },
    {
      kind: 'code',
      title: '座標から乱数を作る',
      code: `// 整数の (x, y, z) と seed を入れると、0〜1 の値が返る。
// 同じ入力なら必ず同じ値。隣とは無関係。覚えておくものは何もない
function hash3(x, y, z, seed) {
  let h = Math.imul(x, 374761393) + Math.imul(y, 668265263) + Math.imul(z, 1274126177);
  h = Math.imul(h + Math.imul(seed, 2246822519), 2654435761);
  h ^= h >>> 15;                 // 上位ビットを下位に混ぜる
  h = Math.imul(h, 2246822519);
  h ^= h >>> 13;
  return (h >>> 0) / 4294967295; // 符号なし32ビットにしてから 0〜1 へ
}`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'なぜ Math.imul なのか ― ふつうの掛け算では壊れる',
      text: `
やりたいのは「$32$ ビット整数どうしの掛け算」です。ところが JavaScript の \`*\` は
**倍精度の実数の掛け算**なので、答えが $2^{53}$ を超えると**下位のビットが落ちます。**

\`374761393 * 123456789\` は $46266838220947080$ ― $2^{53} \\approx 9.007 \\times 10^{15}$ を超えています。

- \`(374761393 * 123456789) | 0\` → **$-1190510968$**
- \`Math.imul(374761393, 123456789)\` → **$-1190510971$**

$3$ ずれています。**ずれているのは、いちばん下のビットです。**

ハッシュは下位ビットの混ざり具合が命なので、ここが壊れると
「隣どうしで似た値が返る」ようになり、**格子が模様として見えてきます。**

\`Math.imul\` は「$32$ ビットの掛け算を、$32$ ビットのまま返す」ための関数です。
ハッシュを書くときは必ずこちらを使ってください。

最後の \`>>> 0\` も同じ理由です。\`>>\` だと符号が残って負の数が返り、
$0$〜$1$ に収まりません。\`>>>\` は**符号なし**として扱います。
`,
    },
    {
      kind: 'md',
      text: `
## あいだを埋める ― 2 次元なら 4 点、3 次元なら 8 点

格子点の値は手に入りました。次は**格子の内側**です。

$2$ 次元で考えます。点 $(2.3,\\; 1.6)$ の値がほしいとき、
まわりの格子点は $(2,1)$・$(3,1)$・$(2,2)$・$(3,2)$ の $4$ つです。

やることは[](#/ch/08-interp)の mix を $3$ 回だけ。

1. **下の辺**を横に混ぜる（$(2,1)$ と $(3,1)$ を、$x$ の端数で）
2. **上の辺**を横に混ぜる（$(2,2)$ と $(3,2)$ を、同じ端数で）
3. その $2$ つを**縦に混ぜる**（$y$ の端数で）

$3$ 次元も同じ形です。格子点が $8$ つに増え、
$x$ で $4$ 回・$y$ で $2$ 回・$z$ で $1$ 回、**合わせて $7$ 回**混ぜます。
`,
    },
    {
      kind: 'formula',
      tex: 'n = \\mathrm{mix}\\big(\\mathrm{mix}(c_{00}, c_{10}, u),\\; \\mathrm{mix}(c_{01}, c_{11}, u),\\; v\\big)',
      readAloud:
        '$c_{00}$ から $c_{11}$ が、まわりの 4 つの格子点の値です。まず下の辺と上の辺をそれぞれ $u$ で横に混ぜ、その 2 つを $v$ で縦に混ぜる。$u$ と $v$ は座標の「端数」を、このあと出てくる fade に通したものです。',
      worked: {
        given:
          '点 $(2.3,\\; 1.6)$。まわりの 4 点の値が $c_{00} = 0.2$（$(2,1)$）、$c_{10} = 0.8$（$(3,1)$）、$c_{01} = 0.5$（$(2,2)$）、$c_{11} = 0.4$（$(3,2)$）だったとします。',
        steps: [
          { calc: '端数 : x は 0.3、y は 0.6' },
          { calc: 'u = fade(0.3) = 0.216', note: '次の節で出す曲線に通す' },
          { calc: 'v = fade(0.6) = 0.648' },
          { calc: '下辺 : 0.2 + (0.8 - 0.2) x 0.216 = 0.3296' },
          { calc: '上辺 : 0.5 + (0.4 - 0.5) x 0.216 = 0.4784' },
          { calc: '縦   : 0.3296 + (0.4784 - 0.3296) x 0.648' },
          { calc: '     = 0.3296 + 0.1488 x 0.648 = 0.4260' },
        ],
        result:
          '**$0.4260$**。$4$ つの値のどれでもなく、そのあいだのどこかに落ちています。**$3$ 回の mix しかしていません** ― ノイズが安いのはこのためです。$3$ 次元でも $7$ 回で済みます。',
      },
    },
    {
      kind: 'md',
      text: `
## 端数をそのまま使うと、格子が見える

さて、上の計算で端数を $0.3$ ではなく $\\mathrm{fade}(0.3) = 0.216$ に通しました。**なぜでしょう。**

端数をそのまま使う（＝直線で補間する）と、値そのものはちゃんとつながります。
$1$ つの格子点を挟んだ左右の式は、その点でぴったり同じ値になるからです。

**つながらないのは、傾きのほうです。**

$1$ 次元で、格子点の値が $x=0$ で $0.2$、$x=1$ で $0.8$、$x=2$ で $0.3$ だとします。

- $0 < x < 1$ の傾きは、ずっと $0.8 - 0.2 = 0.6$
- $1 < x < 2$ の傾きは、ずっと $0.3 - 0.8 = -0.5$

$x = 1$ で、傾きが $0.6$ から $-0.5$ へ**飛びます。**

人の目は明るさそのものより**明るさの変化**に敏感なので、
この折れ目が**格子の線としてはっきり見えます。**
`,
    },
    {
      kind: 'formula',
      tex: "\\mathrm{fade}(t) = t^2(3 - 2t), \\qquad \\mathrm{fade}'(t) = 6t(1 - t)",
      readAloud:
        'fade は 0 から 1 へ「ゆっくり出て、ゆっくり着く」曲線です。右の式は傾きで、$t = 0$ でも $t = 1$ でも 0 になります。[](#/ch/b36-smoothstep)の smoothstep とまったく同じ式です。',
      worked: {
        given: '両端の値と、両端の傾きを確かめます。',
        steps: [
          { calc: 'fade(0) = 0 x (3 - 0)     = 0', note: '格子点では、その格子点の値ぴったり' },
          { calc: 'fade(1) = 1 x (3 - 2)     = 1' },
          { calc: "fade'(0) = 6 x 0 x 1      = 0", note: '入口で傾きが 0' },
          { calc: "fade'(1) = 6 x 1 x 0      = 0", note: '出口でも傾きが 0' },
          { calc: 'fade(0.3) = 0.09 x 2.4    = 0.216' },
          { calc: 'fade(0.5) = 0.25 x 2      = 0.5', note: '真ん中は動かない' },
        ],
        result:
          '**両端で傾きが $0$。** だから格子をまたぐとき、手前の傾きも向こうの傾きも $0$ で、**折れ目が消えます。** 上の例で言えば、$0.6$ から $-0.5$ への飛びが、$0$ から $0$ への連続に変わります。値の上限も下限も変えていない ― 変えたのは**変わり方**だけです。',
      },
    },
    {
      kind: 'md',
      text: `
## 見比べる

理屈より見たほうが早いので、**同じ格子・同じ乱数**で、
補間だけを差し替えた $2$ 枚を並べます。

左が端数をそのまま使ったもの、右が \`fade\` に通したものです。
**等高線を重ねてあります。** 明るさの折れ目より、線の折れ目のほうがずっとよく見えるからです。
左では等高線が格子の境目で**カクッと折れ**、ひし形や星形が並びます。右はどこも丸いままです。
`,
    },
    {
      kind: 'sandbox',
      title: 'fade があるときと、ないとき',
      guide: { focus: ['ハッシュ', '補間 ― ここだけが違う'] },
      code: `import * as THREE from 'three';

const SIZE = 384;      // 1 枚のテクスチャの大きさ
const CELLS = 5;       // 格子は 5 マス。1 マス 77 ピクセル。粗いほうが折れ目は見える

/* ---- ハッシュ ---- */
// 2次元版。3次元でも形は同じで、掛ける素数が 1 つ増えるだけ

function hash2(x, y) {
  let h = Math.imul(x, 374761393) + Math.imul(y, 668265263);
  h = Math.imul(h ^ (h >>> 13), 1274126177);
  return ((h ^ (h >>> 16)) >>> 0) / 4294967295;
}

/* ---- 補間 ― ここだけが違う ---- */

function mix(a, b, t) { return a + (b - a) * t; }

const straight = (t) => t;               // 端数をそのまま使う
const fade = (t) => t * t * (3 - 2 * t); // 両端で傾きが 0 になる曲線

// weight に上のどちらかを渡す。ほかは 1 文字も変えない
function noise2(x, y, weight) {
  const xi = Math.floor(x), yi = Math.floor(y);
  const u = weight(x - xi), v = weight(y - yi);

  const bottom = mix(hash2(xi, yi), hash2(xi + 1, yi), u);
  const top = mix(hash2(xi, yi + 1), hash2(xi + 1, yi + 1), u);
  return mix(bottom, top, v);
}

/* ---- 白黒のテクスチャに焼く ---- */

function bake(weight) {
  const canvas = document.createElement('canvas');
  canvas.width = canvas.height = SIZE;
  const ctx = canvas.getContext('2d');
  const image = ctx.createImageData(SIZE, SIZE);

  for (let row = 0; row < SIZE; row++) {
    for (let col = 0; col < SIZE; col++) {
      const n = noise2((col / SIZE) * CELLS, (row / SIZE) * CELLS, weight);
      let value = Math.round(n * 255);

      // 等高線を重ねる。人の目は「明るさの折れ目」より「線の折れ目」のほうが
      // ずっとよく見えるので、傾きが飛んでいる場所が一目で分かる
      const band = (n * 14) % 1;
      if (band < 0.09) value = Math.max(0, value - 110);

      const at = (row * SIZE + col) * 4;
      image.data[at] = image.data[at + 1] = image.data[at + 2] = value;
      image.data[at + 3] = 255;
    }
  }
  ctx.putImageData(image, 0, 0);
  const texture = new THREE.CanvasTexture(canvas);
  texture.colorSpace = THREE.SRGBColorSpace;
  return texture;
}

/* ---- 2 枚並べる ---- */

const scene = new THREE.Scene();

// 板を真正面から、ちょうど収まる大きさで見る（遠近感は邪魔なので正射影）
const camera = new THREE.OrthographicCamera(-2.25, 2.25, 1.15, -1.15, 0.1, 10);
camera.position.z = 3;

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

[
  { x: -1.1, weight: straight, label: '端数をそのまま（格子が見える）' },
  { x: 1.1, weight: fade, label: 'fade に通す' },
].forEach((panel, index) => {
  const mesh = new THREE.Mesh(
    new THREE.PlaneGeometry(2, 2),
    // ライトの影響を受けない材質。ここで見たいのは陰影ではなく模様そのもの
    new THREE.MeshBasicMaterial({ map: bake(panel.weight) }),
  );
  mesh.position.x = panel.x;
  scene.add(mesh);

  const div = document.createElement('div');
  div.textContent = panel.label;
  div.style.cssText =
    'position:absolute; bottom:14px; transform:translateX(-50%);' +
    'color:#e8e8f2; font:12px sans-serif; pointer-events:none; white-space:nowrap;';
  div.style.left = (25 + index * 50) + '%';
  document.body.appendChild(div);
});

function draw() {
  // 板の縦横比を保ったまま、画面いっぱいに収める
  const aspect = window.innerWidth / window.innerHeight;
  const half = Math.max(2.25, 1.15 * aspect);
  camera.left = -half;
  camera.right = half;
  camera.top = half / aspect;
  camera.bottom = -half / aspect;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
  renderer.render(scene, camera);
}
draw();
window.addEventListener('resize', draw);`,
      caption:
        '左の等高線は $77$ ピクセルごとに角ばって折れ、ひし形や星形が並びます。右の線はどこも丸いままです。**格子の位置も乱数も、まったく同じもの**です ― 違うのは `weight` に何を渡したかだけ。等高線を消す（`band` の行を削る）と、左右の差はぐっと見えにくくなります ― **同じ不具合でも、見せ方で見つけやすさが変わります。** `CELLS` を $3$ にすると折れがさらに目立ち、$32$ にすると細かすぎて差が見えなくなります。',
    },
    {
      kind: 'md',
      text: `
## 3 次元にする

$2$ 次元が書ければ、$3$ 次元は機械的です。**格子点が $4$ から $8$ に、mix が $3$ 回から $7$ 回に増えるだけ**です。

なぜ $3$ 次元が要るのかは[](#/ch/x08-sphere-seam)で扱いますが、
先に言っておくと**球に貼るため**です。
平らな画像の $(u, v)$ でノイズを引くと必ず破綻するので、
「その画素が球のどっちを向いているか」という $3$ つの数で引きます。
`,
    },
    {
      kind: 'code',
      title: '3 次元の value noise（惑星で使うもの）',
      code: `function fade(t) { return t * t * (3 - 2 * t); }
function mix(a, b, t) { return a + (b - a) * t; }

function noise3(x, y, z, seed) {
  const xi = Math.floor(x), yi = Math.floor(y), zi = Math.floor(z);
  const u = fade(x - xi), v = fade(y - yi), w = fade(z - zi);

  // x 方向に 4 回（手前の下・手前の上・奥の下・奥の上）
  const x00 = mix(hash3(xi, yi, zi, seed), hash3(xi + 1, yi, zi, seed), u);
  const x10 = mix(hash3(xi, yi + 1, zi, seed), hash3(xi + 1, yi + 1, zi, seed), u);
  const x01 = mix(hash3(xi, yi, zi + 1, seed), hash3(xi + 1, yi, zi + 1, seed), u);
  const x11 = mix(hash3(xi, yi + 1, zi + 1, seed), hash3(xi + 1, yi + 1, zi + 1, seed), u);

  // y 方向に 2 回、z 方向に 1 回。合わせて 7 回
  return mix(mix(x00, x10, v), mix(x01, x11, v), w);
}`,
    },
    {
      kind: 'callout',
      tone: 'analogy',
      title: '次元が上がると倍になる',
      text: `
格子点の数は $2^{\\text{次元}}$ です。

$1$ 次元で $2$ 点、$2$ 次元で $4$ 点、$3$ 次元で $8$ 点、$4$ 次元なら $16$ 点。

mix の回数はそれより $1$ 少なくて、$1$・$3$・$7$・$15$ 回。

**$4$ 次元のノイズも実際に使います** ― 惑星の雲を時間とともに流すときです。
$3$ 次元の向きに、$4$ つめとして時刻を渡します。
そのぶん $2$ 倍の費用がかかるので、雲は地表より粗いテクスチャで作ることになります。

simplex noise が「次元が上がっても速い」と言われるのは、
この $2^n$ が $n+1$ で済むからです。
`,
    },
  ],
  exercises: [
    {
      prompt: `点 $(5.4,\\; 3.25)$ の値を手で求めてください。

まわりの $4$ 点の値は、$c_{00} = 0.9$（$(5,3)$）、$c_{10} = 0.1$（$(6,3)$）、$c_{01} = 0.4$（$(5,4)$）、$c_{11} = 0.6$（$(6,4)$）です。

$\\mathrm{fade}(0.4) = 0.352$、$\\mathrm{fade}(0.25) = 0.15625$ を使ってかまいません。`,
      hint: '下の辺 → 上の辺 → 縦、の順に 3 回混ぜます。端数は $x$ が $0.4$、$y$ が $0.25$。',
      answer: `**$0.5953$ です。**

**端数を fade に通す**

$x$ の端数は $5.4 - 5 = 0.4$ → $u = \\mathrm{fade}(0.4) = 0.352$

$y$ の端数は $3.25 - 3 = 0.25$ → $v = \\mathrm{fade}(0.25) = 0.15625$

**下の辺**（$c_{00}$ と $c_{10}$ を横に）

$0.9 + (0.1 - 0.9) \\times 0.352 = 0.9 - 0.2816 = 0.6184$

**上の辺**（$c_{01}$ と $c_{11}$ を横に）

$0.4 + (0.6 - 0.4) \\times 0.352 = 0.4 + 0.0704 = 0.4704$

**縦に混ぜる**

$0.6184 + (0.4704 - 0.6184) \\times 0.15625 = 0.6184 - 0.0231 = 0.5953$

**答えの読み方**

$4$ つの格子点の値は $0.1$ から $0.9$ まで散らばっているのに、
答えは $0.5953$ ― **真ん中寄り**に落ちました。

$y$ の端数が $0.25$ と小さいので、$v = 0.15625$ はさらに小さくなり、
**下の辺のほうに強く引かれます。** だから答えは $0.6184$ の近くです。

**fade は端数を端に寄せる**、と覚えてください。
$0.25$ が $0.156$ に、$0.75$ なら $0.844$ に。真ん中の $0.5$ だけが動きません。

**もう 1 つ**

$4$ 点の平均は $(0.9 + 0.1 + 0.4 + 0.6) / 4 = 0.5$ です。
どこを聞いても、答えはこの平均のあたりをうろつきます。

**だから value noise の値は、格子点ほど端まで振れません。**
$1$ 段だけなら $0.02$〜$0.98$ くらいまで出ますが、
次の章で $5$ 段重ねると $0.11$〜$0.88$ に縮みます。`,
      answerCode: `const u = fade(0.4);    // 0.352
const v = fade(0.25);   // 0.15625

const bottom = mix(0.9, 0.1, u);    // 0.6184
const top    = mix(0.4, 0.6, u);    // 0.4704
const n      = mix(bottom, top, v); // 0.5953`,
    },
    {
      prompt: `\`hash3\` の \`Math.imul\` を、ふつうの \`*\` に置き換えたらどうなりますか。

そして \`(h >>> 0)\` を \`(h >> 0)\` にしたら、返る値はどうなりますか。`,
      hint: '$2^{53}$ を超える掛け算と、負の数の右シフトです。',
      answer: `**\`*\` にすると格子が模様として見えてきます。\`>>\` にすると値の半分が負になります。**

**\`Math.imul\` を \`*\` にした場合**

JavaScript の \`*\` は倍精度の実数の掛け算です。
答えが $2^{53} \\approx 9.007 \\times 10^{15}$ を超えると、**下位のビットが落ちます。**

$374761393 \\times 123456789 = 46266838220947080$ ― 超えています。

- \`(374761393 * 123456789) | 0\` → $-1190510968$
- \`Math.imul(374761393, 123456789)\` → $-1190510971$

**ずれているのは、いちばん下のビットです。**

ハッシュは「入力が $1$ 違えば出力が全然違う」ことで成り立っていて、
その仕事をしているのが下位ビットの混ざりです。
そこが落ちると、**隣の格子点どうしで似た値が返る**ようになります。

見た目には、格子が縞や斑として浮き出てきます。
**エラーは 1 つも出ません。** ただ、なんとなく人工的な模様になります。

**\`>>> 0\` を \`>> 0\` にした場合**

\`>>\` は**符号を保つ**シフトです。\`h\` の最上位ビットが $1$ のとき、
つまり**およそ半分の確率で**、負の数がそのまま返ります。

$-2147483648 / 4294967295 = -0.5$ のような値になるので、
戻り値は $0$〜$1$ ではなく **$-0.5$〜$0.5$** に変わります。

そのまま高さに使うと、\`SEA = 0.5\` より上に来る点がほぼ無くなり、
**惑星が全部海になります。**

\`>>>\` は**符号なし**として扱うシフトで、必ず $0$〜$4294967295$ が返ります。

**共通していること**

**どちらもエラーになりません。** 型も合っているし、例外も出ない。
出るのは「なんか変な絵」だけです。

**ハッシュのバグは、こういう形でしか現れません。**
だから、書いたら必ず絵にして見てください。数字を眺めても分かりません。`,
      answerCode: `// 確かめ方：ハッシュの平均が 0.5 前後、最小 0 前後、最大 1 前後になるか
let min = 1, max = 0, sum = 0;
for (let i = 0; i < 100000; i++) {
  const v = hash3(i % 97, (i / 97) | 0, i % 31, 1337);
  min = Math.min(min, v); max = Math.max(max, v); sum += v;
}
console.log(min, max, sum / 100000);
// 期待: 0 前後 / 1 前後 / 0.5 前後
// >> にすると min が -0.5 前後になる`,
    },
    {
      prompt: `サンドボックスの \`CELLS\` を $3$ と $32$ にしてください。

左右の差はどちらで見やすくなりますか。**そこから、不具合を見つけるときの一般則が $1$ つ出ます。**`,
      hint: '格子 1 マスが何ピクセルになるか、を計算してみてください。',
      answer: `**$3$ のほうが見やすくなります。一般則は「不具合は、いちばん粗い設定で探す」です。**

**1 マスの大きさ**

テクスチャは $384$ ピクセル四方です。

- \`CELLS = 3\` … $1$ マス $128$ ピクセル。等高線の折れが**大きくはっきり**出る
- \`CELLS = 5\` … $1$ マス $77$ ピクセル（既定）
- \`CELLS = 32\` … $1$ マス $12$ ピクセル。折れが細かすぎて、**ただのざらつきに見える**

$32$ では、左右どちらも「細かい砂嵐」になって区別がつきません。
**不具合が消えたのではなく、見えなくなっただけ**です。

**一般則**

**確かめたいものは、いちばん大きく・いちばん粗く・いちばん遅くして探す。**

- ノイズの格子 → マスを大きく
- アニメーション → 遅く
- 補間 → 端の値を極端に

このサイトで何度か出てきた形です。
[](#/ch/x03-uniform-sphere)では逆に「**偏りは数が多いほうが見える**」でした。

矛盾しているようですが、どちらも同じことを言っています ―
**確かめたい効果が、いちばん大きく出る設定を選ぶ。**

点の偏りは点が多いほど濃淡として積み上がり、
補間の折れ目は $1$ マスが大きいほど長い線になります。

**本番の設定のまま眺めても、たいてい見えません。**`,
    },
  ],
  quiz: [
    {
      q: '格子点の乱数を、配列ではなくハッシュ関数で作るのはなぜですか。',
      choices: [
        '3 次元では格子点が数十万個になり、しかも範囲を先に決めなければ配列が作れないから',
        'ハッシュのほうが速いから',
        '配列だと同じ値が返らないから',
        'three が配列を受け取れないから',
      ],
      answer: 0,
      explain:
        'いちばん細かい段だけで格子は 1 辺 71 個、71 の 3 乗で 357911 個になります。ハッシュなら覚えておくものが 1 つもなく、座標がどこまで伸びても困りません。同じ入力に同じ値が返る点は、配列と変わりません。',
    },
    {
      q: '補間の重みに `fade(t) = t²(3-2t)` を使う理由はどれですか。',
      choices: [
        '格子をまたぐところで傾きが 0 でつながり、折れ目が見えなくなるから',
        '値が 0〜1 に収まるようにするため',
        '直線補間より速いから',
        '格子点の値が変わってしまうのを防ぐため',
      ],
      answer: 0,
      explain:
        '直線補間でも値そのものはつながります。つながらないのは傾きで、格子点で 0.6 から −0.5 のように飛びます。人の目は明るさより明るさの変化に敏感なので、そこが線として見えます。fade は両端で傾きが 0 なので、手前も向こうも 0 でつながります。',
    },
    {
      q: '3 次元の value noise で、1 点の値を出すのに mix は何回必要ですか。',
      choices: ['7 回', '3 回', '8 回', '12 回'],
      answer: 0,
      explain:
        '格子点は 2 の 3 乗で 8 つ。x 方向に 4 回、y 方向に 2 回、z 方向に 1 回で合計 7 回です。次元が 1 つ上がるごとに格子点は倍、mix は「格子点の数 − 1」になります。',
    },
  ],
};
