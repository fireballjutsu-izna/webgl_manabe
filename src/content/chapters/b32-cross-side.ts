import type { Chapter } from '../types.ts';

export const chapterB32: Chapter = {
  slug: 'b32-cross-side',
  part: 'basics',
  number: 32,
  title: '右か左か ― 外積の符号で側を判定する',
  goal: '内積では区別できない左右を外積で判定できるようになり、右手の法則が何を決めているのか説明できるようになります。',
  requires: ['b30-cross-area', 'b26-dot-facing'],
  threeApis: ['Vector3.crossVectors', 'Object3D.getWorldDirection', 'Vector3.dot'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

内積では「前か後ろか」しか分かりませんでした。
**真横にいる敵は、右も左も内積 0** で区別が付きません。

- 敵は右にいるのか、左にいるのか（どちらに旋回すべきか）
- 点は線のどちら側にあるのか
- カーブをどちら向きに曲がっているのか

どれも「側」の問題です。ここが外積のもう 1 つの出番になります。
`,
    },
    {
      kind: 'md',
      text: `
## 外積の向きが、側を教える

プレイヤーの視線を $\\mathbf{f}$、敵への向きを $\\mathbf{d}$ とします。
$\\mathbf{f} \\times \\mathbf{d}$ を計算すると、**上を向くか下を向くか**のどちらかになります。

- 敵が片側にいるとき … 外積は上向き
- 反対側にいるとき … 外積は下向き
- 真正面か真後ろ … 外積は 0（平行なので）

床の上の話なら、**外積の y 成分の符号を見るだけ**で判定が済みます。

内積で「前後」、外積で「左右」。この 2 つで、平面上の位置関係はすべて言い当てられます。
**片方だけでは足りない**というのが要点です。
`,
    },
    {
      kind: 'formula',
      tex: '(\\mathbf{f} \\times \\mathbf{d})_y > 0 \\;\\Longleftrightarrow\\; \\text{一方の側}',
      readAloud:
        '視線と相手への向きの外積を取り、その y 成分がプラスなら片側、マイナスならもう片側にいる、と言っています。どちらが右かは座標系の取り方で決まるので、実際に見て決めます。',
      worked: {
        given: '視線 $\\mathbf{f} = (0,\\; 0,\\; -1)$（奥向き）のとき、2 つの相手で符号を見ます。',
        steps: [
          { calc: '相手A d = (1, 0, 0)  : y 成分 = f_z d_x - f_x d_z' },
          { calc: '                      = (-1)x1 - 0x0 = -1', note: 'マイナス側' },
          { calc: '相手B d = (-1, 0, 0) : y 成分 = (-1)x(-1) - 0x0 = 1', note: 'プラス側' },
          { calc: '真正面 d = (0, 0, -1) : y 成分 = (-1)x0 - 0x(-1) = 0', note: '平行なので 0' },
        ],
        result:
          '**A と B で符号が反転しました。** どちらが「右」かは座標系と視線の取り方で決まるので、**実際に動かして確かめて決めるのが早い**です。大事なのは「符号が分かれる」ことのほうです。',
      },
    },
    {
      kind: 'code',
      title: '右にいるか左にいるかを判定する',
      code: `import * as THREE from 'three';

const forward = new THREE.Vector3();
player.getWorldDirection(forward);

const toTarget = new THREE.Vector3()
  .subVectors(target.position, player.position)
  .normalize();

// 外積の結果が上を向くか下を向くかで、左右が分かれる
const cross = new THREE.Vector3().crossVectors(forward, toTarget);

if (cross.y > 0) {
  // 一方の側
} else {
  // 反対の側
}

// 前後は内積で。2 つ組み合わせて初めて位置関係が決まる
const isFront = forward.dot(toTarget) > 0;

// 床の上だけで見たいなら、y 成分を消してから
forward.y = 0;
forward.normalize();`,
    },
    {
      kind: 'md',
      text: `
## 右手の法則 ― 向きの決まりかた

$\\mathbf{a}\\times\\mathbf{b}$ がどちら側に出るかは、
[](#/ch/b09-handedness)で見た{{右手系}}で決まっています。

右手の指を $\\mathbf{a}$ から $\\mathbf{b}$ へ**丸めこむ**ように動かしたとき、
親指が指す向きが $\\mathbf{a}\\times\\mathbf{b}$ です。

正直なところ、実装中に手を使って確認することはあまりありません。
**「符号が逆だったら、掛ける順番を入れ替える」で解決する**からです。
ただ、**なぜ逆になったのか**の説明はこれです。

そして「試して直す」が許されるのは、**符号が 2 通りしかないから**でもあります。
どちらかが正しいので、片方を試せば済みます。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '旋回に使うなら、符号だけでなく大きさも使える',
      text: `
「右にいる」が分かったら、あとは決まった速さで回せば追従できます。
ただ、それだと目標に近づいたときにガタガタ揺れます（行き過ぎて戻るのを繰り返す）。

外積の**大きさ**は「どれだけずれているか」に対応するので、
それに比例した速さで回すと、近づくほどゆっくりになってきれいに収まります。
内積と組み合わせれば、真後ろのときだけ特別扱いする、といった作りもできます。
`,
    },
    {
      kind: 'md',
      text: `
## 点が線のどちら側にあるか

同じ考え方で、「点 $P$ は、$A$ から $B$ へ引いた線のどちら側か」も判定できます。

1. 線の向き … $\\mathbf{v} = B - A$
2. 点への向き … $\\mathbf{w} = P - A$
3. $\\mathbf{v} \\times \\mathbf{w}$ の符号を見る

平面（2D）でやるなら、外積の z 成分だけを計算すれば済みます。
$v_x w_y - v_y w_x$ の 1 行です。

この判定は、**多角形の内外判定**や**三角形の中に点があるか**の土台になります。
三角形の 3 辺すべてに対して「同じ側にある」なら、点は三角形の内側です。
マウスで 3D の物体をクリックしたときの当たり判定も、突き詰めるとこれをやっています。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでで、内積と外積は終わりです

この 9 章で手に入れたものを並べておきます。

- **内積** … 揃い具合を数で測る。前後・視界・明るさ・射影
- **外積** … 垂直な向きを作る。法線・面積・左右の判定

そして**この 2 つは対になっています**。

| | 内積 | 外積 |
|---|---|---|
| 返るもの | 数 | ベクトル |
| 順番 | 入れ替えても同じ | 入れ替えると逆向き |
| 最大になるのは | 揃っているとき | 直交しているとき |
| 0 になるのは | 直交しているとき | 平行なとき |

**きれいに裏返しの関係**です。片方が使えない場面で、もう片方が使えます。

次は補間 ― 「2 つの値のあいだを埋める」道具に進みます。
`,
    },
  ],
  exercises: [
    {
      prompt: `視線 $\\mathbf{f} = (1,\\; 0,\\; 0)$ のとき、つぎの相手への向きについて
外積 $\\mathbf{f} \\times \\mathbf{d}$ の y 成分を手で計算してください
（y 成分 $= f_z d_x - f_x d_z$）。

1. $\\mathbf{d} = (0,\\; 0,\\; 1)$
2. $\\mathbf{d} = (0,\\; 0,\\; -1)$
3. $\\mathbf{d} = (1,\\; 0,\\; 0)$`,
      hint: '$f_z = 0$、$f_x = 1$ です。',
      answer: `1. $0 \\times 0 - 1 \\times 1 = $ **−1**
2. $0 \\times 0 - 1 \\times (-1) = $ **1**
3. $0 \\times 1 - 1 \\times 0 = $ **0**

1 と 2 で符号が分かれ、3 は真正面（平行）なので 0 になりました。

**符号が分かれる**ことが判定に使えるのであって、
どちらが「右」かは座標系と視線の取り方で決まります。
実際の画面で 1 回確かめて決めるのが確実です。`,
    },
    {
      prompt: `「順番を逆にした b×a も表示」を入れてください。a×b と b×a はどう違いますか。`,
      hint: '長さと向きを別々に見てください。',
      answer: `長さは同じで、**向きがちょうど反対**になります。

掛け算なのに順番を入れ替えると答えが変わる、というのが外積の最大の特徴です。
面の法線を計算するとき、頂点をどちら回りに並べたかで法線が裏返るのは、このためです。

そして左右の判定でも同じことが起きます。
\`crossVectors(forward, toTarget)\` と \`crossVectors(toTarget, forward)\` では、
**右と左が入れ替わります**。判定が逆だったときの直し方が、これです。`,
    },
    {
      prompt: `敵が**真後ろ**にいるとき、左右の判定はどうなりますか。
そのまま「外積の符号で旋回する」コードを書くと、どんな不具合が起きるか説明してください。`,
      hint: '真後ろのとき、視線と敵への向きはどういう関係ですか。',
      answer: `真後ろだと 2 本は**平行（180 度）**なので、外積は **0** になります。
符号が付かないので、左右が決まりません。

そのまま書くと、\`cross.y > 0\` は false になって常に片側へ回ります。
それ自体は動きますが、**真後ろぴったりのときだけ挙動が不安定**になり、
わずかなぶれで右に回ったり左に回ったりしてガタつきます。

対策は 2 つあります。

1. **内積も見る** … 内積がマイナス（背後）なら、外積の符号によらず片方に決め打ちする
2. **外積の大きさで判定** … 0 に近いときは「真正面か真後ろ」として別扱いにする

[](#/ch/b30-cross-area)で見た「平行だと外積が消える」が、
ここでも同じ形で効いています。`,
    },
  ],
  quiz: [
    {
      q: '内積では区別できないのはどれですか。',
      choices: ['右にいるか左にいるか', '前にいるか後ろにいるか', '直交しているかどうか', '面の明るさ'],
      answer: 0,
      explain:
        '真横にいる相手は、右でも左でも内積が 0 になります。左右を分けるには外積が要ります。内積で前後、外積で左右、と組み合わせて初めて位置関係が決まります。',
    },
    {
      q: '床の上で左右を判定するとき、外積のどの成分を見ますか。',
      choices: ['y 成分', 'x 成分', 'z 成分', '長さ'],
      answer: 0,
      explain:
        '床の上（xz 平面）にある 2 本の外積は、上下（y 軸）方向を向きます。その符号が側を表します。長さは「どれだけずれているか」に対応するので、旋回速度に使えます。',
    },
    {
      q: '左右の判定が期待と逆になっていました。どう直すのがいちばん簡単ですか。',
      choices: [
        '外積の掛ける順番を入れ替える',
        '内積に切り替える',
        '両方を正規化する',
        'y 成分ではなく x 成分を見る',
      ],
      answer: 0,
      explain:
        '外積は順番を入れ替えると向きが反転するので、符号もそのまま反転します。どちらが正しいかは座標系と視線の取り方で決まるため、試して合うほうを採るのが実務では早道です。',
    },
  ],
};
