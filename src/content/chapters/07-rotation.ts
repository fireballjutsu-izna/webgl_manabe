import type { Chapter } from '../types.ts';

export const chapter07: Chapter = {
  slug: '07-rotation',
  part: 'math3d',
  number: 2,
  title: '回転の表し方 ― オイラー角とクォータニオン',
  goal: 'なぜ回転に2つの表し方があるのかが分かり、ジンバルロックを避けて姿勢をなめらかに繋げられるようになります。',
  requires: ['05-trig', '06-matrix'],
  threeApis: [
    'Euler',
    'Object3D.rotation',
    'Quaternion',
    'Quaternion.setFromAxisAngle',
    'Quaternion.slerp',
    'Object3D.quaternion',
    'Object3D.lookAt',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

- カメラを上に向けていくと、ある角度から左右の操作がおかしくなる
- 2 つの向きのあいだをなめらかに繋ぎたいのに、途中で妙にねじれる
- \`rotation.x\` を変えたら、なぜか \`y\` の効き方まで変わった

回転は、移動や拡大とくらべて明らかに厄介です。この章では、その厄介さの正体と、
用意された 2 つの道具の使い分けを見ます。
`,
    },
    {
      kind: 'md',
      text: `
## オイラー角 ― 3つの角度で表す

いちばん素直な方法です。x 軸まわり・y 軸まわり・z 軸まわりに、それぞれ何度回すかを決めます。
Three.js の \`rotation\` がこれで、{{オイラー角}}と呼びます。

- 読める（「y に 90 度」と言えば伝わる）
- 書ける（スライダーに直結できる）
- 数が 3 つで済む

利点は明快です。ではなぜもう 1 つの表し方が必要なのでしょうか。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '3つの回転は独立していません',
      text: `
オイラー角は「x を回してから、y を回して、z を回す」という**手順**です。
そして前の回転が、次の回転の軸ごと動かしてしまいます。
だから 3 つの数字は互いに影響し合い、順番が変われば結果も変わります。
Three.js の既定の順番は XYZ で、\`rotation.order\` で変更できます。
`,
    },
    {
      kind: 'md',
      text: `
## ジンバルロック ― 自由度が1つ消える

3 つの回転を、入れ子になった 3 つのリングだと考えてください。
外側のリングを回すと、内側のリングは丸ごと持っていかれます。

ここで、真ん中のリングを 90 度回すと何が起きるか。
**いちばん外のリングと、いちばん内のリングが同じ向きに重なってしまいます。**
2 つのリングが同じ軸を回すようになるので、回せる方向が 3 つから 2 つに減ります。

これが{{ジンバルロック}}です。数式の都合ではなく、
「角度を 3 つ順番に適用する」という仕組みそのものから来ています。
`,
    },
    {
      kind: 'demo',
      id: 'euler-gimbal',
      caption:
        '「ジンバルロックの状態にする」を押すと、x 軸まわりを 90 度にした状態になります。この状態で y と z のスライダーを動かすと、まったく同じ動きしか作れないことが分かります。',
    },
    {
      kind: 'md',
      text: `
## クォータニオン ― 4つの数で表す

そこで登場するのが{{クォータニオン}}（四元数）です。
考え方はむしろ単純で、**「どの軸のまわりに、何度回すか」**を 1 セットで持ちます。
それを扱いやすい形にすると、数が 4 つ必要になる、というだけの話です。

- 手順ではなく結果の姿勢そのものを持つので、**順番の問題がない**
- 軸が重なることがないので、**ジンバルロックが起きない**
- 2 つの姿勢のあいだを、**最短の回り方でなめらかに繋げる**

代償は、人間が数字を読んでも意味が分からないことです。
`,
    },
    {
      kind: 'formula',
      tex: 'q = \\left(\\;\\mathbf{a}\\sin\\tfrac{\\theta}{2},\\;\\; \\cos\\tfrac{\\theta}{2}\\;\\right)',
      readAloud:
        '回転軸 a と回転角 θ から作られる、4 つの数の組です。半分の角度が出てくるのが特徴ですが、この式を自分で書くことはまずありません。「軸と角度から作れる」ことだけ押さえてください。',
      worked: {
        given: 'y 軸 $\\mathbf{a} = (0,\; 1,\; 0)$ のまわりに、$\\theta = 90$ 度回すクォータニオンを作ります。',
        steps: [
          { calc: 'まず角度を半分にする : 90 / 2 = 45 度' },
          { calc: 'sin 45 度 = 0.7071' },
          { calc: 'cos 45 度 = 0.7071' },
          { calc: 'x = 0 x 0.7071 = 0', note: '軸の各成分に sin(θ/2) を掛ける' },
          { calc: 'y = 1 x 0.7071 = 0.7071' },
          { calc: 'z = 0 x 0.7071 = 0' },
          { calc: 'w =     cos(θ/2) = 0.7071', note: '4 つ目だけは cos' },
        ],
        result: '$q = (0,\; 0.7071,\; 0,\; 0.7071)$。長さは $0.7071^2 + 0.7071^2 = 1$ で、ちゃんと 1 になっています。**90 度回したいのに 45 度が出てくる**のがクォータニオンの見慣れないところですが、覚える必要はありません。`setFromAxisAngle` が代わりにやってくれます。',
      },
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '中身は読まなくていい',
      text: `
クォータニオンの 4 つの数を直接いじることは、実務ではまずありません。
「軸と角度から作る」「2 つのあいだを補間する」「掛け合わせて回転を重ねる」——
この 3 つの操作を通してしか触らないので、中身が分からなくても実用上まったく困りません。
`,
    },
    {
      kind: 'md',
      text: `
## slerp ― 姿勢をなめらかに繋ぐ

クォータニオンの最大の実用的価値がこれです。
{{球面線形補間}}（slerp）は、2 つの姿勢のあいだを **一定の速さで、最短の回り方で**繋ぎます。

オイラー角でも「3 つの角度をそれぞれ直線で混ぜる」ことはできます。
しかしそれは姿勢を混ぜているのではなく、3 つの数字を別々に混ぜているだけです。
結果として、途中でありえない向きを通ったり、速さがガタついたりします。
`,
    },
    {
      kind: 'demo',
      id: 'quaternion-slerp',
      caption:
        '同じ「始まり」と「終わり」をつないでいるのに、機首が描く軌跡がまったく違います。角度を直線で混ぜた機（左）は遠回りし、slerp（右）はきれいな弧を描きます。',
    },
    {
      kind: 'md',
      text: `
## 使い分けの指針

**オイラー角を使う場面**

- 固定の向きを 1 回設定するだけのとき（「壁を y に 90 度回して置く」）
- 人が数字を読み書きする UI（エディタのスライダーなど）
- 1 軸だけ回すとき（回転する床、時計の針）

**クォータニオンを使う場面**

- 向きを補間するとき（カメラワーク、キャラクタの旋回）
- 回転を重ねていくとき（毎フレーム少しずつ回す）
- 上や下を向きうる自由なカメラ

Three.js では \`rotation\`（オイラー角）と \`quaternion\` は同じ姿勢の 2 つの見方で、
片方を変えるともう片方も自動で更新されます。混ぜて使っても壊れません。
`,
    },
    {
      kind: 'code',
      title: 'クォータニオンでなめらかに向きを変える',
      code: `import * as THREE from 'three';

// 軸と角度から作る
const axis = new THREE.Vector3(0, 1, 0);
const q = new THREE.Quaternion().setFromAxisAngle(axis, Math.PI / 2);

// 「目標のほうを向いた姿勢」を、物体を動かさずに作る
const target = new THREE.Object3D();
target.position.copy(model.position);
target.lookAt(enemy.position);
const goal = target.quaternion;

function animate(dt) {
  // 毎フレーム少しずつ目標の姿勢へ寄せる。急に向かず、自然に旋回する
  model.quaternion.slerp(goal, 1 - Math.pow(0.001, dt));

  // 回転を重ねるときは掛け算
  spinner.quaternion.multiply(
    new THREE.Quaternion().setFromAxisAngle(axis, dt * 0.5),
  );
}`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '毎フレーム rotation に足し算しないこと',
      text: `
\`mesh.rotation.y += 0.01\` は 1 軸だけなら問題ありませんが、
複数の軸に対して毎フレーム足し込むと、順番の影響が積み重なって姿勢がじわじわ壊れます。
向きを積み上げていく処理は、クォータニオンの掛け算で書いてください。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「ジンバルロックの状態にする」を押してください。そのあと**いちばん外のリング（y）といちばん内のリング（z）**を動かして、
何が起きるかを確かめます。「回し方が 1 つ減った」とはどういう意味でしょう。`,
      hint: 'リングどうしが重なっていないか見てください。',
      answer: `x 軸まわりを 90 度にすると、y のリングと z のリングが**同じ平面に重なります**。
その状態では、y を回しても z を回しても同じ回り方しかせず、**3 つあったはずの回し方が 2 つになって**しまいます。
残りの 1 方向へは、どのスライダーをどう動かしても行けません。これがジンバルロックです。`,
    },
    {
      prompt: '「機首の軌跡を残す」を入れ、終わりの姿勢を大きく傾けて t を 0 から 1 まで動かしてください。2 機の軌跡はどう違いますか。',
      hint: '通った道のりの長さと、進む速さの均一さを、別々に見てください。',
      answer: `角度を直線で混ぜたほうは**遠回りの弧を描き、途中で速さが変わったり、不自然に傾いたり**します。
slerp のほうは**最短の回り方**で、しかも**一定の速さ**で回ります。
カメラや乗り物の姿勢を補間するときに slerp を使うのは、この 2 つ（最短・等速）がほしいからです。`,
    },
    {
      prompt: '物体の現在の向き（+Z が正面）を、指定した方向 \`target\`（長さ 1）へ向けるクォータニオンを作ってください。',
      hint: '「ある向きから、別の向きへ」を直接作る関数があります。',
      answer: `\`setFromUnitVectors\` は、2 本の単位ベクトルの一方をもう一方へ重ねる回転を、そのまま作ってくれます。
\`lookAt\` でも同じことができますが、\`lookAt\` は内部で \`up\` を使って**ひねり（ロール）まで決めてしまう**ので、
「向きだけ合わせて、ひねりは今のまま残したい」ときはこちらが向いています。
さらに \`slerp\` を挟めば、ぱっと向かずになめらかに向き直ります。`,
      answerCode: `const from = new THREE.Vector3(0, 0, 1); // この物体の正面
const q = new THREE.Quaternion().setFromUnitVectors(from, target);

// いきなり向けず、毎フレーム少しずつ近づける
mesh.quaternion.slerp(q, 0.1);`,
    },
  ],
  quiz: [
    {
      q: 'ジンバルロックとは、どういう現象ですか。',
      choices: [
        '回転軸のうち2つが重なってしまい、回せる方向が1つ減る現象',
        '物体が回転しすぎて画面から消える現象',
        'クォータニオンの計算で誤差がたまる現象',
        '回転が速すぎて描画が追いつかない現象',
      ],
      answer: 0,
      explain:
        '3つの角度を順番に適用する仕組みそのものから生じます。特定の角度で軸が重なり、3方向あったはずの自由度が2方向になります。',
    },
    {
      q: '2つの向きのあいだを、なめらかに一定の速さでつなぎたいときに使うべきものはどれですか。',
      choices: [
        'クォータニオンの slerp',
        'オイラー角の3つの数値をそれぞれ lerp する',
        'position の lerp',
        'scale の乗算',
      ],
      answer: 0,
      explain:
        '角度を個別に混ぜると、姿勢としては遠回りしたり速さがガタついたりします。slerp は姿勢そのものを最短の弧でつなぎます。',
    },
    {
      q: 'Three.js の `mesh.rotation` と `mesh.quaternion` の関係として正しいのはどれですか。',
      choices: [
        '同じ姿勢を2つの方法で見ているだけで、片方を変えるともう片方も更新される',
        'まったく別の姿勢を持っていて、両方を足した向きになる',
        '`quaternion` を使うと `rotation` は無効になる',
        '`rotation` のほうが優先され、`quaternion` は読み取り専用',
      ],
      answer: 0,
      explain:
        '内部では同じ姿勢を指しており、Three.js が相互に同期させています。読みやすさで rotation、補間で quaternion、と場面に応じて使い分けられます。',
    },
  ],
};
