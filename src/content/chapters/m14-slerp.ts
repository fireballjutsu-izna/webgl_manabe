import type { Chapter } from '../types.ts';

export const chapterM14: Chapter = {
  slug: 'm14-slerp',
  part: 'math3d',
  number: 14,
  title: 'slerp ― 姿勢をなめらかに繋ぐ',
  goal: '2 つの姿勢のあいだを最短・等速で繋げるようになり、角度を個別に混ぜると何が壊れるのか説明できます。',
  requires: ['m13-quaternion', '08-interp'],
  threeApis: [
    'Quaternion.slerp',
    'Quaternion.slerpQuaternions',
    'Quaternion.dot',
    'Quaternion.rotateTowards',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

カメラをいまの向きから、目標の向きへ**なめらかに**動かしたい。

[](#/ch/08-interp)の lerp があるのだから、角度にも同じことをすればよさそうです。

**ところが、これが壊れます。** 途中でありえない向きを通ったり、
速さがガタついたり、なぜか一回転して戻ってきたり。
位置の補間では何ともなかったのに、向きだと途端におかしくなる。

**理由は単純です。姿勢は「まっすぐな量」ではないからです。**
`,
    },
    {
      kind: 'md',
      text: `
## 角度を混ぜても、姿勢は混ざらない

$(0^\\circ, 0^\\circ, 0^\\circ)$ から $(90^\\circ, 90^\\circ, 90^\\circ)$ へ、3 つの数をそれぞれ lerp する。
一見よさそうですが、やっているのは**姿勢を混ぜること**ではなく、**3 つの数字を別々に混ぜること**です。

[](#/ch/m10-euler)で見たとおり、3 つの数字は手順であって値ではありませんでした。
手順を半分にしても、姿勢が半分になるとはかぎりません。

結果として起きるのが次の 3 つです。

- **遠回りする** … もっと短い回り方があるのに、通らない
- **速さが変わる** … 同じ $t$ の刻みでも、途中で速くなったり遅くなったりする
- **途中で傾く** … 始点にも終点にも無いロール（ひねり）が、途中だけ現れる
`,
    },
    {
      kind: 'demo',
      id: 'quaternion-slerp',
      caption:
        '「機首の軌跡を残す」を入れて、終わりの姿勢を大きく傾けてください。同じ始点と終点を結んでいるのに、2 機の描く軌跡がまったく違います。ピンクの機（角度を直線で混ぜたもの）は膨らんだ道を通り、アンバーの機（slerp）はきれいな弧を描きます。',
    },
    {
      kind: 'md',
      text: `
## 球の上を滑らせる

正しいやり方は{{球面線形補間}}、略して **slerp** です。

考え方は名前のとおりです。姿勢は球の上の点のようなもので、
その 2 点を繋ぐいちばん短い道は、**球の表面に沿った弧**です。

lerp は 2 点を直線で結ぼうとします ― 球を突き抜ける近道です。
そこを通ると「姿勢ですらない状態」になってしまう。
slerp は球から離れずに、表面を滑って向こう岸へ行きます。

**そして弧の上を等速で進むので、回る速さも一定になります。**
`,
    },
    {
      kind: 'formula',
      tex: '\\mathrm{slerp}(q_0, q_1, t) = \\frac{\\sin((1-t)\\Omega)}{\\sin\\Omega} q_0 + \\frac{\\sin(t\\Omega)}{\\sin\\Omega} q_1',
      readAloud:
        '2 つの姿勢のなす角を Ω とすると、その弧を t の割合で分ける点を返します。分母と分子にサインが並んでいるのは、直線ではなく弧の上で等分するためです。この式を書く機会はありませんが、「弧を等分している」ことだけ読み取れれば十分です。',
      worked: {
        given:
          '$q_0 = $ そのままの姿勢 $(0, 0, 0, 1)$ から、$q_1 = $ y 軸 90 度 $(0,\\; 0.7071,\\; 0,\\; 0.7071)$ へ、$t = \\frac{1}{3}$ の地点を求めます。',
        steps: [
          { calc: '2 つの内積 : 0x0 + 0x0.7071 + 0x0 + 1x0.7071 = 0.7071' },
          { calc: 'なす角 Ω = arccos(0.7071) = 45度', note: '回転角の半分になる' },
          { calc: '係数1 : sin(2/3 x 45度)/sin45度 = sin30度/0.7071 = 0.7071' },
          { calc: '係数2 : sin(1/3 x 45度)/sin45度 = sin15度/0.7071 = 0.3660' },
          { calc: 'y : 0.7071x0 + 0.3660x0.7071 = 0.2588' },
          { calc: 'w : 0.7071x1 + 0.3660x0.7071 = 0.9659' },
        ],
        result:
          '$q = (0,\\; 0.2588,\\; 0,\\; 0.9659)$。これは**y 軸まわり 30 度**です（$\\sin 15^\\circ = 0.2588$、$\\cos 15^\\circ = 0.9659$）。**90 度の $\\frac{1}{3}$ でちょうど 30 度** ― 弧が等分されています。$t$ を $\\frac{2}{3}$ にすれば 60 度になり、進む速さが一定であることが数字で確かめられます。',
      },
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '結局のところ、角度を等分しています',
      text: `
式は複雑ですが、slerp がやっていることは一言で言えます ―
2 つの姿勢のあいだの角度を、t の割合で分ける。

90 度離れた 2 つの姿勢なら、t = 0.5 でちょうど 45 度の地点。
t = 0.25 なら 22.5 度の地点。それだけです。

三角関数が並んでいるのは、その「等分」を
4 つの数の足し算だけで実現するための仕掛けにすぎません。
`,
    },
    {
      kind: 'md',
      text: `
## 遠回りを避ける ― 符号の話

[](#/ch/m13-quaternion)の最後に置いた伏線が、ここで効きます。

**$q$ と $-q$ は同じ姿勢を表す**のでした。姿勢としては区別が付きません。
ところが**補間の道すじは、まったく別のものになります。**

- $q_1$ を選ぶと … 短いほうの弧を通る（たとえば 60 度ぶん）
- $-q_1$ を選ぶと … 長いほうの弧を通る（300 度ぶん、逆回り）

どちらも同じ場所に着きますが、片方は**ぐるりと遠回り**します。
「補間したら物体が一回転してしまった」の正体はこれです。

判定は簡単で、**内積が負なら遠回り**です。そのときは片方の符号を反転してから補間します。
three の \`slerp\` はこれを内部でやってくれるので、ふつうは意識しなくて済みます。
`,
    },
    {
      kind: 'code',
      title: '姿勢をなめらかに変える',
      code: `import * as THREE from 'three';

// 目標の姿勢を、物体を動かさずに作る
const helper = new THREE.Object3D();
helper.position.copy(model.position);
helper.lookAt(enemy.position);
const goal = helper.quaternion;

function animate(dt) {
  // 毎フレーム目標へ少しずつ寄せる。急に向かず、自然に旋回する
  model.quaternion.slerp(goal, 1 - Math.pow(0.001, dt));

  // 2 つの姿勢の中間を、別のオブジェクトに入れる
  mid.quaternion.slerpQuaternions(qa, qb, 0.5);

  // 「1 秒で最大 90 度まで」のように、回る速さに上限をつける
  turret.quaternion.rotateTowards(goal, Math.PI / 2 * dt);
}

// 遠回りしているか自分で確かめたいとき
if (model.quaternion.dot(goal) < 0) {
  // 逆回りの側。three の slerp は自動で短いほうを選ぶ
}`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 'フレームレートに依存させないこと',
      text: `
slerp(goal, 0.1) を毎フレーム呼ぶ書き方は、
描画が速い環境ほど速く回ってしまいます。

[](#/ch/b37-follow)で見た手当てをそのまま使ってください ―
1 - Math.pow(残す割合, dt) を係数にすれば、
何 fps でも同じ時間で同じところまで回ります。

位置の追従で使った式が、姿勢でもそのまま効きます。
`,
    },
    {
      kind: 'md',
      text: `
## どこで使うか

**カメラワーク** … 現在の向きから目標の向きへ。ゲームでも可視化でも、いちばん出番が多い場面です。

**キャラクタや乗り物の旋回** … 進行方向へ機体を向けるとき。
瞬時に向くと不自然なので、毎フレーム少しずつ slerp します。

**キーフレームの補間** … 2 つの姿勢のあいだを $t$ で行き来する。
アニメーションの再生は、内部でこれをやっています。

いずれも「向きを、時間をかけて変える」処理です。
**向きが動くところには、だいたい slerp がいます。**
`,
    },
  ],
  exercises: [
    {
      prompt: `y 軸まわり 0 度の姿勢から、y 軸まわり 120 度の姿勢へ slerp します。
$t = 0.25$ のとき、物体は何度回ったところにいますか。`,
      hint: 'slerp は 2 つの姿勢のあいだの角度を、$t$ の割合で分けます。',
      answer: `**30 度**です。

$120^\\circ \\times 0.25 = 30^\\circ$ ― 角度をそのまま等分するだけです。

同じ軸まわりなら、slerp の結果は「角度の lerp」と一致します。
だから**同じ軸まわりの回転だけを扱っているなら、角度を lerp しても壊れません**。

問題が出るのは**軸が違う 2 つの姿勢を繋ぐとき**です。
そのとき「3 つの角度をそれぞれ混ぜる」は、姿勢としての最短路から外れます。

つまり「オイラー角の補間は必ず駄目」ではなく、
**1 軸なら同じ、多軸だと壊れる**というのが正確なところです。`,
    },
    {
      prompt: `2 つの姿勢 $q_a$ と $q_b$ の内積を計算したら $-0.9$ でした。
これは何を意味しますか。そのまま補間すると、どうなりますか。`,
      hint: '内積の符号は、どちらの弧を通るかを教えてくれます。',
      answer: `**内積が負なので、このままでは遠回りの弧を通ります。**

内積の絶対値 0.9 は「2 つの姿勢がかなり近い」ことを示しています
（$\\arccos(0.9) = 25.8$ 度、姿勢としては約 51.7 度ぶん）。

にもかかわらず符号が負なので、そのまま補間すると
**残りの 308 度ぶんをぐるりと回って**同じ場所に着きます。

対策は、片方の**全成分の符号を反転**してから補間することです。
$-q_b$ は $q_b$ と同じ姿勢を表すので、着く場所は変わらず、道すじだけが最短になります。

three の \`slerp\` は内部でこの判定をしています。
自分でクォータニオンを混ぜる処理を書くときだけ、明示的に必要になります。`,
    },
    {
      prompt: `\`model.quaternion.slerp(goal, 0.08)\` を毎フレーム呼んでいます。
開発機（120fps）では自然だったのに、利用者の環境（30fps）では動きが鈍いと言われました。
原因と直し方を答えてください。`,
      hint: '1 秒あたり何回この行が実行されますか。',
      answer: `**1 秒あたりの呼び出し回数が違うからです。**

120fps なら 1 秒に 120 回、30fps なら 30 回。
毎回 8% ずつ近づくので、**同じ 1 秒でも進む量が 4 倍違います**。

[](#/ch/b37-follow)で位置の追従に使った手当てが、そのまま使えます。
「1 秒あたり残す割合」を決めて、そこから毎フレームの係数を導きます。

残す割合を 0.001 にすると「1 秒で 99.9% 詰める」動きになり、
**何 fps でもまったく同じ速さで回ります**。

なお \`rotateTowards\` を使う手もあります。
こちらは「1 秒で最大何度まで」という上限を直接指定でき、
砲塔の旋回速度のように**速さそのものに意味がある**場合に向いています。`,
      answerCode: `function animate(dt) {
  const keep = 0.001;                    // 1 秒後に残す割合
  model.quaternion.slerp(goal, 1 - Math.pow(keep, dt));
}

// 「1 秒で最大 90 度」と決めたいなら
turret.quaternion.rotateTowards(goal, Math.PI / 2 * dt);`,
    },
  ],
  quiz: [
    {
      q: 'slerp が返すのは、どんな姿勢ですか。',
      choices: [
        '2 つの姿勢のあいだの角度を、t の割合で分けた姿勢',
        '3 つの角度をそれぞれ lerp した姿勢',
        '2 つの姿勢の成分の平均',
        '常に中間の姿勢',
      ],
      answer: 0,
      explain:
        '球の表面に沿った最短の弧を、t の割合で等分します。だから最短の回り方になり、しかも進む速さが一定になります。',
    },
    {
      q: '2 つのクォータニオンの内積が負のとき、何が起きますか。',
      choices: [
        'そのまま補間すると遠回りの弧を通る',
        '補間できない',
        '結果が NaN になる',
        '物体が消える',
      ],
      answer: 0,
      explain:
        '$q$ と $-q$ は同じ姿勢を表しますが、補間の道すじは別物です。片方の符号を反転すれば最短路になります。three の `slerp` は自動でこれを判定しています。',
    },
    {
      q: '`slerp(goal, 0.08)` を毎フレーム呼ぶ書き方の問題点はどれですか。',
      choices: [
        'フレームレートが変わると回る速さも変わる',
        '姿勢が壊れる',
        '遠回りになる',
        '長さが 1 でなくなる',
      ],
      answer: 0,
      explain:
        '1 秒あたりの呼び出し回数が環境で変わるためです。`1 - Math.pow(残す割合, dt)` を係数にするか、`rotateTowards` で角速度そのものを指定します。',
    },
  ],
};
