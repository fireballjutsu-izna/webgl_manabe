import type { Chapter } from '../types.ts';

export const chapterB25: Chapter = {
  slug: 'b25-dot-angle',
  part: 'basics',
  number: 25,
  title: '内積と角度 ― 符号だけで位置関係が分かる',
  goal: '内積が長さと cos の積であることが分かり、単位ベクトルどうしなら内積がそのまま cos になることを使えるようになります。',
  requires: ['03-dot', 'b20-sin-cos'],
  threeApis: ['Vector3.dot', 'Vector3.angleTo', 'Math.acos'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章の計算だけでは、内積の何がうれしいのか分かりません。
「成分を掛けて足す」で出た数に、どんな意味があるのか。

実は内積には**もう 1 つの顔**があり、そちらが 3D での主役です。
この章でその顔を見ます。ここが分かると、内積が急に使える道具になります。
`,
    },
    {
      kind: 'md',
      text: `
## もう 1 つの式

同じ内積は、**長さと角度**からも計算できます。
成分をまったく見なくても、2 本の長さと、そのあいだの角度が分かれば出てしまいます。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathbf{a} \\cdot \\mathbf{b} = \\|\\mathbf{a}\\|\\,\\|\\mathbf{b}\\|\\cos\\theta',
      readAloud:
        'a ドット b は、a の長さ・b の長さ・そして 2 本のなす角のコサインを、3 つ掛け合わせた値に等しい、という意味です。θ（シータ）は角度を表すギリシャ文字です。',
      worked: {
        given:
          '前の章と**まったく同じ** $\\mathbf{a} = (2,\\; 0,\\; 0)$、$\\mathbf{b} = (0,\\; 3,\\; 0)$ を、こんどは長さと角度から計算します。',
        steps: [
          { calc: 'a の長さ  = 2' },
          { calc: 'b の長さ  = 3' },
          { calc: 'なす角    = 90 度' },
          { calc: 'cos 90 度 = 0', note: 'ここが 0 になるので、掛けた結果も 0 になる' },
          { calc: '2 x 3 x 0 = 0' },
        ],
        result:
          '**前の章と同じ 0 が出ました。** 別々に見えた 2 つの式が、同じ答えを返すことをいま自分で確かめたことになります。片方は成分から、もう片方は角度から ― どちらから入っても同じ場所に着きます。',
      },
    },
    {
      kind: 'md',
      text: `
## 符号だけを見れば、位置関係が分かる

コサインは[](#/ch/b20-sin-cos)でやったばかりです。ここで効いてくるのは
「**なす角が 0°のとき 1、90°のとき 0、180°のとき −1 になる、なめらかな値**」という性質で、
単位円の横位置がそのまま出てきていると思ってください。

長さは必ず 0 以上なので、**内積の符号は cos の符号だけで決まります**。
ここから、恐ろしく安上がりな判定が手に入ります。

- 内積が**プラス** … 2 本は同じ側を向いている（なす角は 90°未満）
- 内積が**ゼロ** … ぴったり{{直交}}している（90°）
- 内積が**マイナス** … 逆側を向いている（90°より大きい）

**掛け算 3 回と足し算 2 回だけで、位置関係が分かる。**
角度を求める必要すらありません。これが内積が至るところで使われる理由です。
`,
    },
    {
      kind: 'demo',
      id: 'dot-angle',
      caption:
        '角度のスライダーを 0°から 180°までゆっくり動かして、内積の符号が切り替わる瞬間を見てください。ちょうど 90°で 0 を通過します。「長さを 1 に揃える」を入れると、内積の値がそのまま cosθ になります。',
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '長さ 1 に揃えてから使うのが定石',
      text: `
両方を{{正規化}}しておくと、内積はそのまま cosθ になります（長さ 1 × 長さ 1 × cosθ なので）。
つまり **必ず −1 から 1 のあいだに収まる**扱いやすい値になります。
向きだけを比べたいときは、まず正規化。これを習慣にしてください。
`,
    },
    {
      kind: 'md',
      text: `
## 角度そのものが欲しいときは

内積から角度に戻すには、cos の逆をやる関数（\`Math.acos\`）を通します。
ただし**両方を正規化してから**でないと正しく出ません。

three には \`a.angleTo(b)\` があり、正規化と \`acos\` をまとめてやってくれます。
返るのはラジアンです。

ただ、**角度に戻す必要は思っているより少ない**です。
「正面 60 度以内か」のような判定は、角度に戻さず
**cos のまま比べたほうが速く、しかも安全**です。理由は次の 2 つ。

- \`acos\` は重い計算で、毎フレーム大量に呼ぶと効いてくる
- 丸め誤差で内積がわずかに 1 を超えると、\`acos\` は \`NaN\` を返す

**「角度が本当に必要か」を一度考える**のが、内積を使うときのコツです。
`,
    },
    {
      kind: 'code',
      title: '内積から角度を出す（必要なときだけ）',
      code: `import * as THREE from 'three';

const a = new THREE.Vector3(1, 0, 0);
const b = new THREE.Vector3(1, 1, 0).normalize();

// 単位ベクトルどうしなら、内積がそのまま cos
a.dot(b);            // 0.7071...

// 角度に戻す（ラジアン）
Math.acos(a.dot(b)); // 0.7853... = 45 度
a.angleTo(b);        // 同じ。正規化も内部でやってくれる

// 丸め誤差で 1 を超えると NaN になるので、自分で書くなら挟む
const c = THREE.MathUtils.clamp(a.dot(b), -1, 1);
Math.acos(c);

// 比べるだけなら、角度に戻さないほうがよい
if (a.dot(b) > 0.7) { /* だいたい 45 度以内 */ }`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '正規化を忘れると、しきい値が意味を失います',
      text: `
正規化していないベクトルの内積は、長さが変わるだけで値が跳ね上がります。
「0.7 以上なら正面」といった判定は、**両方が長さ 1 のときにだけ**成り立ちます。
判定がうまく動かないときは、まず正規化を疑ってください。

厄介なのは、**エラーが出ないこと**です。しきい値を超えっぱなしになり、
「常に正面と判定される」という形で、静かに壊れます。
`,
    },
  ],
  exercises: [
    {
      prompt: `デモで「両方の長さを 1 に揃える」を入れてから、角度を 0 度・60 度・90 度・180 度にしてください。
そのときの a·b の値を 4 つ書き出し、**cosθ の値と見比べて**ください。`,
      hint: '長さを両方 1 にすると、内積の式から長さの掛け算が消えます。',
      answer: `0 度で 1、60 度で 0.5、90 度で 0、180 度で −1 になり、**cosθ そのもの**と一致します。
長さが両方 1 なら、内積は $1 \\times 1 \\times \\cos\\theta$ だからです。

だから「単位ベクトルどうしの内積は cos」と覚えてしまってよく、
ライティングの計算はこれを使っています。`,
    },
    {
      prompt: `長さ 3 のベクトルと長さ 4 のベクトルがあり、なす角が 60 度です。
$\\cos 60^\\circ = 0.5$ として、手で計算してください。

1. 内積の値
2. 両方を正規化してから内積を取った場合の値`,
      hint: '2 は長さが両方 1 になります。',
      answer: `1. $3 \\times 4 \\times 0.5 = $ **6**
2. $1 \\times 1 \\times 0.5 = $ **0.5**

**同じ 2 本なのに、値が 12 倍違います。**
1 の 6 という数字からは、角度が 60 度だとは読み取れません。
長さの情報が混ざっているからです。

角度だけを知りたいなら、**必ず正規化してから**内積を取ってください。
そうすれば返る値は必ず −1〜1 で、そのまま cos として読めます。`,
    },
    {
      prompt: `$\\mathbf{a} = (3,\\; 0,\\; 4)$ と $\\mathbf{b} = (0,\\; 5,\\; 0)$ について、
成分から内積を計算し、そこから**なす角**を求めてください。`,
      hint: 'まず成分から内積を出し、つぎに長さの式と突き合わせます。',
      answer: `**成分から** … $3 \\times 0 + 0 \\times 5 + 4 \\times 0 = $ **0**

長さは $\\|a\\| = 5$、$\\|b\\| = 5$ なので、$5 \\times 5 \\times \\cos\\theta = 0$。
$25\\cos\\theta = 0$ より $\\cos\\theta = 0$、つまり **$\\theta = 90$ 度**です。

長さがどちらも 0 でないのに内積が 0 になったら、**直交**しているということです。
成分を眺めただけでは分かりにくい関係が、掛けて足すだけで出てきました。`,
    },
  ],
  quiz: [
    {
      q: '2 本のベクトルの内積が **0** でした。2 本の関係はどれですか。',
      choices: [
        '直角に交わっている',
        '同じ向きを向いている',
        '正反対を向いている',
        'どちらかの長さが必ず 0',
      ],
      answer: 0,
      explain:
        '内積は長さ × 長さ × cosθ で、cos90° は 0 です。どちらかの長さが 0 でも内積は 0 になりますが、両方に長さがある場合は「直交している」ことを意味します。',
    },
    {
      q: '長さ 1 に揃えた 2 本のベクトルの内積が **−1** でした。なす角は何度ですか。',
      choices: ['180°', '0°', '90°', '45°'],
      answer: 0,
      explain:
        '両方が長さ 1 なら内積はそのまま cosθ です。cosθ が −1 になるのは θ が 180°のとき、つまり正反対を向いているときです。',
    },
    {
      q: '正規化していない長いベクトルどうしで内積を取ると、何が起きますか。',
      choices: [
        '長さのぶんだけ値が大きくなり、しきい値による判定が壊れる',
        '常に 0 になる',
        '常に 1 と −1 のあいだに収まる',
        'エラーになって計算できない',
      ],
      answer: 0,
      explain:
        '内積には長さが掛かっています。−1〜1 に収まるのは両方が長さ 1 のときだけなので、角度で判定したいなら先に正規化が必要です。エラーが出ないぶん、気づきにくい壊れ方をします。',
    },
  ],
};
