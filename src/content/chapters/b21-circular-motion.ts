import type { Chapter } from '../types.ts';

export const chapterB21: Chapter = {
  slug: 'b21-circular-motion',
  part: 'basics',
  number: 21,
  title: '円運動 ― cos と sin で物を回す',
  goal: '時間から角度を作り、cos と sin で円軌道を書けるようになります。進む向きに機首を向ける方法も分かります。',
  requires: ['b20-sin-cos'],
  threeApis: ['Math.cos', 'Math.sin', 'Clock.getElapsedTime', 'Object3D.position'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章で、角度から座標が作れるようになりました。
その角度を**時間から作る**と、物が回りはじめます。

- 惑星が太陽のまわりを公転する
- カメラが対象のまわりを旋回する
- 敵が中心のまわりをパトロールする

どれも同じ 3 行で書けます。この章はその 3 行の中身です。
`,
    },
    {
      kind: 'md',
      text: `
## 時間を角度にする

いちばん単純な形はこれです。

- **角度 ＝ 経過時間 × 1 秒あたりに進む角度**

経過時間が 2 秒で、1 秒あたり 90 度なら、角度は 180 度。
これを $\\cos$ と $\\sin$ に入れて座標にすれば、円軌道になります。

ここで[](#/ch/b06-float)の話が効いてきます。
**角度を毎フレーム足していくのではなく、経過時間から毎回計算し直してください。**
足していくと誤差が積み上がり、長時間走らせたときに軌道がずれます。
`,
    },
    {
      kind: 'formula',
      tex: '\\begin{aligned} x &= r\\cos(\\omega t) \\\\ z &= r\\sin(\\omega t) \\end{aligned}',
      readAloud:
        'ω（オメガ）は 1 秒あたりに進む角度、t は経過秒数です。その掛け算で「いまの角度」を作り、cos を x に、sin を z に入れると、半径 r の円の上を回ります。',
      worked: {
        given: '半径 $r = 4$、1 秒あたり 90 度（$\\omega = \\pi/2$）で回すとき、3 つの時刻の位置を出します。',
        steps: [
          { calc: 't = 0 秒 : 角度 0    → (4×1,    4×0)   = (4, 0)', note: '右からスタート' },
          { calc: 't = 1 秒 : 角度 π/2 → (4×0,    4×1)   = (0, 4)', note: '90 度進んだ' },
          { calc: 't = 2 秒 : 角度 π   → (4×(-1), 4×0)   = (-4, 0)', note: '半周' },
          { calc: 't = 4 秒 : 角度 2π  → (4×1,    4×0)   = (4, 0)', note: '1 周して戻った' },
        ],
        result:
          '**4 秒で 1 周**しました。1 秒に 90 度なので当然です。$\\omega$ を 2 倍すれば 2 秒で 1 周、半分にすれば 8 秒で 1 周になります。**$\\omega$ が速さ、$r$ が軌道の大きさ**と、役割が完全に分かれています。',
      },
    },
    {
      kind: 'md',
      text: `
## どの平面で回すかを決める

$\\cos$ と $\\sin$ をどの成分に入れるかで、回る面が変わります。

- **床の上を回る（xz 平面）** … x に $\\cos$、**z に $\\sin$**。惑星の公転や旋回はこれ
- **正面から見て回る（xy 平面）** … x に $\\cos$、y に $\\sin$。時計の針や観覧車
- **横から見て回る（yz 平面）** … y に $\\cos$、z に $\\sin$

3D でいちばん多いのは 1 つ目です。数学の教科書は 2 つ目で描かれているので、
**教科書の式をそのまま写すと、意図せず縦に回ります。**

回す向きを逆にしたいときは、$\\sin$ の符号を反転させるか、角度そのものにマイナスを付けます。
`,
    },
    {
      kind: 'md',
      text: `
## 進む向きに機首を向ける

円軌道に乗せただけだと、物体は向きを変えずに平行移動しているように見えます。
飛行機や車なら、**進行方向を向いていてほしい**はずです。

円の上を進む向き（接線の向き）は、**中心へ向かう向きから 90 度ずれています**。
なので回転角に $90^\\circ$（$\\pi/2$）を足し引きすれば向きが合います。

符号がどちらになるかは、回る向きと軸の取り方で変わります。
**覚えるより、両方試して合うほうを採るのが早いです。**
迷ったら、$\\pi/2$ を足す・引く・角度自体にマイナスを付ける、の組み合わせを順に試してください。
`,
    },
    {
      kind: 'code',
      title: '円運動とゆらぎ',
      code: `const clock = new THREE.Clock();

function animate() {
  const t = clock.getElapsedTime();

  // 原点のまわりを半径 3 でまわる（xz 平面 ＝ 床の上）
  const radius = 3;
  planet.position.x = Math.cos(t) * radius;
  planet.position.z = Math.sin(t) * radius;

  // ふわふわ浮く。0.5 は速さ、0.3 は揺れ幅
  float.position.y = 1 + Math.sin(t * 0.5) * 0.3;

  // 進む向きに機首を向ける（円運動の接線は 90 度ずれている）
  planet.rotation.y = -t + Math.PI / 2;

  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '楕円にしたいなら、r を 2 つに分ける',
      text: `
x と z で違う半径を使うと、円ではなく楕円になります。

\`x = Math.cos(t) * 5\` と \`z = Math.sin(t) * 3\` なら、横に長い楕円軌道です。
惑星の公転や、見下ろし視点のカメラの動きに使えます。
**式は同じで、掛ける数を変えるだけ**です。
`,
    },
    {
      kind: 'md',
      text: `
## 上下のゆらぎも、同じ道具

y に $\\sin$ を入れると、円ではなく上下運動になります。
「浮いている感じ」を出すのに、これだけでかなり効きます。

- **中心の高さ** … $\\sin$ の外に足す
- **揺れ幅** … $\\sin$ に掛ける
- **速さ** … 時間に掛ける

$y = 1 + 0.3\\sin(0.5t)$ なら、高さ 1 を中心に、幅 0.3 で、ゆっくり揺れます。
この「足す・掛ける」の役割分担は、次の章で波として整理します。
`,
    },
  ],
  exercises: [
    {
      prompt: `半径 4 の円の上を、1 秒あたり 90 度の速さで回る物体を書いてください。\`t\` は経過秒数です。
床の上（xz 平面）で回します。`,
      hint: '三角関数はラジアンで受け取ります。1 周は $2\\pi$ ラジアンです。',
      answer: `角度を「経過秒 × 1 秒あたりの角度」で作り、cos を x、sin を z に入れます。

**度のままで \`Math.cos\` に渡すのは典型的な間違い**です。90 を渡すと 90 ラジアン
（約 14 周ぶん）と解釈されます。
\`THREE.MathUtils.degToRad\` を通すか、はじめからラジアンで書いてください。`,
      answerCode: `const speed = THREE.MathUtils.degToRad(90); // 1 秒あたり 90 度
const angle = t * speed;

mesh.position.set(Math.cos(angle) * 4, 0, Math.sin(angle) * 4);`,
    },
    {
      prompt: `半径 5、1 周 6 秒で回る物体があります。手で計算してください。

1. 1 秒あたり何ラジアン進むか（$\\omega$）
2. $t = 1.5$ 秒のときの角度（ラジアン）
3. そのときの位置（$\\cos(\\pi/2) = 0$、$\\sin(\\pi/2) = 1$ を使います）`,
      hint: '1 周 = $2\\pi$ ラジアンを 6 秒で回ります。',
      answer: `1. $\\omega = 2\\pi \\div 6 = \\pi/3 \\approx $ **1.047 ラジアン毎秒**
2. $1.5 \\times \\pi/3 = \\pi/2 \\approx $ **1.571 ラジアン**（＝ 90 度）
3. $(5 \\times 0,\\; 5 \\times 1) = $ **$(0,\\; 5)$**

1.5 秒はちょうど 4 分の 1 周です（6 秒で 1 周なので）。
だから右からスタートして、90 度進んだ位置にいます。
**「1 周にかかる秒数」から $\\omega$ を出す**この計算は、周期を指定したいときに毎回使います。`,
    },
    {
      prompt: `ある人が円運動を書いたところ、物体が**円ではなく斜めの直線を往復**しました。
コードは \`x = Math.cos(t) * r\` と \`z = Math.cos(t) * r\` です。
なぜそうなるのか、絵で考えて説明してください。`,
      hint: 'x と z が常に同じ値なら、点はどこにいますか。',
      answer: `x と z が常に同じ値なので、点は必ず **$x = z$ の直線の上**にいます。
そのうえで $\\cos t$ が $-1$ から $1$ を往復するので、
直線上を行ったり来たりするだけになります。

円になるには、**2 つの座標が 90 度ずれた波である**必要があります。
$\\cos$ と $\\sin$ はまさにその関係で、片方が最大のときもう片方が 0 になります。
その「ずれ」が、点を直線から円へ膨らませています。

なお $\\sin$ と $\\sin$ にしても同じく直線になります。ずれているかどうかが本質です。`,
    },
  ],
  quiz: [
    {
      q: '物体を xz 平面で原点のまわりに回したいとき、位置の作り方として正しいのはどれですか。',
      choices: [
        'x に `Math.cos(t) * r`、z に `Math.sin(t) * r`',
        'x に `Math.sin(t) * r`、y に `Math.sin(t) * r`',
        'x にも z にも `Math.cos(t) * r`',
        'x に `t * r`、z に `t * r`',
      ],
      answer: 0,
      explain:
        '2 つの座標に、90 度ずれた波（cos と sin）を入れると円になります。両方 cos にすると斜めの直線を往復するだけになります。',
    },
    {
      q: '1 周 4 秒で回したいとき、1 秒あたりに進む角度（ラジアン）はいくつですか。',
      choices: ['$\\pi/2$', '$\\pi$', '$2\\pi$', '$4\\pi$'],
      answer: 0,
      explain:
        '1 周が $2\\pi$ ラジアンなので、4 秒で割って $\\pi/2$ です。度で言えば 1 秒あたり 90 度になります。',
    },
    {
      q: '円運動する物体を、進行方向に向かせたいときはどうしますか。',
      choices: [
        '回転角に 90 度ぶんを足す（または引く）',
        '半径を大きくする',
        'cos と sin を入れ替える',
        '回転は不要で、自動的に向く',
      ],
      answer: 0,
      explain:
        '円の接線は、中心へ向かう向きから 90 度ずれています。符号は回る向きと軸の取り方で変わるので、両方試して合うほうを採るのが早道です。',
    },
  ],
};
