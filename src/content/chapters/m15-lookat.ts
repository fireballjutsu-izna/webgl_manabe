import type { Chapter } from '../types.ts';

export const chapterM15: Chapter = {
  slug: 'm15-lookat',
  part: 'math3d',
  number: 15,
  title: 'lookAt ― 向きを 1 行で決める',
  goal: 'lookAt が 3 本の軸を組み立てていることが分かり、物体とカメラで向きが逆になる理由や、真上を狙うと崩れる理由を説明できます。',
  requires: ['m12-axis-angle', 'm03-basis'],
  threeApis: [
    'Object3D.lookAt',
    'Object3D.up',
    'Matrix4.lookAt',
    'Quaternion.setFromRotationMatrix',
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

「あっちを向かせたい」は、3D でいちばん頻繁に書く指示です。
カメラを被写体へ、砲塔を的へ、看板を視点へ。

three には \`lookAt()\` があり、1 行で済みます。ところが実際に使うと、

- **物体が的の反対を向く**
- **真上を狙わせたら、向きが暴れる**
- カメラでは正しく動いたのに、同じコードを物体に書いたら**裏返る**

どれも lookAt の中身を知っていれば、その場で説明が付きます。
中身は、実は**すでに学んだ道具だけ**でできています。
`,
    },
    {
      kind: 'md',
      text: `
## やっているのは、軸を 3 本組み立てること

[](#/ch/m03-basis)で見たとおり、**行列の列は「軸の行き先」**でした。
向きを決めるとは、その 3 本の軸を決めることに他なりません。

lookAt は、次の順で 3 本を作ります。

1. **前** … 的へ向かう向き。$\\hat{z} = $ 正規化した「的 − 自分」
2. **右** … 前と「世界の上」の両方に垂直な向き。$\\hat{x} = \\hat{up} \\times \\hat{z}$ を正規化
3. **上** … 前と右の両方に垂直な向き。$\\hat{y} = \\hat{z} \\times \\hat{x}$

[](#/ch/04-cross)の外積が 2 回出てくるだけです。
**3 本目に外積をもう一度使うのが要点**で、こうすると 3 本が必ず互いに直交します。

なぜ「世界の上」をそのまま $\\hat{y}$ にしないのか。
それは的が斜め上や斜め下にあるとき、**前と直角にならない**からです。
`,
    },
    {
      kind: 'formula',
      tex: '\\hat{z} = \\frac{t - e}{\\|t - e\\|}, \\quad \\hat{x} = \\frac{\\hat{up} \\times \\hat{z}}{\\|\\hat{up} \\times \\hat{z}\\|}, \\quad \\hat{y} = \\hat{z} \\times \\hat{x}',
      readAloud:
        '前の向きは「的から自分を引いて長さ 1 にしたもの」。右の向きは「上と前の外積を長さ 1 にしたもの」。上の向きは「前と右の外積」です。3 本目は既に長さ 1 なので、正規化が要りません。',
      worked: {
        given: '原点にいる物体を、的 $t = (4,\\; 0,\\; 3)$ のほうへ向けます。上は $(0,\\; 1,\\; 0)$ です。',
        steps: [
          { calc: '的への向き : (4,0,3) 長さ √(16+9) = 5' },
          { calc: 'z = (4/5, 0, 3/5) = (0.8, 0, 0.6)', note: '前' },
          { calc: 'up × z : (0,1,0)×(0.8,0,0.6) = (0.6, 0, -0.8)' },
          { calc: '  長さ : √(0.36+0.64) = 1 なので、そのまま x', note: '右' },
          { calc: 'y = z × x : (0.8,0,0.6)×(0.6,0,-0.8) = (0, 1, 0)', note: '上' },
          { calc: '確認 : z·x = 0.8x0.6 + 0.6x(-0.8) = 0', note: '直交している' },
        ],
        result:
          '3 本の軸 $(0.6, 0, -0.8)$、$(0, 1, 0)$、$(0.8, 0, 0.6)$ が組み上がりました。**この 3 本を列に並べた行列が、そのまま姿勢**です（[](#/ch/m03-basis)）。的が水平面上にあったので、上の軸は世界の上と一致しています。',
      },
    },
    {
      kind: 'demo',
      id: 'look-at',
      caption:
        '的を動かすと、3 本の軸が組み替わります。「カメラとして向ける」を入れると、アンバーの z 軸が的の反対を向くことに注目してください。「真上を狙う」を押すと、的の向きが世界の up と重なり、右と上が決められなくなります。',
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: '物体は +z が的を向き、カメラは −z が的を向きます',
      text: `
three の lookAt は、物体とカメラで中身が違います。

ふつうの物体（Mesh、Group など）は +z 軸が的を向きます。
カメラとライトは −z 軸が的を向きます。

カメラが逆なのは、カメラが「自分の −z 方向を写す」ように作られているためで、
これは OpenGL 以来の約束事です。

だから自作のモデルは、機首を +z に向けて作っておくと lookAt がそのまま効きます。
「lookAt したのに背中を向ける」なら、まずモデルの正面の向きを疑ってください。
`,
    },
    {
      kind: 'md',
      text: `
## 真上を狙うと壊れる

$\\hat{x} = \\hat{up} \\times \\hat{z}$ でした。ここで**的が真上にあると、$\\hat{z}$ が $\\hat{up}$ と平行**になります。

[](#/ch/04-cross)で見たとおり、**平行なベクトルの外積はゼロベクトル**です。
長さ 0 なので正規化できず、右の向きが決まりません。

決まらないのは当然で、**真上を向いたとき「右」はどこでもよい**からです。
真上を見上げたまま、その場でくるりと回れます。どの姿勢も等しく正しい。

[](#/ch/m11-gimbal)の北極の話と、まったく同じ縮退です。
three は 0.0001 だけ軸をずらして計算を続けますが、
**その付近では向きが不安定に暴れます**。
`,
    },
    {
      kind: 'md',
      text: `
## up は「ひねりを決める道具」

ここまでで、\`up\` の役割がはっきりします。

前の向きだけでは姿勢は決まりません。**その軸のまわりにひねる自由が残っている**からです。
そのひねりを決めるために持ち込む「もう 1 本の参考の向き」が \`up\` です。

だから \`up\` を変えれば、同じ的を見たまま**ひねりだけを変えられます**。

- 通常の 3D … $(0,\\; 1,\\; 0)$。地面が水平に見える
- 惑星の表面を歩く … 足元から見た「上」（球の中心から外向き）
- 完全に自由な飛行 … lookAt を使わず、クォータニオンで姿勢そのものを持つ

3 つ目が本質的な答えです。**ひねりまで自分で管理したいなら、lookAt は向きません。**
`,
    },
    {
      kind: 'code',
      title: 'lookAt を使う・自分で組む',
      code: `import * as THREE from 'three';

// いちばん単純な形
camera.lookAt(target.position);
mesh.lookAt(0, 5, 0);

// up を変えてひねりを決める（惑星の表面など）
walker.up.copy(walker.position).normalize();  // 中心から外向き
walker.lookAt(destination);

// 物体を動かさずに「目標の姿勢」だけを作る
const helper = new THREE.Object3D();
helper.position.copy(model.position);
helper.lookAt(enemy.position);
model.quaternion.slerp(helper.quaternion, 0.1);   // なめらかに向く

// 中でやっていること
const m = new THREE.Matrix4().lookAt(eye, target, up);
const q = new THREE.Quaternion().setFromRotationMatrix(m);

// 親が回っている物体では、lookAt はワールド座標で解釈される
parent.updateMatrixWorld(true);`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'ひねりを残したいなら setFromUnitVectors',
      text: `
lookAt は up を使って、ひねり（ロール）まで決めてしまいます。
「向きだけ合わせて、いまのひねりは残したい」ときには強すぎます。

そういうときは setFromUnitVectors を使います。
いまの正面を、目標の向きへ重ねるだけの最小の回転を作るので、
余計なひねりが入りません。

[](#/ch/m12-axis-angle)で見た「外積で軸、内積で角度」を、
three が安全に済ませてくれる形です。
`,
    },
    {
      kind: 'md',
      text: `
## ここまでが回転です

7 章で手に入れたものを並べます。

- **回転の式** … $\\cos$ と $\\sin$ で成分が混ざる。長さは変わらない
- **オイラー角** … 3 つの角度。読みやすいが、順番が結果を変える
- **ジンバルロック** … 軸が重なり、自由度が 1 つ消える
- **軸と角度** … どんな向きも 1 回の回転で作れる
- **クォータニオン** … それを 4 つの数に詰め、掛け算で重ねられる形にしたもの
- **slerp** … 2 つの姿勢を最短・等速で繋ぐ
- **lookAt** … 外積 2 回で 3 本の軸を組む

**次からは階層です。** 物体を親子にすると、これまでの変換が入れ子になります。
「どの座標系で考えているか」が、そこで本当に重要になります。
`,
    },
  ],
  exercises: [
    {
      prompt: `原点にいる物体を、的 $(0,\\; 0,\\; 5)$ のほうへ向けます。上は $(0,\\; 1,\\; 0)$ です。
3 本の軸を手で計算してください。`,
      hint: '$\\hat{z}$ → $\\hat{x} = \\hat{up} \\times \\hat{z}$ → $\\hat{y} = \\hat{z} \\times \\hat{x}$ の順です。',
      answer: `**前** … $(0,0,5)$ の長さは 5 なので、$\\hat{z} = (0,\\; 0,\\; 1)$

**右** … $(0,1,0) \\times (0,0,1) = (1 \\times 1 - 0 \\times 0,\\;\\; 0 \\times 0 - 0 \\times 1,\\;\\; 0 - 0) = (1,\\; 0,\\; 0)$

**上** … $(0,0,1) \\times (1,0,0) = (0 \\times 0 - 1 \\times 0,\\;\\; 1 \\times 1 - 0 \\times 0,\\;\\; 0 - 0) = (0,\\; 1,\\; 0)$

3 本は $(1,0,0)$、$(0,1,0)$, $(0,0,1)$ ― **もとの軸のまま**でした。

当然です。物体はもともと $+z$ を向いており、的も $+z$ の方向にあるので、
**回す必要がありません**。この行列は単位行列で、姿勢は変わらないままです。

計算が単位行列に落ちるかどうかは、**式が合っているかの手軽な確認**になります。`,
    },
    {
      prompt: `カメラを \`camera.lookAt(0, 10, 0)\`（真上）としたところ、
向きがフレームごとにちらつきました。原因を、外積の性質から説明してください。`,
      hint: '平行な 2 本のベクトルの外積は、どうなりましたか。',
      answer: `**的への向きが up と平行になり、外積がゼロベクトルになるからです。**

$\\hat{z} = (0,1,0)$、$\\hat{up} = (0,1,0)$ なので、
$\\hat{up} \\times \\hat{z} = (0,\\; 0,\\; 0)$ ― 長さ 0 で、正規化できません。

three はこの場合、軸を 0.0001 だけずらして計算を続けます。
**ずらす向きは計算のたびに同じですが、その付近では入力のごくわずかな違いが
向きを大きく変える**ので、位置が少しでも揺れるとひねりが暴れます。

これは[](#/ch/m11-gimbal)の縮退と同じ構造です。
真上を向いたとき「右」はどこでもよく、**どの姿勢も等しく正しい** ―
一意に決まらないものを、無理に 1 つ選ぼうとしているのが原因です。

対策は、真上ちょうどを避ける（$89.9$ 度で止める）か、
lookAt を使わずクォータニオンで姿勢を持つかのどちらかです。`,
    },
    {
      prompt: `自作の矢印モデルが $+y$（上）を正面として作られています。
これを \`lookAt(target)\` で的に向けたら、真横を向いてしまいました。
2 通りの直し方を挙げてください。`,
      hint: 'lookAt が向けるのは、モデルのどの軸でしたか。',
      answer: `lookAt が的に向けるのは **$+z$ 軸**なのに、モデルの正面が $+y$ だからです。
90 度ずれた向きになります。

**直し方 1 ― モデル側を直す。**
ジオメトリを作った時点で 90 度回しておき、正面を $+z$ に揃えます。
根本的な解決で、以後どこでも lookAt がそのまま効きます。

**直し方 2 ― 入れ物をかぶせる。**
\`Group\` を作って lookAt はそちらに掛け、
中の矢印だけをあらかじめ 90 度傾けておきます。
元のモデルに触れないので、他人が作った glTF などに有効です。

**「lookAt したのに変な向き」の大半はこれです。**
まずモデルの正面がどの軸かを確かめてください。`,
      answerCode: `// 1. ジオメトリ側を直す（作った直後に 1 回だけ）
geometry.rotateX(Math.PI / 2);   // +y 正面を +z 正面へ

// 2. 入れ物をかぶせる
const holder = new THREE.Group();
arrow.rotation.x = Math.PI / 2;
holder.add(arrow);
holder.lookAt(target);           // holder の +z が的を向く`,
    },
  ],
  quiz: [
    {
      q: 'lookAt が内部でやっていることは、どれですか。',
      choices: [
        '外積を 2 回使って、互いに直交する 3 本の軸を組み立てる',
        'オイラー角を 3 つ計算する',
        '物体を的の位置まで移動させる',
        '的との距離に応じて拡大する',
      ],
      answer: 0,
      explain:
        '「前」を的への向きから決め、「右」を up との外積で、「上」を前と右の外積で作ります。3 本を列に並べたものが、そのまま姿勢の行列になります。',
    },
    {
      q: 'Three.js で、的のほうを向くのはどの軸ですか。',
      choices: [
        'ふつうの物体は +z、カメラとライトは −z',
        'どちらも +z',
        'どちらも −z',
        'ふつうの物体は +y、カメラは +z',
      ],
      answer: 0,
      explain:
        'カメラは自分の −z 方向を写すように作られているためです。自作モデルは正面を +z に向けて作っておくと、lookAt がそのまま使えます。',
    },
    {
      q: '真上を lookAt すると向きが不安定になるのは、なぜですか。',
      choices: [
        '的への向きが up と平行になり、外積がゼロになるため',
        '距離が遠すぎるため',
        '角度が 90 度を超えるため',
        'カメラの near が小さすぎるため',
      ],
      answer: 0,
      explain:
        '平行なベクトルの外積はゼロベクトルなので、「右」の向きが決まりません。真上を向いたとき「右」はどこでもよく、一意に決まらないものを無理に選ぼうとしている状態です。',
    },
  ],
};
