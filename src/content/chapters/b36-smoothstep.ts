import type { Chapter } from '../types.ts';

export const chapterB36: Chapter = {
  slug: 'b36-smoothstep',
  part: 'basics',
  number: 36,
  title: 'smoothstep ― 境目をぼかす',
  goal: 'smoothstep を動きにも見た目の境目にも使えるようになり、シェーダで至るところに出てくる理由が分かります。',
  requires: ['b35-easing'],
  threeApis: ['MathUtils.smoothstep', 'MathUtils.smootherstep', 'MathUtils.clamp'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

前の章のイージングのうち、**1 つだけ特別扱いしたいもの**があります。
smoothstep です。

理由は、これが**動き以外にも使われる**からです。

- 光と影の境目をぼかす
- 霧が始まる距離をなめらかにする
- 昼と夜の切り替わりをなだらかにする
- 輪郭線の太さを距離で変える

シェーダを読むと \`smoothstep\` が至るところに出てきます。
この章でその正体を押さえておくと、第3部以降がだいぶ楽になります。
`,
    },
    {
      kind: 'md',
      text: `
## 両端で傾きが 0 になる曲線

smoothstep は、$t = 0$ と $t = 1$ の**両方で傾きが 0** になる曲線です。

- 傾きが 0 ＝ その瞬間の速さが 0
- つまり**静かに出発して、静かに止まる**

前の章の ease-in-out と同じ役割ですが、smoothstep は式がとても短く、
**GLSL に組み込み関数として入っている**ぶん、実装でよく選ばれます。
`,
    },
    {
      kind: 'formula',
      tex: '\\mathrm{smoothstep}(t) = t^2\\,(3 - 2t)',
      readAloud:
        'いちばんよく使われる、ゆっくり始まってゆっくり止まる曲線です。t が 0 のとき 0、1 のとき 1 になり、その両端で傾きが 0 になる――つまり出発と停止がなめらかになります。',
      worked: {
        given: '同じ $t$ を smoothstep に通したときの値を、3 か所で見ます。',
        steps: [
          {
            calc: 't = 0.1 : 0.01 x (3 - 0.2) = 0.01 x 2.8 = 0.028',
            note: '端では、まだほとんど進まない',
          },
          { calc: 't = 0.5 : 0.25 x (3 - 1.0) = 0.25 x 2.0 = 0.5', note: '真ん中は変わらない' },
          {
            calc: 't = 0.9 : 0.81 x (3 - 1.8) = 0.81 x 1.2 = 0.972',
            note: '端では、また進みが鈍る',
          },
        ],
        result:
          '0.1 が 0.028 に、0.9 が 0.972 に**押し出されました**。真ん中はそのまま。これが「ゆっくり始まってゆっくり止まる」の正体で、**式は $t$ を別の $t$ に置き換えているだけ**です。',
      },
    },
    {
      kind: 'md',
      text: `
## 3 引数版 ― しきい値をぼかす道具として

three や GLSL の \`smoothstep\` は、引数を 3 つ取ります。

$\\mathrm{smoothstep}(\\text{edge0},\\; \\text{edge1},\\; x)$

やっていることは、この章までに出てきた**3 段そのもの**です。

1. **逆補間** … $x$ を edge0〜edge1 のあいだで 0〜1 に直す
2. **clamp** … 0〜1 に押し込める
3. **なめらかにする** … $t^2(3-2t)$ を通す

つまり[](#/ch/b34-inverse-lerp)でやった「逆補間 → clamp」に、
仕上げのひとひねりを足しただけです。**新しい概念は 1 つも増えていません。**

これが「境目をぼかす」の中身です。
edge0 より小さければ 0、edge1 より大きければ 1、
そのあいだをなめらかにつなぐ ― まさに**ぼかした境界線**です。
`,
    },
    {
      kind: 'md',
      text: `
## 引数を逆順に書くと、反転する

$\\mathrm{smoothstep}(0.5,\\; 0.2,\\; x)$ のように、
**大きいほうを先に書く**と、値は逆向きに動きます（1 から 0 へ）。

$1 - \\mathrm{smoothstep}(0.2,\\; 0.5,\\; x)$ と書いても同じですが、
引数を入れ替えるほうが短く書けるため、シェーダではよく使われます。

**読むときに引っかかるので、覚えておくと得です。**
「なぜ 0.5 が先に来ているのか」と思ったら、反転を意図しています。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'もっとなめらかにしたいときは smootherstep',
      text: `
smoothstep は「速さ」が両端で 0 になりますが、**「加速度」までは 0 になりません**。
拡大して見ると、動き出しにわずかな段差が残ります。

そこまで気にする場面（カメラのゆっくりした寄りなど）では、
$6t^5 - 15t^4 + 10t^3$ という 5 次の式を使います。**smootherstep** と呼ばれ、
three にも \`MathUtils.smootherstep\` があります。

ふだんは smoothstep で十分です。
`,
    },
    {
      kind: 'code',
      title: 'smoothstep の 2 つの顔',
      code: `import * as THREE from 'three';

// 動きに使う（0〜1 の t を歪ませる）
const eased = (t) => t * t * (3 - 2 * t);
mesh.position.lerpVectors(start, end, eased(t));

// 境目をぼかすのに使う（3 引数版）
THREE.MathUtils.smoothstep(0.3, 0.0, 1.0);   // 逆補間 + clamp + なめらか

// 距離 10〜30 で霧を濃くする
const fog = THREE.MathUtils.smoothstep(distance, 10, 30);
color.lerp(fogColor, fog);

// GLSL でも同じ形（引数の順番に注意）
//   float night = smoothstep(0.08, -0.22, dot(n, sunDir));
//   ↑ 大きいほうが先なので、値は 1 から 0 へ動く`,
    },
    {
      kind: 'md',
      text: `
## ステップ関数との違い

もし「境目」をぼかさずに作るなら、こう書きます。

- $x < 0.5$ なら 0、そうでなければ 1

これは**ステップ関数**で、境目がくっきり出ます。
ドット絵のような表現ならこれでよいのですが、3D では**ギザギザが目立ちます**。

画面の画素は飛び飛びなので、くっきりした境目は
「ある画素は 0、隣の画素は 1」という階段状の縁になります。
[](#/ch/b22-wave)で「頂点より細かい波は作れない」と言ったのと同じ問題です。

smoothstep で 1〜2 画素ぶんぼかしておくと、この階段が消えます。
**ぼかしは手抜きではなく、飛び飛びの画素で連続的なものを描くための手当て**です。
`,
    },
  ],
  exercises: [
    {
      prompt: `$\\mathrm{smoothstep}(t) = t^2(3 - 2t)$ を手で計算してください。

1. $t = 0$
2. $t = 0.25$
3. $t = 1$`,
      hint: '$t^2$ を先に出してから、かっこの中を計算します。',
      answer: `1. $0 \\times 3 = $ **0**
2. $0.0625 \\times (3 - 0.5) = 0.0625 \\times 2.5 = $ **0.15625**
3. $1 \\times (3 - 2) = $ **1**

2 に注目してください。**入力 0.25 が、出力 0.156 に押し下げられています**。
序盤ではまだあまり進まない、ということです。

1 と 3 が変わらないのは、イージング関数の約束（$f(0)=0$、$f(1)=1$）どおりです。`,
    },
    {
      prompt: `距離 $d$ に応じて霧を濃くしたい。$d = 10$ で霧なし、$d = 30$ で完全に霧、としたとき、
$d = 15$ での霧の濃さを手で計算してください。3 段（逆補間 → clamp → smoothstep）で追います。`,
      hint: 'まず 10〜30 の範囲で 15 を 0〜1 に直します。',
      answer: `1. **逆補間** … $(15 - 10) \\div (30 - 10) = 5 \\div 20 = 0.25$
2. **clamp** … 0〜1 の中なのでそのまま 0.25
3. **smoothstep** … $0.25^2 \\times (3 - 0.5) = $ **0.15625**

線形なら 0.25 だったところが、**0.156 に下がりました**。
霧の始まりがゆるやかになる、ということです。

$d = 5$（範囲の外）なら clamp が効いて 0、$d = 40$ なら 1 になります。
**範囲外の扱いが自動で決まる**のが、3 引数版の便利なところです。`,
    },
    {
      prompt: `シェーダで \`smoothstep(0.6, 0.2, x)\` という行を見つけました。
引数が**大きいほうから小さいほうへ**並んでいます。この式は何をしていますか。
また、同じことを別の書き方で表すとどうなりますか。`,
      hint: '順番を入れ替えると、値の動く向きが変わります。',
      answer: `**値が 1 から 0 へ動きます**（反転しています）。

- $x \\le 0.2$ のとき … **1**
- $x \\ge 0.6$ のとき … **0**
- あいだ … なめらかに 1 から 0 へ

同じことは \`1.0 - smoothstep(0.2, 0.6, x)\` と書いても表せます。

引数を入れ替えるほうが短いので、シェーダではこちらがよく使われます。
「なぜ大きいほうが先なのか」と思ったら、**反転を意図している**と読んでください。
たとえば「太陽の側ほど暗い夜の明かり」のような、逆向きの効果に使われます。`,
    },
  ],
  quiz: [
    {
      q: 'smoothstep が両端でなめらかなのは、どういう意味ですか。',
      choices: [
        '$t = 0$ と $t = 1$ で傾きが 0 になり、静かに出発して静かに止まる',
        '値が必ず 0 から 1 のあいだにある',
        '直線に近い形をしている',
        '計算が速い',
      ],
      answer: 0,
      explain:
        '傾きが 0 ということは、その瞬間の速さが 0 だということです。だから動き出しと止まりに段差が出ません。値が 0〜1 に収まるのは、それとは別の性質です。',
    },
    {
      q: '3 引数の `smoothstep(edge0, edge1, x)` がやっていることはどれですか。',
      choices: [
        '逆補間して、clamp して、なめらかにする',
        'x を 2 乗する',
        'edge0 と edge1 を lerp する',
        'x を 0 か 1 に丸める',
      ],
      answer: 0,
      explain:
        'この部でやってきた 3 段そのものに、仕上げのひとひねりを足しただけです。範囲外が自動で 0 か 1 になるので、境目をぼかす用途にそのまま使えます。',
    },
    {
      q: '`smoothstep(0.6, 0.2, x)` のように引数を逆順にすると、どうなりますか。',
      choices: [
        '値が 1 から 0 へ、逆向きに動く',
        'エラーになる',
        '常に 0 が返る',
        '順番は関係ない',
      ],
      answer: 0,
      explain:
        '`1.0 - smoothstep(0.2, 0.6, x)` と同じ結果になります。短く書けるのでシェーダでよく使われますが、読むときに引っかかるところなので覚えておくと得です。',
    },
  ],
};
