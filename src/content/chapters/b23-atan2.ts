import type { Chapter } from '../types.ts';

export const chapterB23: Chapter = {
  slug: 'b23-atan2',
  part: 'basics',
  number: 23,
  title: 'atan2 ― 座標から角度に戻す',
  goal: '2 つの座標から向きの角度を求められるようになり、「マウスのほうを向く」「進む向きに機首を向ける」を自分で書けるようになります。',
  requires: ['b20-sin-cos', 'b14-vector-sub'],
  threeApis: ['Math.atan2', 'Math.atan', 'MathUtils.radToDeg', 'Object3D.lookAt'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

ここまでは「角度 → 座標」でした。実際のコードでは、**その逆**が要ります。

- マウスのほうを向かせたい（マウスの位置は座標で来る）
- 進む向きに機首を向けたい（速度は座標で持っている）
- 2 つの物体が、どちら向きに並んでいるかを知りたい

どれも「座標が分かっていて、角度が欲しい」形です。
これを 1 行でやってくれるのが \`Math.atan2\` で、
**知らないと自力では書けない**種類の関数です。
`,
    },
    {
      kind: 'md',
      text: `
## なぜ割り算では足りないのか

点 $(x,\\; y)$ の角度を求めたい。$\\tan\\theta = \\dfrac{y}{x}$ という関係があるので、
$y/x$ から角度が出せそうに見えます（この逆をやる関数が \`Math.atan\` です）。

ところが、これでは**足りません**。2 つの理由があります。

- **$(1,\\; 1)$ と $(-1,\\; -1)$ で $y/x$ が同じ 1 になる。**
  前者は右上（45 度）、後者は左下（225 度）で、まったく逆の向きなのに区別が付きません
- **$x = 0$ のとき 0 で割ることになる。** 真上と真下が扱えません

つまり割り算をした時点で、**符号の情報が失われている**わけです。
そこで「割る前の $x$ と $y$ を、両方そのまま受け取る」関数が用意されています。
`,
    },
    {
      kind: 'formula',
      tex: '\\theta = \\operatorname{atan2}(y,\\; x)',
      readAloud:
        'atan2 に y と x をこの順で渡すと、原点から点 (x, y) へ向かう向きの角度が返ってきます。y が先、x があとという順番が、この関数のいちばんの引っかかりどころです。',
      worked: {
        given: '4 つの点について、返る角度を見ます（度に直した値を併記します）。',
        steps: [
          { calc: 'atan2(1, 1)   =  0.785 rad =   45°', note: '右上' },
          { calc: 'atan2(1, -1)  =  2.356 rad =  135°', note: '左上' },
          { calc: 'atan2(-1, -1) = -2.356 rad = -135°', note: '左下。y/x はどちらも 1 だが区別できた' },
          { calc: 'atan2(1, 0)   =  1.571 rad =   90°', note: '真上。0 で割らずに済む' },
        ],
        result:
          '返る範囲は **$-\\pi$ から $\\pi$**（$-180^\\circ$ 〜 $180^\\circ$）です。0 度が右、プラスが反時計回りという[](#/ch/b18-angle)の約束どおりで、**下半分がマイナス**で返ってきます。',
      },
    },
    {
      kind: 'demo',
      id: 'atan2-angle',
      caption:
        '点 P を動かすと、原点から P へ向かう角度が出ます。P を下半分に持っていくとマイナスの値になること、真上や真下（x が 0）でもちゃんと答えが返ることを確かめてください。いちばん下の行は、出た角度を sin と cos で座標に戻したもので、元の P と一致します。',
    },
    {
      kind: 'md',
      text: `
## 引数の順番は (y, x)

**ここがこの章でいちばん間違えやすいところ**です。座標は $(x, y)$ の順で書くのに、
\`atan2\` は $y$ を先に受け取ります。

理由は $\\tan\\theta = y/x$ という分数の形にあり、
**分子（$y$）が先、分母（$x$）があと**という並びになっています。

順番を逆にしてもエラーは出ません。**それらしい角度が返ってきてしまいます。**
$(x, y)$ の順で渡すと、$45^\\circ$ を軸に折り返した角度になります
（$30^\\circ$ のつもりが $60^\\circ$ になる、というような形）。

見分け方があります。**「なんとなくずれている」ときは引数の順番、**
**「きれいに 90 度ずれている」ときは軸の取り違え**を疑ってください。
`,
    },
    {
      kind: 'md',
      text: `
## 3D で使うときは、どの平面かを決める

3D の座標は 3 つあるので、そのうち 2 つを選んで渡します。

- **床の上で向きを変える（y 軸まわり）** … \`Math.atan2(dz, dx)\`。いちばん多い
- **正面から見た向き（z 軸まわり）** … \`Math.atan2(dy, dx)\`

床の上の向きを求めるときに \`atan2(dy, dx)\` と書いてしまう間違いが定番です。
高さの差で向きを決めることになるので、**上下に動いたときだけ変な方向を向きます。**

なお床の上で使うときは、符号にもう 1 つ注意が要ります。
Three.js の y 軸まわりの回転は、**上から見て反時計回りがプラス**ですが、
xz 平面で \`atan2(z, x)\` が返す角度は z が増える向きを正としています。
結果として \`rotation.y = -Math.atan2(dz, dx)\` のようにマイナスが要ることが多いです。
**両方試して合うほうを採ってください。**
`,
    },
    {
      kind: 'code',
      title: '座標から向きを作る',
      code: `import * as THREE from 'three';

// 敵をプレイヤーのほうに向かせる（床の上の向きなので y 軸まわり）
const dx = player.position.x - enemy.position.x;
const dz = player.position.z - enemy.position.z;

enemy.rotation.y = -Math.atan2(dz, dx);

// 進行方向に機首を向ける（速度から作る）
mesh.rotation.y = -Math.atan2(velocity.z, velocity.x);

// 度で見たいとき
THREE.MathUtils.radToDeg(Math.atan2(1, 1));   // 45
THREE.MathUtils.radToDeg(Math.atan2(-1, -1)); // -135

// 高さも含めて向けたいなら、角度を経由せず lookAt のほうが早い
enemy.lookAt(player.position);`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: '角度が要らないなら、lookAt を使う',
      text: `
「向かせたい」だけなら、\`object.lookAt(target)\` で済みます。
内部で向きを計算してくれるので、平面の選択も符号も気にせずに済みます。

\`atan2\` が要るのは、**角度そのものが欲しいとき**です。
角度を補間したい、角度で条件分岐したい、扇形の範囲内かを判定したい ―
そういう場面では \`lookAt\` では足りません。
`,
    },
    {
      kind: 'md',
      text: `
## 視界の判定にも使える

「敵の正面 60 度以内にプレイヤーがいるか」のような判定も、これで書けます。

1. 敵からプレイヤーへの向きを \`atan2\` で角度にする
2. 敵自身の向いている角度との差を取る
3. 差を $-180^\\circ$ 〜 $180^\\circ$ に収める（[](#/ch/b18-angle)でやった処理）
4. その絶対値が 30 度以下かを見る（正面 60 度なら片側 30 度）

3 を忘れると、**真後ろにいるプレイヤーを正面と判定する**ことがあります。
角度の差を扱うときは、必ず範囲に収めてから使ってください。

なお同じ判定は、次の章の内積を使うともっと短く書けます。
角度に戻さずに済むぶん、そちらのほうが速く、符号の事故も起きません。
**「角度が本当に必要か」を一度考える**のが、この先のコツになります。
`,
    },
  ],
  exercises: [
    {
      prompt: `つぎの \`atan2\` の値を、度で答えてください（デモで確かめられます）。

1. \`Math.atan2(0, 1)\`
2. \`Math.atan2(1, 0)\`
3. \`Math.atan2(0, -1)\`
4. \`Math.atan2(-1, 0)\``,
      hint: '引数は (y, x) の順です。それぞれ点がどこにあるか図に描いてください。',
      answer: `1. 点 $(1, 0)$ ＝ 右 → **0 度**
2. 点 $(0, 1)$ ＝ 上 → **90 度**
3. 点 $(-1, 0)$ ＝ 左 → **180 度**
4. 点 $(0, -1)$ ＝ 下 → **−90 度**

4 が **270 度ではなく −90 度**で返るのが要点です。
\`atan2\` の返す範囲は $-180$ 〜 $180$ 度なので、下半分はマイナスになります。
0〜360 で扱いたいときは、負のときだけ 360 を足してください。`,
    },
    {
      prompt: `敵が $(2,\\; 0,\\; 3)$、プレイヤーが $(5,\\; 0,\\; 7)$ にいます。
床の上で敵をプレイヤーのほうへ向かせたい。

1. 差 $(dx,\\; dz)$ を求める
2. \`Math.atan2\` に渡す引数を、**順番も含めて**書く
3. 2 点の距離も求める`,
      hint: '「行き先 引く 出発点」でした。atan2 は y（この場合 z）が先です。',
      answer: `1. $dx = 5 - 2 = 3$、$dz = 7 - 3 = 4$
2. \`Math.atan2(4, 3)\` … **dz が先、dx があと**。約 0.927 ラジアン（53.1 度）
3. $\\sqrt{9 + 16} = \\sqrt{25} = $ **5**

3 と 4 と 5 の組がここでも出てきました。
**1 回の引き算から、向き（atan2）と距離（長さ）の両方が出せる**のは、
[](#/ch/b14-vector-sub)で見たとおりです。

なお実際に \`rotation.y\` に入れるときは符号の調整が要ることが多いので、
**両方試して合うほうを採ってください。**`,
      answerCode: `const dx = player.position.x - enemy.position.x;  // 3
const dz = player.position.z - enemy.position.z;  // 4

const angle = Math.atan2(dz, dx);   // 0.927 rad
const dist = Math.hypot(dx, dz);    // 5

enemy.rotation.y = -angle;  // 符号は実際に見て決める`,
    },
    {
      prompt: `\`Math.atan2(x, y)\` と引数を逆に書いてしまいました。
点 $(2,\\; 1)$ の場合、**正しい答えと間違った答え**をそれぞれ度で求め、
どういう関係になっているか説明してください。
（$\\arctan(0.5) \\approx 26.6^\\circ$、$\\arctan(2) \\approx 63.4^\\circ$ を使います）`,
      hint: '正しくは atan2(1, 2)、間違いは atan2(2, 1) です。',
      answer: `**正しい** … \`atan2(1, 2)\` ＝ $\\arctan(1/2) \\approx $ **26.6 度**

**間違い** … \`atan2(2, 1)\` ＝ $\\arctan(2) \\approx $ **63.4 度**

2 つを足すと $26.6 + 63.4 = 90$ 度になります。
つまり **45 度を軸に折り返した角度**が返っています
（$x$ と $y$ を入れ替えるのは、$y = x$ の線で鏡に映すことだからです）。

エラーは出ず、それらしい角度が返るのが厄介なところです。
**45 度ちょうどのときだけは正解と一致してしまう**ので、
テストする位置を 45 度以外に取ることも大事です。`,
    },
  ],
  quiz: [
    {
      q: '`Math.atan2` の引数の順番として正しいのはどれですか。',
      choices: [
        '`Math.atan2(y, x)` … y が先',
        '`Math.atan2(x, y)` … x が先',
        'どちらでも同じ',
        '3 つの引数を取る',
      ],
      answer: 0,
      explain:
        '$\\tan\\theta = y/x$ の分子が先、分母があとという並びです。逆にしてもエラーは出ず、45 度を軸に折り返した角度が返るので、間違いに気づきにくくなります。',
    },
    {
      q: '`Math.atan(y / x)` ではなく `Math.atan2(y, x)` を使う理由はどれですか。',
      choices: [
        '割り算で符号の情報が失われ、真逆の向きを区別できなくなるから',
        'atan2 のほうが計算が速いから',
        'atan は 3D では使えないから',
        'atan2 は度で返してくれるから',
      ],
      answer: 0,
      explain:
        '$(1, 1)$ と $(-1, -1)$ はどちらも $y/x = 1$ ですが、向きは正反対です。atan2 は割る前の 2 つを受け取るので区別でき、$x = 0$ でも 0 で割らずに済みます。',
    },
    {
      q: '`Math.atan2` が返す角度の範囲はどれですか。',
      choices: [
        '$-\\pi$ 〜 $\\pi$（$-180^\\circ$ 〜 $180^\\circ$）',
        '$0$ 〜 $2\\pi$（$0^\\circ$ 〜 $360^\\circ$）',
        '$-\\pi/2$ 〜 $\\pi/2$',
        '制限はない',
      ],
      answer: 0,
      explain:
        '下半分はマイナスで返ります。0〜360 で扱いたいときは、負のときだけ 360 度ぶんを足してください。角度の差を取るときにも、範囲を意識する必要があります。',
    },
  ],
};
