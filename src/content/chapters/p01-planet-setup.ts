import type { Chapter } from '../types.ts';

export const chapterP01: Chapter = {
  slug: 'p01-planet-setup',
  part: 'project',
  number: 1,
  title: '作品を作る ― 完成を、1 行で書く',
  goal: '作るものを先に決めてから手を動かす順番が身につき、動く骨組みを最初の 40 行で立てられるようになります。',
  requires: ['w50-small-forest', 't07-controls'],
  threeApis: [
    'Scene',
    'PerspectiveCamera',
    'WebGLRenderer',
    'OrbitControls',
    'OrbitControls.minDistance',
    'OrbitControls.maxDistance',
    'Clock',
  ],
  mathRecall: [
    { slug: 't01-first-scene', note: 'シーン・カメラ・レンダラの 3 点セット' },
    { slug: 't07-controls', note: '視点の操作と、その制限' },
  ],
  blocks: [
    {
      kind: 'md',
      text: `
## ここからは、作品を作ります

第3部までは「部品の使い方」を $1$ つずつ見てきました。
第4部は逆で、**作品を $1$ つ決めて、それを完成させるために必要なものを取りに行きます。**

作るのは $2$ つです。

- **惑星ビューアー** … 惑星が回り、月がまわり、大気が光る。触って眺めるもの
- **ローポリの街** … 街路と建物を手続き的に生成し、朝から夜へ移る風景

**素材は $1$ つも用意しません。** 画像もモデルも音も、すべてコードで作ります。
そのぶん「なぜこの形になるのか」が全部見えるので、作ったあとに好きに変えられます。

[](#/ch/w50-small-forest)で作った小さな森が、そのまま大きくなったものだと思ってください。
**部品を作る関数 → 組み立てる本体 → 毎フレーム動かす**、という $3$ 層はここでも同じです。
`,
    },
    {
      kind: 'callout',
      tone: 'tip',
      title: 'まず「完成」を 1 行で書く',
      text: `
作り始める前に、完成した姿を 1 行で書いてみてください。ここではこうします。

「星空の中に惑星が 1 つ浮かび、月がまわり、マウスで自由に眺められる」

この 1 行があると、迷ったときに戻れます。

逆に、書けないまま手を動かすと、きれいな球は出るのに
「で、これは何なのか」が分からないものができあがります。

1 行で書けないなら、まだ作るものが決まっていません。
`,
    },
    {
      kind: 'md',
      text: `
## 段取りを決める ― 各段階の終わりで、必ず何か映る

$1$ 行の仕様を、動かせる単位に割ります。
**割り方のコツは「各段階の終わりで必ず画面に何か出る」ようにすること**です。

- **この章** … 骨組み（シーン・カメラ・操作・描画ループ）。惑星の場所には仮の球を置く
- **[](#/ch/x02-depth-precision)** … 遠くの星空まで写す設定を決める
- **[](#/ch/x03-uniform-sphere)** … 球面に偏りなく点を散らす
- **[](#/ch/x04-star-look)** … 星空を仕上げる
- そのあと … 惑星の地表、大気、雲、自転と公転、月、クリックで寄る視点

**「全部できてから動かす」は、どこで間違えたのか分からなくなるので避けます。**

$1$ つ足すたびに動かす ― これが実践編で唯一いちばん大事な作法です。

$10$ 個足してから動かして壊れていたら、犯人は $10$ 人います。
$1$ 個足して壊れたら、犯人は $1$ 人です。**切り分けの費用が、桁で違います。**
`,
    },
    {
      kind: 'md',
      text: `
## 骨組みは、第3部の総復習

最初のコードに新しいものはほとんどありません。

[](#/ch/t01-first-scene)の $3$ 点セット、[](#/ch/t06-loop-clock)の{{描画ループ}}、
[](#/ch/t07-controls)の \`OrbitControls\` を並べるだけです。

**新しいのは $2$ 行だけ。**

- \`controls.minDistance\` … 惑星の中に入り込めないようにする
- \`controls.maxDistance\` … 遠くへ行きすぎて見失わないようにする

この $2$ 行が、作品としての「見せてよい範囲」を決めています。
`,
    },
    {
      kind: 'sandbox',
      title: '骨組み ― まだ星も地表もない',
      code: `import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

/* ---- シーン ---- */

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x05060d);

// far は次の章で決めます。いまは仮に 3000
const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.5, 3000);
camera.position.set(0, 1.4, 6.5);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

/* ---- 操作。見せてよい範囲を、ここで決める ---- */

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.minDistance = 2.6;   // 惑星（半径 1.6）の中に入り込めないようにする
controls.maxDistance = 40;    // 遠くへ行きすぎて見失わないようにする

/* ---- 惑星の場所に、いまは仮の球を置く ---- */

const planet = new THREE.Mesh(
  new THREE.SphereGeometry(1.6, 64, 48),
  new THREE.MeshStandardMaterial({ color: 0x6f7d95, roughness: 0.85 }),
);
scene.add(planet);

// 太陽。位置ではなく「向き」だけが意味を持つ
const sun = new THREE.DirectionalLight(0xfff2e0, 3.2);
sun.position.set(5, 2, 3);
scene.add(sun);

// 影の側が真っ黒に潰れないよう、ごく弱い環境光を足す
scene.add(new THREE.AmbientLight(0x3a4a6a, 0.35));

/* ---- 描画ループ ---- */

const clock = new THREE.Clock();

renderer.setAnimationLoop(() => {
  const dt = clock.getDelta();
  planet.rotation.y += dt * 0.08;
  controls.update();
  renderer.render(scene, camera);
});

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});`,
      caption:
        '**灰色の球が $1$ つ、ゆっくり回っているだけです。それでいい。** 第3部の総復習が動くことを確かめる段階で、ここに星や地表を足していきます。ドラッグで見回し、ホイールで寄ってみてください ― `minDistance` があるので、中には入れません。',
    },
    {
      kind: 'md',
      text: `
## 壊れた見え方は、直すのではなく「行けなくする」

\`controls.minDistance = 2.6\` を $0$ にして、思いきり寄ってみてください。

**惑星の中に入れてしまい、球の裏側を見て世界が消えます。**
面は表からしか描かれない（背面カリング）ので、内側からは何も見えません。

これを「内側からも描く」で直すのは筋が悪い。
**そもそも、そこへ行かせなければいい。**

[](#/ch/w29-controls-ux)で \`maxPolarAngle\` を使って床下に潜れないようにしたのと、
まったく同じ考え方です。

**到達できない場所の見た目は、直す必要がありません。**
作品として見せる範囲を決めるのは、演出であると同時にいちばん安い品質管理です。
`,
    },
    {
      kind: 'md',
      text: `
## ファイルの分け方

この章のコードはまだ $1$ 枚に収まりますが、完成までに $400$ 行を超えます。
**手元で作るなら、最初から分けておくのが楽です。**

- \`main.js\` … 骨組み（シーン・カメラ・レンダラ・ループ）
- \`stars.js\` … 星空を作って返す
- \`planet.js\` … 惑星を作って返す
- \`textures.js\` … テクスチャを生成する関数を集める

**分け方の基準は「そのファイルが返すものを $1$ 語で言えるか」**です。
言えないなら、まだ $2$ つのことをしています。

サイト内のサンドボックスは $1$ ファイルで動く必要があるので、以降も $1$ 枚で書きます。
**関数の切れ目が、そのままファイルの切れ目**になると思って読んでください。

長いサンドボックスには[](#/ch/w50-small-forest)で作った**コードの地図**が付くので、
どこが何をしているかは、その帯を見れば分かります。
`,
    },
  ],
  exercises: [
    {
      prompt: `\`controls.minDistance = 2.6\` を \`0\` にして、思いきり寄ってみてください。

**何が起きますか。** この $1$ 行は何を守っていますか。`,
      hint: '惑星の半径と、面がどちら側から描かれるかを思い出してください。',
      answer: `**惑星の中に入れてしまい、世界が消えます。**

**何が起きるか**

惑星の半径は $1.6$。\`minDistance\` が $0$ なら、カメラは中心まで行けます。

球の中に入ると、見えるのは球の**内側**です。
ところが面は既定で**表からしか描かれません**（背面カリング）。

だから球は消え、背景だけが残ります。
そして \`OrbitControls\` の注視点は原点のままなので、**どちらを向いても何もない。**

読者から見れば「壊れた」としか思えない状態です。

**この $1$ 行が守っているもの**

**「作品として見せてよい範囲」**です。

$2.6$ は惑星の半径 $1.6$ に $1$ ぶんの余裕を足した値で、
**大気の層（半径 $1.7$ 前後）にもめり込みません。**

**直すのではなく、行けなくする**

「内側も描く」（\`side: DoubleSide\`）で直すこともできます。
けれど、それは**誰も見ない絵のために倍の画素を塗る**ということです。

到達できない場所の見た目は、直す必要がありません。

同じ考え方が[](#/ch/w29-controls-ux)の \`maxPolarAngle\` にもありました ―
床下に潜れなくすれば、床の裏を作り込まなくて済みます。

**作品では、この判断がとても多い**

- 寄れる距離を決める … \`minDistance\`
- 見上げる角度を決める … \`maxPolarAngle\`
- 離れられる距離を決める … \`maxDistance\`

**制限は演出であると同時に、いちばん安い品質管理です。**`,
    },
    {
      prompt: `「完成を $1$ 行で書く」を、**自分の作りたいもの**でやってみてください。

書けたら、それを**各段階の終わりで必ず何か映る**ように $4$ 〜 $6$ 段に割ってください。
割り方が正しいかを判定する基準を、$1$ つ挙げてください。`,
      hint: '「全部できてから動かす」段取りは、何が困りますか。',
      answer: `**基準は「その段階が終わった時点で、動かして見られるか」です。**

**$1$ 行の例**

- 「$1$ 本の木が季節をひとめぐりして、葉が色を変えて落ちる」
- 「文字を打つと、その形に粒子が集まって浮かぶ」
- 「地図の上を歩くと、行った場所だけ霧が晴れる」

**割り方の例**（$1$ つ目）

1. 木を $1$ 本立てる（幹と葉。季節はまだ無い）
2. スライダーで季節を動かし、**葉の色だけ**が変わる
3. 葉が**落ちる**（$1$ 枚だけ、ゆっくり）
4. 落ちる葉を $200$ 枚に増やす
5. 風で流れる方向を足す
6. 地面に積もる

**どの段階でも、止めて眺められます。**

**正しい割り方かを判定する基準**

**「その段階だけを作って、実際に画面で見られるか。」**

見られないなら、それは段階ではなく**途中**です。

たとえば「葉の物理計算を書く」は段階ではありません ―
書き終わっても、画面には何も起きないからです。
「葉が $1$ 枚落ちる」なら段階です。

**なぜこれが効くのか**

**壊れたときの犯人の数**が変わります。

$10$ 個足してから動かして壊れていたら、犯人は $10$ 人。
どれが原因かを切り分けるところから始まります。

$1$ 個足して壊れたら、犯人は $1$ 人。**直前に書いた行を見ればいい。**

**切り分けの費用が、桁で違います。**

**もう $1$ つの効き目**

**途中でやめても、作品が残ります。**

段階 $4$ まで作って飽きたとしても、「木が季節で色を変え、葉が落ちる」ものは完成しています。
「全部できてから動かす」段取りだと、飽きた時点で**何も残りません。**`,
    },
    {
      prompt: `惑星ビューアーで、次の $3$ つはどのファイルに置きますか。
$1$ 語で言えない場合は、どう分け直しますか。

1. 地表・雲・夜の明かりの $3$ 枚のテクスチャを、$1$ 回のループで作る関数
2. 太陽の向きから、大気の色と明るさを決める関数
3. クリックされた天体へカメラを寄せる関数`,
      hint: '基準は「そのファイルが返すものを 1 語で言えるか」でした。',
      answer: `**$1$ は \`textures.js\`、$2$ は \`planet.js\`、$3$ は \`camera.js\`（新設）です。**

**$1$ ― $3$ 枚のテクスチャを作る関数**

返すのは**テクスチャ**。$1$ 語で言えます。

\`textures.js\` に置いて、\`createPlanetTextures(size)\` が
\`{ surface, clouds, night }\` を返す形にします。

**「$3$ 枚まとめて」を $1$ つの関数にしているのは、$1$ 回のループで作るため**です
（同じ座標に対する計算を $3$ 回繰り返さない）。
返すものが $3$ つでも、**種類が $1$ つなら $1$ 語**と数えます。

**$2$ ― 太陽の向きから大気を決める関数**

返すのは**色と明るさ**。惑星の見た目の一部なので \`planet.js\` です。

ただし**大気が育ってきたら \`atmosphere.js\` に分けます。**
判断は行数ではなく、「\`planet.js\` の中身を $1$ 語で言えなくなったとき」。

「惑星と、その大気と、雲と、夜の明かりを作る」と言い始めたら、分けどきです。

**$3$ ― クリックで寄せる関数**

返すのは……**何も返しません。** カメラを動かす副作用だけです。

**$1$ 語で言えないもの**なので、いまの $4$ ファイルには入りません。

これは \`planet.js\` でも \`main.js\` でもなく、**視点の振る舞い**という別の関心事です。
\`camera.js\`（または \`view.js\`）を新設して、
「カメラの動き」に関わるものをまとめてください。

**分けそこねると何が起きるか**

\`main.js\` に入れると、\`main.js\` は
「骨組みを組み、ついでにクリックの処理もして、カメラも動かす」ものになります。

**$1$ 語で言えなくなった時点で、そのファイルは探しにくくなっています。**

「あのカメラの処理、どこに書いたっけ」と迷ったら、それが合図です。`,
    },
  ],
  quiz: [
    {
      q: '作り始める前に「完成を 1 行で書く」のは、なぜですか。',
      choices: [
        '迷ったときに戻る基準になるから。書けないなら、まだ作るものが決まっていない',
        '仕様書として提出するため',
        'コード量を見積もるため',
        '使う API を決めるため',
      ],
      answer: 0,
      explain:
        '1 行が書けないまま手を動かすと、きれいな球は出るのに「で、これは何なのか」が分からないものができます。1 行は、途中で足すかどうかを決めるときの判断基準になります。',
    },
    {
      q: '段取りの割り方として正しいのはどれですか。',
      choices: [
        '各段階の終わりで、必ず画面に何か映るように割る',
        '機能ごとに割り、全部そろってから動かす',
        'ファイルごとに割る',
        '行数が均等になるように割る',
      ],
      answer: 0,
      explain:
        '10 個足してから壊れていたら犯人は 10 人、1 個足して壊れたら 1 人です。切り分けの費用が桁で違います。加えて、途中でやめても作品が残ります。',
    },
    {
      q: '`controls.minDistance = 2.6` は何を守っていますか。',
      choices: [
        '惑星の中に入って世界が消えるのを、到達できなくすることで防いでいる',
        '描画の速さ',
        'メモリの使用量',
        '影の品質',
      ],
      answer: 0,
      explain:
        '球の内側は背面カリングで描かれないので、中に入ると何も見えません。「内側も描く」で直すこともできますが、誰も見ない絵のために倍の画素を塗ることになります。到達できない場所の見た目は、直す必要がありません。',
    },
  ],
};
