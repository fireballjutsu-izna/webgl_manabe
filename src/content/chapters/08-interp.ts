import type { Chapter } from '../types.ts';

export const chapter08: Chapter = {
  slug: '08-interp',
  part: 'basics',
  number: 33,
  title: 'lerp ― 2 つの値を混ぜる',
  goal: '2 つの値のあいだを割合で行き来できるようになり、位置・色・大きさが同じ 1 つの式で扱えることが分かります。',
  requires: ['b13-vector-add', 'b05-ratio'],
  threeApis: ['MathUtils.lerp', 'Vector3.lerp', 'Vector3.lerpVectors', 'Color.lerp'],
  blocks: [
    {
      kind: 'md',
      text: `
## まず、どこで困るか

物体を A から B へ動かす。色を赤から青へ変える。大きさを 1 から 2 にする。
どれも「**2 つの値のあいだを、指定した進み具合で埋める**」という同じ問いです。

これを {{lerp|線形補間}} と呼びます。名前は linear interpolation の略で、
[](#/ch/b05-ratio)で「割合から値に戻す」と言ったものの正式名称です。

**このあと 5 章、補間の話が続きます。** すべての土台がこの 1 つの式です。
`,
    },
    {
      kind: 'md',
      text: `
## 差を取って、割合を掛けて、足し戻す

やっていることは 3 歩です。

1. **差を出す** … $b - a$。「a から b までどれだけあるか」
2. **割合を掛ける** … $t$ 倍。「そのうちどれだけ進むか」
3. **足し戻す** … $a$ に足す。「a から、そのぶん進んだ場所」

[](#/ch/b14-vector-sub)で「引いてから測る」をやりましたが、これはその親戚です。
**引いて、割合を掛けて、足す。**
`,
    },
    {
      kind: 'formula',
      tex: '\\mathrm{lerp}(a,\\, b,\\, t) = a + (b - a)\\,t',
      readAloud:
        'a から b へ向かう差（b 引く a）に、割合 t を掛けて、a に足します。t が 0 なら a のまま、1 なら b にぴったり、0.5 ならちょうど真ん中になります。',
      worked: {
        given: '$a = 10$ から $b = 30$ へ、$t = 0.25$ のときの位置を求めます。',
        steps: [
          { calc: 'b - a   = 30 - 10 = 20', note: 'まず「差」を出す' },
          { calc: '20 x t  = 20 x 0.25 = 5', note: 'その差の 25% ぶん' },
          { calc: 'a + 5   = 10 + 5 = 15' },
        ],
        result:
          '$t = 0.25$ で **15**。$t = 0$ なら 10、$t = 1$ なら 30 になります。**「差を取って、割合を掛けて、足し戻す」**という 3 歩は、位置でも色でも角度でも同じです。',
      },
    },
    {
      kind: 'md',
      text: `
## 数でも、ベクトルでも、色でも

この式は **成分ごとに同じことをするだけ**なので、そのままベクトルに広がります。

- 位置を lerp → 直線移動
- 色を lerp → グラデーション
- 大きさを lerp → なめらかな拡大縮小

**同じ 1 つの式が全部に効く**のが lerp の良いところです。
「色を混ぜる計算」を別に覚える必要はありません。

大事なのは、**$t$ は「時間」ではなく「進み具合」**だということです。
0 から 1 に正規化された割合であり、秒でもフレーム数でもありません。
秒から $t$ を作るのは、こちらの仕事です。
`,
    },
    {
      kind: 'callout',
      tone: 'warn',
      title: 't は 0〜1 に閉じ込める',
      text: `
経過時間から $t$ を作ると、うっかり 1 を超えることがあります。
lerp は 1 を超えても素直に計算してしまうので、**物体が目的地を通り過ぎて飛んでいきます。**

$t = 2$ なら b の 2 倍の位置、$t = -1$ なら a より手前です。
\`THREE.MathUtils.clamp(t, 0, 1)\` を挟む習慣をつけてください
（この「はみ出しを押し込める」は次の章で扱います）。
`,
    },
    {
      kind: 'md',
      text: `
## 別の書き方 ― どちらも同じもの

lerp の式は、展開すると別の顔になります。

$a + (b - a)t = a(1 - t) + bt$

こちらは「**a を $(1-t)$ の重みで、b を $t$ の重みで混ぜる**」と読めます。
$t = 0.25$ なら「a を 75%、b を 25%」。重みの合計は必ず 1 です。

**同じ式の 2 つの読み方**です。

- 「a から出発して、差のぶんだけ進む」… 移動として読む
- 「a と b を重み付きで混ぜる」… 混色として読む

位置なら前者、色なら後者のほうがしっくりきます。好きなほうで考えて構いません。
`,
    },
    {
      kind: 'code',
      title: 'lerp を使う',
      code: `import * as THREE from 'three';

// 数どうし
THREE.MathUtils.lerp(0, 10, 0.25);   // 2.5

// ベクトルどうし（呼ばれた側が書き換わる）
mesh.position.lerp(target, 0.25);

// 2 つの値から新しく作る（どちらも壊さない）
const p = new THREE.Vector3().lerpVectors(start, end, 0.25);

// 色も同じ式で混ざる
const c = new THREE.Color(0xff0000).lerp(new THREE.Color(0x0000ff), 0.5);

// t が 1 を超えると通り過ぎる
THREE.MathUtils.lerp(0, 10, 2);   // 20 ← 目的地の 2 倍`,
    },
    {
      kind: 'md',
      text: `
## 使う対象を間違えないこと

lerp が向かないものが 1 つあります。**姿勢（回転）**です。

3 つの角度をそれぞれ lerp すると、途中で遠回りしたり、
不自然にねじれたりします。回転には専用の道具（slerp）があり、
クォータニオンを扱う[](#/ch/07-rotation)で正面から見ます。

- **数・位置・色・大きさ** … \`lerp\`
- **姿勢（回転）** … \`slerp\`

この使い分けだけ守れば事故は起きません。
`,
    },
  ],
  exercises: [
    {
      prompt: `手で計算してください。

1. $\\mathrm{lerp}(0,\\; 100,\\; 0.3)$
2. $\\mathrm{lerp}(20,\\; 60,\\; 0.75)$
3. $\\mathrm{lerp}(10,\\; 4,\\; 0.5)$`,
      hint: '差を出して、割合を掛けて、足し戻します。3 は差がマイナスです。',
      answer: `1. $0 + 100 \\times 0.3 = $ **30**
2. $20 + 40 \\times 0.75 = 20 + 30 = $ **50**
3. $10 + (-6) \\times 0.5 = 10 - 3 = $ **7**

3 のように **b が a より小さくても、式はそのまま使えます**。
差がマイナスになるだけで、場合分けは要りません。
「大きいほうから小さいほうへ」を別扱いしなくてよいのが、この式の便利なところです。`,
    },
    {
      prompt: `$\\mathrm{lerp}(10,\\; 30,\\; t)$ が **25** になるのは $t$ がいくつのときですか。
式を逆にたどって求めてください。`,
      hint: 'まず「25 は 10 から見てどれだけ進んだか」を出します。',
      answer: `1. **差** … $30 - 10 = 20$
2. **進んだぶん** … $25 - 10 = 15$
3. **割合** … $15 \\div 20 = $ **0.75**

やったのは「引いて、幅で割る」 ― [](#/ch/b05-ratio)で出てきた、
**割合に直す計算そのもの**です。

lerp が「割合 → 値」なら、これは「値 → 割合」。
**逆向きの操作**で、次の章で正式に扱います。`,
    },
    {
      prompt: `位置 $(0,\\; 0,\\; 0)$ から $(4,\\; 8,\\; -4)$ へ、$t = 0.25$ で lerp した結果を手で求めてください。
また、**$t = 1.5$ を渡すとどこに行くか**も計算してください。`,
      hint: '成分ごとに同じ計算をします。',
      answer: `**$t = 0.25$** … $(1,\\; 2,\\; -1)$。それぞれ 4 分の 1 だけ進みました

**$t = 1.5$** … $(6,\\; 12,\\; -6)$。**目的地を 1.5 倍まで通り過ぎています**

lerp は $t$ の範囲を確かめません。経過時間から $t$ を作るときは、
**必ず 0〜1 に押し込めてから渡してください。**
物体が「目的地を通り過ぎてどこかへ飛んでいく」バグは、ほぼこれです。`,
    },
  ],
  quiz: [
    {
      q: '`lerp(a, b, t)` で `t = 0` のとき、結果はどれになりますか。',
      choices: ['a', 'b', 'a と b の中間', '0'],
      answer: 0,
      explain:
        't は「どれだけ b 寄りにするか」の割合です。0 なら a のまま、1 なら b、0.5 でちょうど中間になります。',
    },
    {
      q: '$\\mathrm{lerp}(a, b, t) = a(1-t) + bt$ という書き方は、何と読めますか。',
      choices: [
        'a を $(1-t)$、b を $t$ の重みで混ぜている',
        'a と b を掛け合わせている',
        't を 2 回使うことで精度を上げている',
        'a から b を引いている',
      ],
      answer: 0,
      explain:
        '重みの合計が必ず 1 になる混ぜ方です。もとの式と同じものですが、色を混ぜるときはこちらの読み方のほうがしっくりきます。',
    },
    {
      q: '2つの**姿勢（回転）**のあいだを補間するとき、使うべきものはどれですか。',
      choices: [
        '`Quaternion.slerp`',
        '`Vector3.lerp`',
        '`MathUtils.lerp` を3つの角度に',
        '`Matrix4.multiply`',
      ],
      answer: 0,
      explain:
        '位置には lerp、姿勢には slerp です。オイラー角の3つの数値を別々に lerp すると、途中で遠回りしたり不自然にねじれたりします。',
    },
  ],
};
